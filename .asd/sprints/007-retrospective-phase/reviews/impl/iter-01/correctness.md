[REVIEW-impl-correctness]: CONCERNS

# Review — correctness

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low)
- **Change surface**: 29 scoped files, `main...bb9b760`

**Dispatch note.** This review was split into two halves by the phase workflow after two full-scope dispatches died — the first on its 50-turn limit, the second on a session rate limit — losing their work with no resume path. Half A covered executable correctness, routing integrity and retirement completeness over 12 files; half B covered the AC trace and UI/template conformance over 17. The two file sets partition the 29-file scope exactly. Section coverage is complementary: each half marks the other's sections `n/a` with the split as predicate. Merged verdict below.

## Findings

| # | Sev | Location | Description | Fix |
|---|---|---|---|---|
| CORR-A-01 | medium | `sprint-lifecycle.md:101` | Surviving branch routing impl-review straight to `pr`. The phase-table `impl-review` row still ends "→ `pr`", contradicting `:22`, `:87`, `:102` and `asd-phase-impl-review.md:60`, `:99-101`. `sprint-lifecycle.md` is the phase-contract SSoT. An orchestrator resolving the conflict in favour of the table dispatches `pr` after a green suite, skipping `retro`; `pr` then blocks on the absent `retrospective.html`, so the sprint stalls at the DoD check with a misleading "missing artefact". | Change the row's exit criteria to `retro`; leave the red and unresolved-finding branches unchanged. |
| CORR-A-02 | medium | `.asd/migrations/6.0.0.js:31`, `:61` vs `:86` | Wrong-depth key match causing silent data loss. The presence guard is top-level only, but the member regex matches any line that is wholly `"escalations": [ ... ]` at any nesting depth, and `findIndex` takes the first. A consumer whose `state.json` carries a nested `"escalations": []` serialized above the top-level member loses the nested one: the guard passes, the splice removes the wrong member, `JSON.parse` still succeeds, the file is written, and the report records `stripped` — while the retired top-level key survives and the migration is recorded applied, so it never re-runs. The non-empty warning then misreports, quoting an array that was not the one dropped. Not covered by the tests. | Anchor the match to top-level depth, or compare `Object.keys` of the parse before and after (minus `escalations`) before writing. A key-set diff is the cheapest hard guarantee. |
| CORR-A-03 | low | `.asd/migrations/6.0.0.js:59`, `:66` | CRLF integrity is per-file, not per-line: `eol` becomes `\r\n` if the file contains any CRLF, and every line is rejoined with it. A mixed-EOL `state.json` — easily produced by successive agent writes on Windows — is rewritten wholesale, contradicting the module header's own claim that every other byte including line endings is preserved, and turning a one-line migration into a whole-file diff in the consumer's history. | Preserve per-line endings, or remove the matched line's byte range from the raw string rather than splitting and joining. |
| CORR-A-04 | low | `.asd/migrations/6.0.0.js:102` | Non-atomic write of the sprint's sole recovery point. `writeFileSync` truncates in place; an interrupt during the write leaves a truncated `state.json` with no backup — the consumer loses phase, verdict and latch state for the active sprint, and the runner records the migration failed with the file already destroyed. This is exactly the half-written-input case the module defends against on the read side. | Write to a sibling temp file and rename — atomic on both NTFS and POSIX. |
| CORR-A-05 | low | `.asd/migrations/6.0.0.js:61` | Duplicate-member case reports success while the key survives. Two `"escalations": [...]` lines parse legally (last wins); only the first is spliced, the result parses, is written, and is reported `stripped`. The runner then records the migration applied, so it never re-runs and the retired key remains permanently. | Re-parse after the splice and assert the key is gone; otherwise fall through to the skip-and-warn path. |
| CORR-A-06 | low | `.asd/release-manifest.json:4` vs the migration filename | `asd_version` is still `5.0.0` while the release ships `migrations/6.0.0.js`. `pendingMigrations` selects `version <= newVersion`, where `newVersion` is the incoming manifest's `asd_version`; if this release publishes below `6.0.0`, the migration is inert in every consumer and the retired key is never stripped — silently. The bump is a `pr`-phase step, so this is a release trap rather than a present bug. | Bump to exactly `6.0.0` at `pr`; treat "a shipped migration whose version exceeds the manifest's `asd_version`" as release-blocking. |
| CORR-A-07 | low | `asd-phase-retro.md:25` vs `:32-36`; `asd-phase-retro/SKILL.md:5` | Contract drift between declared capability and behaviour. The workflow states it delegates to no agent and dispatches no skill, yet step 9 handles `ADVICE_NEEDED` "from any dispatched agent" — unreachable. The skill correspondingly grants `Task` (needed only for that dead branch) and `AskUserQuestion`, though the phase adds no gate and lists no user-decision operation. Not a crash but capability creep: a phase declared gate-free and dispatch-free holds the tools to pause the user and spawn agents, and a future edit reads the dead step as licence. | Drop step 9 and narrow the skill's tools, or state advisor consultation explicitly in "Operations used" and keep `Task` — do not leave both standing. (Same defect as EFF-1.) |
| CORR-A-08 | low | `asd-sprint/SKILL.md:47` vs `asd-phase-pr.md:11-12` | Archival-timing contradiction at the chain's tail, pre-existing rather than introduced here, but both files are in scope. `asd-sprint` describes `NEXT: await-merge` as "sprint folder already archived"; `asd-phase-pr.md` open mode says the sprint remains at its normal path until closure approval. A resume between PR-open and merge follows the former, expects state under `archived/`, and reads the wrong location — masked only by the union search at step 1. | Correct the `asd-sprint` parenthetical, or state the archived-pre-merge case explicitly as legacy-only. Pre-existing drift: defer to the user if judged out of scope. |
| CORR-B-1 | medium | `asd-phase-retro.md:10-12` (absent line) | Ten of eleven phase workflows carry the friction-append reference; `asd-phase-retro.md` is the only one without it, declaring only read and write operations. AC-2 requires any phase to be able to append at any point. Concretely: during `retro` the orchestrator finds the empty-log fragment shape unconformable or a placeholder ambiguous — exactly the "template that could not be conformed to" class the rule names — and either records nothing (friction lost permanently, since `retro` is the last analysing phase) or performs an undeclared write. Self-referential friction, the highest-value kind for this feature, is what gets dropped. | Add the identical one-line reference to "Operations used". No rule text changes. (Same defect as DOC-3.) |
| CORR-B-2 | medium | `README.md:168` | The retro phase-table row describes only the remediation class and frames all output as log-derived. AC-10 makes systemic proposals a distinct class, not bounded by the entries, and AC-7 makes an entry-free log still yield a non-empty retrospective. A user with an empty log reads README, concludes retro has nothing to produce, and reports the delivered proposals section as spurious — or a maintainer syncing README back onto the rules deletes the class. This is the only user-facing prose description of the phase. | Extend the row to name both classes and the empty-log behaviour. |

## AC coverage trace

| AC | Status | Satisfying artefacts |
|---|---|---|
| AC-1 | covered | `t_friction-log.md` template and entry format; `artifact-layout.md:170-172` path, owner and Markdown-class classification; `sprint-lifecycle.md:233-248` records/never-records plus owner boundary table; sprint-tree row |
| AC-2 | **partial** | Mechanism stated once at `sprint-lifecycle.md:250`, referenced by ten phase workflows, and `impl.md:88` now appends `F-N` instead of `escalations[]`. Gap: retro has no append channel — CORR-B-1 |
| AC-3 | covered | Skill and workflow exist and are registered; `core.md:15` lists eleven phases with retro between impl-review and pr; `tests/run.js:2610-2639` asserts chain position and skill/workflow bijection |
| AC-4 | covered | `sprint-lifecycle.md:258-261` two classes with remediation traced per `F-N`; `asd-phase-retro.md:19-20`; template Analysis and both action sections, each row linking its `F-N` |
| AC-5 | covered | Fragment template; `artifact-layout.md:111`, `:119`, `:122`, `:174-176`; shell `delegates_to` includes it; both templates registered in `upstream_hashes` with correctly no `canon_hashes`, guarded by `tests/run.js:2671` |
| AC-6 | covered | `asd-phase-retro.md:23` closing chat summary of both classes, `:40` return contract; `sprint-lifecycle.md:265` adds no gate of its own |
| AC-7 | covered | `sprint-lifecycle.md:263`; `asd-phase-retro.md:18`, `:21` — absent or zero-entry log branches, appends nothing, mutates no state; template empty-log fragment shape |
| AC-8 | covered | `core.md:15`; `review-policy.md:141`; `artifact-layout.md:40-41`; README chain sites; `AGENTS.md:107` contract now naming the previously omitted sites; `asd-phase-impl-review.md` and `asd-phase-pr.md:9`; manifest entries; `tests/run.js:2605-2660` asserts the ordered mirrors. Two residual mirror defects: CORR-A-01 and CORR-B-2 |
| AC-9 | covered | Migration exists at the plan's MAJOR target; `asd-phase-retro.md:18` guarantees the in-flight-sprint path lands on the empty-log branch with no state mutation. Migration content carries CORR-A-02..06 |
| AC-10 | **partial** | `sprint-lifecycle.md:258-263`; template `#systemic-proposals` with the consumer/asd split, kept on the empty-log branch; `asd-phase-retro.md:20`, `:21`, `:23`. Gap: README mirror omits the class — CORR-B-2 |

## Verified clean, no finding

Routing: `impl-review` exits are `NEXT: retro` on DoD-met plus green terminal suite, `NEXT: impl` on unresolved findings and on code-defect red. No executable path emits `NEXT: pr`; the only stale statement is CORR-A-01, prose SSoT rather than a workflow branch. `retro` exits `NEXT: pr` on both branches, matching `asd-sprint/SKILL.md:47` and `sprint-lifecycle.md:265`. `PHASE_CHAIN` yields `retro` after `impl-review` and `pr` after `retro`; the `pr` special case is untouched; generated views are in sync.

`pr` cannot demand an artefact the empty-log branch never writes: `checkpoints.md:49` requires review DoD plus a completed retro, `asd-phase-pr.md:9` makes `retrospective.html` the blocking input, and the workflow writes that file on both branches — the empty-log branch skips only the remediation class.

`escalations` retirement is complete: no surviving reader or writer outside the migration and its tests; `sprint-lifecycle.md:250` positively asserts state holds no parallel escalation list.

Migration idempotence and the archive guard hold: absent key, unparsable JSON and unexpected multi-line shape all behave as specified; last-member removal drops the predecessor comma correctly including the sole-member case. Report shape is valid — the runner never inspects it.

Friction-append reference consistency: all ten are substantively identical and cite the same anchor, none restates the mechanism. The two placements track whether the workflow file has an "Operations used" section at all. The read-only-dispatch phases declare orchestrator write operations, so the reference does not contradict what they can do.

`t_friction-log.md` boundary maps one-to-one onto the rule's owner table with no overlap and no claim over another owner's content; the refs field is id-only.

UI conformance under the self-hosting reduced rubric: fragment purity holds; the responsibility comment carries all three keys plus `provenance: original` and empty source; `{{DOC_TYPE}}` and `{{STATUS}}` values both exist in the enums the same change extended, and the workflow fills exactly those; `{{TOC_NAV}}` reasoning is correct on both branches — five `h2` on the full branch, two on the empty one, below the threshold and stated as omitted rather than emitted-empty. Shell `delegates_to` lists the new fragment. No raw colour outside the shell's token block.

## Verdict

CONCERNS: 10 (5 medium, 5 low). No FAIL. Nothing blocks the DoD by itself and no finding requires an architecture change, new abstraction, contract break or scope expansion.

## Next action

Route to `impl` review-fix mode. CORR-A-01, CORR-B-1 and CORR-B-2 are one-line edits; CORR-A-02 is the only data-integrity fix and also needs a test. CORR-A-06 must be honoured at `pr`. CORR-A-08 is pre-existing drift outside this sprint's change intent — defer to the user if judged out of scope.

## Escalations

None.

## Coverage ledger

Merged from both halves. No dispatcher manifest was supplied to either split dispatch, so `manifest_digest` is null and rows are keyed by scope path.

```json
{"manifest_digest":null,"split":["A","B"],"head_ref":"bb9b7608f0764dc7b4ddb7c39ae498540c54e0af",
"findings":["CORR-A-01","CORR-A-02","CORR-A-03","CORR-A-04","CORR-A-05","CORR-A-06","CORR-A-07","CORR-A-08","CORR-B-1","CORR-B-2"],
"files":[{"i":".asd/hooks/session-start.js","s":"checked"},{"i":".asd/migrations/6.0.0.js","s":"checked","f":"CORR-A-02"},{"i":".asd/release-manifest.json","s":"checked","f":"CORR-A-06"},{"i":".asd/rules/artifact-layout.md","s":"checked"},{"i":".asd/rules/checkpoints.md","s":"checked"},{"i":".asd/rules/core.md","s":"checked"},{"i":".asd/rules/review-policy.md","s":"checked"},{"i":".asd/rules/sprint-lifecycle.md","s":"checked","f":"CORR-A-01"},{"i":".asd/skills/asd-phase-impl-review/SKILL.md","s":"checked"},{"i":".asd/skills/asd-phase-retro/SKILL.md","s":"checked","f":"CORR-A-07"},{"i":".asd/skills/asd-sprint/SKILL.md","s":"checked","f":"CORR-A-08"},{"i":".asd/templates/t_friction-log.md","s":"checked"},{"i":".asd/templates/t_html-shell.html","s":"checked"},{"i":".asd/templates/t_retrospective.html","s":"checked"},{"i":".asd/templates/t_state.json","s":"checked"},{"i":".asd/workflows/asd-phase-audit.md","s":"checked"},{"i":".asd/workflows/asd-phase-design-promote.md","s":"checked"},{"i":".asd/workflows/asd-phase-design-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-design.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-test.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl.md","s":"checked"},{"i":".asd/workflows/asd-phase-plan.md","s":"checked"},{"i":".asd/workflows/asd-phase-pr.md","s":"checked","f":"CORR-A-08"},{"i":".asd/workflows/asd-phase-retro.md","s":"checked","f":"CORR-B-1"},{"i":".asd/workflows/asd-phase-scope.md","s":"checked"},{"i":"AGENTS.md","s":"checked"},{"i":"README.md","s":"checked","f":"CORR-B-2"},{"i":"tests/run.js","s":"checked"}],
"rules":[{"i":"migration-idempotence","s":"pass"},{"i":"migration-archive-guard","s":"pass"},{"i":"migration-json-integrity","s":"finding","f":"CORR-A-02"},{"i":"migration-eol-integrity","s":"finding","f":"CORR-A-03"},{"i":"migration-write-durability","s":"finding","f":"CORR-A-04"},{"i":"migration-silent-data-loss","s":"finding","f":"CORR-A-05"},{"i":"migration-version-gating","s":"finding","f":"CORR-A-06"},{"i":"routing-impl-review-exits","s":"finding","f":"CORR-A-01"},{"i":"routing-retro-exits","s":"pass"},{"i":"routing-phase-chain-vs-next","s":"pass"},{"i":"routing-pr-precondition-vs-retro-output","s":"pass"},{"i":"escalations-retirement","s":"pass"},{"i":"contract-skill-tools-vs-workflow","s":"finding","f":"CORR-A-07"},{"i":"contract-return-line-vs-branch-prose","s":"pass"},{"i":"contract-pr-archival-timing","s":"finding","f":"CORR-A-08"},{"i":"ac-1","s":"pass"},{"i":"ac-2","s":"finding","f":"CORR-B-1"},{"i":"ac-3","s":"pass"},{"i":"ac-4","s":"pass"},{"i":"ac-5","s":"pass"},{"i":"ac-6","s":"pass"},{"i":"ac-7","s":"pass"},{"i":"ac-8","s":"pass"},{"i":"ac-9","s":"pass"},{"i":"ac-10","s":"finding","f":"CORR-B-2"},{"i":"ds-6-token-usage","s":"pass"},{"i":"ds-4-token-comment","s":"n/a","p":"self-hosting framework-templates carve-out: no DESIGN.md token pipeline"},{"i":"ds-7-disabled-state","s":"n/a","p":"no interactive component in scoped templates"},{"i":"ds-11-lint-exclusions","s":"n/a","p":"self-hosting framework-templates carve-out: no designmd-lint pipeline"},{"i":"component-fidelity","s":"pass"},{"i":"design-system-completeness","s":"pass"},{"i":"ux-principles","s":"pass"},{"i":"accessibility-wcag-aa","s":"pass"}],
"sections":[{"i":"bugs","s":"reviewed"},{"i":"security","s":"reviewed"},{"i":"contracts","s":"reviewed"},{"i":"best-practices","s":"reviewed"},{"i":"ac-coverage-trace","s":"reviewed"},{"i":"ui-conformance","s":"reviewed"}]}
```
