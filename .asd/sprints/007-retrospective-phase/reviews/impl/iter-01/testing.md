[REVIEW-impl-testing]: CONCERNS

# Review — testing

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low)
- **Change surface**: 29 scoped files, `main...bb9b760`

Method note: read-only reviewer with no shell, so the diff was derived by reading the scope files directly. Reported runs were corroborated arithmetically — `tests/run.js` holds exactly 132 top-level `test(` declarations, matching 127 + 5 with no silent removal; the existing assertions most exposed to narrowing (`tests/run.js:969-996`, `:1919-1940`) carry no new exemption.

## Findings

| # | Sev | Location | Description | Failure scenario |
|---|---|---|---|---|
| TST-01 | medium | `test-plan.md:34`; `asd-phase-impl-review.md:60`, `:101`; `asd-phase-retro.md:40` | The `none` decision for the `NEXT:` routing edits is not honest. Its reason — routing is prose interpreted at runtime with no executable surface — is contradicted by the tester's own work: `NEXT: retro` and `NEXT: pr` are literal tokens in a file, as machine-checkable as the `core.md` sentence and the `sprint-lifecycle.md` arrow line that WERE asserted (`tests/run.js:2642-2662`). Both edges are derivable from `PHASE_CHAIN` adjacency at the cost of two regexes on the same ladder rung already in use. | A later edit or bad revert restores `NEXT: pr` in `asd-phase-impl-review.md:101` while `PHASE_CHAIN` still contains `retro`. Every test stays green — the bijection test only checks that the skill and workflow files exist, the mirror test only two rule sentences. `asd-sprint` treats `NEXT:` as authoritative, so every sprint silently skips the phase this sprint delivers, and nothing fails. |
| TST-02 | medium | `test-plan.md:23-34`, `:68-73` | AC-4, AC-7 and AC-10 carry no check at any level, and AC-10 is absent from `test-plan.md` entirely — no row, no `none` decision, no manual step — despite being added mid-impl and having its own template section. §17 requires every AC to carry a check at some level; a `none` outcome is first-class only when the decision and reason are recorded. | These three ACs ship unverified by construction. The empty-log branch is the only path AC-7 and half of AC-10 live on, and no automated check, manual step or recorded reasoning touches it. If step 5 omitted the systemic class on that branch — precisely what AC-10 exists to guarantee — nothing would detect it, and the one specced manual run uses an entry-bearing log, never exercising that branch. |
| TST-03 | medium | `test-plan.md:31`; `README.md:130`, `:157-169`; `checkpoints.md:46` | The blanket `none` for rule docs and README justifies itself with "chain-relevant slices ARE covered by the mirror test", which is over-broad: that test covers exactly two sequences, in `core.md` and `sprint-lifecycle.md`. Two further ordered machine-checkable mirrors inside scope are unasserted — `checkpoints.md:46` (the test's regex is anchored `/^scope\s*(?:→\|⇄)/m` and cannot match it) and `README.md` (phase-table rows, mermaid edges, the literal count word). AC-8 names README explicitly. | A twelfth phase is added later. `PHASE_CHAIN`, `core.md` and `sprint-lifecycle.md` are updated, suite green, while `checkpoints.md:46` still shows the old precondition chain and README still says "eleven" with a stale table and graph. The user-facing entry point and the precondition SSoT both describe a workflow that no longer exists, with a green suite as cover. |
| TST-04 | low | `test-plan.md:49-50`; `tests/run.js:2610-2640`, `:2563-2592` | Two fail-first records name an assertion that could not have fired. Removing `'retro'` from `PHASE_CHAIN` throws first at `:2616` (`retro must be in the chain`), 22 lines before the bijection asserts the record credits. Deleting the `dropTrailingCommaBefore` call makes the file unparsable, so the helper returns `null` and the file is untouched — the first failure is the `JSON.parse` comparison at `:2579`, not `report.stripped` at `:2584`. The mutations are real and the tests do fail under them, so the proof holds in substance, but the evidence reads as reconstructed rather than transcribed. | A future reader trusts the record, deletes or weakens the assertion it names believing that one carries the proof, and keeps a test whose actual guard is gone — with a `test-plan.md` entry still claiming mutation coverage. |
| TST-05 | low | `tests/run.js:2504-2561`; `.asd/migrations/6.0.0.js:96-101` | The migration's only data-recovery affordance — the warning naming the dropped escalations and telling the consumer to re-record them — is exercised by the fixture but asserted by nothing. Capturing it is affordable and precedented: stub `process.stdout.write` with save/restore per §17's global-state rule. | Someone tightens the output, moves the warn above the guard, or drops it as noise. The suite stays green because every assertion is about file bytes and report arrays. A consumer then runs `/asd-update`, their recorded escalations vanish with no message, and the data is gone with no pointer to where it should have been re-recorded. |
| TST-06 | low | `test-plan.md:25`; `.asd/release-manifest.json:4`; `plan.md:29`, `:128` | The AC-9 row covers only the migration's internal behaviour. The precondition that makes it run at all — `asd_version` bumped to `6.0.0` — is unrowed, though the plan makes it a sprint DoD item and names it as a risk. An assertion cannot be green today (correctly — the bump lands at `pr`), so the honest record was a `none` row naming its owner, not silence. | The `pr` phase bumps to `6.1.0`, or not at all. `6.0.0.js` is then permanently outside every consumer's pending window, the retired key survives in every in-flight sprint state, and AC-9 is unmet in the shipped release — with two green migration tests standing as apparent evidence to the contrary. |
| TST-07 | low | `test-plan.md:72-73`; `t_retrospective.html:11`; `artifact-layout.md:130` | The AC-5 manual step is not branch-scoped: it offers "run a sprint through retro OR render the template with placeholders filled" and then expects a sticky TOC linked to every section. On the empty-log branch that is false by design — two `h2` sections, below the 3-section `{{TOC_NAV}}` threshold, so the nav is correctly omitted. Separately, both manual rows are executable only downstream of this gate, and the plan does not say when they come due or who confirms them. | A verifier renders the empty-log fragment, sees no TOC, reports AC-5 failed, and someone "fixes" the shell to always emit the nav — breaking the threshold contract for every artifact type. Or, likelier, both rows are never executed and AC-5/AC-6 close on no evidence. |
| TST-08 | low | `test-plan.md:23-34`; `t_html-shell.html:6` | `t_html-shell.html` is in the change scope but appears in no risk-table row; the templates row names only the two new templates. The edit is a one-line `delegates_to` addition, so `none` is plainly right — the gap is the missing recorded decision, not the decision. | The decision ledger is no longer exhaustive against the change surface, so the next reader cannot distinguish "considered, judged risk-free" from "not looked at" — exactly the distinction TST-02's AC-10 omission turned into a real hole. |

### Judged and accepted, no finding

Check-ladder fit for what was authored: all five sit on the cheapest rung catching their risk — unit tests only for the one piece of executable logic that rewrites consumer data, static assertions for the rest. No e2e, no new infrastructure, no dependency. `readPhaseChain`'s regex over source is coupling, but justified (the hook has no exports and requiring it would execute it) and documented by its assert message.

The retro skill/workflow `none` decision holds: `tests/run.js:969-996` independently enumerates `.asd/skills/*` from disk and asserts both generated views are planned and current, and the bijection assertions require both files to exist.

The archived-untouched caveat is correctly recorded and keeping the assertion is right: with the guard removed the candidate path does not exist in the fixture, so nothing observable changes. Recording the limitation rather than claiming proof is what §17 asks for.

Removals, meaningfulness, determinism, edge cases: "0 removed" corroborated by test-count arithmetic and inspection. No meaningless or mock-confirming assertion; no sleep, wall-clock, randomness or ordering dependence; literal boundary fixtures are what §17 permits. Edge cases on the core executable path are genuinely good — empty/absent, last-member boundary, unparsable, unexpected multi-line shape, re-run idempotence.

Stub-resolution verification passes: no stub closed by this sprint, no in-code `TODO(sprint-` marker in any scope file.

Manual-verification necessity is justified for what was routed there: rendered HTML layout has no automatable oracle short of a DOM harness, and a chat-posted summary is interaction, not output. The defect is precision (TST-07) and the ACs left out (TST-02), not the routing of these two.

## Verdict

CONCERNS: 8 (3 medium, 5 low). Every finding is autofixable inside `impl-test` without escalation — three cheap static assertions, one stdout assertion, and four `test-plan.md` record corrections. Nothing blocks the DoD or requires a user decision.

## Next action

Route to `impl` review-fix mode, then back through `impl-test` for TST-01, TST-03, TST-05 (new assertions) and TST-02, TST-04, TST-06, TST-07, TST-08 (record corrections). No manual-verification result is requested this iteration: both specced steps are executable only after this gate, and TST-07 asks the plan to say so.

## Escalations

None.

## Coverage ledger

No ordered manifest with digest was supplied to this dispatch (only `scope.json`); rows are keyed by the 29 scope paths and by stable rubric ids.

```json
{"manifest_digest":null,"findings":["TST-01","TST-02","TST-03","TST-04","TST-05","TST-06","TST-07","TST-08"],
"files":[{"i":".asd/hooks/session-start.js","s":"checked"},{"i":".asd/migrations/6.0.0.js","s":"checked","f":"TST-05"},{"i":".asd/release-manifest.json","s":"checked","f":"TST-06"},{"i":".asd/rules/artifact-layout.md","s":"checked"},{"i":".asd/rules/checkpoints.md","s":"checked","f":"TST-03"},{"i":".asd/rules/core.md","s":"checked"},{"i":".asd/rules/review-policy.md","s":"checked"},{"i":".asd/rules/sprint-lifecycle.md","s":"checked"},{"i":".asd/skills/asd-phase-impl-review/SKILL.md","s":"checked","f":"TST-01"},{"i":".asd/skills/asd-phase-retro/SKILL.md","s":"checked","f":"TST-02"},{"i":".asd/skills/asd-sprint/SKILL.md","s":"checked"},{"i":".asd/templates/t_friction-log.md","s":"checked"},{"i":".asd/templates/t_html-shell.html","s":"checked","f":"TST-08"},{"i":".asd/templates/t_retrospective.html","s":"checked","f":"TST-07"},{"i":".asd/templates/t_state.json","s":"checked"},{"i":".asd/workflows/asd-phase-audit.md","s":"checked"},{"i":".asd/workflows/asd-phase-design-promote.md","s":"checked"},{"i":".asd/workflows/asd-phase-design-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-design.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-review.md","s":"checked","f":"TST-01"},{"i":".asd/workflows/asd-phase-impl-test.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl.md","s":"checked"},{"i":".asd/workflows/asd-phase-plan.md","s":"checked"},{"i":".asd/workflows/asd-phase-pr.md","s":"checked"},{"i":".asd/workflows/asd-phase-retro.md","s":"checked","f":"TST-02"},{"i":".asd/workflows/asd-phase-scope.md","s":"checked"},{"i":"AGENTS.md","s":"checked"},{"i":"README.md","s":"checked","f":"TST-03"},{"i":"tests/run.js","s":"checked","f":"TST-04"}],
"rules":[{"i":"RS-1-check-ladder-risk-fit","s":"finding","f":"TST-03"},{"i":"RS-2-removal-reason-validity","s":"pass"},{"i":"RS-3-no-test-decision-honesty","s":"finding","f":"TST-01"},{"i":"RS-4-fail-first-regression-proof","s":"finding","f":"TST-04"},{"i":"RS-5-meaningfulness-and-determinism","s":"pass"},{"i":"COV-1-ac-coverage-AC-1..AC-10","s":"finding","f":"TST-02"},{"i":"EDGE-1-edge-cases-on-core-paths","s":"pass"},{"i":"STUB-1-stub-resolution-verification","s":"pass"},{"i":"MAN-1-manual-verification-necessity","s":"finding","f":"TST-07"}],
"sections":[{"i":"test-plan/risk-check-decisions","s":"reviewed"},{"i":"test-plan/removed-tests","s":"reviewed"},{"i":"test-plan/added-tests","s":"reviewed"},{"i":"test-plan/suite-run","s":"reviewed"},{"i":"test-plan/defects","s":"reviewed"},{"i":"test-plan/manual-verification","s":"reviewed"}]}
```
