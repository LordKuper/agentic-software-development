---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/iter-NN/testing.md (verdict)
---

# Test plan — sprint 008-retro-007-remediation

## Entry log

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | `a0eac63` | full change surface (base `39709446`, head `f886441`, 18 files, self-hosting exclusions applied) |
| 2 | iter-01 review-fix (impl-review CONCERNS) | delta only: testing.md T-1..T-9, correctness.md C-7, documentation.md D-5, efficiency.md E-5 — decision-record corrections in this file plus `tests/run.js` §19 edits (no re-derivation of the whole change surface) |
| 3 | (unfilled — orchestrator fills at phase exit; delta is `a0eac63...121485a`) | delta only: correctness.md C-1 and C-4 (`sync.js` fail-closed bare-`--apply` guard, `runtime.js` `RESERVED_CHANGE_RISKS` guard) — the two commits (`8423c32`, `139becf`) that shipped new executable branches without entry 2 reaching them, since entry 2 scoped itself to testing.md's own findings only. C-5/C-6/C-8/C-12/C-14 and the review-policy.md split-merge fourth condition analysed and found prose-only or already covered — see rows below; no re-derivation of the whole change surface |

## Risk → check decisions

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `.asd/runtime.js` `routeTask` typed risks (AC-10) | silent misroute: an artifact-risk wrongly escalates/de-escalates, or a malformed `risks` entry routes lower instead of failing closed | unit (pure function, deterministic) | add | new coverage: `target:"change"` parity with legacy string, `target:"artifact"` never escalates by itself, no-downgrade clamp reason outranks artifact-risk reason, any risk of either kind forces `execution:agent`, every malformed shape throws, mixed change+artifact array picks the change reason. Pre-existing test at line ~2251 already covers the legacy bare-string case (item 1 of the handed-over surface) — kept, not duplicated |
| `.asd/runtime.js` `validateCoverageLedger` + `review-policy.md` split-dispatch partition (AC-1/AC-6) | a malformed partition scheme (two partial ledgers against one unpartitioned manifest) silently passes, or a correct two-manifest partition wrongly rejected | unit | add | new test: each half validates unchanged against its own complete manifest, halves' `files` are disjoint and union to the unpartitioned set, `rules`/`sections` unchanged in each half; counter-check that a partial ledger against the unpartitioned manifest fails `files rows incomplete` |
| `.asd/templates/t_state.json` `derived_handoff` field (AC-11) | template ships the wrong shape (non-empty / wrong type), breaking the pure-cache contract for every new sprint | static (template content) | add | targeted assertion that `derived_handoff` ships as `{}`. Generic "every template JSON parses" is already covered by the existing recursive template-JSON test — not duplicated |
| `.asd/hooks/session-start.js` × `derived_handoff` (AC-11) | future/accidental coupling of the hook to a field it must never read; a malformed value could throw and break a hook that must fail silently | component (subprocess hook run) | add | 3-fixture test (absent / well-formed / malformed `derived_handoff`) asserting byte-identical `additionalContext` — guards the "hook reads no new key" contract as a regression, mirroring the file's existing absent-key-fallback test style (iteration_heads, latched) |
| `sprint-lifecycle.md` "State recovery" `derived_handoff` shape vs. both impl-phase workflows (AC-11 SSoT) | two workflow-file prose copies of the shape drift apart over time | static/architecture (content-contract, mirrors §16's mirror-check style) | add | asserts the shape literal exists once, in `sprint-lifecycle.md`, and that neither `asd-phase-impl-test.md` nor `asd-phase-impl-review.md` inlines it — each only carries the "sole SSoT" citation |
| `.asd/release-manifest.json` hash ledger refresh | stale/incorrect recorded hash for a changed file | static (existing §6b forward + §17 reverse hash-coverage tests) | none | this diff only updates recorded hash values; the check itself (every managed file has a matching, present entry) already runs unconditionally and would fail if any value here were wrong |
| `.asd/rules/artifact-layout.md` "Agent memory" section (AC-3) | none — the carve-out documents behaviour already true: `.claude/agent-memory/` has no canonical source and `sync.js` was never going to plan it (`sync.js` itself is untouched by this diff) | n/a | none | prose-only clarification of existing behaviour; no code changed, nothing to regress |
| `.asd/rules/code-style.md` §1 proactive-checklist line | none — restates an existing `review-policy.md` checklist as an authoring instruction; no new mechanism | n/a | none | prose-only, no observable behaviour |
| `.asd/rules/git-strategy.md` orchestrator-commits-bookkeeping line | none — documents an existing commit-ownership split; no scripted orchestrator exists in this repo to exercise | n/a | none | prose-only process rule; not a surface this test suite can exercise (orchestration is LLM-driven, not scripted code) |
| `.asd/rules/providers.md:41` file-write/never-heredoc paragraph (AC-2) | an agent pipes artifact content through a shell heredoc/here-string instead of the write-a-file operation, letting shell quoting corrupt content | n/a | none | restated honestly (T-1): this is an LLM tool-selection instruction, not a scripted code path — no dispatcher in this repo mediates which write mechanism an agent chooses, so there is nothing for a Node test to call. Same class as the git-strategy.md orchestrator-commit row below |
| `.asd/rules/providers.md:93` `asd-dev` role-scoped-context row edit (AC-7) | a role loads the wrong context list, missing the over-engineering/structure-cohesion checklist pointer | n/a | none | prose-only role-context list read by the LLM at dispatch; no role-context loader exists in this repo's Node code to exercise. Same class as the other role-scoped-context rows |
| `.asd/rules/providers.md:111` typed-risk routing prose | restated (T-7): the `runtime.js` unit tests above pin `routeTask`'s *behaviour*, not that `providers.md:111`'s prose still *describes* that behaviour accurately — these are two different failure modes, not one duplicated check | n/a | none | accepted as an unguarded prose↔code mirror for this pass — a token-mirror assertion was considered and deliberately deferred, not added, to keep this entry's scope to the AC-1/6/10/11 boundary `code-style.md` §17 draws for new coverage; this is a recorded residual risk, not equivalent coverage to the row above |
| `.asd/skills/asd-update/SKILL.md:37` wording fix (`node .asd/sync.js --apply` targets generated provider-view paths) | a wording error teaches the removed bare `--apply` form (no target list) | n/a | none | restated (T-6): `tests/run.js:1545`/`:1614` assert `sync.js`'s fail-closed behaviour on an *unmatched* target — they say nothing about this documentation's wording, so citing them as coverage was wrong. Decision stands as `none` because a grep assertion would over-fit — `--apply <file...>` remains valid usage notation at `providers.md:23`, `README.md:110`, `asd-sync/SKILL.md:23-24`, `sync.js:1480` — and because the behaviour a wording regression would encourage (silent no-op) is now itself caught: bare `--apply` exits 1 as of this round's correctness fix |
| `.asd/agents/asd-dev.md:66` matching wording fix | same as above | n/a | none | same restated reason as the row above |
| `.asd/templates/t_plan.md` `Material risk:` line/examples (AC-10 plan-side half) | the template's example grammar (`change:`/`artifact:`/`none`) drifts from `sprint-lifecycle.md`'s "Plan file format" grammar or from `routeTask`'s accepted `target` literals, teaching the wrong syntax | n/a | none | restated honestly (T-2), and re-traced from AC-2 to AC-10 (T-1) — the risk is real, not absent: this sprint's own `plan.md` (lines 45, 52, 73, 87, 102, 110) carries `Material risk:` prose inside `- [x]` checkbox subtasks, a third, off-grammar shape neither canonical statement permits. `sprint-lifecycle.md:302` has since gained an explicit fallback for exactly that case (absent or off-grammar → `change: unclassified`, routes `critical`), which is why the decision still stands at `none`: the fallback, not a test, is what makes an off-grammar line safe rather than silently swallowed. No scripted plan parser exists in this repo to validate the grammar mechanically, and a check that only re-asserts the template's own three example strings would be the no-value pattern `code-style.md` §17 rules out |
| `.asd/workflows/asd-phase-design-review.md`, `asd-phase-impl-review.md`, `asd-phase-impl.md`, `asd-phase-impl-test.md` interrupted/split-dispatch prose (AC-1/AC-6) | a workflow drops or diverges from the trigger/partition/union-property/merge-rule/durable-record contract it cites from `review-policy.md` | static/architecture (content-contract) | add | split out of the former catch-all row (T-3): new §19 test asserts the re-dispatch record's presence in `review-policy.md` and that both `*-review` workflows carry the identical reject-and-re-dispatch-fresh phrase plus the SSoT citation, rather than restating the contract — mirrors the AC-11 SSoT test's style. The manifest-partition union property itself is pinned at the `runtime.js` level (row above) |
| `.asd/workflows/asd-phase-impl-test.md`, `asd-phase-impl-review.md` `derived_handoff` wiring prose (AC-11) | two workflow-file prose copies of the read/write wiring drift from `sprint-lifecycle.md` | static/architecture | keep | split out of the former catch-all row (T-3) — already covered by the dedicated AC-11 SSoT row above; kept as its own row so AC-11's decision is not conflated with AC-1/6's or AC-8's |
| `asd-phase-impl-review.md` step 1a/1b preflight-before-manifest reordering (AC-8) | External Review's scope manifest gets assembled before the availability preflight, wasting the build when the provider is unavailable | n/a | none | split out of the former catch-all row (T-3): pure step-ordering prose, verified by direct read (`external-review.md:31` already required this order; step 1b brings the workflow into agreement, no new code path or behaviour to regress) — no scripted workflow-step-order checker exists in this repo to exercise, and step order is not evidence a Node test can assert against without parsing prose as if it were code |
| `.asd/sync.js` bare `--apply` fail-closed guard (correctness.md C-1) | a bare `--apply` (no target list) silently returns `ok:true, applied:[]`, exit 0, **and still refreshes the hash ledger** — a stale generated view then hides behind a green `--check` and green ledger tests indefinitely | unit/CLI (subprocess, mirrors the existing not-found-target CLI test's style) | add | new §19 test: bare `--apply` exits 1, `report.ok === false`, `report.applied` is empty, `report.hashLedger === null`, and both `release-manifest.json`/`sync-state.json` are byte-identical to before the call. Confirmed fail-first: neither this guard nor the exit path existed at entry-1 HEAD (`a0eac63`), so the test fails against that revision and passes at current HEAD |
| `.asd/runtime.js` `riskEntry` reserved-class guard (correctness.md C-4) | a reserved change-risk class (`security`/`authentication`/`migration`/`public contract`/`workflow gate`) declared `target:"artifact"` silently routes on evidence alone instead of failing closed, letting a migration/security task reach `tier: mechanical` | unit (pure function) | add | new §19 test: every reserved name (plus case variant, hyphen variant, underscore variant, double-space variant) typed `artifact` throws `reserved risk class is change by definition`; the same names typed `change` remain valid (unaffected); a name that only contains a reserved word as a substring (`security-audit-tool`) does not throw, proving the match is exact post-normalization, not substring; a genuinely non-reserved artifact name still routes on evidence unaffected. Confirmed fail-first: `RESERVED_CHANGE_RISKS` did not exist at `a0eac63`, so this test fails against that revision |
| `sprint-lifecycle.md` "Plan file format" `Material risk:` absent/off-grammar fallback (correctness.md C-5) | none — restated in the AC-10 plan-side row above (added in entry 1 as `sprint-lifecycle.md:302`'s fallback text); this delta only confirms that row's reasoning still matches current wording | n/a | none | already decided `none` in the existing `.asd/templates/t_plan.md` row above; no scripted plan-grammar parser exists in this repo, so the fallback's safety comes from the rule text itself, not a test. Re-verified this entry: the fallback text is present verbatim at `sprint-lifecycle.md`'s "Plan file format" section |
| `sprint-lifecycle.md` "State recovery" `derived_handoff.head` redefinition — newest commit touching `pathspec` in `<base>..HEAD`, not raw `HEAD` (correctness.md C-6) | a bookkeeping commit between derivation and phase exit could invalidate a would-be-valid cross-phase cache hit, or a reader could still compare against raw `HEAD` after this redefinition | static/architecture (existing AC-11 SSoT-mirror + hook tests) | keep | none of the three existing AC-11 tests assert the old `head = git rev-parse HEAD` definition or any git-log-based derivation (that formula runs in workflow prose the phase workflows execute, not in scripted Node code this suite calls) — all three (shape ships `{}`, hook byte-identical output, SSoT-mirror citation) test properties that remain true unchanged under the redefinition. Re-verified this entry: the shape literal the SSoT test greps for is byte-identical in the new "State recovery" prose, so no revision needed |
| `review-policy.md` "Union property, checked before merge" gained a fourth merge condition (every rule/section id resolves non-`n/a` in at least one half) plus a `validate-partition` helper contract | a two-half split silently drops coverage of a rule/section id that neither half's ledger actually evidenced | n/a (documented as a future-helper contract, not existing code) | none | the rule text is explicit that conditions (a)-(d) are "the contract for a `validate-partition --whole <path> --halves <path...>` helper in `.asd/runtime.js`; until it exists the orchestrator runs them by hand" — no such function exists at current HEAD (confirmed: `grep -n validate-partition .asd/runtime.js` finds nothing), so there is no code surface for a Node test to call. The existing AC-1/6 `validateCoverageLedger` test (row above) still covers what code does exist (each half validates against its own manifest; a partial ledger against the unpartitioned manifest throws) and stands unrevised |

## Removed tests

In-scope partial removal (T-4): four lines inside "AC-1/6: a reviewer split partitions..." were deleted as tautological, not the whole test. `unionFiles = [...half1.files, ...half2.files].sort()` compared against `allFiles` — and `half1.rules`/`half1.sections` compared against `whole.rules`/`whole.sections` — were assembled from the same hand-written literal arrays on both sides of each assertion, so all four passed regardless of any change to `runtime.js`, `review-policy.md`, or the workflows; they asserted the fixture against itself. No scripted partitioner exists in this repo to make the halves the *output* of the documented partition rule instead of literals, so removal (not reformulation) was the honest fix. The rest of the test — each half validating unchanged against its own complete manifest, and the `files rows incomplete` throw against the unpartitioned manifest — stays; it does exercise `validateCoverageLedger`.

Otherwise none. No test in or out of the change scope met a removal criterion (`code-style.md` §17) — nothing trivial, duplicate, mock-confirming, implementation-coupled, or flaky was found among impacted tests.

Entry 3: none. No test met a removal criterion this delta either.

## Added tests

Level and AC/risk covered are visible in the test file itself (name, path) — not restated here. All twelve live in `tests/run.js`, new §19, appended after §18 (existing numbering collisions at `// 7.` and `// 15.` left untouched per instruction). `buildManifest` (shared fixture builder, no digest attached) is hoisted above §15 and reused by `validCoverageFixture` and the pre-existing CLI `manifest-digest` test, per efficiency E-5 — not a new test, not listed below.

| Test | Regression proof |
|---|---|
| `tests/run.js`: "AC-1/6: a reviewer split partitions the manifest files list into two disjoint halves..." | corrected (T-9): design-conformance proof, not new-code-path coverage — `validateCoverageLedger` itself is unchanged by this diff; the test proves the split-dispatch partition scheme this sprint's rule text newly authorizes is compatible with that existing, untouched function. It never reads `review-policy.md`, would pass unchanged at base HEAD, and does not guard against the rule's own prose drifting from this behaviour |
| `tests/run.js`: "AC-10: a typed target:\"change\" risk routes exactly like the legacy bare-string form" | n/a — new coverage of new `riskEntry` typed-object path |
| `tests/run.js`: "AC-10: a typed target:\"artifact\" risk never escalates by itself..." | n/a — new coverage |
| `tests/run.js`: "AC-10: the no-downgrade clamp outranks an artifact-risk reason..." | n/a — new coverage |
| `tests/run.js`: "AC-10: routing fails closed on every malformed risks shape..." | n/a — new coverage |
| `tests/run.js`: "AC-10: a mixed array of one change risk and one artifact risk still routes critical..." | n/a — new coverage |
| `tests/run.js`: "AC-10: a failed objective check with a correction attempt outranks a co-occurring artifact risk in the reason..." | added for C-3/T-8: pins the `reason` precedence boundary the ternary chain gives `failedObjectiveCheck && attempted >= 1` over `artifactRisk` — a reorder would mislabel an escalated task's evidence in `state.json.task_routing` without changing `tier` |
| `tests/run.js`: "AC-10: an artifact risk whose priorTier does not outrank the computed tier keeps the artifact-risk reason..." | added for C-3/T-8: pins that the no-downgrade clamp does not fire merely because `priorTier` is set — only when it outranks the computed tier — so the reason stays `artifact-risk:<name>` rather than falsely reporting `no-downgrade` |
| `tests/run.js`: "AC-11: t_state.json ships derived_handoff as an empty object..." | n/a — new coverage of new template field |
| `tests/run.js`: "AC-11: SessionStart output is byte-identical whether state.json.derived_handoff is absent, well-formed, or malformed..." | n/a — new coverage, guards against future coupling. Anchored (T-5): also asserts the shared output pins this fixture's sprint id/phase/branch, so the three-way equality is known to be comparing the real active-sprint summary path rather than three vacuously identical "no active sprint" strings |
| `tests/run.js`: "AC-11: derived_handoff's shape/validity rule lives ONLY in sprint-lifecycle.md \"State recovery\"..." | n/a — new coverage, mirrors §16's SSoT-mirror style |
| `tests/run.js`: "AC-6: the interrupted-dispatch re-dispatch record is stated in review-policy.md and cited (not restated) by both *-review workflows" | added for C-7: AC-13 named this as required new coverage; previously only the manifest-partition union property (AC-1/6) and the `derived_handoff` shape (AC-11) were pinned, leaving AC-6's re-dispatch record as the sole named-but-untested surface. Mirrors the accepted AC-11 SSoT test's style |

Entry 3 additions (2, both new §19 tests, appended after the entry-2 additions above):

| Test | Regression proof |
|---|---|
| `tests/run.js`: "sync.js CLI: bare --apply with no targets fails closed (exit 1) and skips the hash-ledger recompute..." | added for C-1: confirmed fail-first against `a0eac63` (bare `--apply` returned `ok:true, applied:[], exit 0` and still refreshed the ledger there — no guard existed); passes at current HEAD where `--apply` with an empty target list now exits 1 before `runApply`/`recomputeAndWriteHashLedgers` |
| `tests/run.js`: "AC-10: a reserved change-risk class name declared target:\"artifact\" fails closed, case- and separator-normalized, without over-matching a merely-similar name" | added for C-4: confirmed fail-first against `a0eac63` (`RESERVED_CHANGE_RISKS` did not exist, no reserved name was rejected); passes at current HEAD. Also proves the normalization boundary both ways — reserved names in case/hyphen/underscore/double-space variants all still match, and a merely-similar substring name (`security-audit-tool`) does not falsely match |

## Suite run

- Command: `node tests/run.js` (`test` from commands.yaml) — this repo has no `test_affected` selector and a single flat runner file, so every invocation runs the whole file; the **safety valve independently fires** for this entry regardless, since the change surface touches shared/framework-wide infrastructure (`.asd/runtime.js`, `.asd/rules/**`, `.asd/templates/**`, `.asd/workflows/**`, `.asd/release-manifest.json` — self-hosting means these ARE the framework). Impacted-set definition used: search-derived set (diff test files + reference search + AC-tag search) → safety valve fires on shared-infrastructure touch → degrades to full suite. Mechanically identical outcome to `impl-review`'s later full-suite run, but recorded here as the `impl-test` gate, per `sprint-lifecycle.md` "Impacted test set".
- Scope: impacted (safety valve fired → full suite, ran twice this sprint: 148 at entry 2, 150 at entry 3)
- Entry 2 result: pass — 148/148 passed, 0 failed, 0 skipped (136 pre-existing + 9 iter-01 added − 4 tautological assertion lines removed from one test (T-4, not a whole test) + 3 review-fix additions (2× T-8 boundary cases, 1× C-7 re-dispatch-record test) = 148 top-level `test(` declarations)
- Entry 3: pre-strategy run (before authoring) was 148/148 pass, 0 failed — confirming entry 2's tests still hold against the C-1/C-4/C-5/C-6/review-policy fix commits before any new test was added. Post-authoring result: pass — 150/150 passed, 0 failed, 0 skipped (148 + 2 new: bare-`--apply` fail-closed CLI test (C-1), reserved-risk-class guard unit test (C-4)). Both new tests confirmed fail-first against entry-1 HEAD `a0eac63` (neither guard existed there)
- Lint / build: pass — `git diff --check` clean (only pre-existing CRLF-normalization warnings on orchestrator-owned files, not errors); `node .asd/sync.js --check` reports `"ok": true` for every generated target
- HEAD: entry 3 analysed the delta `a0eac63...121485a` (five review-fix dispatches); impl-test's own commit for this entry follows this test-plan/tests update — re-run after committing confirmed 150/150 unchanged

## Defects

None. No code defect found — all 150 tests pass against the implementation as delivered.

## Manual verification (optional)

None. This sprint's change surface is entirely rule/workflow/template prose plus three pure-function/data surfaces (`routeTask`, `validateCoverageLedger`, `t_state.json`/hook interaction) — no visual UI, no third-party live integration, no UX-feel surface exists to verify manually.
