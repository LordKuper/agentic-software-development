---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/iter-NN/testing.md (verdict)
---

# Test plan — sprint 008-retro-007-remediation

## Entry log

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | `a0eac63` | full change surface (base `39709446`, head `f886441`, 18 files, self-hosting exclusions applied) |
| 2 | `1cb6507` | delta only: testing.md T-1..T-9, correctness.md C-7, documentation.md D-5, efficiency.md E-5 — decision-record corrections in this file plus `tests/run.js` §19 edits (no re-derivation of the whole change surface) |
| 3 | `d902245` | delta only: correctness.md C-1 and C-4 (`sync.js` fail-closed bare-`--apply` guard, `runtime.js` `RESERVED_CHANGE_RISKS` guard) — the two commits (`8423c32`, `139becf`) that shipped new executable branches without entry 2 reaching them, since entry 2 scoped itself to testing.md's own findings only. C-5/C-6/C-8/C-12/C-14 and the review-policy.md split-merge fourth condition analysed and found prose-only or already covered — see rows below; no re-derivation of the whole change surface |
| 4 | `f17ee2e` | delta only: impl-review iter-02 testing.md T-1..T-4 and external.md X-2 (review-fix round `8e83182` "cite SSoT instead of restating it" reconciled the workflow/rule contradiction T-1 named, reduced `derived_handoff` to one phase-exit write, restated the union property's surviving condition over the out-of-half predicate only, deleted `custom.sync-apply`, and consolidated the `--apply` explanatory parenthetical to `AGENTS.md` alone). This entry: extends AC-11 coverage to the `head` derivation itself (not just the object literal), rows the five previously-unrowed files plus two unrowed in-scope hunks, extends the AC-2/AC-7 statement-durability checks symmetrically with the already-accepted AC-6 style, records an AC-14 row citing the existing real-repo `--check` coverage while acknowledging the partial-`--apply` ledger gap, and restates the AC-13/union-property row so the record matches what shipped (manual orchestrator check, `validate-partition` deliberately not built — decisions-log 2026-09-08) |
| 5 | `9c36503` (re-entry after impl-review's iter-02 fix round formally closed) | delta since entry 4 proper (`f17ee2e...9c36503`) is bookkeeping-only — `decisions-log.md`, `friction-log.md` wording, `state.json` phase transition back to `impl-test` and `review_fixes_pending` clearing — no rule/workflow/code change, so no new risk. This entry instead closes two residual gaps the dispatch named against the wider `d902245...8e83182` review-fix delta that entry 4 already tested: (a) `.asd/project/custom-coding-rules.md`'s own `--apply` parenthetical consolidation (item 7) had no mirror coverage — the existing T-2 AGENTS.md-SSoT test's `otherSites` list omitted it; extended, confirmed fail-first against `d902245` (file still carried the full parenthetical there); (b) `.asd/project/commands.yaml`'s `custom.sync-apply` deletion (item 6) had no risk→check row — added below, decision `none` with reason, not a false "covered by existing test" claim. **Correction (testing.md T-2, iter-03):** "closes two residual gaps against the wider ... delta" overstated its own completeness — `f17ee2e` (inside that wider delta, and entry 4's own analysed HEAD) rewrote `.claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md`, and neither entry 4 nor this entry rowed it. That gap, plus this round's further rewrite of the same file and two new dev-critical memory files, is closed below |
| 6 | `caed0e7` (own commit `081fa38` was HEAD at authoring time; `caed0e7` is the immediate next commit, bookkeeping-only, confirmed below) | delta since entry 5 (`9c36503...081fa38`): impl-review iter-03 review-fix commits `c8e7a2e` (drop the unreachable impl-review-side `derived_handoff` write; reduce the rule and both workflows to one writer/one reader), `09c9abc` (correct four false statements in `feedback_no-shell-review-method.md` per C-2/C-3/D-2, plus two stale claims in `asd-dev-critical`'s sync-apply memory), `89a605c` (new `asd-dev-critical/project_crlf-canon-edits.md` memory plus its `MEMORY.md` index line, friction F-7). This entry resolves testing.md T-1 (the `derived_handoff` wiring row's `keep` cited a test that could not fail on the wiring statement — extended the existing AC-11/T-1 test in place rather than duplicating it) and T-2 (five changed `.claude/agent-memory/**` files had no row at all; rowed each below, three `add` and two `none`, and corrected entry 5's overstated completeness claim above) |
| 7 | `090253d` (current HEAD at this re-entry) | delta since entry 6 (`081fa38...090253d`): two commits, both orchestrator bookkeeping only — `caed0e7` (`friction-log.md`, +10 lines, records F-7) and `090253d` (`decisions-log.md` +7, `state.json` phase-transition fields). Verified via `git show --stat` on each: no rule, workflow, code, or test file touched by either. No new risk, nothing to row, nothing to author. Re-ran the impacted set (full suite, safety valve already fired per entry 6's standing reason) to confirm entry 6's tests still hold through these two commits: 159/159 unchanged. This entry also fills entry 6's own HEAD-analysed placeholder, left `TBD` because the orchestrator's bookkeeping commit for entry 6 landed after this tester's own `081fa38` commit but before this dispatch |
| 8 | TBD | impl-review iter-04 review-fix round, dispatched for testing.md T-1 and T-2 plus AC-11's retirement and AC-15's addition (decisions-log 2026-09-08, commit `4904805` "retire derived_handoff"). Delta since entry 7 (`090253d...4904805`, plus the orchestrator's own `89ac144`/`3dada06`/`f667810` bookkeeping): AC-11 closed as not delivered, `derived_handoff` deleted (state field, rule paragraph, both workflows' wiring) — four covering tests removed (three red by the retirement, one already-vacuous held-over test judged and removed rather than repurposed); the T-2 tool-grant test's token ban (`/\bWrite\b/`) reformulated to fail on a disagreeing tool-grant *enumeration* against the cited frontmatter instead of banning the token itself; AC-15's three landed changes (read-only reconciliation, host-verified/trust frontmatter-field record, Russian-comment removal) each given a risk→check row. The T-1 durable-record overstatement (guard narrowed while described as extended, `Removed tests: none` inaccurate) is corrected below rather than re-guarded, since the guard itself is retired |

## Risk → check decisions

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `.asd/runtime.js` `routeTask` typed risks (AC-10) | silent misroute: an artifact-risk wrongly escalates/de-escalates, or a malformed `risks` entry routes lower instead of failing closed | unit (pure function, deterministic) | add | new coverage: `target:"change"` parity with legacy string, `target:"artifact"` never escalates by itself, no-downgrade clamp reason outranks artifact-risk reason, any risk of either kind forces `execution:agent`, every malformed shape throws, mixed change+artifact array picks the change reason. Pre-existing test at line ~2251 already covers the legacy bare-string case (item 1 of the handed-over surface) — kept, not duplicated |
| `.asd/runtime.js` `validateCoverageLedger` + `review-policy.md` split-dispatch partition (AC-1/AC-6) | a malformed partition scheme (two partial ledgers against one unpartitioned manifest) silently passes, or a correct two-manifest partition wrongly rejected | unit | add | new test: each half validates unchanged against its own complete manifest, halves' `files` are disjoint and union to the unpartitioned set, `rules`/`sections` unchanged in each half; counter-check that a partial ledger against the unpartitioned manifest fails `files rows incomplete` |
| `.asd/templates/t_state.json` `derived_handoff` field (AC-11) | **retired, entry 8**: AC-11 closed 2026-09-08 as not delivered (decisions-log) — `derived_handoff` deleted from `t_state.json` entirely, so the field this row guarded no longer exists | static (template content) | removed | the covering test asserted a field that no longer ships; kept would be a false claim about the template's shape. See "Removed tests" entry 8 |
| `.asd/hooks/session-start.js` × `derived_handoff` (AC-11) | **retired, entry 8**: same closure — no field remains for the hook to (not) read, so "the hook ignores this key" has nothing left to be a regression guard for | component (subprocess hook run) | removed | the covering test was already vacuous before the field's own deletion (it proved the hook ignores an unknown key, not anything AC-11-specific) and is now retitled-nowhere-to-point; judged against §17's hypothetical-risk criterion and removed rather than repurposed into a generic "hook tolerates any unrecognized state.json key" claim, which no identified failure mode or stated requirement supports — see "Removed tests" entry 8 |
| `sprint-lifecycle.md` "State recovery" `derived_handoff` shape vs. both impl-phase workflows (AC-11 SSoT) | **retired, entry 8**: the shape literal and both workflows' citation of it are deleted along with the field — nothing left to mirror | static/architecture (content-contract, mirrors §16's mirror-check style) | removed | see "Removed tests" entry 8 |
| `.asd/release-manifest.json` hash ledger refresh | stale/incorrect recorded hash for a changed file | static (existing §6b forward + §17 reverse hash-coverage tests) | none | this diff only updates recorded hash values; the check itself (every managed file has a matching, present entry) already runs unconditionally and would fail if any value here were wrong |
| `.asd/rules/artifact-layout.md` "Agent memory" section, path-map `<reviewer>.part-N.md` addition, and the new sprint-folder-purity sentence (AC-3 / testing.md T-2) | corrected (T-2): the row's prior justification ("`sync.js` itself is untouched by this diff") is now false — entry 3 added the C-1/C-4 guards to `sync.js` in this same sprint's diff — and two further in-scope hunks in this file (the `part-N.md` path-map addition, the "holds only the artifacts named above" purity sentence) carried no row at all. The real risk across all three is statement durability: a later edit deletes or reverses the sentence, not that `sync.js` code regresses (agent-memory genuinely has no canonical source or sync-plan row, unaffected by any `sync.js` change) | static (statement-presence, AC-6/T-3 style) | add | new coverage: one test asserts the Agent-memory carve-out sentence, the `<reviewer>.part-N.md` path-map naming (both review dirs), and the sprint-folder-purity sentence are all present in `artifact-layout.md`, plus that `README.md`'s folder map and prose mirror the agent-memory hand-authored exception, and that `t_review.md` actually ships the `Interrupted attempts: {{count}} ({{cause}})` placeholder and the `<reviewer>.part-N.md` split-form comment `review-policy.md`'s durable-record rule (AC-6 row above) depends on being filled in somewhere |
| `.claude/agent-memory/**` not-excluded from the self-hosting review scope — restated in `sprint-lifecycle.md` "Self-hosting" change-surface line, `external-review.md`'s table row and `exclude_paths` sentence, and `t_prompt-external-impl.md` (testing.md T-2, converges with X-1) | a future edit to any one of these four statements silently re-widens the self-hosting exclusion and hides `.claude/agent-memory/**` from External Review again, without the other three copies catching the drift | static (content-contract mirror, `tests/run.js:2031`'s style) | add | new test: all three files (`sprint-lifecycle.md`, `external-review.md` twice, `t_prompt-external-impl.md`) state `.claude/agent-memory/**` is explicitly NOT excluded, matched by a single regex tolerant of each site's own phrasing — the class of check the repo already machine-checks at `tests/run.js:2031`, which "prose, nothing to check" was not an available reason against (T-2). Confirmed fail-first against `591abfb^` (parent of the X-1 fix commit): `external-review.md` and `t_prompt-external-impl.md` carried no such statement there (`sprint-lifecycle.md`'s copy predates that fix and already matched) |
| `.claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md` content, rewritten this round to correct C-2/C-3/D-2 (testing.md T-2, iter-03) | corrected (T-2, iter-03): this file had no risk→check row at all — the only row naming `.claude/agent-memory/**` was about *other* files declaring the tree not-excluded, not this file's own content. The content risk is real, not hypothetical: this file already carried four false statements this sprint (wrong tool grant, wrong collateral-failure count, a false "no in-suite --check test" claim, a self-contradicting closed-vocabulary sentence), reasserted into context on every dispatch of this reviewer, and cost two full review re-dispatches (friction F-5) before being caught by direct read rather than by any test | static (content-contract + mechanism check) | add | two new tests: (1) asserts the file cites `asd-reviewer-testing.md`'s frontmatter as the tool-grant home instead of enumerating a list, never claims a `Write` grant, and that the cited frontmatter really is `["Read","Glob","Grep","AskUserQuestion"]` — guards the exact D-2 regression class; (2) asserts `computeCanonHashes` only ever produces keys under `agents/*.md` or `skills/*/SKILL.md`, and never under `hooks/` — pins the mechanism the memory's corrected three/two/one collateral-count arithmetic (C-2) depends on, so a future change widening `canon_hashes`'s tree scope is caught at the mechanism, forcing the memory's count to be revisited rather than silently going stale again. The closed-vocabulary correction (C-3) and the "no in-suite --check test" correction have no independent mechanism to pin beyond direct read against `review-policy.md`'s own wording (already a static test above) and `tests/run.js`'s real `--check` test (already covered, AC-14 row above) — not re-guarded here to avoid a no-value duplicate |
| `.claude/agent-memory/asd-dev-critical/MEMORY.md` index line for the new CRLF-hazard entry (documentation.md scope, iter-03) | a broken or missing index pointer is invisible until an agent tries to follow it — the risk that already exists for every `MEMORY.md` in this repo, landing here because this round added the entry | static (link-existence check, scoped to the changed file) | add | new test: every markdown link in `asd-dev-critical/MEMORY.md` resolves to a file that exists in the same directory, and the index explicitly lists `project_crlf-canon-edits.md`. Scoped to this one file (not a repo-wide sweep of every `MEMORY.md`) because a repo-wide version surfaced an unrelated, pre-existing broken link in `asd-pm/MEMORY.md` outside this sprint's change surface — fixing or flagging that is not this entry's scope; a wider version of this test is a candidate for a future sprint that actually touches `asd-pm`'s memory |
| `.claude/agent-memory/asd-dev-critical/project_crlf-canon-edits.md`, new this round (friction F-7) | none — hand-authored operational hazard note (detection recipe for a CRLF/lone-CR scripted-edit artifact) with no canonical source to drift from and no fact about repo/code state that could go stale the way C-2/D-2's claims did; its own prescribed detection command (`git diff --check`) is already this entry's own completion gate, run directly against this round's real edits, not simulated | n/a | none | content verified by direct read against the actual CRLF mechanics it describes (confirmed accurate); no scripted consumer reads agent-memory prose, and there is no drifting counterpart elsewhere in canon to mirror it against — unlike the reviewer-testing memory above, this file makes no claim about `tests/run.js` or `sync.js` behavior that a test could pin |
| `.claude/agent-memory/asd-dev-critical/project_sync-apply-ledger-gotcha.md`, corrected this round (bare-`--apply` behavior updated to match C-1's fix) | restated: the underlying behavioral claim ("a bare `--apply` writes nothing and needs a real target") is the exact contract already pinned by the sync.js CLI bare-`--apply` test below (added for C-1) | n/a | none | would duplicate existing coverage of the same mechanism without adding failure-detection power for this memory file's own text — the no-value pattern `code-style.md` §17 rules out. If the underlying `sync.js` behavior regresses, that existing test fails first |
| `.claude/agent-memory/asd-dev-critical/project_sync-apply-target-form.md`, corrected this round (parenthetical-consolidation note updated to say `AGENTS.md` alone) | restated: the underlying fact ("the explanatory parenthetical lives only in `AGENTS.md`") is the exact contract already pinned by the AGENTS.md-SSoT test below | n/a | none | same reasoning as the row above — duplicate coverage of an already-tested fact, not this memory file's own risk |
| `AGENTS.md`'s `--apply <generated-view-path...>` explanatory parenthetical vs. the six other sites that used to duplicate it in full (README.md ×3, `asd-dev.md`, `asd-update/SKILL.md`, `asd-phase-impl.md`) (testing.md T-2) | the full explanation drifts back out of sync across sites once any one of the seven is edited alone — exactly the duplication this round's fix collapsed to one owner | static (SSoT-mirror, AC-11-style) | add | new test: `AGENTS.md` alone carries the full parenthetical text; every other named site carries neither that full text nor an independent restatement, only the `providers.md` "Canonical path -> per-provider path" citation. Confirmed fail-first against commit `8423c32` (README.md still carried the full parenthetical duplicated); passes at current HEAD (`8e83182` consolidated it to `AGENTS.md` only) |
| `README.md`'s AC-10 routing-prose hunk ("escalates on a risk declared against the change or a failed check — a risk declared against the artifact alone does not") (testing.md T-2) | restated (T-2): same class as the already-accepted `providers.md:111` row above — a prose mirror of `routeTask`'s unit-tested behaviour, not itself exercised | n/a | none | same reasoning as the `providers.md:111` row: the `runtime.js` AC-10 unit tests pin the *behaviour*; a token-mirror check that this specific prose sentence still describes it accurately was deliberately deferred there and stays deferred here for the identical reason — adding it only for README while leaving `providers.md:111` unguarded would be an arbitrary asymmetry, not a principled fix |
| `.asd/rules/providers.md:41` file-write/never-heredoc paragraph (AC-2) | corrected (T-3): restated as "no dispatcher exists to exercise" in error — this is a statement-durability contract (failure mode: a later edit deletes the sentence, not that a Node dispatcher regresses), the same shape this sprint already accepted a check for at AC-6 (`tests/run.js:3022-3025`) | static (statement-presence, AC-6 style) | add | new coverage: asserts `providers.md` states the never-heredoc rule verbatim — cheap, symmetric with the AC-6 treatment, closes the asymmetry T-3 named rather than resting on the withdrawn "nothing testable" claim |
| `.asd/rules/providers.md:93` `asd-dev` role-scoped-context row edit (AC-7) | corrected (T-3): same asymmetry as AC-2 above — restated honestly as a statement-durability contract, not an untestable one | static (statement-presence, AC-6 style) | add | new coverage: asserts the `asd-dev` role-scoped-context row cites the `review-policy.md` over-engineering/structure-cohesion checklists — same treatment as AC-2, AC-6 |
| AC-14 "no generated view left stale" — no dedicated row previously (testing.md T-4) | corrected (T-4): a partial `--apply` target list still recomputes both hash ledgers whole-repo while leaving another, unnamed target stale, and the suite stays green through that gap — the empty-target guard (C-1) only closes the bare-`--apply` case | CLI (existing real-repo `--check` invariant, `tests/run.js:969` "sync.js --check CLI is green ... (Stage 0)", which runs the `sync.js --check` subprocess with `cwd: REPO_ROOT`, not a temp mini-repo) | keep | the pre-existing Stage-0 test already runs `--check` against the real repo and would fail today if any generated view were stale — it is the same class of real-repo invariant the ledger tests use (`tests/run.js` §6b/§17), just not previously tagged AC-14. The partial-target-list gap T-4 names (canon_hashes/upstream_hashes recompute whole-repo on ANY successful apply, independent of which targets were named) is real but is a `release-manifest.json` upstream-diff-classification concern for `update.js` consumers, not a defect in this repo's own `--check` content-diff (which compares rendered output to actual target bytes directly, never via the hash ledger) — recorded here as an acknowledged, untested edge case rather than closed |
| `.asd/rules/code-style.md` §1 proactive-checklist line | none — restates an existing `review-policy.md` checklist as an authoring instruction; no new mechanism | n/a | none | prose-only, no observable behaviour |
| `.asd/rules/git-strategy.md` orchestrator-commits-bookkeeping line | none — documents an existing commit-ownership split; no scripted orchestrator exists in this repo to exercise | n/a | none | prose-only process rule; not a surface this test suite can exercise (orchestration is LLM-driven, not scripted code) |
| `.asd/rules/providers.md:41` file-write/never-heredoc paragraph (AC-2) | an agent pipes artifact content through a shell heredoc/here-string instead of the write-a-file operation, letting shell quoting corrupt content | n/a | none | restated honestly (T-1): this is an LLM tool-selection instruction, not a scripted code path — no dispatcher in this repo mediates which write mechanism an agent chooses, so there is nothing for a Node test to call. Same class as the git-strategy.md orchestrator-commit row below |
| `.asd/rules/providers.md:93` `asd-dev` role-scoped-context row edit (AC-7) | a role loads the wrong context list, missing the over-engineering/structure-cohesion checklist pointer | n/a | none | prose-only role-context list read by the LLM at dispatch; no role-context loader exists in this repo's Node code to exercise. Same class as the other role-scoped-context rows |
| `.asd/rules/providers.md:111` typed-risk routing prose | restated (T-7): the `runtime.js` unit tests above pin `routeTask`'s *behaviour*, not that `providers.md:111`'s prose still *describes* that behaviour accurately — these are two different failure modes, not one duplicated check | n/a | none | accepted as an unguarded prose↔code mirror for this pass — a token-mirror assertion was considered and deliberately deferred, not added, to keep this entry's scope to the AC-1/6/10/11 boundary `code-style.md` §17 draws for new coverage; this is a recorded residual risk, not equivalent coverage to the row above |
| `.asd/skills/asd-update/SKILL.md:37` wording fix (`node .asd/sync.js --apply` targets generated provider-view paths) | a wording error teaches the removed bare `--apply` form (no target list) | n/a | none | restated (T-6): `tests/run.js:1545`/`:1614` assert `sync.js`'s fail-closed behaviour on an *unmatched* target — they say nothing about this documentation's wording, so citing them as coverage was wrong. Decision stands as `none` because a grep assertion would over-fit — `--apply <file...>` remains valid usage notation at `providers.md:23`, `README.md:110`, `asd-sync/SKILL.md:23-24`, `sync.js:1480` — and because the behaviour a wording regression would encourage (silent no-op) is now itself caught: bare `--apply` exits 1 as of this round's correctness fix |
| `.asd/agents/asd-dev.md:66` matching wording fix | same as above | n/a | none | same restated reason as the row above |
| `.asd/templates/t_plan.md` `Material risk:` line/examples (AC-10 plan-side half) | the template's example grammar (`change:`/`artifact:`/`none`) drifts from `sprint-lifecycle.md`'s "Plan file format" grammar or from `routeTask`'s accepted `target` literals, teaching the wrong syntax | n/a | none | restated honestly (T-2), and re-traced from AC-2 to AC-10 (T-1) — the risk is real, not absent: this sprint's own `plan.md` (lines 45, 52, 73, 87, 102, 110) carries `Material risk:` prose inside `- [x]` checkbox subtasks, a third, off-grammar shape neither canonical statement permits. `sprint-lifecycle.md:302` has since gained an explicit fallback for exactly that case (absent or off-grammar → `change: unclassified`, routes `critical`), which is why the decision still stands at `none`: the fallback, not a test, is what makes an off-grammar line safe rather than silently swallowed. No scripted plan parser exists in this repo to validate the grammar mechanically, and a check that only re-asserts the template's own three example strings would be the no-value pattern `code-style.md` §17 rules out |
| `.asd/workflows/asd-phase-design-review.md`, `asd-phase-impl-review.md`, `asd-phase-impl.md`, `asd-phase-impl-test.md` interrupted/split-dispatch prose (AC-1/AC-6) | a workflow drops or diverges from the trigger/partition/union-property/merge-rule/durable-record contract it cites from `review-policy.md` | static/architecture (content-contract) | add | split out of the former catch-all row (T-3): new §19 test asserts the re-dispatch record's presence in `review-policy.md` and that both `*-review` workflows carry the identical reject-and-re-dispatch-fresh phrase plus the SSoT citation, rather than restating the contract — mirrors the AC-11 SSoT test's style. The manifest-partition union property itself is pinned at the `runtime.js` level (row above) |
| `.asd/workflows/asd-phase-impl-test.md`, `asd-phase-impl-review.md` `derived_handoff` wiring prose (AC-11 / testing.md T-1, iter-03) | **retired, entry 8**: AC-11 closed 2026-09-08 as not delivered — both workflows' `derived_handoff` clauses are deleted along with the field and the rule paragraph they tracked. **Correction (entry 8, iter-04 T-1)**: this row's entry-6/7 text overstated what the entry-6 extension actually reached — see the correction note above "Removed tests" | static/architecture | removed | see "Removed tests" entry 8 |
| `asd-phase-impl-review.md` step 1a/1b preflight-before-manifest reordering (AC-8) | External Review's scope manifest gets assembled before the availability preflight, wasting the build when the provider is unavailable | n/a | none | split out of the former catch-all row (T-3): pure step-ordering prose, verified by direct read (`external-review.md:31` already required this order; step 1b brings the workflow into agreement, no new code path or behaviour to regress) — no scripted workflow-step-order checker exists in this repo to exercise, and step order is not evidence a Node test can assert against without parsing prose as if it were code |
| `.asd/sync.js` bare `--apply` fail-closed guard (correctness.md C-1) | a bare `--apply` (no target list) silently returns `ok:true, applied:[]`, exit 0, **and still refreshes the hash ledger** — a stale generated view then hides behind a green `--check` and green ledger tests indefinitely | unit/CLI (subprocess, mirrors the existing not-found-target CLI test's style) | add | new §19 test: bare `--apply` exits 1, `report.ok === false`, `report.applied` is empty, `report.hashLedger === null`, and both `release-manifest.json`/`sync-state.json` are byte-identical to before the call. Confirmed fail-first: neither this guard nor the exit path existed at entry-1 HEAD (`a0eac63`), so the test fails against that revision and passes at current HEAD |
| `.asd/runtime.js` `riskEntry` reserved-class guard (correctness.md C-4) | a reserved change-risk class (`security`/`authentication`/`migration`/`public contract`/`workflow gate`) declared `target:"artifact"` silently routes on evidence alone instead of failing closed, letting a migration/security task reach `tier: mechanical` | unit (pure function) | add | new §19 test: every reserved name (plus case variant, hyphen variant, underscore variant, double-space variant) typed `artifact` throws `reserved risk class is change by definition`; the same names typed `change` remain valid (unaffected); a name that only contains a reserved word as a substring (`security-audit-tool`) does not throw, proving the match is exact post-normalization, not substring; a genuinely non-reserved artifact name still routes on evidence unaffected. Confirmed fail-first: `RESERVED_CHANGE_RISKS` did not exist at `a0eac63`, so this test fails against that revision |
| `sprint-lifecycle.md` "Plan file format" `Material risk:` absent/off-grammar fallback (correctness.md C-5) | none — restated in the AC-10 plan-side row above (added in entry 1 as `sprint-lifecycle.md:302`'s fallback text); this delta only confirms that row's reasoning still matches current wording | n/a | none | already decided `none` in the existing `.asd/templates/t_plan.md` row above; no scripted plan-grammar parser exists in this repo, so the fallback's safety comes from the rule text itself, not a test. Re-verified this entry: the fallback text is present verbatim at `sprint-lifecycle.md`'s "Plan file format" section |
| `sprint-lifecycle.md` "State recovery" `derived_handoff.head` redefinition — newest commit touching `pathspec` in `<base>..HEAD`, not raw `HEAD` (correctness.md C-6 / testing.md T-1) | **retired, entry 8**: AC-11 closed 2026-09-08 as not delivered — the `head`/`base` formula and the whole cache it derived are deleted; nothing remains to redefine or drift | static/architecture | removed | see "Removed tests" entry 8 |
| `.asd/project/commands.yaml`'s `custom.sync-apply` entry deleted (correctness.md C-3 / documentation.md D-2, entry 5) | before: the registered command was a bare `--apply` (no targets), which the C-1 fail-closed guard turns into a deterministic exit-1 for any agent that ran it verbatim per `asd-dev.md`'s "run `custom.*` commands verbatim" contract — a live footgun. After deletion: no risk of a broken registered command remains, but a future edit could re-add a bare-`--apply` alias and reintroduce the same footgun undetected | n/a | none | no code in this repo parses `commands.yaml` values (`grep -rn commands.yaml .asd/*.js .asd/hooks` finds no reader — it is a machine-readable-but-LLM-consumed fixed-string dictionary, `asd-dev` reads it at dispatch, no Node dispatcher exists to exercise); confirmed no rule, workflow, or template still references `sync-apply` (`grep -rn sync-apply` outside this sprint's own history/decision docs returns nothing). A test asserting the key's mere absence would be the no-value coverage-number pattern `code-style.md` §17 rules out — it can't fail on the actual risk (a *re-added* bare alias), only on the current snapshot. `.asd/project/**` is normally excluded from self-hosting review scope, but this file changed and a dev did author it under a one-off write authorization (decisions-log 2026-09-08); the honest position is that no automated check reaches an LLM-consumed dictionary of fixed command strings |
| `review-policy.md` "Union property, checked before merge" — condition (c) (unreachable) deleted, the surviving out-of-half-predicate condition relabelled (c) (AC-13 / external.md X-2) | a two-half split silently drops coverage of a rule/section id that neither half's ledger actually evidenced | n/a (explicitly scoped as manual, `validate-partition` deliberately not built this sprint — decisions-log 2026-09-08) | none | restated (X-2): this round deleted the former condition (c) — a half reaches merge only after `validate-ledger`, which already rejects a mismatched digest, so it could never fail where it runs — and restated the surviving out-of-half-predicate condition (now labelled (c)) to exclude an id the unpartitioned manifest already authorizes `n/a` under a different predicate, which may legitimately be `n/a` in both halves without blocking the merge. No `validate-partition` helper exists in `.asd/runtime.js` (confirmed: `grep -n validate-partition .asd/runtime.js` finds nothing) and the user decision on 2026-09-08 was explicitly not to build it this sprint. The existing AC-1/6 `validateCoverageLedger` test (row above) still covers what code does exist (each half validates against its own manifest; a partial ledger against the unpartitioned manifest throws). Per X-2: this `none` is the honest scoping half — condition (c) remains a manual orchestrator check pending automation, not a hidden regression, and this record states that plainly so the sprint's DoD claim matches what shipped |
| `review-policy.md` reviewer read-only reconciliation ("cannot write at all" → scoped artifact-write claim + `memory: project` channel) vs. `providers.md`'s citation of it (AC-15, entry 8) | the reconciled claim's two halves (what reviewers cannot write; the one channel they do use) drift apart again — either a future edit re-widens `review-policy.md`'s claim back to a false absolute, or `providers.md` stops citing it and independently restates a copy that then disagrees | static (statement-presence + anti-restatement mirror, AC-11/AC-6-style) | add | new §19 test: `review-policy.md` states both halves of the reconciled claim ("Reviewers write no review artifact, code or doc" and the `memory: project` carve-out); `providers.md` states the artifact-level grant fact it independently owns (tool config) and cites `review-policy.md` "Gate Verdict Format" rather than restating the reconciliation sentence itself |
| `providers.md` host-verified vs. emitted-on-trust agent-frontmatter field list (AC-15, entry 8) | the distinction (previously a `sync.js` code comment, banned by `code-style.md` §8) has no rule-doc home and silently stops being maintained once nobody reads the comment it was moved out of | static (statement-presence) | add | new §19 test: `providers.md` names the host-honoured fields and states `effort`/`maxTurns` are emitted on trust, not verified by either host |
| `.asd/sync.js`, `.asd/skills/asd-update/update.js` non-English (Russian) comments quoting a project plan document (AC-15, entry 8) | a future edit reintroduces non-English or document-quoting text into these two files, repeating the exact `language-policy.md`/`code-style.md` §8 violation this round fixed | static (content-contract, mechanical) | add | new §19 test: neither file contains a non-Latin-script character (Cyrillic/Greek/Hebrew/Arabic/Devanagari/Thai/CJK/Hangul ranges), banning the specific regression class (Russian text) cheaply and durably. **Scope decision, deliberate**: does NOT also ban the remaining **English** references to a project plan document in the same two files (`sync.js` comments at "the plan fixes...", "plan: ..." citations; `update.js`'s doc-block references) — same `code-style.md` §8 class, but outside AC-15's authorized wording, pre-existing since commit `11710df` (long before this sprint), and already flagged as unfixed in `sprint.md` AC-15 and the dev's own decisions-log entry rather than silently missed. Adding a test that fails against currently-shipped, out-of-scope code would misrepresent this entry's own gate as red on a defect nobody authorized fixing this round; recorded here as a named residual risk for a future sprint's scope, not a `D-N` (not a regression introduced this round, not in this sprint's authorized change surface) |

## Removed tests

In-scope partial removal (T-4): four lines inside "AC-1/6: a reviewer split partitions..." were deleted as tautological, not the whole test. `unionFiles = [...half1.files, ...half2.files].sort()` compared against `allFiles` — and `half1.rules`/`half1.sections` compared against `whole.rules`/`whole.sections` — were assembled from the same hand-written literal arrays on both sides of each assertion, so all four passed regardless of any change to `runtime.js`, `review-policy.md`, or the workflows; they asserted the fixture against itself. No scripted partitioner exists in this repo to make the halves the *output* of the documented partition rule instead of literals, so removal (not reformulation) was the honest fix. The rest of the test — each half validating unchanged against its own complete manifest, and the `files rows incomplete` throw against the unpartitioned manifest — stays; it does exercise `validateCoverageLedger`.

Otherwise none. No test in or out of the change scope met a removal criterion (`code-style.md` §17) — nothing trivial, duplicate, mock-confirming, implementation-coupled, or flaky was found among impacted tests.

Entry 3: none. No test met a removal criterion this delta either.

Entry 4: none. Entry 5: none — no test in or out of scope met a removal criterion; the one change this entry made to existing tests was an in-place extension (`otherSites` array), not a removal.

Entry 6: none. The AC-11/T-1 wiring test was extended in place (see Added tests below), not removed and re-added — no test met a removal criterion this delta.

Entry 7: none. Bookkeeping-only delta, nothing to prune.

Entry 8 (impl-review iter-04 review-fix, testing.md T-1/T-2): AC-11 was retired 2026-09-08 (decisions-log) after three review rounds established its purpose unreachable by construction; `derived_handoff` is deleted entirely — the state field, its rule paragraph, and both workflows' wiring. This is a **retirement, not a coverage regression**: the criterion it guarded no longer exists, and canon carries nothing left for a test to check. Four tests removed:
- `"AC-11: t_state.json ships derived_handoff as an empty object..."` — the field is gone.
- `"AC-11: derived_handoff's shape/validity rule lives ONLY in sprint-lifecycle.md..."` — the SSoT paragraph and both workflow citations are gone.
- `"AC-11/T-1: the derived_handoff wiring ... track sprint-lifecycle.md 'State recovery' by construction..."` — the wiring it tracked is gone.
- `"AC-11: SessionStart output is byte-identical whether state.json.derived_handoff is absent, well-formed, or malformed..."` — this one still passed (the hook never read the key) but was already vacuous before AC-11's own retirement: it proved only that the hook ignores an unrecognized key, never anything specific to `derived_handoff`. Judged against `code-style.md` §17's hypothetical-risk criterion: no identified failure mode or stated requirement supports a generic "hook tolerates any unknown state.json key" claim (the hook's plain destructuring style makes that trivially true regardless of implementation, and no schema-validation mechanism exists that could plausibly break it) — **removed, not repurposed**, rather than kept under a name that no longer names anything real.

**Correction (T-1, iter-04) to the durable record — the orchestrator should append this to `decisions-log.md`** (this tester cannot edit that file): the 2026-09-08 "impl fix for iter-03" entry's note — *"the AC-11 guard was rewritten ... so it now fails on a prose-phrased contradiction as well as the backticked shape that previously slipped through"* — and this file's own Entry-6 row/Added-tests description of the same test overstated what shipped. `tests/run.js:2951-2956`'s testing.md review (iter-04, T-1) established the widened guard was real but narrower than claimed: the `git`/`HEAD` ban applied only to the fragment containing `derived_handoff`, not the whole workflow file the deleted predecessor test scanned; `stepLines` saw only top-level `^\d+\. ` lines, missing the sub-bullet the "one reader" fix actually landed in; the invariant was never checked against the other nine workflow files; and a same-shaped prose formula in lowercase (`head is the newest commit...`) passed because the ban was case-sensitive on `HEAD`. The guard was real on the narrow shapes it named, but `Removed tests: none` recorded against that same round was inaccurate — the four-line tautological removal from the AC-1/6 partition test (entry 2, T-4) predates it and was already recorded, but no entry recorded that the AC-11 guard itself had been *narrowed* (predecessor whole-file `git`/`HEAD` regex deleted, not extended) while being described as extended. This correction is now moot as a live coverage gap — the guard and everything it covered is retired above — but the record must say what actually happened rather than what was claimed.

## Added tests

Level and AC/risk covered are visible in the test file itself (name, path) — not restated here. All twelve live in `tests/run.js`, new §19, appended after §18 (existing numbering collisions at `// 7.` and `// 15.` left untouched per instruction). `buildManifest` (shared fixture builder, no digest attached) is hoisted above §15 and reused by `validCoverageFixture` and the pre-existing CLI `manifest-digest` test, per efficiency E-5 — not a new test, not listed below.

| Test | Regression proof |
|---|---|
| `tests/run.js`: "AC-1/6: a reviewer split partitions the manifest files list into two disjoint halves..." | corrected (T-9): design-conformance proof, not new-code-path coverage — `validateCoverageLedger` itself is unchanged by this diff; the test proves the split-dispatch partition scheme this sprint's rule text newly authorizes is compatible with that existing, untouched function. It never reads `review-policy.md`, would pass unchanged at base HEAD, and does not guard against the rule's own prose drifting from this behaviour |
| `tests/run.js`: "AC-10: a typed target:\"change\" risk routes exactly like the legacy bare-string form" | n/a — new coverage of new `riskEntry` typed-object path |
| `tests/run.js`: "AC-10: a typed target:\"artifact\" risk never escalates by itself..." | n/a — new coverage |
| `tests/run.js`: "AC-10: the no-downgrade clamp outranks an artifact-risk reason..." | n/a — new coverage |
| `tests/run.js`: "AC-10: routing fails closed on every malformed risks shape..." | n/a — new coverage |
| `tests/run.js`: "AC-10: a mixed array of one change risk and one artifact risk still routes critical..." | n/a — new coverage |
| `tests/run.js`: "AC-10: a failed objective check with a correction attempt outranks a co-occurring artifact risk in the reason..." | added for C-3/T-8: pins the `reason` precedence boundary the ternary chain gives `failedObjectiveCheck && attempted >= 1` over `artifactRisk` — a reorder would mislabel an escalated task's evidence in `state.json.task_routing` without changing `tier` |
| `tests/run.js`: "AC-10: an artifact risk whose priorTier does not outrank the computed tier keeps the artifact-risk reason..." | added for C-3/T-8: pins that the no-downgrade clamp does not fire merely because `priorTier` is set — only when it outranks the computed tier — so the reason stays `artifact-risk:<name>` rather than falsely reporting `no-downgrade` |
| `tests/run.js`: "AC-11: t_state.json ships derived_handoff as an empty object..." | n/a — new coverage of new template field |
| `tests/run.js`: "AC-11: SessionStart output is byte-identical whether state.json.derived_handoff is absent, well-formed, or malformed..." | n/a — new coverage, guards against future coupling. Anchored (T-5): also asserts the shared output pins this fixture's sprint id/phase/branch, so the three-way equality is known to be comparing the real active-sprint summary path rather than three vacuously identical "no active sprint" strings |
| `tests/run.js`: "AC-11: derived_handoff's shape/validity rule lives ONLY in sprint-lifecycle.md \"State recovery\"..." | n/a — new coverage, mirrors §16's SSoT-mirror style |
| `tests/run.js`: "AC-6: the interrupted-dispatch re-dispatch record is stated in review-policy.md and cited (not restated) by both *-review workflows" | added for C-7: AC-13 named this as required new coverage; previously only the manifest-partition union property (AC-1/6) and the `derived_handoff` shape (AC-11) were pinned, leaving AC-6's re-dispatch record as the sole named-but-untested surface. Mirrors the accepted AC-11 SSoT test's style |

Entry 3 additions (2, both new §19 tests, appended after the entry-2 additions above):

| Test | Regression proof |
|---|---|
| `tests/run.js`: "sync.js CLI: bare --apply with no targets fails closed (exit 1) and skips the hash-ledger recompute..." | added for C-1: confirmed fail-first against `a0eac63` (bare `--apply` returned `ok:true, applied:[], exit 0` and still refreshed the ledger there — no guard existed); passes at current HEAD where `--apply` with an empty target list now exits 1 before `runApply`/`recomputeAndWriteHashLedgers` |
| `tests/run.js`: "AC-10: a reserved change-risk class name declared target:\"artifact\" fails closed, case- and separator-normalized, without over-matching a merely-similar name" | added for C-4: confirmed fail-first against `a0eac63` (`RESERVED_CHANGE_RISKS` did not exist, no reserved name was rejected); passes at current HEAD. Also proves the normalization boundary both ways — reserved names in case/hyphen/underscore/double-space variants all still match, and a merely-similar substring name (`security-audit-tool`) does not falsely match |

Entry 4 additions (6, all new §19 tests, appended after the entry-3 additions above):

| Test | Regression proof |
|---|---|
| `tests/run.js`: "AC-11/T-1: neither asd-phase-impl-test.md nor asd-phase-impl-review.md ever inlines an alternate \`head\` = \`git ...\` formula for derived_handoff..." | added for T-1: confirmed fail-first against `a9030be` (`asd-phase-impl-review.md` step 13 stated \`head\` = \`git rev-parse HEAD\`, contradicting sprint-lifecycle.md's formula); passes at current HEAD (`8e83182` deleted the inline formula) |
| `tests/run.js`: "T-2: \`.claude/agent-memory/**\` is stated as NOT excluded from the self-hosting review scope everywhere the exclude_paths list itself is restated..." | added for T-2: confirmed fail-first against `591abfb^` (`external-review.md` and `t_prompt-external-impl.md` carried no such statement) |
| `tests/run.js`: "T-2: AGENTS.md is sole SSoT for the --apply <generated-view-path...> explanatory parenthetical..." | added for T-2: confirmed fail-first against `8423c32` (README.md still restated the full parenthetical); passes at current HEAD (`8e83182` consolidated to AGENTS.md alone) |
| `tests/run.js`: "T-2/AC-3: artifact-layout.md documents the <reviewer>.part-N.md split-review naming and the sprint-folder-purity statement..." | added for T-2/AC-3: confirmed fail-first against sprint base `39709446` (none of the six asserted substrings existed pre-sprint) |
| `tests/run.js`: "T-3/AC-2: providers.md states the never-heredoc file-write rule for artifact content" | added for T-3: confirmed fail-first against sprint base `39709446` (statement did not exist pre-sprint) |
| `tests/run.js`: "T-3/AC-7: providers.md's asd-dev role-scoped-context row cites the review-policy.md over-engineering/structure-cohesion checklists" | added for T-3: confirmed fail-first against sprint base `39709446` (statement did not exist pre-sprint) |

Entry 5: no new test added — the pre-existing T-2 AGENTS.md-SSoT test (row above) was extended in place, not duplicated. `otherSites` gained `.asd/project/custom-coding-rules.md`; test count stays 156. Confirmed fail-first against `d902245` (that file still carried the full `--apply` parenthetical there; `git show d902245:.asd/project/custom-coding-rules.md` line 14).

Entry 6 additions (3 new §19 tests; the AC-11/T-1 test above is extended in place, not duplicated — same test name/location, broadened assertions):

| Test | Regression proof |
|---|---|
| `tests/run.js`: "AC-11/T-1: the derived_handoff wiring ... track sprint-lifecycle.md 'State recovery' by construction ..." (extended in place) | resolved T-1 (iter-03): derives the canonical writer/reader file+step directly from the rule's "Written once, at \`<workflow>\` step N's green exit ... Read once, by \`<workflow>\` step N" sentence, asserts `derived_handoff` appears in exactly that step of each workflow, asserts the writer's clause carries a write verb and the reader's a read verb (never the reverse), and bans `git`/`HEAD` tokens from either clause — catching a prose-phrased contradiction, not only a backticked one. Confirmed fail-first against pre-fix HEAD `5ca4d30` (the two-writer wiring predates this shape of the rule sentence) |
| `tests/run.js`: "T-2: feedback_no-shell-review-method.md cites asd-reviewer-testing.md's frontmatter for the reviewer's tool grant instead of re-enumerating it..." | added for T-2/D-2: confirmed fail-first against pre-fix HEAD `5ca4d30` (memory enumerated `Read/Glob/Grep/Write`, no frontmatter citation) |
| `tests/run.js`: "T-2/C-2: canon_hashes covers only .asd/agents/*.md and .asd/skills/*/SKILL.md..." | added for T-2/C-2: pins the mechanism the memory's corrected three/two/one collateral-count arithmetic depends on; not fail-first against `5ca4d30` (mechanism itself is unchanged by this round's fix — this test guards future mechanism drift, not the prior wrong-count claim, which the citation-based test above and direct read already correct) |
| `tests/run.js`: "T-2: asd-dev-critical/MEMORY.md's index entries each link to a file that exists in the same directory..." | added for T-2 (documentation.md scope): confirmed fail-first against pre-fix HEAD `5ca4d30` (`project_crlf-canon-edits.md` did not exist yet, so the index could not yet list it) |

Entry 7: no new test added — bookkeeping-only delta (`friction-log.md`, `decisions-log.md`, `state.json`), no rule/workflow/code change to cover. Test count stays 159.

Entry 8 (impl-review iter-04 review-fix, testing.md T-1/T-2, AC-11 retirement/AC-15 addition): 4 removed (see "Removed tests" above), 1 in-place reformulation (no count change), 3 new — net 159 − 4 + 3 = 158. Not appended as a further §19 block — the AC-15 tests are placed immediately after the last surviving §19 test, no new numbering needed.

| Test | Regression proof |
|---|---|
| `tests/run.js`: "T-2: feedback_no-shell-review-method.md cites asd-reviewer-testing.md's frontmatter for the reviewer's tool grant instead of re-enumerating it..." (reformulated in place, same name/location) | resolved T-2 (iter-04): replaced `assert.strictEqual(memory.match(/\bWrite\b/), null, ...)` (under-inclusive against any other casing/phrasing of a re-enumerated grant; over-inclusive against a correct negative statement using the word "Write") with a case-insensitive scan for any tool-name enumeration (2+ known tool tokens joined by `/` or `,`) and asserts every named tool is present in the cited frontmatter's actual grant. Confirmed fail-first against pre-fix HEAD `5ca4d30` (memory enumerated `Read/Glob/Grep/Write`, which the new scan catches as `Write` not in `[Read, Glob, Grep, AskUserQuestion]`); passes at current HEAD (memory cites frontmatter, no enumeration present to flag); also confirmed the landed prose (containing the word "write" inside "a separate write channel it does use", not an enumeration) does not false-positive |
| `tests/run.js`: "AC-15: review-policy.md is sole SSoT for the reviewer read-only reconciliation..." | n/a — new coverage: asserts `review-policy.md` states both halves of the reconciled claim and `providers.md` cites it (`Gate Verdict Format`) rather than restating it |
| `tests/run.js`: "AC-15: providers.md records which emitted agent frontmatter fields are host-verified vs. emitted on trust" | n/a — new coverage: asserts the `effort`/`maxTurns`-on-trust distinction now lives in `providers.md`, not only in the deleted `sync.js` comment |
| `tests/run.js`: "AC-15: .asd/sync.js and .asd/skills/asd-update/update.js carry no non-Latin-script text..." | added for AC-15: confirmed fail-first against pre-fix commit `4904805^` (both files carried Cyrillic text quoting a project plan document — `sync.js`: "Digest и нормализация", "останавливает sync до первой записи"; `update.js`: "пофайловая state machine"); passes at current HEAD. Deliberately scoped to non-Latin script only — does not also assert against the remaining **English** plan-document references in the same two files (out of AC-15's authorized wording; see the risk→check row's reasoning) |

## Suite run

- Command: `node tests/run.js` (`test` from commands.yaml) — this repo has no `test_affected` selector and a single flat runner file, so every invocation runs the whole file; the **safety valve independently fires** for this entry regardless, since the change surface touches shared/framework-wide infrastructure (`.asd/runtime.js`, `.asd/rules/**`, `.asd/templates/**`, `.asd/workflows/**`, `.asd/release-manifest.json` — self-hosting means these ARE the framework). Impacted-set definition used: search-derived set (diff test files + reference search + AC-tag search) → safety valve fires on shared-infrastructure touch → degrades to full suite. Mechanically identical outcome to `impl-review`'s later full-suite run, but recorded here as the `impl-test` gate, per `sprint-lifecycle.md` "Impacted test set".
- Scope: impacted (safety valve fired → full suite, ran seven times this sprint: 148 at entry 2, 150 at entry 3, 156 at entry 4, 156 at entry 5, 159 at entry 6, 159 at entry 7, 158 at entry 8)
- Entry 2 result: pass — 148/148 passed, 0 failed, 0 skipped (136 pre-existing + 9 iter-01 added − 4 tautological assertion lines removed from one test (T-4, not a whole test) + 3 review-fix additions (2× T-8 boundary cases, 1× C-7 re-dispatch-record test) = 148 top-level `test(` declarations)
- Entry 3: pre-strategy run (before authoring) was 148/148 pass, 0 failed — confirming entry 2's tests still hold against the C-1/C-4/C-5/C-6/review-policy fix commits before any new test was added. Post-authoring result: pass — 150/150 passed, 0 failed, 0 skipped (148 + 2 new: bare-`--apply` fail-closed CLI test (C-1), reserved-risk-class guard unit test (C-4)). Both new tests confirmed fail-first against entry-1 HEAD `a0eac63` (neither guard existed there)
- Entry 4: pre-strategy run (before authoring) was 150/150 pass, 0 failed against `f17ee2e` (dev round `8e83182` reconciled, no assertion broken). Post-authoring result: pass — 156/156 passed, 0 failed, 0 skipped (150 + 6 new: AC-11/T-1 head-formula guard, T-2 agent-memory-not-excluded mirror, T-2 AGENTS.md --apply-parenthetical SSoT, T-2/AC-3 artifact-layout/README/t_review.md statement mirror, T-3/AC-2 heredoc statement, T-3/AC-7 role-context-checklist statement). All six confirmed fail-first (commits cited in Added tests above)
- Entry 5: pre-strategy run (before authoring) was 156/156 pass, 0 failed against `9c36503` — confirming entry 4's tests still hold through the bookkeeping-only commits (`b16eac3`, `f17ee2e`'s own descendant `32baa3f`, `9c36503`) that closed the iter-02 fix round. Post-authoring result: pass — 156/156 passed, 0 failed, 0 skipped (0 net new — one existing test's `otherSites` list extended to cover `custom-coding-rules.md`, confirmed fail-first against `d902245`)
- Entry 6: pre-strategy run (before authoring) was 156/156 pass, 0 failed against `caed0e7` (pre-fix HEAD `5ca4d30` plus the orchestrator's own iter-03 review-record commits, no assertion broken by the review-fix commits `c8e7a2e`/`09c9abc`/`89a605c` on top). Post-authoring result: pass — 159/159 passed, 0 failed, 0 skipped (156 + 3 new: T-2 tool-grant-citation test, T-2/C-2 canon_hashes tree-scope test, T-2 MEMORY.md index-link test; the AC-11/T-1 wiring test was extended in place, net zero on count). All three new tests confirmed fail-first against pre-fix HEAD `5ca4d30`; the extended AC-11/T-1 test confirmed fail-first there too (commits cited in Added tests above)
- Entry 7 result: pass — 159/159 passed, 0 failed, 0 skipped, re-run against `090253d` (no new test — bookkeeping-only delta, see Entry log/Added tests)
- Entry 8: pre-strategy run (before this round's authoring) was 156/159 pass, 3 failed, 0 skipped against `4904805` — the dev round retired AC-11/`derived_handoff` from canon but the three now-orphaned tests were left red by design, pending this dispatch. Post-authoring result: pass — 158/158 passed, 0 failed, 0 skipped (159 − 4 removed (3 red-by-retirement + 1 vacuous, see "Removed tests") + 3 new AC-15 tests; the T-2 tool-grant test was reformulated in place, no count change). All three new AC-15 tests confirmed fail-first against `4904805^`/current-HEAD pairs cited in Added tests above; the reformulated T-2 test confirmed fail-first against pre-fix HEAD `5ca4d30`
- Lint / build: pass — `git diff --check` clean (exit 0); `node .asd/sync.js --check` reports `"ok": true` for every generated target (both re-run at entry 8, current HEAD)
- HEAD: entry 6 analysed the delta since entry 5 (`9c36503...081fa38`, the iter-03 review-fix commits `c8e7a2e`/`09c9abc`/`89a605c`) and its own commit was `081fa38`. Entry 7 analysed the delta since entry 6 (`081fa38...090253d`) and found it bookkeeping-only (`caed0e7`, `090253d`) — no code/rule/workflow/test change, nothing to author, suite re-confirmed 159/159 unchanged at HEAD `090253d`. Entry 8 (this dispatch) analysed the delta since entry 7 (`090253d...4904805`, plus the orchestrator's own `89ac144`/`3dada06`/`f667810` bookkeeping) and its own HEAD-analysed placeholder is left `TBD` per this dispatch's own convention, filled by the next re-entry once this tester's own commit hash is known

## Defects

| # | Location | Symptom | Failing test | Status |
|---|---|---|---|---|
| D-1 | `.asd/workflows/asd-phase-impl-review.md` step 13 (pre-fix, commit `a9030be`) | `derived_handoff.head` was written as `git rev-parse HEAD` at phase-exit, contradicting `sprint-lifecycle.md` "State recovery"'s formula (`git log -1 --format=%H <base>..HEAD -- <pathspec>`, never raw `HEAD`), so a next-phase reader comparing `head` per the rule never matched and the cached `files` list was dead on arrival | new AC-11/T-1 test ("neither asd-phase-impl-test.md nor asd-phase-impl-review.md ever inlines an alternate \`head\` = \`git ...\` formula...") fails against `a9030be`, passes at current HEAD | fixed — dev round commit `8e83182` deleted the inline formula and cites the rule instead; `Defects: None` in the prior entry-3 record was inaccurate for this surface (testing.md T-1) |

No other code defect found — all 158 tests pass against the implementation as delivered (entry 8).

## Manual verification (optional)

None. This sprint's change surface is entirely rule/workflow/template prose plus three pure-function/data surfaces (`routeTask`, `validateCoverageLedger`, `t_state.json`/hook interaction) — no visual UI, no third-party live integration, no UX-feel surface exists to verify manually.
