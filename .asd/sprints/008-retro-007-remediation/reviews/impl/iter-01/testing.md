[REVIEW-impl-testing]: CONCERNS

# Review — testing

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low — all severities reported)
- **Method note**: read-only reviewer, no shell. The diff payload was read in full and every claim corroborated by direct file reads. Suite-run record corroborated structurally: `tests/run.js` carries exactly 145 top-level `test(` declarations, matching 145 = 136 + 9; §6b (`tests/run.js:1919`, `:1930`) recomputes both hash maps against disk on every run, so a stale ledger entry cannot be green. The `tests/run.js` diff is a single append-only hunk (`@@ -2859,6 +2859,158 @@`, +152/-0), so "Removed tests: None" is verified, not merely accepted.

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| T-1 | medium | `test-plan.md` "Risk → check decisions" (all 14 rows) vs `providers.md:40` | AC-2's actual change — the file-write/never-heredoc paragraph added to `providers.md` — has **no row at all**: neither `add` nor `none`. The only `providers.md` row covers the typed-risk routing prose. The `asd-dev` role-scoped-context row edit (`providers.md:96`, AC-7) is likewise unlisted. Compounding it, the `t_plan.md` row is mis-traced to "(AC-2)" when its content is AC-10's plan-side half — so the table *appears* to cover AC-2 while the AC-2 change is undeclared. `code-style.md` §17 requires a recorded decision per change; an unlisted change is indistinguishable from an overlooked one. | Add explicit rows for the `providers.md` file-write paragraph (AC-2) and the `asd-dev` role-context row (AC-7) with their own honest decisions, and correct the `t_plan.md` row's trace from AC-2 to AC-10. |
| T-2 | medium | `test-plan.md` row "`t_plan.md` `Material risk:` line/examples" | The row's Material-risk cell states a real risk ("drift between `t_plan.md`'s example grammar and `sprint-lifecycle.md`'s … could teach the wrong syntax"), then its Reason concludes "no qualifying material risk under §17" — self-contradictory within one row. "No scripted plan parser" answers the wrong question: the risk is a **three-way mirror** (`t_plan.md` examples ↔ `sprint-lifecycle.md:298` grammar ↔ `runtime.js:38`'s accepted `target` literals), exactly what this repo machine-checks elsewhere (§16 phase-chain mirrors, §18 `t_retrospective.html` contract, and this sprint's own accepted `derived_handoff` SSoT test). Drift is live, not hypothetical: `plan.md:45,52,73,87,102,110` carries `Material risk:` **inside `- [x]` checkbox subtasks in untyped prose form**, a third shape both new canonical statements forbid. No test references `t_plan.md` at all. | Either restate the reason honestly (declaring the mirror risk accepted, and why), or cover the mirror: the template's declared risk kinds must be exactly the kinds `sprint-lifecycle.md` defines and exactly the `target` values `routeTask` accepts. Do not add a check that merely re-types the template's own sentences. |
| T-3 | medium | `test-plan.md` catch-all row covering `design-review.md`, `impl-review.md`, `impl.md`, `impl-test.md` | One blanket `none` covers four files and three distinct contracts (split/interrupted dispatch, `derived_handoff` handoff, AC-8's preflight-before-manifest reordering) — none assessed individually, so AC-8's change has no decision of its own. The waiver reason ("would restate the diff, implementation-coupled") is the same objection that did **not** block the `derived_handoff` SSoT test, which was accepted and is the strongest of the nine. Meanwhile the split-dispatch procedure is now restated near-identically in both `*-review` workflows, both declaring `review-policy.md` "sole SSoT … not restated here" — a larger, verdict-affecting duplication surface than the one judged test-worthy. | Split the catch-all into per-contract rows with individual decisions. For the split-dispatch prose apply the standard already applied to `derived_handoff`: either pin the two workflow copies against `review-policy.md`, or record explicitly why this larger duplication is accepted while the smaller one was not. |
| T-4 | low | `tests/run.js:2626-2629` | The "union property" assertions are tautological: `unionFiles` is assembled from `half1.files`/`half2.files`, which are the hand-written literals `allFiles` was written from, and `half1.rules`/`half1.sections` are compared to `whole.rules`/`whole.sections` built from the same variables. These four lines pass regardless of any change to `runtime.js`, `review-policy.md` or the workflows — they assert the fixture against itself. The rest of the test is load-bearing: both halves validating against their own complete manifests, and the `files rows incomplete` throw at `:2633-2637`, do exercise `validateCoverageLedger`. | Remove the self-proving assertions as noise, or make the halves the *output* of the documented partition rule applied to the whole manifest rather than literals. Since no scripted partitioner exists, removal is the honest option. |
| T-5 | low | `tests/run.js:2981-2998` | The three-fixture SessionStart test asserts only that the three outputs equal **each other**, never that the output is the sprint-aware summary. Today the fixture does reach the active-sprint branch, but if it ever stops matching — state-shape change, root-resolution change, `findActiveSprints` rejecting the record — all three variants collapse to the identical "no active sprint" string and the test still passes while covering nothing. A guard test that can go vacuous silently is the failure mode this rubric treats as worse than no test. | Anchor non-vacuity: assert the shared output actually pins the fixture (sprint id / phase / branch) so the equality assertion is known to be comparing the real summary path. |
| T-6 | low | `test-plan.md` rows "`asd-update/SKILL.md` wording fix" and "`asd-dev.md` matching wording fix" | The recorded reason — "behaviour already covered by §11's orphan-detection/apply-target tests" — attributes coverage that does not exist for this risk. `tests/run.js:1545` and `:1614` assert `sync.js` fails closed on an unmatched `--apply` target; neither says anything about the AC-5 documentation wording. The decision `none` is itself defensible (the `<file...>` form legitimately survives as usage notation at `providers.md:23`, `README.md:110`, `asd-sync/SKILL.md:23-24`, `sync.js:1480`, so a grep assertion would over-fit), but recurrence is live: this sprint's own `plan.md:34,116` still instructs `--apply <file...>`. | Restate the reason as what it is (no coverage; a text assertion rejected as over-fitting because the `<file...>` form remains valid usage notation), or scope an assertion to the eight sites AC-5 enumerates. Do not cite unrelated tests as coverage. |
| T-7 | low | `test-plan.md` row "`providers.md` typed-risk routing prose" | Reason "duplicate of the `runtime.js` row" conflates two things: the unit tests pin `routeTask`'s behaviour, not that `providers.md:104-111` still *describes* that behaviour. The prose is what the orchestrator reads at dispatch time and names exact tokens that live in code (`artifact-risk:<name>`, `change`/`artifact`); drift misleads dispatch without failing any test. Same prose↔code binding class the accepted `derived_handoff` SSoT test covers. | Record the honest reason (mirror deliberately unguarded, and why), or add the token-mirror assertion in the idiom already at `tests/run.js:3000-3012`. |
| T-8 | low | `tests/run.js:2946-2974` vs `.asd/runtime.js:175` | Reason precedence around the new `artifact-risk` branch is untested at its boundaries: no case pins `failedObjectiveCheck && attempted >= 1` **plus** an artifact risk (must yield `failed-objective-check`, not `artifact-risk:…`), and none pins an artifact risk with `priorTier` equal to or below the computed tier (clamp must not fire). A reorder of the ternary chain would relabel an escalated task as "a risk was seen and deliberately not escalated" while the tier stays critical — misleading routing evidence persisted into `state.json.task_routing[taskId]`. The malformed-shape table itself is genuinely complete for `riskEntry`, and the `execution: agent` fail-closed cases at `:2668-2671` really would fail if the `risks.length === 0` guard were removed. | Cover the two reason-precedence boundaries; the tier is already safe, the mislabelled `reason` is the observable defect. |
| T-9 | low | `tests/run.js:2604-2638`; `test-plan.md` "Added tests" row 1 | The AC-1/6 test never reads `review-policy.md`. It proves the partition scheme is compatible with the *existing, untouched* `validateCoverageLedger`, so AC-1's load-bearing clause — "two partial ledgers against one manifest … are never the mechanism" — is unguarded against drift in the rule text this sprint actually wrote. Consequently the regression-proof cell ("new coverage of new code path") overstates: there is no new code path, `validateCoverageLedger` is unchanged by this diff and the test would pass at base HEAD. | Correct the regression-proof cell to describe it as a design-conformance proof, and consider binding the rule text in the same idiom as the accepted `derived_handoff` SSoT test. |

## Verified without finding

`ac-4` — `tests/run.js:1545` (API level: `not-found`, whole batch aborted, no partial write) and `:1614` (CLI level: exit 1, `ok:false`, hash-ledger recompute skipped) do assert the unmatched `--apply` target failing closed twice, so the "already satisfied at HEAD, no new test" predicate holds. Release-manifest `none` is honest: §6b recomputes both hash maps against disk, so a stale value cannot pass. `git-strategy.md` orchestrator-commit `none` is honest — orchestration is LLM-driven, no scripted surface. `artifact-layout.md` Agent-memory `none` is sound in effect: the live `.claude/agent-memory/**` tree is already inside the `sync.js --check` all-items-current assertion. Determinism: every new fixture uses `fs.mkdtempSync`, no test reads the repo's live `state.json`, no ordering or shared-state coupling between the nine.

## Coverage

Validated compact ledger: [`testing.ledger.json`](./testing.ledger.json) against immutable manifest [`testing.manifest.json`](./testing.manifest.json), findings [`testing.findings.json`](./testing.findings.json). `node .asd/runtime.js validate-ledger` → `{"ok":true}`. 19/19 files, 14/14 rules, 5/5 sections resolved; `stub-resolution` and `manual-verification` `n/a` per manifest-authorized predicates, `ac-3`/`ac-7`/`ac-9` `n/a` as recorded-`none` decisions, `ac-4` `n/a` as closed at scope.

## Verdict

CONCERNS: 9

## Next action

Route to `impl` review-fix mode. Two groups, no dependency between them:

- **`test-plan.md` decision-record edits** (T-1, T-2, T-3, T-6, T-7, T-9): add the missing AC-2/AC-7 rows, correct the AC-2→AC-10 trace, split the four-workflow catch-all into per-contract rows, and restate the three reasons that either contradict themselves or cite coverage that does not exist. Where a `none` stands after restatement, it stands — the requirement is an honest reason, not a new test.
- **`tests/run.js` §19 edits** (T-4, T-5, T-8), owner `asd-tester`: drop or ground the tautological union assertions, anchor the SessionStart test against a vacuous pass, and add the two reason-precedence boundary cases.

No manual verification is required or requested: the change surface is rule/workflow/template prose plus three pure-function/data surfaces, so `test-plan.md`'s "Manual verification: None" is correct.

## Escalations

None.
