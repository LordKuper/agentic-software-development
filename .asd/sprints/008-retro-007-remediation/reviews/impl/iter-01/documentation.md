[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low — all severities reported)

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| D-1 | medium | `asd-phase-impl-review.md:48,50` and `asd-phase-design-review.md:40,42` vs home `review-policy.md:140,142` | Both workflows open step 7a/8a with "`review-policy.md` … is the **sole SSoT** for trigger, partition, union property and merge rule, **not restated here**" — and then restate the trigger ("Second consecutive interruption on the same digest → split") and near-verbatim the partition recipe ("own file half, full `rules`/`sections`, `n_a` filtered to the ids that half keeps" vs the home's "own file half, full rubric (`rules`, `sections`), `n_a` filtered to retained ids"). One rule, three copies, with a self-contradicting SSoT label that makes future drift invisible — the exact defect AC-1/AC-7 exist to prevent. | In both workflows keep only the operational parts (the `manifest-digest --manifest <path> --write` invocation, which half gets which payload) and the citation; delete the trigger sentence and the parenthetical partition recipe. |
| D-2 | medium | `.asd/project/custom-coding-rules.md:15` vs new carve-out `artifact-layout.md:89` | Task 5's own stated risk landed: line 15 still reads "Never hand-edit `.claude/`, `.codex/`, or `.agents/skills/` — always edit the `.asd/` canonical source" — unqualified by "generated", so it flatly forbids the hand-authored `.claude/agent-memory/<agent>/` that the new carve-out and `sprint-lifecycle.md:249` now permit. This file is read by `asd-dev`/`asd-tester` on **every** impl dispatch, and F-3/F-7 (memory written to the wrong place) is precisely the friction AC-3 fixes. `asd-dev.md:65` has the same shape but is rescued by line 66's self-hosting extension; `AGENTS.md:74` and `asd-phase-impl.md:49` are safe because both say *generated*. | Qualify line 15 — "Never hand-edit **generated** `.claude/`, `.codex/`, or `.agents/skills/` … (carve-out: `artifact-layout.md` 'Agent memory')" — under the same one-off authorization already recorded in the decisions log for line 14 of this file. See Escalations. |
| D-3 | low | `asd-phase-impl-test.md:34` and `asd-phase-impl-review.md:26` vs home `sprint-lifecycle.md:316` | Same pattern as D-1 in lighter form: each cites the rule as "sole SSoT for its shape, validity rule and absent-key fallback" and then restates the validity rule (all three of `base`/`head`/`pathspec` equal), the absent/empty fallback, and the cache rationale ("it is a cache only, so a miss costs exactly today's re-derivation and blocks nothing" — pure duplicated rationale with no operational role). The AC-11 test only guards the object literal, so this duplication is unguarded. | Reduce each workflow to the action: "reuse `derived_handoff.files` when valid per `sprint-lifecycle.md` 'State recovery'; otherwise re-derive by running the diff above and overwrite the slot." Drop the restated equality rule and the cache rationale. |
| D-4 | low | `sprint-lifecycle.md:316` | The paragraph contradicts itself: it opens "holds the one file list a phase derived and **hands to the next**" but later establishes that any commit invalidates the record and that "hits are within one phase". With `git-strategy.md:39` (added this sprint) requiring the orchestrator to commit bookkeeping at phase exit, and `impl-test` committing its own output, the cross-phase handoff the field name and opening sentence promise is unreachable by construction. | State the intra-phase scope in the first sentence ("the file list a phase derived, reusable by its own later steps at the same `HEAD`") so the opening no longer promises a hand-off the invalidation rule forbids. |
| D-5 | high | `tests/run.js:2907-2908` | In-body comment added inside a test callback ("// The rejected alternative: two PARTIAL ledgers …"). `code-style.md` §7 bans comments inside any function body except a `// TODO(sprint-<NNN-slug>): …` marker, and `AGENTS.md` states §7 governs this repo's own Node code with `tests/run.js` named explicitly, no exemption. The content is already carried by the adjacent assertion message ("that is the mechanism the split rule explicitly forbids"), so the comment is pure duplication. | Delete the two comment lines. Note the pattern is pervasive and pre-existing in this file (~50 in-body comments); if commentary is intended for content-contract tests, add an explicit §7 carve-out in a follow-up rather than diverging silently — at HEAD the rule is unqualified. |
| D-6 | low | `asd-phase-impl.md:72` | The new checklist instruction is indented 6 spaces while its siblings under `- instruction:` (lines 70-71, 73+) use 7. It therefore does not nest under `- instruction:` and renders as a sibling of it, detaching the AC-7 binding from the dev-instruction payload block it was meant to join. (Same defect as correctness C-11.) | Add one space so line 72 matches lines 71 and 73. |
| D-7 | low | `.asd/sync.js:908` and `.asd/sync.js:1470`, made stale by `AGENTS.md:74` | Both comments quote AGENTS.md verbatim — `AGENTS.md: "run node .asd/sync.js --apply <file...> after editing canon"` — but AGENTS.md no longer contains that wording after AC-5. The attributed quote is now false and reintroduces the misleading argument form AC-5 removed. (`.github/workflows/sync-check.yml:36`, `asd-update/update.js:559` and `asd-sync/SKILL.md:23-25` keep `--apply <file...>` but each names generated view paths as the referent and matches the CLI usage string at `sync.js:1480` — correct, not variants.) | Update the two quotes to the current wording, or drop the attribution entirely (`code-style.md` §8 forbids code comments referencing project documents). |
| D-8 | low | `.asd/templates/t_review.md` (unchanged) vs `review-policy.md:136` | The new contract requires the written review file to carry `Interrupted attempts: <count> (<cause>)` below the verdict token, but `t_review.md` — the canonical structure of that file — was not extended with the optional line, so the template no longer describes every shape a review file may take. | Add the conditional line to `t_review.md` directly beneath the verdict-token line, marked "only when the reviewer was interrupted". |

## Verified clean

- **AC-7 zero-copy claim holds**: the 13 over-engineering items and the structure/cohesion item exist only at `review-policy.md:46-58,64`; `code-style.md:11`, `asd-phase-impl.md:72` and `providers.md:93` all link. (`asd-reviewer-efficiency.md:66-68` copies them, but that is pre-existing and outside this change surface.)
- **AC-3 carve-out stated once** at `artifact-layout.md:89`; `sprint-lifecycle.md:249`, `README.md:353` and `README.md:326` link to it, no copy — except D-2.
- **AC-10 prose/code agreement**: `providers.md:207-211` matches `runtime.js` `riskEntry`/`routeTask` exactly — bare name → `target: change`, any declared risk forces `execution: agent`, artifact risk annotates `reason: artifact-risk:<name>` without escalating, malformed entries fail closed. `README.md:207` mirrors it correctly.
- **Framework mode**: README mirrors re-checked against the diff — folder map gained the `agent-memory/` line, routing sentence updated, phase list / agent roster / model tiers / config schema / command list unaffected by this sprint. `core.md` "See also" is a correct no-op (no rule doc added). Step-number references survive the `1b`/`7a`/`8a` insertions. `release-manifest.json` needs no `managed_paths` change; hash-ledger currency is machine-gated by `tests/run.js` §6b.
- **Template adherence**: `t_plan.md` frontmatter intact, `Material risk:` is plain text and cannot trip the checkbox parser; `t_state.json` parses as valid JSON with `derived_handoff: {}`.
- **AC-14**: all six generated `asd-dev*` views and both generated `asd-update` skill views carry the corrected wording; no generated file hand-edited in the diff.
- **`runtime.js`**: the one added doc comment states purpose, not implementation; no in-body comments added.

## Coverage

Validated compact ledger: [`documentation.ledger.json`](./documentation.ledger.json) against immutable manifest [`documentation.manifest.json`](./documentation.manifest.json), findings [`documentation.findings.json`](./documentation.findings.json). `node .asd/runtime.js validate-ledger` → `{"ok":true}`. 19/19 files, 10/10 rules, 9/9 sections resolved; `html-shell-wrapping`, `provenance`, `traceability` and `persistent-actuality` `n/a` per manifest-authorized predicates.

## Verdict

CONCERNS: 8 (1 high, 2 medium, 5 low)

## Next action

Route D-1 … D-8 to `impl` review-fix mode. All eight are mechanical, in-place text edits; none requires a new abstraction, contract change or scope expansion.

## Escalations

- **D-2**: the fix edits `.asd/project/custom-coding-rules.md`, outside the `sprint-lifecycle.md` "Self-hosting" write allowlist. The decisions log already records a one-off authorization for line 14 of this same file (2026-09-07); confirm it extends to line 15 before the fix, or obtain the same one-off authorization again.
