[REVIEW-impl-correctness]: CONCERNS

# Review — correctness

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low — all severities reported)

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| C-1 | high | `.asd/agents/asd-dev.md:66`, `.asd/workflows/asd-phase-impl.md:49`, `AGENTS.md:74`, `README.md:38`, `README.md:106`, `README.md:440`, `.asd/skills/asd-update/SKILL.md:37` | AC-5's wording fix removed the `<file...>` argument placeholder entirely, leaving a bare command: "run `node .asd/sync.js --apply` for generated provider-view targets". A bare `--apply` with no file list is a **silent no-op**: `runApply` gets `requestedFiles = []` → `results = []` → `hasInvalidTargets = false` → `ok: true`, exit 0, zero views written, **and** `recomputeAndWriteHashLedgers` still runs (`.asd/sync.js:1466-1478`), refreshing `canon_hashes`/`upstream_hashes` so the ledger tests and `--check` stay green over stale generated views. Contradicts `README.md:110`, which still correctly documents "explicitly named in the `--apply <file...>` target list", and `sync.js:1480`'s usage string. F-4's complaint was the *wrong* argument form, not the presence of an argument. **Verified by the orchestrator**: bare `--apply` returns `{"ok":true,"applied":[]}`, exit 0. | Restore an explicit placeholder naming the correct targets at all seven sites, e.g. `node .asd/sync.js --apply <generated-view-path...>` with one parenthetical mapping canon → view. Optionally make `--apply` with an empty target list exit non-zero in `sync.js:1457-1478`. |
| C-2 | medium | `.asd/rules/review-policy.md:142-144`; `asd-phase-impl-review.md:50`; `asd-phase-design-review.md:42` | The partition splits only `files` but gives each half the **full** `rules`/`sections` arrays. `validateCoverageLedger`/`rowsById` (`.asd/runtime.js:179-193`) then force each half to emit a row for every rule with status `pass\|finding\|n/a`, and `n/a` needs a manifest-authorized predicate. A half whose file subset contains none of an AC's evidence has no truthful status available and must record `pass` — manufactured evidence. The union property checks only that the row sets union; it cannot detect a rule resolved `pass` by two reviewers neither of which saw its evidence. So the union property does **not** prevent a half-reviewed rubric merging into `APPROVE`. | Add an out-of-half `n/a` predicate to each half manifest's `n_a.rules`/`n_a.sections` (e.g. `evidence outside this half; covered by <reviewer>.part-N`), and add to the merge check: every rule/section must be resolved non-`n/a` in at least one half. |
| C-3 | medium | `.asd/rules/artifact-layout.md:68` | "A sprint folder holds **only** the artifacts named above" is absolute, but the path map names only `reviews/<phase>/iter-NN/<reviewer>.md`. It therefore outlaws (a) the coverage manifest/ledger/finding files `review-policy.md:105` and `t_review.md:26` mandate be persisted "alongside this report", and (b) the `<reviewer>.part-1.md`/`.part-2.md` files this sprint adds. An agent obeying the sentence would refuse to write, or delete, mandatory gate evidence. | Name the review-evidence files in the path map (`<reviewer>.manifest.json`, `<reviewer>.part-N.md`) or scope the sentence to allow the machine review evidence `review-policy.md` "Coverage ledger" persists beside each `<reviewer>.md`. |
| C-4 | medium | `.asd/runtime.js:167-175`; `providers.md:111`; `sprint-lifecycle.md:299` | `riskEntry` validates only that `target` is `change`/`artifact` — never `name`. `{"name":"migration","target":"artifact"}` (or `security`, `authentication`, `public contract`, `workflow gate`) is accepted and routes on evidence alone: an otherwise-mechanical migration task returns `{tier:'mechanical', reason:'artifact-risk:migration'}`. `sprint-lifecycle.md:299` declares those classes `change` by definition and `providers.md:111` reads as if they always return `critical`, but nothing enforces it — an existing risk class can silently lose its critical routing, in a helper whose contract is fail-closed. | Fail closed in `riskEntry` on a reserved class name typed `artifact` (one shared list), or state explicitly in `providers.md:111` that typing any of those names `artifact` is invalid input, and what the orchestrator must do instead. |
| C-5 | medium | `sprint-lifecycle.md:298-302`; `asd-phase-impl.md:62` | The `Material risk:` line is "one required line per `### Task N:` block" and step 5a sources `risks` exclusively from it — but no fallback is defined for a `plan.md` authored before this sprint (a consumer upgrading mid-sprint, exactly the case `iteration_heads` and `derived_handoff` both handle explicitly). With no lines present the orchestrator passes `risks: []`, so a task that would previously have been declared risky routes on evidence alone and can reach `tier: mechanical`. Fail-open, in the one input that used to be the escalation trigger. | State the absent-line fallback in "Plan file format": a Task block with no `Material risk:` line is treated as `change: unclassified` (routes `critical`) until the plan is updated — consistent with the section's own "When in doubt … declare `change`." |
| C-6 | medium | `sprint-lifecycle.md:316`; `asd-phase-impl-test.md:34`; `asd-phase-impl-review.md:29` | AC-11's stated purpose is a **cross-phase** handoff, but the delivered design can never hit across a phase boundary. impl-test writes at derivation time (step 2) then *must* commit at step 7 and step 10 (`git status --porcelain` MUST be empty before COMPLETED). HEAD therefore always moves after the write, and any commit invalidates the record, so impl-review's step 1 read is guaranteed to miss. The remaining hit sites are near-empty: step 7a re-partitions an existing manifest rather than re-deriving, leaving only a mid-phase session-loss resume. | Re-record `derived_handoff` at phase exit, after the final bookkeeping commit, with `head` at that moment (keeping the derivation-time write as the intra-phase cache) so impl-test entry 1 and impl-review iteration 1 share `base`/`pathspec`; or drop the cross-phase claim from AC-11 and document the field as an intra-phase resume cache only. |
| C-7 | medium | `tests/run.js:2864-2872`, `:2904`; `test-plan.md:33` | AC-13 names four things new coverage must reach; only three are reached. "AC-6's re-dispatch record" has no test — the sole AC-1/6 test proves the manifest-partition union property and nothing about the interrupted-dispatch contract. The natural analogue exists in this sprint (the AC-11 SSoT-mirror test at `tests/run.js:3000`). Without it, a future edit can drop the `7a`/`8a` step from one workflow while the rule still claims it, undetected. | Add one §19 content-contract test asserting the re-dispatch contract's presence in `review-policy.md` and both `*-review` workflows, mirroring the AC-11 SSoT test's style; or record an explicit follow-up stub. |
| C-8 | low | `review-policy.md:136`, `:140`; `asd-phase-impl-review.md:48`; `asd-phase-design-review.md:40` | The interrupted-attempt counter is "counted here, for the current iteration, never as a `state.json` field". A session-loss resume mid-iteration therefore loses both the count — so the "second consecutive interruption on the same digest" split trigger can never fire across a resume, and the reviewer re-dispatches on the oversize manifest indefinitely — and the `Interrupted attempts:` record, the sole mechanism AC-6 relies on to make the loss visible. Interruption and session loss are correlated, so the record is weakest exactly where it is needed. | Persist the counter per reviewer per iteration (e.g. `reviews.<phase>.interrupts["iter-NN"][<reviewer>]`), or state that the count resets on resume and that the decisions-log entry is appended at the moment of interruption rather than at step 8/9. |
| C-9 | low | `review-policy.md:148` vs `:130`; `t_review.md:8-19,26` | The merged `<reviewer>.md` "holds the merged token plus links to both parts, no content copied", but `:130` says the workflow writes the verdict token, findings **and** validated coverage evidence to that exact path. A reader following `:130` or `t_review.md`'s section shape finds neither findings nor ledger there. Separately `t_review.md` has no slot for the `Interrupted attempts:` line or the merged link-only form, so both new file shapes are template-less. | Add the split carve-out to `review-policy.md:130`, and add an optional `Interrupted attempts:` line plus a merged-parts variant note to `t_review.md`. |
| C-10 | low | `review-policy.md:144` vs `:103` | Every other completeness rule in this pipeline is machine-enforced precisely because "the generated manifest, not model prose or totals, defines completeness" (`:103`). The union property — the one gate between a partitioned review and a merged `APPROVE` — is prose executed by the orchestrator, with no `runtime.js` subcommand and no test over product code. | Add a `validate-partition --whole <path> --halves <path...>` subcommand to `.asd/runtime.js` (disjointness, file-union equality, identical `rules`/`sections`, per-half digest self-consistency) and call it at step 7a/8a before the merge. |
| C-11 | low | `asd-phase-impl.md:72` | The new AC-7 bullet is indented 6 spaces while its siblings under `- instruction:` (lines 70, 71, 73) use 7. Since `- instruction:` starts at column 5, a 6-space marker parses as a **sibling of `instruction:`**, so the checklist instruction silently leaves the instruction block and joins the payload list. | Re-indent line 72 to 7 spaces. |
| C-12 | low | `.asd/runtime.js:175`; `providers.md:105`, `:111` | `:111` states an artifact risk routes "with `reason: artifact-risk:<name>` recording that a risk was seen and deliberately not escalated", and `:105` states `execution` + `tier` + `reason` "fully determine and evidence the dispatch choice". But the `reason` chain puts `clamped` and `failed-objective-check` ahead of `artifactRisk`, so when `priorTier` clamps or a failed check fires, `task_routing[taskId]` records `no-downgrade`/`failed-objective-check` and the evidence that a risk was declared disappears from state. | State the precedence explicitly in `providers.md:111`, or make the annotation additive (`no-downgrade+artifact-risk:<name>`). |
| C-13 | low | `asd-phase-impl-review.md:27` vs `:28` | New step 1b is gated on `review.external_review=enabled`; step 1a, which now precedes it, is not. A project with external review disabled still pays the preflight every iteration — two bounded `spawnSync` probes at 3000 ms each (`.asd/runtime.js:120-141`) — and 1a's availability-skip nominally fires for a provider nobody enabled. Same asymmetry at `asd-phase-design-review.md:27`. | Add the same `review.external_review=enabled` guard to step 1a and design-review 3a. |
| C-14 | low | `sprint-lifecycle.md:316` | Three precision defects in the `derived_handoff` contract: (a) the declared shape is `{"base": "<sha>", …}` but both writers record a **ref name** for their first entry, so the comparison is on a mutable ref — benign for `main...HEAD` but not for a force-updated base branch; (b) the fallback covers only "absent or empty", not a **malformed** record (non-object, or matching keys with `files` missing or not an array) — the hook is tested for this (`tests/run.js:2981`) but the two readers have no stated rule; (c) "never a gate or DoD input" is inaccurate: at `asd-phase-impl-review.md:26` the reused list *is* the scope file list, hence the coverage manifest and the step-5 fan-out predicates. | Record `base` as a resolved sha; extend the fallback to "absent, empty, or failing shape validation → treat as `{}`"; restate (c) as "never a *verdict* or DoD input — it may only reproduce a list the reader would derive identically". |

Dropped as nitpick: wording polish across the five edited rule docs; alternative phrasings for `artifact-risk:<name>`; `t_plan.md`'s singular "a `Material risk:` line" vs `sprint-lifecycle.md`'s "one line per declared risk".

## Verified, not accepted on assertion

- **AC-4 `n/a` predicate verified**: `.asd/sync.js:1400`, `:1406-1424`, `:1469-1478` confirm `--apply` fails closed on an unmatched target at HEAD; `tests/run.js:1614` asserts it. The deliberate `orphan-unmarked` ok-result is contracted.
- **AC-3's sync.js claim verified**: `ORPHAN_TREES` (`sync.js:1031-1035`) covers `.claude/agents`, `.claude/skills`, `.codex/agents`, `.agents/skills` only — `.claude/agent-memory/` is never walked, generated or reconciled. The carve-out is genuinely narrowed.
- **AC-8 order verified**: `external-review.md:31` already required preflight before manifest assembly; step 1b brings the workflow into agreement. No edit to `external-review.md` was needed.
- **`routeTask` core logic sound**: legacy bare-string parity, `\0`/empty-name rejection, no-downgrade clamp intact, `risks.length === 0` correctly gating `execution: 'command'`. Residual issues are C-4 and C-12 only.
- `asd_version` un-bumped at impl-review is expected (bump lands at `pr`), not a finding.

## Coverage

Validated compact ledger: [`correctness.ledger.json`](./correctness.ledger.json) against immutable manifest [`correctness.manifest.json`](./correctness.manifest.json), findings [`correctness.findings.json`](./correctness.findings.json). `node .asd/runtime.js validate-ledger` → `{"ok":true}`. 19/19 files, 14/14 rules, 6/6 sections resolved; `ui-conformance` `n/a` per the phase-supplied predicate, `ac-4` `n/a` per its manifest-authorized predicate.

## Verdict

CONCERNS: 14 (1 high, 6 medium, 7 low)

## Next action

Route to `impl` (review-fix mode). All 14 are creator-fixable without escalation.

## Escalations

None.
