[REVIEW-impl-efficiency]: FAIL
Interrupted attempts: 1 (session rate limit)

# Review — efficiency

- **Phase**: impl-review
- **Iteration**: 2 (severity floor: medium)

## Findings

| # | Severity | Category | Location | Description | Suggested fix |
|---|---|---|---|---|---|
| E-1 | critical | escalate | `.asd/rules/review-policy.md:144` | **oe-7, defensive check for an impossible-by-contract case.** Union-property condition **(c) "each half's digest verifies against its own manifest"** can never fail where it is run. The union property is defined as running "over both half manifests and **their validated ledgers**", and every half ledger has already passed the mandatory `validate-ledger` gate. `runtime.js:209` is `if (manifest.digest !== digest \|\| ledger.manifest_digest !== digest) fail(...)` — it recomputes the digest and rejects a half whose stored manifest digest disagrees, which is exactly (c). A half reaching merge has already proven (c). Corroborating that (a)–(d) is unverified prose: this diff **deleted** the three union assertions from the AC-1/6 test, so nothing pins the four conditions either. | Delete `(c) each half's digest verifies against its own manifest;` and relabel `(d)`→`(c)`. Keeps (a) disjoint/union, (b) full rubric in both, (d) no id `n/a` in both halves — the three conditions the hand-built partition genuinely does not guarantee. |
| E-2 | medium | simplify | `.asd/rules/review-policy.md:144` (last sentence) | Contract published for a helper that does not exist: "(a)–(d) are the contract for a `validate-partition --whole <path> --halves <path...>` helper in `.asd/runtime.js`; until it exists the orchestrator runs them by hand". `validate-partition` is not a subcommand of `runtime.js` (no occurrence anywhere in canon). ~35 words of speculative API in a rule doc every reviewer and both review workflows load, plus a live hazard: an orchestrator skimming the sentence and not its trailing clause invokes a subcommand that does not parse. The four conditions read identically without it. | Delete the sentence from `; (a)–(d) are the contract` to `by hand over those files.` If the helper is later built, its CLI contract ships with it. |
| E-3 | high | simplify | `.asd/workflows/asd-phase-impl-review.md:74` (step 13) | **Cite-then-restate, and the restatement contradicts the SSoT** — the exact defect class this sprint exists to remove. `sprint-lifecycle.md:316` defines `head` = `git log -1 --format=%H <base>..HEAD -- <pathspec>`, **"never raw `HEAD`"**. Step 13 writes "`head` = `git rev-parse HEAD` at that moment". Because step 13 runs after the exit bookkeeping commit and its own write must itself be committed, a raw-`HEAD` value is stale the instant it is recorded — the next phase's validity check then always misses, so the handoff record impl-review produces can never be read. Second divergence in the same sentence: step 13 rewrites the slot "with **step 1's** scope file list", while `:317` specifies "the same diff **re-run** in the final bookkeeping write" — which is what `asd-phase-impl-test.md:53` actually does. Two of three statements of one rule disagree. | Collapse step 13 to a binding with no restated field semantics: "**Phase-exit re-record** — on every COMPLETED route (steps 8–10), after the orchestrator's exit bookkeeping commit, re-run step 1's diff and rewrite `state.json.derived_handoff` per `sprint-lifecycle.md` 'State recovery' (sole SSoT for base/head/pathspec)." Deletes the `head =` clause and both trailing sentences and removes the contradiction. |
| E-4 | medium | escalate | `.asd/rules/sprint-lifecycle.md:316-319`, `asd-phase-impl-review.md:29,74`, `asd-phase-impl-test.md:34,53` | **Complexity-vs-value: the `derived_handoff` cache nets ≈ zero.** To decide whether it may use `files`, a reader must first compute the key — `git log -1 --format=%H <base>..HEAD -- <pathspec>` — a git invocation of the same order as the `git diff --name-only` it is trying to avoid. Against that saving stands: a `t_state.json` field, a bespoke `head` definition that exists only because the record's own write must be committed, two write sites per phase plus the "no write when step 1 reused a matching record" exception, four paragraphs in an always-loaded rule doc, clauses in two workflows, and three tests. The cross-phase hit is further bounded: impl-test's re-entry base is `<prior-sha>` while impl-review's iter≥2 base is `iteration_heads["iter-(NN-1)"]`, so `base` differs and the record misses by construction on every iteration after the first. | Escalate (below). If AC-11 stands as-is, mark `keep-as-is` and apply E-3 only. If not, the cheapest honest form is: keep one exit-time write, delete the step-2 write clauses and the parenthetical exception, and reduce `sprint-lifecycle.md:317` to one sentence. |
| E-5 | medium | simplify | `AGENTS.md:74`, `README.md:38/106/439`, `.asd/agents/asd-dev.md:66`, `.asd/workflows/asd-phase-impl.md:49`, `.asd/skills/asd-update/SKILL.md:37` | **The fix round grew the always-loaded surface.** AC-5 required correcting the sync argument wording at every occurrence; what landed is the corrected token **plus** a verbatim ~32-word parenthetical copied into 8 canonical locations (13 including generated mirrors), ≈ 260 duplicated words. One copy sits in `AGENTS.md`, loaded on every dispatch; three sit in README prose about unrelated topics. The path shapes are already the content of `providers.md` "Canonical path -> per-provider path". Duplication in a compression sprint, and 8 places to keep in sync when a provider path changes. | Keep `--apply <generated-view-path...>` at all 8 sites — that is the whole correction AC-5 needs. Keep the parenthetical at exactly one home (`AGENTS.md:74`) and delete it from the other 7, then `node .asd/sync.js --apply` the affected views. |
| E-6 | medium | simplify | `.asd/rules/sprint-lifecycle.md:299` vs `.asd/rules/providers.md:111` | Same new rule stated in two rule docs — sprint-lifecycle: "Those five classes are reserved: declaring one `artifact` is invalid input, rejected by `route-task`"; providers: the same, plus a consequence clause. It is a **routing rejection** rule, and `sprint-lifecycle.md:302` — two lines below the copy — already names `providers.md` "Task-class variants and routing" as the file "which owns the routing semantics". `runtime.js:12` is the third copy of the class list (that one is load-bearing code). | Delete the sentence from `sprint-lifecycle.md:299`; providers.md's copy is the owner and is already cited. (Or delete providers.md's copy instead — one home either way, not both.) |

Dropped below floor: 4 `low` findings (not listed).

## Checked clean

`.asd/sync.js:1469-1472` — the empty-target guard is placed **once**, in `main()`'s `--apply` branch; `runApply` carries no duplicate, and the guard returns before the whole-repo hash-ledger recompute, so the no-op invocation now does strictly less work than before. Both `*-review` split blocks were genuinely cut (4 bullets → citation + 1 bindings bullet, ≈ −140 words each) and the "sole SSoT … not restated here" label is now truthful in both. `tests/run.js`: `buildManifest` is defined once and `validCoverageFixture` consumes it; no duplicate helper, no fixture shape built three ways, and the new tests exercise paths no adjacent test pins. `artifact-layout.md` and the design-review workflow are net shorter.

**Performance sections**: no findings at or above floor. Anti-patterns — none added; the sync guard *removes* a whole-repo read-and-hash pass from a no-op invocation. Algorithmic — `RESERVED_CHANGE_RISKS.includes(...)` is linear over 5 elements on a short-circuited branch, over risk arrays of a handful of entries. Regression — no baseline defined; both behaviour changes are fail-fast guards, and suite growth 136→150 adds one process spawn plus a mini-repo fixture, matching the existing §11 pattern. Hot paths — the one real hot path here is per-dispatch context load; that is where E-5 lands.

## Coverage

Validated compact ledger: [`efficiency.ledger.json`](./efficiency.ledger.json) against immutable manifest [`efficiency.manifest.json`](./efficiency.manifest.json), findings [`efficiency.findings.json`](./efficiency.findings.json). `node .asd/runtime.js validate-ledger` → `{"ok":true}`. 19/19 files, 14/14 rules, 8/8 sections resolved; `perf-budget-compliance` the single `n/a`. `sc-1` marked `pass` per the standing user override on `runtime.js`'s single-module shape; only new code judged.

## Verdict

FAIL: 6 (1 critical, 1 high, 4 medium)

## Next action

Route to impl review-fix mode after the escalations are decided. E-2, E-3, E-5 and E-6 are pure deletions a dev applies without further approval; E-5 additionally requires `node .asd/sync.js --apply` for the affected generated views, and `.asd/project/custom-coding-rules.md:14` carries a 9th copy outside the review surface — fold it into the same edit under AC-5's existing one-off authorization. `tests/run.js` needs no change for any finding; if E-1 is accepted, consider restoring union assertions so the surviving conditions are pinned.

## Escalations

- **E-1 — contract change** (removing a stated condition from a workflow contract). *Complication*: the union property gained a fourth named condition, `(c) each half's digest verifies against its own manifest`. *Why unnecessary*: the merge point is reached only after both halves' ledgers passed `validate-ledger`, and `runtime.js:209` already rejects any half whose stored manifest digest fails recomputation — (c) can never fail where it is run. *Simpler alternative*: delete (c), relabel (d)→(c). *Cost of keeping*: one unreachable condition in the sole SSoT that four reviewers and both review workflows load, which a future orchestrator spends a step verifying. *Decision requested*: approve deletion, or override to keep (c) deliberately, in which case it is marked `keep-as-is` and never re-raised.
- **E-4 — scope** (narrowing an AC-11 deliverable). *Complication*: `derived_handoff` is a cache whose validity key costs about as much to compute as the `git diff` it saves, and whose cross-phase hit is structurally limited to the first handoff. *Simpler alternative*: single phase-exit write; delete the step-2 write sites and the reuse exception; one sentence in `sprint-lifecycle.md:317`. *Note*: the decisions log records the deliberate choice to keep both writes rather than narrow or delete the field. If that decision stands, answer "keep" — E-3 must still be fixed either way, since it is a contradiction inside the chosen design, not an argument against it.
