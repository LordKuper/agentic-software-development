---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/wave-<K>/iter-NN/testing.md (verdict)
---

# Test plan — sprint 019-retro-intake

<!--
Written in impl-test, after the implementation exists. First entry writes this file fresh;
every re-entry AMENDS it (append/update rows) — never a full rewrite. Defects rows persist
(resolved ones kept for the record). Narrative rows of prior entries rotate into
test-plan.entry-NN.md: .asd/rules/artifact-layout.md "Test plan". Change surface is not restated here — it's the diff
itself (`git diff --stat`), computed by asd-phase-impl-test.md step 2 (full on entry 1, delta
since the prior entry's `HEAD analysed` on re-entry).
Rules: .asd/rules/sprint-lifecycle.md (impl-test phase), .asd/rules/code-style.md §17.
-->

## Entry log

Appended each entry, never rewritten. `HEAD analysed` is the commit the strategy/prune passes
were scoped through; the next re-entry's delta is `git diff <this sha>...HEAD`.

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | 88ceb3b | full change surface |

## Risk → check decisions

Pre-strategy run (whole runner = impacted set, at `66b7994`): `node tests/run.js` → exit 1, 225/229. The 4 red tests were the wording pins plan.md Risks assigns to this entry; all are rewritten in place below, none deleted.

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `.asd/runtime.js` `retroRows`, `t_retrospective.html` row ids (AC-7) | Ids drift from the 1-based ordinal (a covered-by row skipped in the count readdresses every later row); a legacy retro without `<tr id>` gets different ids; a declared id off its ordinal, a missing systemic table, an unknown `Acts on` or a reshaped row read leniently; template and parser disagree | unit/property | add | Pure parser over HTML the template emits. The fixture is built from `t_retrospective.html` itself (its `id="A-1"`/`id="P-1"` rows, filled), so a template change the parser cannot read fails here, and so does a template that stops emitting ids (sanity assert). |
| `.asd/runtime.js` `backlogRows`, `t_retro-backlog.md` (AC-4) | The shipped template does not parse; an off-template backlog (unknown disposition or side, repeated or malformed address, short row, second table, broken separator) is read leniently, so a disposed row is offered again or a deferred one never is | unit/property | add | Fail-closed contract of a parser; one case per rejection, template-derived input. |
| `.asd/runtime.js` shared `tableCells` backtick change | A cell that opens and closes with different code spans lost its outer backticks, corrupting backlog guardrail text | unit (through `backlogRows`) | add | The consumer that motivated the change. `defect-stalemate`'s single-span unwrap is already pinned by the sprint-013 AC-1/AC-2 test (a backticked `` `.asd/runtime.js:88` `` location still matches its bare form) — keep. No separate `defect-stalemate` assertion: both entries parse through the same function, so identity matching is unchanged; only a digest recorded across the upgrade differs, which re-asks the question (fail-safe, decisions-log). |
| `.asd/runtime.js` `retroCandidates` + `retro-candidates` CLI (AC-1, AC-2, AC-5) | Wrong source sprint (not done / no retro); disposed rows offered; deferred rows missing, duplicated, or carrying the wrong text/home; covered-by or `asd` rows leak into a consumer project; absent backlog or no prior retro not a clean `[]`; unreadable input skipped instead of failing; `--self-hosting` not parsed as a boolean | unit + CLI | add | Temp-dir fixtures: four sprints (done+retro, done+retro, done without retro, not-done+retro), a backlog with every disposition, and three fail-closed variants. Output compared as a set (sorted), since order is not part of the contract. |
| `asd-phase-scope.md` step 2a, `sprint-lifecycle.md` "Retro intake" (AC-1, AC-5) | The command the workflow tells the orchestrator to run drifts from the runtime's flags or paths, so intake fails at scope | executable (canon command run as written) | add | The literal is extracted and run from a fixture project root; its flags must equal the rule's. |
| `.asd/project/retro-backlog.md` (outside the review surface; AC-4 seed) | Wrong seed disposition; a row's text or side drifts from its retro (a deferred row is re-offered with the backlog's copy); a seed row dropped; `/asd-update` manages the file; live intake re-offers a disposed row or misses a deferred one | static on the live file | add | Durable invariants only: each row resolves to its archived retro row with equal `Acts on`/text; every seed address present; every row still `Decided in` 019 holds its seed disposition (a later re-decision restamps the row and leaves the check); no `managed_paths` entry covers the file; live candidates ⊇ deferred and ∩ disposed = ∅. The DoD's exact check is not pinned, because it goes red by design once 019's own retro is archived: run once at `66b7994`, `node .asd/runtime.js retro-candidates --sprints .asd/sprints --backlog .asd/project/retro-backlog.md --self-hosting` → exit 0, exactly `016-remove-terra-family#P-1`, `017-review-waves#P-1`, `017-review-waves#P-3`; without `--self-hosting` → `[]`. |
| `asd-phase-scope.md` step 2 (AC-6) | The removed cleanup/quality-criteria question comes back | static | keep (rewritten in place) | Sprint-015 test's AC-10 pin (≈5505) now asserts no sentence in the scope workflow asks for cleanup or quality criteria. Test renamed to the new contract. |
| `checkpoints.md` hard list + inventory (AC-3) | Retro intake dispositions lose their hard-gate class, so an adaptive orchestrator decides them alone | static | add | Folded into the sprint-015 test's existing per-sprint-skip loop (same two-site shape). |
| `git-strategy.md` "Commit before review" one-command form (AC-8) | The command as written sweeps a sibling's staged file, fails on a sibling's whitespace error, misses a never-tracked path or a rename side, or leaves a path staged after a failed check (018 F-1) | executable (sandbox git) | add | The compound command and its reset are extracted and run in an isolated temp repo with a sibling's staged file. Both halves of the claim — commit and failure — are exercised. |
| `providers.md` "Dispatch payload header", `review-policy.md` payload list, `external-review.md` "Phase-scoped payload", design/impl-review workflows (AC-9, AC-10) | A header line missing from an exhaustive "carries only" list, a review workflow not opening payloads with it, or a turn-budgeted agent whose `maxTurns` leaves no report turn | static | add | Header keys derived from providers.md; the report reserve derived from the `report by turn <maxTurns − N>` literal; caps read from canon. |
| `providers.md` verified-vs-trusted (AC-10) | `maxTurns` listed as emitted on trust again, or the host-scoped claim diverging from what `sync.js` renders | static, bound to the rendered views | keep (rewritten in place) | Pin ≈3427 now asserts the trusted clause names `effort` and not `maxTurns`, and binds the claim to the views: every canon `claude.maxTurns` renders in `.claude/agents/`, no `.codex/agents/*.toml` carries a turn cap. |
| `review-policy.md` "Gate Verdict Format" (AC-14) | The removed "separate write channel reviewers do use" claim returns, or the scope statement stops pointing at the memory-fix dispatch | static | keep (rewritten in place) | Pins ≈3412/3417: a sentence of the scope line must name `memory: project`, the memory-fix dispatch and "Autofix vs escalation"; providers.md must not carry the `MEMORY-FIX` contract. Asserted by clause, not by phrase; a full reword (R1) stays green. |
| `asd-external-review.md` write ban (AC-14) | The only reviewer holding Bash reads its own memory as writable | static | keep (rewritten in place) | Pin ≈3452: the write-ban bullet must route memory changes through the memory-fix dispatch and cite "Autofix vs escalation"; the `claude.memory === 'project'` binding stays. |
| Review-file write step, `asd-phase-impl.md` steps 3 and 9 (AC-14) | The review-file commit claims to carry reviewer-authored memory again; the memory-fix text is applied but not committed, or the step-9 path gate rejects it | static | keep (rewritten in place) | Pin ≈3738: the write steps must not claim reviewer-authored memory. The acting site moved to impl step 3 (route, apply verbatim, commit) and step 9 (admit the path), and both are asserted there. |
| `review-policy.md` "Autofix vs escalation" memory-fix dispatch (AC-14) + leftover-term check (AC-16), entry 1 | The routing rule loses one of its duties (owner, non-owner never authors, `MEMORY-FIX` return with no verdict token, apply-commit-log); a removed claim survives in memory an agent reloads on every dispatch | static sweep | add | Sweep over canon, README, AGENTS.md and every `.claude/agent-memory/**` file, orphan `asd-pm/` included (sanity-asserted). **Red at HEAD → D-1, D-2.** Leftover-term check run (terms: the scope cleanup/quality question and its three items; `write channel`/`memory: project` channel/`host serves`; `Emitted on trust` with `maxTurns`; one live tester for the whole phase; newest `decisions-log.NNN.md`): canon, README and AGENTS.md clean. Memory: 2 hits, D-1 and D-2. D-2's file also calls External Review's former carve-out a scope for "its own memory directory" at L19, which the fix covers too. Judged not defects: `asd-dev-critical/feedback_false-ssot-declarations.md` L14 narrates what sprint 010 did (past tense, not a live claim); archived retro 017 `P-4` (`covered by: asd-phase-scope.md step 2`) is immutable and out of scope (sprint.md), and intake drops covered-by rows. |
| `review-policy.md` consumer search (AC-11), `artifact-layout.md` rotation boundary + readers (AC-12), fresh tester (AC-13), review-fix tester rows (AC-15), leftover-term rule (AC-16) | A single-home rule is deleted or loses a duty, or an acting site stops pointing at it; the rotation cycle stops matching the impl..impl-review span; a within-cycle reader reads a rotated segment | static | add | One test. The rotation cycle is derived from PHASE_CHAIN, the granted rows must be `t_test-plan.md` headings, and each acting site is located by its citation, not its wording. |
| Sprint-014 AC-7 rotation test | The resume-never-rotates readers | static | keep | Re-run green at `66b7994`; the in-cycle clause it does not reach is pinned by the row above. |
| `t_decisions-log.md` durability list gains the backlog | A missing entry drops a pointer only | none | none | The home it points at, `artifact-layout.md` "Retro backlog", is cited by `sprint-lifecycle.md` "Retro intake". The citation sweep fails if that section goes, and the persistence property is pinned by the managed-paths check above. |
| `asd-phase-retro.md` step 6 row-id sentence (AC-7) | The retro phase stops emitting ids | none | none | Agent-runtime instruction. The template it fills carries the ids, and the retroRows test pins that. Legacy retros without ids resolve identically, so a missed id changes no address. |
| AC-3 per-candidate HEAD verification + recommendation, AC-5 "one decisions-log line" | The orchestrator offers a resolved candidate or logs noise | none | none | Orchestrator runtime judgement at scope; no command writes that line, so there is nothing to execute. The helper half (empty array → no-op) is pinned by the candidates test. Owner: the scope-phase orchestrator. |
| README.md scope/retro/reviewer/folder-map/update rows, `CHANGELOG.md` v13.2.0 | Mirror drift | static (existing) | keep | The README phase table and count words are held by the §16 mirror tests. The CHANGELOG head version matches `asd_version` (sprint-015 AC-12 assert). The rest is prose summary with no SSoT literal to derive. |
| `.asd/release-manifest.json`, generated views (AC-17) | Stale hashes or view drift | static (existing) | keep | The upstream/canon hash tests and the §9 `sync.js --check` test cover it. `node .asd/sync.js --check` → exit 0, `ok: true`, 0 non-current items. |
| `.claude/agent-memory/asd-dev-critical/*` (new memory file, index line) | Index ↔ file bijection | static (existing) | keep | The T-2/T-4 bijection test covers it (green). |

## Removed tests

| Test | Reason | In change scope |
|---|---|---|

None. The four red pins were rewritten in place to the new contract (rows above). Deleting them would have left the superseded claims unguarded.

## Added tests

All in `tests/run.js`. Every mutation was run as `node tests/run.js` with the mutation applied and restored in the same call (scratchpad script, byte-compared restore). Each mutated run also fails the expected hash-ledger/`--check` tests for a managed file, and the pre-existing D-1/D-2 sweep. The failing test named is the one the mutation targets; its first failing assertion is quoted.

| Test | Regression proof |
|---|---|
| `sprint-019 AC-7: retroRows reads the rows t_retrospective.html emits as A-N/P-N by 1-based ordinal - covered-by rows counted, legacy rows without <tr id> addressed identically - and fails closed on a declared id off its ordinal, a missing systemic table, an unknown Acts on or a reshaped row` | mutation M2 declared-id check disabled (`if (false) fail(`): `node tests/run.js` → exit 1, this test: "a declared id off its ordinal means rows were inserted or reordered…". M10 systemic section made optional → exit 1, "the systemic class ships on every branch…". M36 cell-count check disabled → exit 1, "cells are read by position…" |
| `sprint-019 AC-4: backlogRows parses the one backlog table t_retro-backlog.md ships - escaped pipes, a multi-span guardrail kept whole, a single code span unwrapped - and fails closed on anything off-template` | mutation M1 `tableCells` reverted to `` /^`(.*)`$/ ``: `node tests/run.js` → exit 1, this test: "a cell that is one code span is unwrapped; a guardrail that opens and closes with different code spans is kept whole…". M9 disposition validation disabled → exit 1, "Missing expected exception: an unknown disposition must fail closed…" |
| `sprint-019 AC-1/AC-2/AC-5: retroCandidates offers the latest done retro's undisposed rows plus every deferred backlog row, drops covered-by rows and - outside self-hosting - asd rows, is [] with no prior retro, and fails closed on unreadable input; the CLI prints exactly that` | `node tests/run.js` → exit 1 in each, this test. M3 covered-by filter removed: "AC-1: 017-b is the latest archived sprint…". M5 phase `done` ignored: same assert. M6 disposed rows kept: same assert (plus the live-backlog test). M4 consumer filter removed: "AC-2: a consumer project gets only consumer rows…". M7 missing deferred source silently skipped: "a deferred row its retro does not hold must fail closed…". M8 CLI `--self-hosting` hardwired false: "the CLI scope runs must print what the function returns…" |
| `sprint-019 AC-1/AC-5/AC-6: scope step 2a runs retro intake by the command sprint-lifecycle.md "Retro intake" names - run as written from a project root it prints the candidates, and [] with no prior retro` | mutation M11 step 2a `--backlog` → `--backlog-file`: `node tests/run.js` → exit 1, this test: "the acting command and its rule must name the same flags". M8 above also fails this test: "the command as the workflow writes it, run from the project root…" |
| `sprint-019 AC-4: the live retro backlog parses, each row resolves to its archived retro row with the same Acts on and text, the 019 triage seed stands until a later sprint re-decides a row, /asd-update never manages the file, and live intake re-offers every deferred row and no disposed one` | `node tests/run.js` → exit 1 in each, this test. M32 `017-review-waves#P-3` deferred → included: "AC-4: a row still stamped Decided in 019-retro-intake carries the disposition…". M33 `016#P-2` guardrail text drifted: "016-remove-terra-family#P-2: the backlog's Acts on and Guardrail are the retro row's own…". M34 `.asd/project` added to `managed_paths`: "AC-4: /asd-update rewrites every managed path…". M6 (above): "AC-1/AC-3: live intake offers every deferred row again…" |
| `sprint-019 AC-8: the one-command commit form git-strategy.md "Commit before review" gives a dispatched agent, run as written, commits exactly its own paths - a never-tracked file and both sides of a rename included - never checks or sweeps a sibling's staged file, and after a failed check its reset leaves none of its paths staged` | `node tests/run.js` → exit 1 in each, this test. M12 `git commit --only -- <paths>` → `git commit`: "the commit carries exactly the agent's paths…". M13 check made whole-index: "the check is path-scoped, so a sibling's staged whitespace error must not fail this agent's commit". M14 reset → `git status --short`: "AC-8: after the reset the failed path is unstaged…". Not a mutation: dropping only `--only` leaves `git commit -- <paths>`, which git already runs with `--only` semantics, so that edit changes nothing to catch. |
| `sprint-019 AC-9/AC-10: the dispatch payload header providers.md defines is admitted by both exhaustive reviewer payload lists and cited by both review workflows, and every agent handed a turn budget declares a maxTurns that leaves a report turn` | `node tests/run.js` → exit 1 in each, this test. M15 `Turn budget:` dropped from review-policy's payload list: "review-policy.md's reviewer payload list is exhaustive… `Turn budget:`…". M35 design-review step 7 header cite removed: "…design-review.md step 7 builds every reviewer payload…". M16 `asd-advisor` `maxTurns` 30 → 5: "asd-advisor: the Turn budget line is filled from canon maxTurns… (got 5)" |
| `sprint-019 AC-11/AC-12/AC-13/AC-15/AC-16: each review-fix, rotation and tester-lifecycle rule keeps its single home and every acting site points at it` | `node tests/run.js` → exit 1 in each, this test. M30 consumer-search bullet deleted: "AC-11: the consumer search must name its three duties…". M25 `impl-review` dropped from the rotation cycle: "AC-12: the no-rotation cycle must be exactly the impl..impl-review span…". M26 impl step 6 reverted to the newest segment: "AC-12: a within-cycle reader…". M27 impl-test operations line reverted to one live tester: "AC-13: .asd/workflows/asd-phase-impl-test.md dispatches or is the tester…". M28 `Entry log` added to the review-fix grant: "AC-15: a review-fix tester amends only the risk and added-test rows". M29 orphan-directory clause dropped: "AC-16: the leftover-term check covers all agent memory…". Reword R2 (consumer-search bullet rewritten, same duties) → only the hash-ledger and D-1/D-2 failures, this test green. |
| `sprint-019 AC-14/AC-16: review-policy.md "Autofix vs escalation" routes a memory finding to its owner through the memory-fix dispatch, and no canon, README or agent-memory file - orphan agent directories included - still claims a reviewer writes its own memory` | Fail-first vs D-1/D-2: `node tests/run.js` at `685ff27` → exit 1, this test, "AC-14/AC-16: … Found: …project_reviewer-write-scope-declaration.md:9, …feedback_no-shell-review-method.md:8". M31 "no verdict token" clause dropped → exit 1, this test: "AC-14: an owner without a write tool returns only a MEMORY-FIX block and no verdict token…" |
| `AC-15/sprint-019 AC-14: review-policy.md is sole SSoT for the reviewer write scope - its own memory included, which it reaches only through the memory-fix dispatch - and providers.md cites it instead of restating it` (rewritten pin) | mutation M18 Gate Verdict Format sentence reverted to the old write-channel claim: `node tests/run.js` → exit 1, this test: "sprint-019 AC-14: the host serves a reviewer no memory write tool, so the scope statement must say how a reviewer's memory changes…". Reword R1 (same substance, new wording) → this test green. |
| `AC-15/sprint-019 AC-10: providers.md records which emitted agent frontmatter fields are host-verified vs. emitted on trust, and its host-scoped maxTurns claim matches what sync.js renders per host` (rewritten pin) | mutation M17 `maxTurns` put back into the trusted clause: `node tests/run.js` → exit 1, this test: "sprint-019 AC-10: `effort` stays emitted on trust, but `maxTurns` is host-enforced on Claude…". M16 (above) also fails it: "asd-advisor: providers.md says Claude enforces maxTurns, so the Claude view must render the canon cap 5" |
| `AC-15/iter-05: providers.md names External Review as the sole Bash carve-out among read-only reviewers, and that claim matches actual frontmatter grants` (rewritten assert) | mutation M19 write-ban bullet reverted to the old carve-out: `node tests/run.js` → exit 1, this test: "sprint-019 AC-14: External Review holds Bash, so its blanket write ban…" |
| `AC-13b/sprint-010 AC-3/sprint-019 AC-14: git-strategy.md "Commit before review" names every agent-memory write whose author cannot commit it - the reviewer's and a concurrent co-author's - and the acting site is impl's memory-fix routing, never the review-file write step` (rewritten pin) | `node tests/run.js` → exit 1 in each, this test. M20 reviewer-authored-memory clause restored in impl-review: "sprint-019 AC-14: .asd/workflows/asd-phase-impl-review.md: a reviewer holds no write tool…". M21 "commits" dropped from impl step 3: "sprint-019 AC-14: the orchestrator applies the owner's MEMORY-FIX text verbatim and commits it…". M22 step-9 memory-fix admission removed: "sprint-019 AC-14: step 9's authorised-paths gate must admit the memory file…" |
| `sprint-015 AC-1/AC-6/AC-7/AC-8/AC-10/AC-12: no canon tells anyone to clear context, compaction keeps its preserve list, free-form input never goes through a decision prompt, BA/UX renames route to the orchestrator, the per-sprint document skip and retro intake dispositions are hard, scope no longer asks the cleanup criteria, and the changelog heads the released version` (rewritten pin + folded assert) | `node tests/run.js` → exit 1 in each, this test. M23 cleanup question restored in scope step 2: "sprint-019 AC-6 (supersedes sprint-015 AC-10): scope no longer asks…". M24 retro-intake inventory row removed: "AC-8/sprint-019 AC-3: the retro intake dispositions is hard in both modes and in the inventory…" |

## Suite run

Written twice per cycle: `impl-test`'s suite gate records an **impacted-set** run here each entry
(`.asd/rules/sprint-lifecycle.md` "Impacted test set"); `impl-review`'s terminal step overwrites
it with the cycle's one **full-suite** run once the last review wave's reviewers are
APPROVE/latched. The `pr` gate always reads whatever is recorded here last — the full-suite
record, by the time `pr` runs. Each per-entry record measures only the tree that entry
analysed, not any tree produced later
(`.asd/rules/sprint-lifecycle.md` "Impacted test set").

- Command: `node tests/run.js` (the impacted set is the whole runner)
- Scope: impacted
- Result: fail — 237/238 passed, 1 failed, 0 skipped (exit 1); the one failure is the D-1/D-2 sweep
- Lint / build: pass — `git diff --cached --check` exit 0; `node .asd/sync.js --check` exit 0, `ok: true`, 0 non-current items
- HEAD: 685ff27 — commit this run was verified at; pr phase compares current HEAD against this to decide whether to skip re-running

## Defects

Code defects found by the suite. Resolved in `impl` test-fix mode. `Entry` through `Failing test` are never edited once written — the stalemate check compares them (`.asd/rules/sprint-lifecycle.md` "Impl-test phase"). One table only: append rows, never a second `## Defects` section — the check fails on one.

| ID | Entry | Location | Symptom | Failing test | Status | Fix commit |
|---|---|---|---|---|---|---|
| D-1 | 1 | .claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md | AssertionError [ERR_ASSERTION]: AC-14/AC-16: the host serves a reviewer no memory write tool, so a line calling `memory: project` a write channel it uses restates the claim this sprint removed - its owner rewrites it through the memory-fix dispatch. Found: .claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md:9, .claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md:8 | sprint-019 AC-14/AC-16: review-policy.md "Autofix vs escalation" routes a memory finding to its owner through the memory-fix dispatch, and no canon, README or agent-memory file - orphan agent directories included - still claims a reviewer writes its own memory | fixed | 1353ea8 |
| D-2 | 1 | .claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md | AssertionError [ERR_ASSERTION]: AC-14/AC-16: the host serves a reviewer no memory write tool, so a line calling `memory: project` a write channel it uses restates the claim this sprint removed - its owner rewrites it through the memory-fix dispatch. Found: .claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md:9, .claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md:8 | sprint-019 AC-14/AC-16: review-policy.md "Autofix vs escalation" routes a memory finding to its owner through the memory-fix dispatch, and no canon, README or agent-memory file - orphan agent directories included - still claims a reviewer writes its own memory | fixed | 3cf27e5 |
