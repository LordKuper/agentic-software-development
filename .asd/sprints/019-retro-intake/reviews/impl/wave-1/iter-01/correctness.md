[REVIEW-impl-correctness]: CONCERNS

# Review — correctness

- **Phase**: impl-review
- **Iteration**: wave-1/iter-01

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| COR-1 | high | `.asd/rules/review-policy.md:146` ("Gate Verdict Format"), `:53` (Diff reachability), `:91` (Memory-fix dispatch "reviewers, External Review included"); `.asd/agents/asd-external-review.md:67`; `README.md:215`; `CHANGELOG.md` v13.2.0 "Memory-finding routing"; `.claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md:8-10`; `.claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md:8`; pinned by `tests/run.js` (AC-15/sprint-019 AC-14 test; AC-14/AC-16 sweep) — **AC-14** | AC-14 asks that the claim be "corrected to match the host". The new claim, "the host serves a reviewer no write tool" and "`memory: project` only loads it", is false on the current Claude Code host. The host docs (code.claude.com/docs/en/sub-agents, "memory") say "Read, Write, and Edit tools are automatically enabled so the subagent can manage its memory files". Reviewer canon `disallowedTools` removes only `Edit` (plus `Bash`/`WebFetch`), so `Write` gets through. This dispatch shows it: `asd-reviewer-correctness` canon `tools` = Read/Glob/Grep/WebFetch/WebSearch, yet it holds `Write`, and the host-injected prompt tells it to write memory with Write. External Review (`disallowedTools: ["Edit","WebFetch"]`) gets Write the same way. 017 F-4 recorded the host's behaviour at that time, which no longer holds. Consequences: (a) the sole-statement home, README, CHANGELOG and two memory files state a false fact; (b) the new sweep test blocks anyone from restoring the true statement; (c) the review workflows dropped their "commit reviewer-authored memory" clause, so reviewer writes now rely only on `git-strategy.md`'s bookkeeping line. | Pick one (see Escalations): (a) make the claim true by config: add `"Write"` to `claude.disallowedTools` of the 4 internal reviewers and `asd-external-review` (and `asd-advisor` if it applies), re-run `sync.js --apply`, update the hashes, and confirm on a live dispatch that `Write` is gone; or (b) scope the claim to the host: Claude serves `Write` to a `memory` subagent unless it is disallowed, so a reviewer owner fixes its own memory in its chain, and the memory-fix dispatch stays the path for an owner without a write tool. Update the mirrors and tests to match either way. |
| COR-2 | medium | `.asd/runtime.js:524` (`retroCandidates` `/^covered by:/i`); `.asd/rules/sprint-lifecycle.md:285` (Retro row id English-literal list); `.asd/templates/t_retrospective.html:11` — **AC-1** | Intake drops `covered by:` rows by matching the English prefix, but canon never makes that prefix an English literal: the retro is written in `language.docs`, and only row ids and `Acts on` values are declared English. In a consumer with `language.docs != en`, a translated "covered by:" row passes the filter and is offered as a candidate, which breaks AC-1. This repo's `language.docs=en` hides the problem from the suite. | Add "the `covered by:` prefix" to the English-literal sentence at `sprint-lifecycle.md:285` and to the `t_retrospective.html` row-id comment. One phrase each; the runtime stays as it is. |
| COR-3 | medium | `.asd/rules/review-policy.md:91` (Memory-fix dispatch); `.asd/workflows/asd-phase-impl.md` step 3 (review-fix / test-fix memory routing); `.asd/rules/artifact-layout.md:98` — **AC-14 × AC-16** | AC-16 extends the leftover-term check to "directories of no longer existing agents" (`.claude/agent-memory/asd-pm/` exists today, and the new test asserts it). AC-14 routes a memory finding to `<owner>`, and "a non-owner never authors memory text". A hit in an orphan directory has no owner to dispatch, so the finding or `D-N` the check exists to produce cannot be routed and the round stalls. | Add one clause to the Memory-fix dispatch: a finding in the directory of an agent that no longer exists has no owner, so the orchestrator deletes the stale file (or the whole directory) and logs one decisions-log line; no memory text is authored. Cite it from impl step 3. |
| COR-4 | low | `.asd/rules/artifact-layout.md:187` ("Test plan" review-fix tester grant) vs `.asd/agents/asd-tester.md:64` ("In-scope test deletions proceed with a recorded reason") — **AC-15** | A review-fix tester may amend only the `Risk → check decisions` and `Added tests` rows. A testing-reviewer finding that says to delete a redundant or flaky test routes to that tester, but the deletion's reason belongs in `Removed tests` (the SSoT for "why a test was removed"), which it may not touch. It has two obligations that cannot both be met. | Keeping the AC-15 wording, state at `artifact-layout.md:187` that a review-fix tester never deletes a test, and that a removal finding is carried to the next `impl-test` entry, which always follows `impl`. |
| COR-5 | low | `.asd/workflows/asd-phase-scope.md:7` (step 2a); `.asd/rules/sprint-lifecycle.md:9` ("Retro intake") | `retro-candidates` fails closed (exit 2) on a malformed backlog, on a retro row whose `Acts on` is not `consumer`/`asd` (for example a legacy consumer retro written in a non-English `language.docs`), and on a deferred row whose retro is not archived. Neither the acting site nor the rule gives a branch for a non-zero exit, so the orchestrator has to improvise inside the hard scope gate. | Add one clause to step 2a: a non-zero exit blocks intake, and the stderr goes to the user as a scope-gate decision (fix the backlog or retro, or skip intake for this sprint with one decisions-log line). |
| COR-6 | low | `.asd/rules/providers.md:50` vs `.asd/workflows/asd-phase-impl.md` step 6 payload list (L66-68), `.asd/workflows/asd-phase-impl-test.md` step 1a, `.asd/workflows/asd-phase-impl-review.md` step 9 tester dispatch — **AC-9** | The header rule ("before every `delegate to agent`") is cited explicitly only at the review payload sites. 018 F-2's evidenced case is `asd-dev` dispatches in `impl`, but the payload list there does not mention the `Repo root:` line. The only thing that reaches it is the generic "Operation-mapping to host tools" pointer, which is weaker than the explicit citation the review sites got. | Add "opens with the header per `providers.md` "Dispatch payload header"" to impl step 6's payload bullet and to the tester dispatches in impl-test 1a and impl-review step 9. |
| COR-7 | low | `README.md:186` (`/asd-update` row) | The same sentence says `/asd-update` "never touches your config (beyond a migration's …)" and also never touches "`.asd/project/**`". Config lives at `.asd/project/config.yaml` and migrations edit it, so the two statements contradict each other. | Change it to "the rest of `.asd/project/**` (stubs, custom rules, the retro backlog)", or list those files explicitly. |

## Coverage (internal reviewers only)

```json
{"manifest_digest":"68724708fe60aaa5ec3a596f31816eaf0752fe5c013255e622af702225594543","findings":["COR-1","COR-2","COR-3","COR-4","COR-5","COR-6","COR-7"],"files":[{"i":".asd/agents/asd-external-review.md","s":"checked"},{"i":".asd/agents/asd-tester.md","s":"checked"},{"i":".asd/release-manifest.json","s":"checked"},{"i":".asd/rules/artifact-layout.md","s":"checked"},{"i":".asd/rules/checkpoints.md","s":"checked"},{"i":".asd/rules/external-review.md","s":"checked"},{"i":".asd/rules/git-strategy.md","s":"checked"},{"i":".asd/rules/providers.md","s":"checked"},{"i":".asd/rules/review-policy.md","s":"checked"},{"i":".asd/rules/sprint-lifecycle.md","s":"checked"},{"i":".asd/runtime.js","s":"checked"},{"i":".asd/templates/t_decisions-log.md","s":"checked"},{"i":".asd/templates/t_retro-backlog.md","s":"checked"},{"i":".asd/templates/t_retrospective.html","s":"checked"},{"i":".asd/workflows/asd-phase-design-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-test.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl.md","s":"checked"},{"i":".asd/workflows/asd-phase-retro.md","s":"checked"},{"i":".asd/workflows/asd-phase-scope.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/MEMORY.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/project_parallel-agent-commit-sweep.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/project_parallel-wave-home-citations.md","s":"checked"},{"i":".claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md","s":"checked"},{"i":".claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/feedback_fail-first-and-none-honesty.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/project_mutation-runs-trip-the-hash-ledger.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/project_testability-envelope.md","s":"checked"},{"i":"CHANGELOG.md","s":"checked"},{"i":"README.md","s":"checked"},{"i":"tests/run.js","s":"checked"}],"rules":[{"i":"Draft correctness [design-review]","s":"n/a","p":"outside phase gate"},{"i":"Bugs [impl-review]","s":"finding","f":"COR-2"},{"i":"Security [impl-review]","s":"pass"},{"i":"Contracts [impl-review]","s":"finding","f":"COR-3"},{"i":"Best practices [impl-review]","s":"finding","f":"COR-5"},{"i":"AC coverage trace [impl-review]","s":"finding","f":"COR-1"},{"i":"UI conformance [design-review — `n/a: outside phase gate` without a ux-spec/design-system draft; impl-review — conditional on a UI surface in scope]","s":"pass"},{"i":".asd/project/custom-common-rules.md","s":"pass"},{"i":".asd/project/custom-coding-rules.md","s":"pass"}],"sections":[{"i":"Draft correctness [design-review]","s":"n/a","p":"outside phase gate"},{"i":"Bugs [impl-review]","s":"reviewed"},{"i":"Security [impl-review]","s":"reviewed"},{"i":"Contracts [impl-review]","s":"reviewed"},{"i":"Best practices [impl-review]","s":"reviewed"},{"i":"AC coverage trace [impl-review]","s":"reviewed"},{"i":"UI conformance [design-review — `n/a: outside phase gate` without a ux-spec/design-system draft; impl-review — conditional on a UI surface in scope]","s":"reviewed"}]}
```

Section notes:
- **UI conformance**: reviewed under the self-hosting framework-templates carve-out. `t_retrospective.html` only gains `<tr id>` attributes and a comment, and the ids do not collide with the `F-N` anchors.
- **Security**: the backlog `Row` regex keeps the sprint segment from carrying `..` or `/`, so `rowsOf` cannot be steered outside `archived/`. No secrets, no shell spawn.
- **AC trace**:
  - Met: AC-1..AC-8, AC-10..AC-13, AC-15 and AC-16, with the COR-2/4/5 caveats.
  - Partial: AC-9 (COR-6) and AC-14 (COR-1, COR-3).
  - AC-17: suite not run (no shell), but the views and hashes are updated.
- **Custom rules**: `runtime.js` stays zero-dependency, and the canon agent edits are synced.

## Verdict
CONCERNS: 7

## Next action
impl review-fix: `asd-dev` resolves COR-2..COR-7. COR-1 goes to the user first (question below); once answered, `asd-dev` applies the chosen option across canon, README, CHANGELOG and tests/run.js, and the memory lines are routed per the memory-fix rules.

## Escalations (optional)
- finding #COR-1: requires user approval (contract change: reviewer tool grants, or reversing the direction of the AC-14 correction)
- question: COR-1 — the Claude host auto-enables `Write` for `memory: project` subagents, and reviewer `disallowedTools` strip only `Edit`, so reviewers do hold a memory write tool today (this dispatch holds `Write`). Which way should AC-14 be made true?; options: (a) add `Write` to every reviewer's and External Review's `disallowedTools` (read-only enforced by config, memory changes only via the memory-fix dispatch) / (b) restore a host-scoped statement that reviewers write their own memory on Claude, keeping the memory-fix dispatch only for owners with no write tool
  answer: (b) — on Claude a reviewer holds `Write` for its own memory via `memory: project`; canon states that truthfully (policy, not host, keeps it to its memory dir); a finding in its memory is fixed by that owner in a fresh memory-fix dispatch using its own write tool, the orchestrator commits it; no disallowedTools change (user, 2026-09-26)

Memory note: the reviewer added `.claude/agent-memory/asd-reviewer-correctness/feedback_check-host-claims-against-own-dispatch.md` and one index line in `MEMORY.md`, written with the host-served `Write` tool (the COR-1 evidence). Both are left uncommitted for the orchestrator's bookkeeping commit.
