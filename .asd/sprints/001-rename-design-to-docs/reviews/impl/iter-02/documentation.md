[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: 2
- **Severity floor**: medium (low findings dropped from verdict computation)

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| 1 | medium | `.asd/templates/t_test-plan.md:5` | Wrong root-vs-sibling classification, and it contradicts a file inside this same diff. `delegates_to: plan.md (tasks), design/ docs (requirements), reviews/impl/iter-NN/testing.md (verdict)` names the sprint-local draft folder as the home of requirements, but `.asd/workflows/asd-phase-impl-test.md:11` — reworded in this very fix pass — declares the impl-test phase reads `persistent docs (PRD ACs, api, ux-spec)`. The template a phase writes and the workflow that runs it now point at two different homes for the same fact (SSoT iron rule, `artifact-layout.md`). Corroboration all points to the root reading: `audit.md` §2 classifies `t_test-plan.md:5` as in-scope; `sprint.md` AC-1 enumerates `t_test-plan.md`; `decisions-log.md` (2026-09-01, impl fix for iter-01, item (c)) records `t_test-plan.md` as a **confirmed docs-root reference** given the "persistent docs" wording — which the file does not contain. test-plan.md is authored in impl-test, i.e. after design-promote, so the SSoT for requirements at that moment is `docs/product/requirements/`, not the superseded sprint draft. The user-approved iter-01 escalation (decisions-log) lists only `t_config.yaml:13`, `README.md:227`, `t_sprint.md:5` and the `config.yaml:15` mirror as reverts — `t_test-plan.md` was never covered by that approval. | Apply the wording (not the revert): `delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/iter-NN/testing.md (verdict)`. Templates have no sync target, so no regeneration needed; recompute the manifest ledger (bare `node .asd/sync.js --apply`). |
| 2 | medium | `.asd/templates/t_sprint.md:5` (+ `t_test-plan.md:5` until #1 lands) | The approved AC-3 exception is not recorded anywhere in the sprint's acceptance/traceability chain, so the sprint's own completeness gate is now self-contradicting. `sprint.md` AC-1 still explicitly enumerates `t_sprint.md` and `t_test-plan.md` among the files whose root reference must become `docs/`; `sprint.md` AC-7's exclusion set (`<sprint>/design/`, `reviews/design/`, `asd-phase-design*`, `design/design-review`, `design-system*`, `design-principles*`, `t_design-md-delta.yaml`, `.asd/sprints/**`, `decisions-log.md`, `CHANGELOG.md`) does not cover a bare `design/ docs` in a template frontmatter, so a literal re-run of the AC-7 grep pair — which `plan.md`'s DoD names as the sprint's *only* real completeness gate (risk R-11) — now returns in-scope hits and reports a false failure at the `pr` phase. `plan.md:61` (`t_sprint.md — line 5`) and `plan.md:60` (`t_test-plan.md — line 5`) are still ticked `[x]`, asserting a rename that was deliberately undone. The decision itself is sound and user-approved (decisions-log, impl-review iter-01 escalation); only the record is stale. | PM/BA: record the approved exception where the gate reads it — add the `delegates_to:` sprint-sibling references to AC-7's exclusion set and narrow AC-1's file enumeration, and annotate the two `plan.md` Task 2 subtasks as "reverted per iter-01 escalation" instead of leaving them ticked as renamed. No new decision required — the exception is already approved; this is bookkeeping to match. |

## Coverage ledger

### File coverage

| File | Status |
|---|---|
| `.asd/agents/asd-reviewer-simplification.md` | checked — l.98 now `Never modify code or persistent docs`; iter-1 finding #3 (part 1) resolved. Provider mirrors regenerated and consistent (`.claude/agents/asd-reviewer-simplification.md:97`, `.codex/agents/asd-reviewer-simplification.toml:92`), per custom-coding-rules.md sync-after-canon-edit rule |
| `.asd/release-manifest.json` | checked — clean ledger recompute, no stray edits: `managed_paths` still `.asd/`-only (7 entries), `model_families` unchanged, `asd_version: 1.2.0` correctly *not* bumped (owned by `pr` phase per git-strategy, matching `## Unreleased` in CHANGELOG). Every canonical file touched this pass has an `upstream_hashes` entry (`t_config.yaml`, `t_plan.md`, `t_sprint.md`, `t_test-plan.md`, `language-policy.md`, `review-policy.md`, `asd-reviewer-simplification.md`, `asd-init/SKILL.md`, the 4 workflows); `canon_hashes` correctly covers agents/skills only (workflows have no sync target). Every remaining `design`-bearing key is an AC-3/AC-4 protected file name |
| `.asd/rules/language-policy.md` | checked — l.8 `persistent docs` in the artifact-type matrix; row still reads correctly as a doc-class enumeration, no root path lost. Rest of the matrix unaffected |
| `.asd/rules/review-policy.md` | checked — l.136 framework-mode sentence reads `…consistency against the framework diff, independent of persistent docs`, consistent with this agent's own rubric wording and with the `self_hosting` clause in `asd-phase-impl-review.md:22`. Verdict-token grammar, coverage-ledger contract, and severity-floor table unchanged and still mirrored by the impl-review workflow |
| `.asd/skills/asd-init/SKILL.md` | checked — description l.4 `persistent docs`; post-init artefact checks l.58–60 `docs/product/concept.html`, `docs/architecture/stack.html`, `docs/ux/DESIGN.md`; both separator forms of the designmd aliases correct (l.101/103 `docs\ux\DESIGN.md`, l.107/109 `docs/ux/DESIGN.md`); `@google/design.md` package name correctly untouched. JSON frontmatter still well-formed; both provider views carry the same description |
| `.asd/templates/t_config.yaml` | checked — l.13 back to `prd: enabled  # design/prd.html + persistent requirements`; sibling-row pattern restored (l.12 `<sprint>/audit.md`, l.14 ux-spec, l.15 `adr.html`, l.16 `c4-full`), all naming sprint-draft artifacts. **iter-1 finding #1 resolved.** l.8/l.48 prose `persistent docs` correct |
| `.asd/templates/t_plan.md` | checked — l.5 `persistent docs (requirements/design)`; l.23–25 now `../../../docs/...`. Depth verified: from `.asd/sprints/<NNN-slug>/plan.md`, `../../../` resolves to repo root, so the links land on `<root>/docs/product/requirements/…`, `<root>/docs/architecture/adr/…`, `<root>/docs/ux/…`. **iter-1 finding #4 resolved.** Frontmatter responsibility block intact; `delegates_to` root reading corroborated by its own Context links |
| `.asd/templates/t_sprint.md` | checked — l.5 reverted to `design/ docs (decisions)`. Classification itself is sound: `sprint.md` is authored at `scope`, before design-promote, and its sibling `delegates_to` items (`plan.md`, `audit.md`) are all sprint-local, so no persistent-root reading is available; the revert is user-approved (decisions-log iter-01 escalation). Record-side gap → **finding #2** |
| `.asd/templates/t_test-plan.md` | checked — l.5 reverted to `design/ docs (requirements)`. Contradicts `asd-phase-impl-test.md:11`, `audit.md` §2, `sprint.md` AC-1 and the decisions-log fix record → **finding #1** |
| `.asd/workflows/asd-phase-impl-review.md` | checked — l.12 `persistent docs` in the read list, l.32 `persistent docs actuality vs implementation, SSoT, traceability` matching this agent's declared scope. Reviewer read-only contract (§"Reviewer read-only contract", step 5/6) and the `[REVIEW-impl-<reviewer>]` token still agree with `review-policy.md`; coverage-ledger gate at step 6 unchanged |
| `.asd/workflows/asd-phase-impl-test.md` | checked — l.11 `persistent docs (PRD ACs, api, ux-spec)`; correct root reading, and the corroborating evidence for finding #1. l.28/29 pathspec + AC-source logic unchanged |
| `.asd/workflows/asd-phase-impl.md` | checked — l.12 read list and l.35 `requirement ambiguity unresolvable from plan + persistent docs`; **iter-1 finding #3 (part 2) resolved.** (l.41 "Within plan + design-doc scope" left as-is — reads as "the design documents", not the root; below floor, not raised) |
| `.asd/workflows/asd-phase-plan.md` | checked — l.7 `design-promote done: persistent docs reflect approved sprint design`, l.11 and l.19 read lists likewise; consistent with `sprint-lifecycle.md:53` (`plan | PM | promoted persistent docs | plan.md`) and with `t_plan.md`'s own `../../../docs/...` Context links |
| `CHANGELOG.md` | checked — l.8 extended migration entry is internally consistent across both cases: case A (no existing `docs/`) prescribes `git mv design docs`; case B (existing `docs/`) explicitly forbids that same command with a correct explanation of the `docs/design/...` nesting outcome, and substitutes `git mv design/product design/architecture design/ux docs/` guarded by a stated precondition and a manual-collision fallback. The three moved subtrees match the `artifact-layout.md` root exactly (both decomposition modes), so nothing is left behind. Ordering (`commands.yaml` aliases → `/asd-update` → `/asd-sync`), the split-brain-window warning, and the "nothing errors, corpus silently splits" failure mode are all consistent with `asd-update/SKILL.md`'s "never touches consumer-owned config/persistent docs" contract. No self-contradiction between the two cases |
| `README.md` | checked — l.227 `prd: enabled  # design/prd.html + persistent requirements`, byte-identical in meaning to `t_config.yaml:13`; **iter-1 finding #1 resolved on the mirror side too.** Framework-mode mirror re-verified: config-schema excerpt (l.223–244) matches `t_config.yaml`; folder map (l.306–320) matches the `docs/` tree in `artifact-layout.md` line for line; l.83 `docs/ (your persistent docs)` and l.329 flat-mode note consistent; agent roster (15), skill count (17), and phase list untouched by this pass and still accurate. A `design[/\\]` grep over README returns exactly one hit — l.227, the intended sprint-draft reference |
| `tests/run.js` | checked — l.950–965 drift assertion now asserts every `--check` item is `current` rather than trusting `ok: true`, with `SELF_SOURCED_ALLOWLIST = new Set(['AGENTS.md'])`. The explanatory comment (l.956–961) accurately restates the documented rule: root `AGENTS.md` is self-sourced under `self_hosting: enabled` (AGENTS.md "What this repo is"; `providers.md:9` "read directly; no generation"), so `modified-foreign` is expected state, not drift. No doc/code contradiction introduced. Non-doc test mechanics are outside this reviewer's rubric (Testing reviewer) |

### Rule coverage

| Rubric item | Status |
|---|---|
| SSoT — each fact one home, downstream links not copies | finding #1 (requirements home split between `t_test-plan.md:5` and `asd-phase-impl-test.md:11`) |
| Template adherence — responsibility frontmatter present, content respects `owns`/`excludes` | pass — all four scoped templates (`t_config.yaml` has none by design, being a config file; `t_plan.md`, `t_sprint.md`, `t_test-plan.md`) retain a complete `responsibility` block with `owns`/`excludes`/`delegates_to`; no section strays outside declared scope. The `delegates_to` *target* correctness is finding #1, not a block-structure defect |
| HTML shell wrapping + placeholder fill + fragment invariants | n/a: no user-facing HTML artifact in scope — this diff touches Markdown, YAML, JSON and JS only |
| Provenance flag correctness (`original` default, badge omission) | n/a: no provenance-bearing artifact in scope (no HTML docs; framework source carries no `provenance` field) |
| Traceability — ACs map to ADRs and to code | finding #2 (`sprint.md` AC-1/AC-7 and `plan.md` Task 2 ticks contradict the shipped state of `t_sprint.md:5`/`t_test-plan.md:5`). ADR mapping n/a — `documents.adr` disabled in this repo's lean self-hosting profile |
| Persistent actuality (impl-review) — stack/commands/api/adr/requirements reflect code | n/a: no persistent `docs/` tree exists in this repo (`prd`/`ux_spec`/`adr`/`c4` disabled per `.asd/project/config.yaml`), so there is nothing to drift-check. This is exactly the case `review-policy.md:136` covers by redirecting the framework-mode check to README/`.asd/rules/**` |
| Framework mode (`self_hosting: enabled`) — README + `.asd/rules/**` consistent with the canonical diff | pass — README config excerpt, folder map, phase list, agent roster and model-tier tables all consistent with the post-fix canon; `language-policy.md` and `review-policy.md` wording consistent with the agent files and workflows that mirror them; provider views regenerated for both touched canonical agent/skill sources |
| Custom rules consistency (`custom-common-rules.md` glossary + `custom-coding-rules.md`, impl phase) | pass — "canonical source / provider view / consumer" vocabulary used consistently across CHANGELOG, README and the skills; no hand-edited generated file (both `asd-reviewer-simplification` and `asd-init` mirrors match canon); zero-dependency constraint untouched by `tests/run.js` |
| Language policy | pass — all scoped files are workflow infrastructure or machine-readable, all English and dense per `language-policy.md:9–11`; `language.docs: en` matches |
| Naming (`artifact-layout.md`) | pass — no file added, renamed or moved this pass; `t_` prefix and kebab-case conventions intact |
| Severity floor + nitpick discipline (`review-policy.md`) | pass — floor `medium` applied: pure-wording items (e.g. `asd-phase-impl.md:41` "design-doc scope"; the run-on connector style in `CHANGELOG.md:8`; `t_plan.md:5` "requirements/design" reading as a path) observed but dropped, not raised. Both reported findings carry a concrete cross-file contradiction, not a style preference |

## Verdict

CONCERNS: 2

Both iter-1 findings that survive this floor are verified resolved, and so are the three that were dropped by it: #1 (`t_config.yaml:13` + `README.md:227` restored to `design/prd.html`, sibling-row pattern intact), #3 (`asd-reviewer-simplification.md:98` and `asd-phase-impl.md:35` now "persistent docs"), #4 (`t_plan.md:23-25` now `../../../docs/…`, depth verified correct from `.asd/sprints/<NNN-slug>/plan.md`).

On the iter-1 finding #2 split I was asked to re-verify: the split was applied, but **only half of it is correct**. `t_sprint.md:5` → `design/` is right (no persistent-root corroboration exists; sprint.md predates design-promote; user-approved). `t_test-plan.md:5` → `design/` is wrong: the corroboration the disambiguation test asks for does exist, in the phase workflow that writes the file (`asd-phase-impl-test.md:11`, reworded to "persistent docs" in this same commit range), and it is further confirmed by `audit.md` §2, `sprint.md` AC-1 and the decisions-log entry that records `t_test-plan.md` as a *confirmed docs-root reference*. No file was missed — a `design/(product|architecture|ux|c4|adr|api|stack|concept|requirements|\*\*| docs)` plus `design\\ux` sweep across the repo returns only `t_sprint.md:5`, `t_test-plan.md:5`, the intentional `CHANGELOG.md:8` migration text, and legitimate `<sprint>/design/…` draft paths.

No new inconsistency in the other three areas I was asked to probe: `t_plan.md`'s `../../../` Context links resolve correctly to the repo root, `CHANGELOG.md`'s two migration cases do not contradict each other, and `release-manifest.json` is a clean ledger recompute with no stray edits.

## Next action

Route to `impl` (review-fix mode) per `review-policy.md` "Autofix vs escalation" — impl-review does not fix findings itself.

- Finding #1 → `asd-backend-dev`: reword `t_test-plan.md:5` to `persistent docs (requirements)`; no provider sync needed (templates have no sync target), but re-run the bare `node .asd/sync.js --apply` ledger recompute.
- Finding #2 → `asd-pm`: bring `sprint.md` AC-1/AC-7 and the two `plan.md` Task 2 subtasks into line with the already-approved `t_sprint.md` exception, so the AC-7 grep gate the `pr` phase re-runs does not report a false failure.

## Escalations

None required. Finding #2 touches approved AC text, but the underlying decision was already granted by the user at the iter-01 escalation (decisions-log, 2026-09-01, "impl-review iter-01: CONCERNS/FAIL → impl review-fix") — recording it is bookkeeping, not a new requirement change. If PM judges the AC-1/AC-7 edit to constitute a fresh requirement change under `review-policy.md`, confirm with the user before editing `sprint.md`.
</content>
