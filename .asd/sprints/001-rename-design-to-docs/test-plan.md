---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests
  excludes: task breakdown, requirements, review verdicts, code
  delegates_to: plan.md (tasks), docs/ docs (requirements), reviews/impl/iter-NN/testing.md (verdict)
---

# Test plan — sprint 001-rename-design-to-docs

## Change surface

11 commits, 91 files changed (658 insertions, 458 deletions), `git diff main...HEAD --stat`:

- `.asd/rules/*.md` (8 files) — prose/path rename, docs-root segment `design/` → `docs/`: `artifact-layout.md`, `core.md`, `sprint-lifecycle.md`, `checkpoints.md`, `language-policy.md`, `design-system.md`, `review-policy.md`, `external-review.md` (`ux-principles.md`, `code-style.md`, `design-principles.md`, `providers.md`, `git-strategy.md` untouched — none contains a doc-root reference; corrected from "9" per review-fix iter-01 finding #5 — with 8 the group counts reconcile exactly to the 49-file review scope)
- `.asd/templates/*` incl. `external-review/t_prompt-external-impl.md` (10 files) — path/prose rename, backslash-spelled command aliases
- `.asd/agents/*.md` (14 files) — path/prose rename, incl. 3 write-access allowlist lines
- `.asd/skills/*/SKILL.md` (6 files) — path/prose rename inside JSON frontmatter `description` fields
- `.asd/workflows/*.md` (7 files) — path/prose rename, no generated counterpart
- `.asd/project/config.yaml` — comment + prose rename
- `.asd/release-manifest.json` — hash ledger recompute only (no hand edit)
- `README.md`, `AGENTS.md` — mirror rename
- `CHANGELOG.md` — new migration entry (content addition, not a rename)
- `.claude/agents/*.md`, `.codex/agents/*.toml`, `.claude/skills/*/SKILL.md`, `.agents/skills/*/SKILL.md` — regenerated provider views (sync.js output, not hand-written)

No file under `.asd/sync.js` or `.asd/skills/asd-update/update.js` (the two engines `tests/run.js` covers) appears in the diff. Confirmed by `git grep -l design .asd/sync.js .asd/skills/asd-update/update.js tests/run.js` → no matches in any of the three: the rename touched zero engine code.

## Risk → check decisions

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `.asd/rules/*.md` rename (Task 1) | in-scope/out-of-scope `design/` occurrences on adjacent or same lines, regex sweep corrupts `design`-phase paths | static (repo-wide grep with fixed exclusion set) | none | No executable logic — a prose/path string change. Completeness already verified mechanically in impl Task 11 via two grep patterns (`design/` and `design\\`) over a fixed exclusion set, re-run here independently (see Suite run) with zero unexplained hits. A unit/property/component test cannot express "is this markdown string correct" more reliably than the grep already does. |
| `.asd/templates/*` incl. backslash-spelled command aliases (Task 2) | separator-blind occurrences (`design\\ux\\DESIGN.md`) invisible to a `design/` grep | static (second grep pattern `design\\`) | none | Same reasoning — impl Task 11 ran a second, separator-aware grep specifically to close this gap (audit R-10). Re-verified independently below. |
| `.asd/agents/*.md` write-access allowlists (Task 3) | a missed rename silently revokes an agent's write permission to the promoted doc, surfacing sprints later | static (targeted line-level grep against audit.md's per-file line list) | none | This is a permission-string content risk, not a behaviour a unit test can exercise — there is no code path in this repo that reads/enforces these allowlist strings at runtime (they are consumed by an LLM reading its own prompt in a downstream project, not by any engine in `tests/run.js`'s coverage). Re-confirmed by inspection during this pass: all three lines carry `docs/product/...` etc., none still read `design/`. |
| `.asd/skills/*/SKILL.md` frontmatter (dispatch-trigger `description` strings) (Task 4) | malformed JSON edit breaks dispatch on both providers | static (JSON parse) | none | `node .asd/sync.js --check` parses every skill's frontmatter as part of computing sync status; a parse failure would surface as a build failure, not a silent pass. Build gate below is green, so all 6 edited `SKILL.md` frontmatters parse. No new test needed — `sync.js --check`'s own JSON parsing is the correct-level check and it already ran. |
| `.asd/workflows/*.md` (Task 5) — no sync target at all (audit G-10) | `sync.js --check` cannot see this directory; a stale rename here is invisible to the build gate | static (repo-wide grep, same exclusion set) | none | Same as rules/templates — covered by the grep re-run below, which does not exclude `.asd/workflows/`. |
| `.asd/release-manifest.json` hash ledger recompute (Task 10) | hand-edit would corrupt the hash ledger; stale `upstream_hashes` would make a consumer's `update.js` silently skip renamed files | unit (existing) | keep | Already covered by the two existing `tests/run.js` cases "release-manifest.json: every canon_hashes entry matches the actual file" and "...every upstream_hashes entry matches the actual file" — both ran green in this suite pass, proving the ledger is internally consistent post-recompute. No new test needed. |
| Provider view regeneration `.claude/`, `.codex/`, `.agents/` (Task 9) | drift between canon and generated view | unit (existing, strengthened) + build (`sync.js --check`) | keep, strengthened | Review-fix iter-01 finding #1: `sync.js --check` always exits 0 with `ok: true` — drift only shows as a per-item `status` string a human must read, so the prior fixture (`parsed.ok === true` + `Array.isArray(parsed.items)`) could not fail on drift. Extended the existing `tests/run.js:950` fixture (not new infrastructure) to assert every reported item's `status === 'current'`, with an explicit allowlist for the self-sourced `AGENTS.md` (legitimately `modified-foreign` under `self_hosting: enabled` — self-sourced managed blocks are check-only, not a sync target). See Suite run for the rerun result. |
| README.md + AGENTS.md mirror (Task 8) | folder map / command table half-updated relative to the renamed `artifact-layout.md`, `commands.yaml` (plan.md R-7; sprint.md AC-8) | static (line-for-line diff of README's folder-map block and config excerpt against `artifact-layout.md`'s decomposition-enabled/flat trees) | none | Review-fix iter-01 finding #3: risk row was missing entirely (silent, not a stated `none`). Re-checked by inspection during this pass: README's folder-map block (lines ~300-320) reads `docs/` matching `artifact-layout.md:45,67,70,84,141`'s `docs/` root; `AGENTS.md`'s self-sourced prose (line 32) also reads `docs/`. No executable logic in a mirror-prose diff — static line comparison is the correct, cheapest tier and was already performed by impl Task 8's own last subtask; this is independent re-verification, not a new check class. |
| AC-3/AC-4 over-rename (sprint-local `<sprint>/design/`, `reviews/design/`, phase names `design`/`design-promote`/`design-review`, file names `DESIGN.md`/`design-system*`/`design-principles*`/`t_design-md-delta.yaml` must NOT be renamed) | regex rename sweep over-fires and renames a sprint-local or phase-name occurrence that only coincidentally matches `design/` | static (inverse repo-wide grep for over-rename tokens: `reviews/docs`, `<sprint>/docs`, `docs-review`, `docs-promote`, `docs-system`, `docs-md-delta`, `asd-phase-docs`, `custom-docs-rules`, `docs-principles`, `DOCS.md`, `docs/iter-`, repo-wide minus `.asd/sprints/**`) | none | Review-fix iter-01 finding #2: only the under-rename direction had a recorded check; AC-3/AC-4 are pure must-NOT-change criteria and plan.md's top-rated content risk (R-1: "~180 of 426 raw hits are exclusions", the same-line confusion class) had no row. Added this check; see Suite run for the verbatim command and result. |
| `CHANGELOG.md` migration entry (Task 7) | new prose content, not a rename — could be wrong/misleading guidance | manual read | none | Pure documentation content with no acceptance-testable behaviour; verified by inspection during this pass, matches plan.md Task 7's required elements (breaking-change framing, `git mv design docs` migration steps, split-brain window note). Not a candidate for automated test — no runtime behaviour to assert. |
| Whole-sprint regression risk: could `design/` as the root spelling silently reappear in a future change | future dev accidentally reintroducing `design/` into a rule/template/agent/skill/workflow prose or path, unnoticed because it's not code | considered: standing "no bare `design/`" content-guard fixture in `tests/run.js` | none (deliberately not added) | Rejected on value grounds, not feasibility (corrected per review-fix iter-01 finding #4 — the prior wording claimed this "structurally cannot" be done without new test infrastructure, which is false: `tests/run.js` already reads the live repo tree in four places, e.g. the manifest/sync-state load at `:860`, the `sync.js --check` spawn at `:950`, and the canon-file hash walks at `:1290`/`:1301` — a repo-content grep guard would be an extension of that existing pattern, not a new fixture class). The real reason to reject it: low recurrence value for a one-time rename, weighed against the maintenance cost of the exclusion allowlist a standing guard would need (`<sprint>/design/`, `reviews/design/`, `design`/`design-promote`/`design-review` phase names, `DESIGN.md`/`design-system*`/`design-principles*`/`t_design-md-delta.yaml` file names) — that allowlist decays faster than the low-probability, hand-edit-only regression it guards, and impl-review's documentation/quality reviewers already read renamed prose line-by-line every sprint at no extra cost. Decision: no test added. |

## Removed tests

None. No test in `tests/run.js` was made obsolete, duplicate, or otherwise stale by this change — the suite's 77 tests all cover `.asd/sync.js`/`update.js` engine behaviour, none of which the rename touched. Confirmed by full green run (see Suite run) with identical pass count/names to the pre-sprint baseline noted in `plan.md` Task 11.

## Added tests

No new test files or fixture classes added. One existing fixture was strengthened, not added: `tests/run.js:950` (`node .asd/sync.js --check` runs clean) now asserts every reported item's `status === 'current'` instead of just the response shape — review-fix iter-01 finding #1, see the Provider view regeneration row and Suite run. This is an extension of an existing test, not new infrastructure. Otherwise every material risk in this sprint is already discharged by an existing mechanical check (impl's own repo-wide grep, `sync.js --check`'s JSON-parse-as-part-of-status-computation, or the two existing hash-ledger unit tests), and the one candidate new check (a standing "no bare `design/`" content-guard fixture) was deliberately rejected as new test infrastructure out of proportion to a one-time rename's regression risk.

## Suite run

Re-run at impl-test re-entry (after impl review-fix iter-01 closed), against commit `56a0c11` (`chore: close impl review-fix for iter-01`) — head includes `7a2d299` (`test: strengthen sync.js --check drift assertion`) and `430796f` (`docs: correct test-plan.md record per impl-review testing findings`). No new files entered the change surface since the prior test-plan.md update; the fixes since the last recorded run were content corrections only. Full gate re-run below confirms green post-fix.

- Command: `test` — `node tests/run.js`
- Result: pass — 77/77 passed, 0 failed, 0 skipped (including the strengthened `sync.js --check` drift-assertion case: `"node .asd/sync.js --check" reports every item current (no drift), except the self-sourced AGENTS.md`)
- Lint: `git diff main...HEAD --check` — pass, exit 0, no whitespace errors
- Build: `node .asd/sync.js --check` — pass, exit 0, `"ok": true`. All items report `status: "current"` except `AGENTS.md` (`"modified-foreign"`, expected: `self_hosting: enabled` makes it self-sourced/hand-edited, not a sync target — the strengthened `tests/run.js:950` fixture allowlists exactly this one item and passed).

Below (grep completeness/over-rename checks) unchanged from the prior recorded run — the underlying files were not touched again since that pass, only `test-plan.md`'s own record and `tests/run.js`'s fixture were edited in the review-fix pass:
- Completeness grep 1 (under-rename, AC-1/AC-7 — verbatim, reproducible): `git grep -n "design/" -- . ':(exclude).asd/sprints/**' ':(exclude)CHANGELOG.md'` → 127 raw hits, piped through `grep -viE "<sprint>/design/|reviews/design/|design-review|design-promote|design-system|design-principles|design-md-delta|DESIGN\.md|custom-design-rules"` (the documented out-of-scope classes) → 16 residual hits, all inspected and legitimate: 5 in `.asd/project/decisions-log.md:48,51,57,93,99` (prose narrating the rename itself, append-only per AC-7), 2 in `.asd/rules/artifact-layout.md:31,41` (`<sprint>/design/`, `reviews/design/iter-NN/` — sprint-local draft paths, out of scope per plan.md Task 1), 4 in generated-skill/manifest entries naming the `asd-phase-design` skill file (`.agents/skills/asd-phase-design/SKILL.md:2`, `.claude/skills/asd-phase-design/SKILL.md:2`, `.asd/release-manifest.json:54,99` — file name, AC-4, not a path), 5 in the sprint-draft-path comment/delegate lines fixed by the parallel backend-dev pass (`.asd/project/config.yaml:15`, `.asd/templates/t_config.yaml:13`, `t_sprint.md:5`, `t_test-plan.md:5`, `README.md:227` — all correctly read `design/prd.html` / `design/ docs`, the `<sprint>/design/` draft, not the persistent docs root). Zero unexplained hits.
- Completeness grep 2 (separator-blind variant, R-10): `git grep -n "design\\\\" -- . ':(exclude).asd/sprints/**' ':(exclude)CHANGELOG.md'` → 4 hits, all in `.asd/project/decisions-log.md:56,80,86,93` (prose narrating the `design\\` grep pattern itself, append-only). Zero occurrences of an actual Windows-path `design\\` alias — all confirmed renamed to `docs\\`.
- Over-rename check (AC-3/AC-4, new per finding #2 — verbatim, reproducible): `git grep -niE "reviews/docs|<sprint>/docs|docs-review|docs-promote|docs-system|docs-md-delta|asd-phase-docs|custom-docs-rules|docs-principles|DOCS\.md|docs/iter-" -- . ':(exclude).asd/sprints/**'` → exit 1, 0 hits. No sprint-local path, phase name, or preserved file name was over-renamed to a `docs`-prefixed spelling.

## Defects

None found.

## Manual verification (optional)

None. This sprint has no visual UI, no third-party live integration, and no UX-feel surface — it is a textual rename of infrastructure prose/paths, fully covered by the static checks above.
