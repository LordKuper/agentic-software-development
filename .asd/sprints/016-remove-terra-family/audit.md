---
responsibility:
  owns: brownfield findings for sprint scope (existing docs, code, gaps incl. dependencies/migration, risks)
  excludes: requirements, decisions, plan, code
  delegates_to: prd.html (requirements), adr.html (decisions), plan.md (tasks)
---

# Audit

An absent optional section below means an empty finding set for that section, never an unperformed check (`.asd/rules/sprint-lifecycle.md` "Audit phase").

## Scope reference
[sprint.md](./sprint.md)

## Touched areas
- `.asd/agents/asd-dev.md:10`, `.asd/agents/asd-tester.md:10`: base `codex.model` `"terra"` → `"sol"`; effort `medium`, sandbox `workspace-write` unchanged; `mechanical`/`critical` variants unchanged.
- `.asd/agents/asd-external-review.md:13`: wrapper `codex.model` `"terra"` → `"sol"`; effort `medium`, sandbox `read-only` unchanged; `wraps_model` (Claude-side `sol` :9, Codex-side `opus` :14) unchanged.
- `.asd/release-manifest.json:27`: drop `model_families.codex.terra`; `canon_hashes`/`upstream_hashes` for edited files change (regenerated by sync); `asd_version` 11.1.0 → 12.0.0 at pr.
- `.asd/rules/providers.md:60` (family table), `:70`, `:73` (tier matrix).
- `.asd/sync.js:175`: Codex model-ID regex `(sol|terra|luna)` → drop `terra`.
- `README.md:38` (ChatGPT section), `:195` (roster intro), `:204`–`:205` (dev/tester rows), `:207` (task-variant prose "Sonnet/Terra"), `:219` (External Review row).
- `tests/run.js:136`, `:157`, `:2335`: fixtures and regex assertion.
- Generated `.codex/agents/asd-dev.toml:4`, `asd-tester.toml:4`, `asd-external-review.toml:4` (`gpt-5.6-terra`): regenerate from canon.
- `CHANGELOG.md`: new `## v12.0.0` section at pr (Removed / Changed / Migration).

## Existing docs found
- [providers.md model-family table](../../rules/providers.md): mirror of the manifest, "not a second source of truth" (:52); family bump is a one-line manifest edit (:63).
- [providers.md agent tier matrix](../../rules/providers.md): mirrors README model-tier table (:66); dev/tester base and external-review wrapper rows are `terra / medium`.
- [core.md](../../rules/core.md) :30: a release migration may edit settings only, limited to release-mandated key renames/removals and intent-carrying mappings.
- [asd-update SKILL.md](../../skills/asd-update/SKILL.md) :4: update never touches consumer custom skills/agents/hooks.
- [git-strategy.md](../../rules/git-strategy.md) "Versioning & Changelog": `!`/`BREAKING CHANGE` → MAJOR; DoD `max(.asd/migrations/*.js version) <= asd_version`.
- [update.js migration contract](../../skills/asd-update/update.js) :281–297: migrations run after managed-path write, idempotent, throw halts runner.
- [CHANGELOG.md v11.0.0](../../../CHANGELOG.md) :17–18: precedent `### Migration` with "**No migration script.**" and manual guidance.

## Contradictions
- `sprint.md` AC-5 (leaves open an automated migration rewriting consumer custom agents) vs `core.md:30` + `asd-update/SKILL.md:4` (release migration writes settings only; custom agents never touched): winner=`core.md` (canonical). No automated agent-rewriting migration; AC-5 is satisfied by the CHANGELOG migration note.

## Existing implementation found
- Live `terra` occurrences in AC-4 scope (20 lines): `.asd/agents/asd-dev.md:10`, `asd-tester.md:10`, `asd-external-review.md:13`; `.asd/release-manifest.json:27`; `.asd/rules/providers.md:60,70,73`; `.asd/sync.js:175`; `README.md:38,195,204,205,207,219`; `tests/run.js:136,157,2335`; `.codex/agents/asd-dev.toml:4`, `asd-tester.toml:4`, `asd-external-review.toml:4`. None in `AGENTS.md`, `.claude/**`, `.agents/skills/**`, `.asd/{runtime.js,migrations,templates,workflows,skills,hooks}`.
- Out of AC-4 scope, untouched: `CHANGELOG.md:10,211,255` (released), `.asd/sprints/archived/**`, this sprint's own files, `plans/multi-provider-support.md`.
- `sync.js:171–173`: an absent family already throws `unknown model family` before the regex — AC-1 failure comes from the manifest removal alone; the regex edit serves AC-4.
- `sync.js:162–165,178`: Codex effort vocabulary accepts `medium` for `sol`.
- `sync.js:434–452`: view status compares content digest only — only the three base `.codex/agents/*.toml` go stale. `node .asd/sync.js --check` currently clean (72 items).
- `sync.js:917–1030`: `recomputeAndWriteHashLedgers` rewrites `canon_hashes`/`upstream_hashes` on every valid `--apply` (called :1491).
- Consumer path: `buildSyncPlan` (`sync.js:1204–1229`) renders every `.asd/agents/*.md` incl. consumer-authored; `/asd-update` never rewrites a local-only file. After update, `runCheck` throws `Codex agent "<name>": unknown model family (family "terra", …)`, `update.js` exits 1 after applying files and version; later `sync --check` fails until canon is fixed; the old generated `.toml` keeps working at runtime. A consumer keeping a locally edited `asd-dev`/`asd-tester`/`asd-external-review` hits the same path via update conflict.

## Gaps
- All Q1 lines above still name `terra`.
- `tests/run.js:157`: regex → `(sol|luna)`. `:136` ("mismatched family model", `sol: 'gpt-5.6-terra'`) → `sol: 'gpt-6-luna'` so the case keeps exercising the `endsWith(-family)` branch rather than the regex. `:2335`: any distinct ID (`gpt-6-luna`). No test mirrors the providers.md tables or README tier table.
- `README.md:38` "Do not substitute the API-style `gpt-5.6` identifier" loses its referent — reword to the unsuffixed API-style identifier (e.g. `gpt-6`).
- Manifest beyond the family map: `canon_hashes` for the three agents and `upstream_hashes` for those plus `providers.md` and `sync.js` — regenerated by `sync.js --apply`, never by hand; `managed_paths` unchanged.
- External dependency gaps: none (`sol` → `gpt-6-sol` already live).
- Migration gaps: consumer custom or kept-local agent canon: `codex.model: "terra"` (and any `wraps_model: "terra"`) → `"sol"` — manual, documented in CHANGELOG `### Migration`.

## Risks
- Stale hash ledger: impact=ledger freshness tests (`tests/run.js:1923,1934`) fail if `providers.md`/`sync.js` are edited after the last `--apply`; mitigation=run `--apply` last, after every managed-path edit.
- Higher Codex cost on the most-used agents: impact=base dev/tester and Codex-side wrapper move to the flagship family; mitigation=intended by scope; state it under CHANGELOG Changed.
- Consumer `/asd-update` exits 1 after writing: impact=only consumers with a custom/kept-local `terra` agent; `sync --check` broken until fixed; mitigation=CHANGELOG migration note with grep-able instruction; diagnostic names agent and family.
- Commit type: impact=pr-phase version inference yields MINOR without a breaking marker; mitigation=the removal commit carries `!`/`BREAKING CHANGE`.
