---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/iter-NN/testing.md (verdict)
---

# Test plan — sprint 002-lean-workflow

## Entry log

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | b13d77c70e171e34abea6e3a52ed777fa51e3577 | full change surface |
| 2 | 2837b31e0be63b4cbcddfdcb7724f5564fd9446d | delta since entry 1 — impl-review iter-01 review-fix (62 files, 4 review-fix batches: phase-skill Write/Edit grants, external-review pathspec fix, architect.md contract corrections, t_html-shell.html/t_adr.html dead-code cleanup + color tokens + dark-mode + TOC_ASSETS conditional + multi-ADR id fixes, residual api/adr reference removal, write-allowlist widening, AC-source pointer fixes, pr-phase gate fixes, ~25 misc text/citation corrections) |
| 3 | 9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05 | delta since entry 2's own endpoint (`27ef1ffd90d2027c838b8ded5785357d7c5ce01e`, the commit entry 2's Suite run verified — not the earlier `2837b31e` marker, which only anchors entry 2's *code*-surface analysis and predates entry 2's own test-plan/test commits) — impl-review iter-02 review-fix (76 files, 2 review-fix batches): impl-test/impl-review tool-grant additions; `state.json.reviews.impl.iteration_heads` schema field; hardcoded floor-line removal; write-allowlist fixes; likec4-build command removal; pr-phase tests/lint skip made content-scoped (`git diff --quiet <recorded HEAD>...HEAD` over a scoped pathspec) instead of raw HEAD-sha equality; `t_html-shell.html`/`t_adr.html` visual fixes (TOC anchor target, per-decision `<h2>` heading); `{{TOC_ASSETS}}` canonical fill value inlined into `artifact-layout.md` (script removed — sticky-CSS-only, no JS); writer-attribution and residual-id cleanup; `artifact-layout.md` section reorder; one `CHANGELOG.md` note; `.asd/release-manifest.json` hash-ledger resync |
| 4 | ea55e35a1e2237cec3ff3b50a45aebea1c7fe662 | delta since entry 3 (`9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05`) — impl-review iter-03 review-fix (31 files): `state.json.reviews.impl.iteration_heads` absent-key fallback fix; `.asd/hooks/session-start.js`'s `lastReviewVerdict` now treats a `"skipped: <predicate>"` verdict string as satisfied (was falling through to `mixed`) — real code fix, regression test added directly to `tests/run.js`; `review.scoped_fan_out` wording fixes in `review-policy.md`/`external-review.md`; `{{TOC_ASSETS}}` placeholder deleted, its CSS merged straight into `t_html-shell.html`; stale scrollspy comment fix in `t_html-shell.html`; UI-predicate dedup across reviewer agent files; E-1 token-usage scoping in `asd-test-engineer.md`/`sprint-lifecycle.md`; test-selection rule-set dedup (`asd-test-engineer.md` now points at `code-style.md` §17 instead of restating it); a light-mode contrast fix in `t_adr.html`; new iter-03 review artifacts (`reviews/impl/iter-03/*.md`); `.asd/release-manifest.json` hash-ledger resync |

## Risk → check decisions

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| Task 1: ledger-summary review compression (`t_review.md`, 8 agent files regenerated) | `sync.js --check` goes dirty if `--apply` didn't run for all 8 regenerated agent files | static (existing `sync.js --check` CLI test) | none | `tests/run.js`'s `` `node .asd/sync.js --check` reports every item current `` test already asserts every agent-derived target is `current` — this is a generic drift check, not hardcoded per-file, so it automatically covers these 8 files with no test change needed |
| Task 2/16: `t_audit.md` section removal, BA/Architect parallel dispatch | prose/workflow-dispatch change only — no code path in `sync.js`/`update.js`/`session-start.js` reads `t_audit.md` structure | none | none | no behaviour change in tested code; `audit.md` assembly is agent-authored prose, outside `tests/run.js` scope per `sprint-lifecycle.md` gap G-13 |
| Task 3: `t_plan.md` compression, DoD moved to rule doc | none — parser-critical checkbox format explicitly re-confirmed unchanged in the task itself | none | none | no code parses `plan.md` in this repo (task parsing is agent behaviour, not `tests/run.js`); format re-verified by inspection in Task 20 |
| Task 4/17: `t_test-plan.md` compression, incremental re-entry | none — template/workflow prose only | none | none | no code path |
| Task 5: `t_state.json` — delete `subsystems_touched`/`new_subsystems`, document `archived_at` | schema drift: a stale `canon_hashes`/`upstream_hashes` entry would misclassify the file on the next `sync.js --check` or `update.js` run | unit (existing hash-consistency tests) | keep | *(corrected entry 2, per reviewer finding #2)* `t_state.json`'s digest lives only in `upstream_hashes` (matched via `managed_paths`'s `.asd/templates` expansion) — templates are never part of `canon_hashes` (`computeCanonHashes` only walks `.asd/agents`/`.asd/skills`). Neither ledger is actually read by `sync.js --check`'s drift classification (`statusFullFile` etc. re-render straight from canon at runtime); the real risk is `--apply`'s `recomputeAndWriteHashLedgers` going stale if a canon/template edit isn't followed by a re-run. The `upstream_hashes` test recomputes the sha256 straight from the live file and fails if the recorded digest doesn't match — since Task 5 changed `t_state.json`'s bytes, that recompute only stays green if the ledger was correctly refreshed alongside the edit, which it was; confirmed green this run |
| Task 6: decisions-log moved from `.asd/project/decisions-log.md` to `<sprint>/decisions-log.md` | none machine-checkable — no code reads or writes this path in `sync.js`/`update.js`/hooks | none | none | grep of `tests/` and `tests/fixtures/` for `decisions-log` returns no hits; purely a workflow-dispatch/rule-doc convention |
| Task 7/8: `t_prd.html`/`t_ux-spec.html`/`t_accessibility.html` section compression | none — HTML artifact templates are agent-authored content, never parsed by `sync.js` | none | none | no code path |
| Task 9: delete `.asd/templates/t_api.html`; remove its `release-manifest.json` `canon_hashes`/`managed_paths`/`upstream_hashes` entries | (a) a stale reference to the deleted file left in the manifest would be caught by nothing generic; (b) full-file sync-plan enumeration could silently miss a target if hardcoded | (a) unit — hash-consistency tests; (b) static — sync-plan enumeration test | keep | *(corrected entry 2, per reviewer finding #2)* (a) only the `upstream_hashes` hash-consistency test applies — templates (including the deleted `t_api.html`) are never tracked in `canon_hashes` (`computeCanonHashes` only walks `.asd/agents`/`.asd/skills`), so there was never a `canon_hashes` entry to clean up. The `upstream_hashes` test explicitly `fs.existsSync`-checks each entry's path and reports it stale/missing if absent; since Task 9 deleted the file and its manifest entry together, there is nothing left to flag, confirmed green. (b) unchanged: the sync-plan enumeration test (`` `node .asd/sync.js --check` reports every item current `` ) derives its expected-target set by walking `.asd/agents`/`.asd/skills` from disk, not from a hardcoded list or `.asd/templates`, so a deleted template was never in its scope to begin with — confirmed by grep that no fixture or assertion names `t_api.html` |
| Task 10: stop committing C4 `dist/`+`architecture.html`; add `.gitignore` entries; seed a build-to-view command in `asd-init`'s `commands.yaml` template | none machine-checkable — `.gitignore` and template seeding aren't exercised by `tests/run.js` | none | none | no code path; the seeded command only ever lands in a future consumer's own `commands.yaml`, never this repo's |
| Task 11/12: trim `t_html-shell.html`; change design-system regen cadence | none — HTML shell trimming and regen-cadence wording are agent-followed rules, not code | none | none | no code path |
| Task 13: `review.scoped_fan_out` config field added to `t_config.yaml`; new `state.json.reviews.impl.verdicts["iter-NN"]` value `"skipped: <predicate>"` | *(corrected iter-03, per testing.md#1)* `review.scoped_fan_out` itself is config surface consumed only by workflow prose, no code reads it — that part of the `none` decision stands. But the new `"skipped: <predicate>"` verdict value IS parsed by real code: `session-start.js`'s `lastReviewVerdict` classifies `reviews.impl.verdicts` entries into red/yellow/green, and an unrecognized string fell through to "mixed" — a real bug, fixed this iteration (`lastReviewVerdict` now treats `/^skipped:/` as satisfied) | unit (new `SessionStart hook` test) | keep | new test constructs a fixture `state.json` with a verdicts map mixing `APPROVE` and `"skipped: ..."` entries and asserts the hook prints "green", not "mixed"; confirmed green this run |
| Task 14: collapse three no-op design phases; write `phase="design-promote"` + `skipped_phases` | `PHASE_CHAIN`/`nextPhase()` in `session-start.js` unchanged per task's own note (verified — task explicitly does not touch them) | static (existing `SessionStart hook` tests) | none | *(corrected entry 2, per reviewer finding #2)* grep of `tests/run.js` for `PHASE_CHAIN`/`nextPhase` returns no hits — no test exercises `session-start.js`'s phase-chain logic at all. The two `SessionStart hook` tests that do exist only assert on the printed skill form (`/asd-*` vs `$asd-*`), unrelated to `PHASE_CHAIN`/`nextPhase()`; their staying green is not evidence covering this task's phase-chain claim. The `none` decision instead rests on direct code inspection: `session-start.js`'s `PHASE_CHAIN` array and `nextPhase()` function are untouched by this task's diff, so there is no code-level change to verify in the first place |
| Task 15: phase workflows write `state.json` inline for non-gate writes | none — no hook/CLI code writes or validates `state.json` shape in this repo (that happens at runtime inside a dispatched agent) | none | none | no code path |
| Task 18/19: conditional PR DoD checks; dedupe review-edge ownership | none — workflow/rubric prose only | none | none | no code path |
| Task 20: consistency sweep | verification-only task, no canonical file changes | none | none | task itself states "Affected canonical files: none" |
| `AGENTS.md` line 2 wording tweak (self-hosting exemption text) | none — the `sync.js --check` test's `SELF_SOURCED_ALLOWLIST` already expects `AGENTS.md` to report `modified-foreign` regardless of its content | static (existing test) | none | test asserts allowlist membership, not content, so unaffected |
| **Entry 2 delta (impl-review iter-01 review-fix, 62 files):** phase-skill `Write`/`Edit` tool grants (`asd-phase-*/SKILL.md`); external-review pathspec fix; `architect.md` contract corrections; `t_html-shell.html`/`t_adr.html` dead-code cleanup + color tokens + dark-mode + `TOC_ASSETS` conditional + multi-ADR id fixes; residual api/adr reference removal; write-allowlist widening; AC-source pointer fixes; pr-phase gate fixes (`asd-phase-pr.md`, `asd-phase-impl-test.md`); ~25 misc text/citation corrections across rule docs, agent files, workflows, `README.md`, `t_stack.html`, `t_tech-reference.md`, `t_review.md`, `t_plan.md`, external-review templates | none — every touched item is prose/frontmatter tool-grants/HTML-template markup/agent-contract wording; nothing edits `.asd/sync.js`, `.asd/skills/asd-update/update.js`, or `.asd/hooks/session-start.js` (confirmed by `git diff --stat` over the delta — none of those three paths appear), so no code path exercised by `tests/run.js` is touched | none | none | `sync.js --check`'s generic drift check (same mechanism as Task 1) already covers the 8 agent/skill files regenerated for the tool-grant and text fixes; the HTML/markdown/rule-doc/workflow edits have no parser in this repo (agent-authored content); `git diff --stat` for the pathspec confirms zero hits on the three files `tests/run.js` actually exercises |
| `.asd/release-manifest.json` hash-ledger resync (134 lines, batch-91e8143) — recomputed `canon_hashes`/`upstream_hashes` after the agent/text edits above | a stale ledger entry (edited file, un-refreshed hash) would fail the existing hash-consistency tests | unit (existing hash-consistency tests) | keep | same generic, non-hardcoded assertions as Task 5/Task 9 (`release-manifest.json: every canon_hashes/upstream_hashes entry matches the actual file`) — these tests recompute every digest from live file bytes on every run, so any ledger drift from this batch's edits is automatically caught with no test change; confirmed green this run |
| `t_test-plan.md` gains a `- HEAD: {{sha}}` line under Suite run (pr-phase gate fix: sha-based suite-skip instead of HEAD-based) | template/prose schema addition only — no code in `sync.js`/`update.js`/`session-start.js` parses `test-plan.md`'s Suite run section (it's read by the `pr` phase workflow as agent-authored prose, not a machine parser under `tests/run.js`) | none | none | no code path; this test-plan's own Suite run section below is updated to include the new `HEAD:` line for consistency with the amended template, satisfying the schema change by example rather than by a new automated check |
| Finding #1 (`impl-review` iter-01 testing.md): `.asd/templates/t_state.json` has no machine check that it still parses as valid JSON after Task 5 deleted two fields — `sync.js --check` never reads `.asd/templates/` | a template edit that breaks JSON syntax (e.g. a stray trailing comma) would go undetected by every existing gate | unit (new, zero-dependency `JSON.parse` guard in `tests/run.js`) | add | declining was considered (citing `AGENTS.md`'s "no template content" verification charter) but rejected: charter covers *content* correctness (agent-authored prose/HTML), not basic *syntax* validity of a machine-parsed format (JSON) — that gap is exactly what the reviewer flagged, and a 3-line guard costs nothing and stays zero-dependency (`custom-coding-rules.md`) |
| **Entry 3 delta (impl-review iter-02 review-fix, 76 files):** `asd-phase-impl-test`/`asd-phase-impl-review` `SKILL.md` tool-grant additions; `t_state.json` gains `reviews.impl.iteration_heads: {}` (schema addition — same category as Task 5/Task 9); hardcoded floor-line removal from a workflow; write-allowlist fixes; likec4-build command removal from `asd-init`'s seeded `commands.yaml`; `asd-phase-pr.md`'s tests/lint skip changed from raw `git rev-parse HEAD` equality to content-scoped `git diff --quiet <recorded HEAD>...HEAD` over a pathspec; `t_html-shell.html`/`t_adr.html` visual fixes (TOC anchor retarget to each ADR's own `<h2 id="adr-{{N}}-title">`, per-decision title heading added); `{{TOC_ASSETS}}`'s canonical fill value moved out of a script tag and inlined verbatim (sticky-CSS-only, script dropped) into `artifact-layout.md`; writer-attribution/residual-id text cleanup across agent files and rule docs; `artifact-layout.md` "Review verdict placeholder namespace" section moved below "Badge omission"/"Fragment invariants" (pure reorder, no content change); one `CHANGELOG.md` entry note; `.asd/release-manifest.json` hash-ledger resync (94 lines, matching the 15 regenerated agent/skill files) | none — confirmed by `git diff --stat 27ef1ffd90d2027c838b8ded5785357d7c5ce01e...HEAD -- .asd/sync.js .asd/skills/asd-update/update.js .asd/hooks/session-start.js tests/run.js`: zero hits, all four files `tests/run.js` actually exercises (plus the test file itself) are untouched by this delta. Every touched item is prose/frontmatter tool-grants/HTML-template markup/schema-field addition/workflow wording — nothing new for `tests/run.js` to parse | none | none | (a) the `t_state.json` schema addition (`iteration_heads`) is the same drift category as Task 5/Task 9 — templates are tracked only in `upstream_hashes`, never `canon_hashes`, and the existing hash-consistency test recomputes the digest from live bytes every run, so a stale ledger entry is automatically caught; confirmed the ledger was correctly refreshed alongside the edit (green this run). (b) the JSON-parse guard added at entry 2 (`every .asd/templates/*.json file parses as valid JSON`) already covers `t_state.json` staying syntactically valid after this field addition — no new guard needed, confirmed green. (c) the `asd-phase-pr.md` gate-logic rewrite (sha-equality → content-scoped diff) and the `{{TOC_ASSETS}}` script-to-CSS-only change are workflow/template prose with no parser in this repo — `git diff --stat` for the pathspec confirms zero hits on the files `tests/run.js` exercises. (d) `sync.js --check`'s generic, non-hardcoded drift check (same mechanism as Task 1/entry 2) already covers the 15 agent/skill files regenerated for this batch's tool-grant and text fixes, with no test change needed |
| **Entry 4 delta (impl-review iter-03 review-fix, 31 files) — `.asd/hooks/session-start.js`'s `lastReviewVerdict` fix for `"skipped: <predicate>"` verdict values** | this is the one real code-behaviour change in the delta: `lastReviewVerdict` classifies the latest iteration's verdict map into `red`/`yellow`/`green`/`mixed`, gating what `session-start.js` prints as "Last review verdict" on every session boot. Before this fix, a verdict map containing only `APPROVE`/`green` plus one or more `"skipped: <predicate>"` strings (the value `review.scoped_fan_out` writes when a reviewer role is out of scope, per Task 13's schema) fell through every `verdicts.every(...)` branch and was misreported as `mixed` even though nothing was actually outstanding — a real bug, not a cosmetic one, since `mixed` is the signal an agent/human reads to decide whether more review work is needed | unit (`SessionStart hook` test, spawns the real hook against a fixture `state.json` in a temp repo and asserts on stdout, not on `lastReviewVerdict` internals) | keep — independently verified, not merely accepted because the fix batch supplied it | the fix batch (impl-review iter-03) already added `tests/run.js`'s `` `SessionStart hook: a "skipped: <predicate>" verdict counts as satisfied, not "mixed"` `` test alongside the fix; reviewed it directly rather than taking it on faith: it builds a fixture `iter-01` verdicts map mixing `APPROVE` and two `skipped: ...` strings, spawns the actual hook binary (not a unit-level call into `lastReviewVerdict`) via `execFileSync` against a temp `.asd/` tree, and asserts the printed `additionalContext` contains `Last review verdict: green` — this is the right check: it exercises the real fallthrough path end-to-end (would have failed pre-fix, since the old `.every()` had no `isSkipped` branch and every-such-map fell to `mixed`), asserts on external process output rather than internals, and needs no new test infrastructure (reuses the existing temp-repo harness pattern from the two adjacent `SessionStart hook` tests). No additional test needed from this pass; confirmed green in the full-suite run below (79/79) |
| **Entry 4 delta (remaining 30 files — prose/workflow/template/agent-doc only)** | `review.scoped_fan_out` wording fixes (`review-policy.md`, `external-review.md`); `{{TOC_ASSETS}}` placeholder deletion with its CSS merged directly into `t_html-shell.html`; a stale scrollspy comment fix in `t_html-shell.html`; UI-predicate dedup across reviewer agent files; E-1 token-usage scoping (`asd-test-engineer.md`, `sprint-lifecycle.md`); test-selection rule-set dedup (`asd-test-engineer.md` now points at `code-style.md` §17 rather than restating it); a light-mode contrast fix in `t_adr.html`; `state.json.reviews.impl.iteration_heads` absent-key fallback fix (workflow-read schema field, no `sync.js`/`update.js`/hook code parses it); new `reviews/impl/iter-03/*.md` review artifacts; `.asd/release-manifest.json` hash-ledger resync | none — confirmed by `git diff --stat 9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05...HEAD -- .asd/sync.js .asd/skills/asd-update/update.js .asd/hooks/session-start.js tests/run.js`: only `.asd/hooks/session-start.js` and `tests/run.js` appear (both covered by the row above); `.asd/sync.js` and `.asd/skills/asd-update/update.js` are untouched by this delta | none | none | every item here is prose/HTML/frontmatter/workflow wording or a schema field read only by agent-authored workflow logic, not by any code path `tests/run.js` exercises; the release-manifest hash-ledger resync is covered by the same generic, non-hardcoded hash-consistency tests used at every prior entry (Task 5/Task 9/entry 2/entry 3) — confirmed green this run |

## Removed tests

None. No test in `tests/` targets code, config shape, or fixture content this sprint's 20 tasks touched — the whole change surface is prose/workflow/agent/template edits plus two generic, self-verifying `release-manifest.json` bookkeeping updates (Tasks 5 and 9), both of which the existing hash-consistency and drift-check tests already cover without modification.

**Entry 2:** None. The delta (62 review-fix files) is entirely prose/frontmatter/HTML-template edits plus a hash-ledger resync — no test targets any of it.

**Entry 3:** None. The delta (76 review-fix files) is entirely prose/frontmatter/HTML-template edits, a template schema-field addition, and a hash-ledger resync — no test targets any of it.

**Entry 4:** None. No existing test targeted `lastReviewVerdict`'s pre-fix behaviour (nothing to delete/rewrite there — it's a net-new test, see Added tests), and nothing else in the 31-file delta is machine-checked code.

## Added tests

None. Every material, machine-checkable risk in this sprint's change surface (state.json schema drift, release-manifest.json ledger integrity after `t_api.html`'s deletion, sync-plan target enumeration) is already covered by existing, non-hardcoded assertions in `tests/run.js` (see Risk → check decisions). No sprint task touched `.asd/sync.js`, `.asd/skills/asd-update/update.js`, or `.asd/hooks/session-start.js` — the only files `tests/run.js` exercises — so there is no new code path requiring a new test. The remaining 18 tasks are prose/structure edits to rule docs, templates, workflows and agent instructions verified by Task 20's cross-file consistency sweep (not automatable — `sprint-lifecycle.md` gap G-13), which is inspection already performed and recorded in `plan.md`, not test-engineer scope to redo.

**Entry 2:** one test added, resolving Finding #1 from `impl-review` iter-01 `testing.md` (see Risk → check decisions row above).

**Entry 3:** none. The `t_state.json` schema addition (`iteration_heads`) is covered by the entry-2 JSON-parse guard plus the pre-existing hash-consistency tests (see Risk → check decisions row above); no other file in this delta is machine-parsed by `tests/run.js`.

**Entry 4:** one test, added by the impl-review iter-03 fix batch alongside the `lastReviewVerdict` bug fix (see Risk → check decisions row above) — independently reviewed and confirmed to be the right check rather than re-authored.

| Test | Regression proof |
|---|---|
| `tests/run.js`: `every .asd/templates/*.json file parses as valid JSON` | n/a — no `D-N` defect; this is a coverage-gap fix (closes finding #1), not a regression fix. **Corrected at entry 3 per `impl-review` iter-02 `testing.md` finding #1** (original entry-2 record wrongly claimed "the rest of the suite stayed green"). Re-verified fail-first directly: mutated `.asd/templates/t_state.json` in place (added a trailing comma after `"escalations": []`), reran `node tests/run.js` with no intervening `sync.js --apply` — result: **two** tests failed, not one — this new JSON-parse guard (`Expected double-quoted property name in JSON at position 584`) *and* the pre-existing `release-manifest.json: every upstream_hashes entry matches the actual file` test (correctly flags `.asd/templates/t_state.json` as stale, since that test recomputes the sha256 straight from the mutated file's bytes and no longer matches the ledger's recorded digest) — 76/78 passed. Both failures are expected and correct: the guard closes the finding, and the `upstream_hashes` test would independently catch the same class of edit if it altered file bytes without a ledger refresh. Reverted the mutation immediately after; `git diff --stat .asd/templates/t_state.json` confirmed zero residual diff |
| `tests/run.js`: `SessionStart hook: a "skipped: <predicate>" verdict counts as satisfied, not "mixed"` | fail-first re-verified independently this entry: temporarily reverted `.asd/hooks/session-start.js`'s `lastReviewVerdict` to its pre-fix `verdicts.every(v => v === 'green' \|\| v === 'APPROVE')` line (no `isSkipped` branch, matching the actual pre-fix code, not a synthetic mutation), reran `node tests/run.js` — **three** tests failed, not one: this new test (asserted `Last review verdict: green`, observed `mixed`, confirming the exact pre-fix bug), plus two expected collateral failures from editing tracked source without a hash-ledger refresh (`` `node .asd/sync.js --check` reports every item current `` and `release-manifest.json: every upstream_hashes entry matches the actual file`) — 76/79 passed. All three failures are correct and expected for an uncommitted revert of ledgered source; restored the real fix immediately after, reran, 79/79 green, `git diff --stat .asd/hooks/session-start.js` confirmed zero residual diff. Confirms this is a genuine regression-proof test, not a mock-confirming or trivially-always-green one |

## Suite run

**Entry 1** (HEAD `b13d77c70e171e34abea6e3a52ed777fa51e3577`):
- Test — command: `node tests/run.js` (per `commands.yaml` `test`)
  Result: exit 0, pass — 77/77 passed, 0 failed, 0 skipped
- Lint — command: `git diff --check` (per `commands.yaml` `lint`)
  Result: exit 0, clean — no output (no whitespace errors in the diff)
- Build — command: `node .asd/sync.js --check` (per `commands.yaml` `build`)
  Result: exit 0, `"ok": true` — 68 items reported, all `status: "current"` except `AGENTS.md: "modified-foreign"` (expected self-hosting exemption per `SELF_SOURCED_ALLOWLIST`, asserted by an existing `tests/run.js` test, not a failure)

**Entry 2** (HEAD `27ef1ffd90d2027c838b8ded5785357d7c5ce01e`):
- Test — command: `node tests/run.js` (per `commands.yaml` `test`)
  Result: exit 0, pass — 78/78 passed, 0 failed, 0 skipped (77 pre-existing + 1 new, resolving finding #1)
- Lint — command: `git diff --check` (per `commands.yaml` `lint`)
  Result: exit 0, clean — only CRLF-normalization advisory warnings (not whitespace errors), no failures
- Build — command: `node .asd/sync.js --check` (per `commands.yaml` `build`)
  Result: exit 0, `"ok": true` — 68 items reported, all `status: "current"` except `AGENTS.md: "modified-foreign"` (expected self-hosting exemption, unchanged from entry 1)
- HEAD: `27ef1ffd90d2027c838b8ded5785357d7c5ce01e` — the code commit (`tests/run.js`'s new guard) the suite was verified at; committed separately from this doc-only test-plan update so the sha is a real, stable value rather than a self-reference to a commit that doesn't exist yet

**Entry 3** (HEAD `9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05`):
- Test — command: `node tests/run.js` (per `commands.yaml` `test`)
  Result: exit 0, pass — 78/78 passed, 0 failed, 0 skipped (no new tests added this entry — see Added tests)
- Lint — command: `git diff --check` (per `commands.yaml` `lint`)
  Result: exit 0, clean — only CRLF-normalization advisory warnings (not whitespace errors), no failures
- Build — command: `node .asd/sync.js --check` (per `commands.yaml` `build`)
  Result: exit 0, `"ok": true` — 68 items reported, all `status: "current"` except `AGENTS.md: "modified-foreign"` (expected self-hosting exemption, unchanged from entries 1/2)
- HEAD: `9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05` — the review-fix-close commit (impl-review iter-02 review-fix, batch B) the suite was verified at; this doc-only test-plan amendment is committed separately so the sha stays a real, existing commit rather than a self-reference

**Entry 4** (HEAD `ea55e35a1e2237cec3ff3b50a45aebea1c7fe662`):
- Test — command: `node tests/run.js` (per `commands.yaml` `test`)
  Result: exit 0, pass — 79/79 passed, 0 failed, 0 skipped (78 pre-existing + 1 new, the `lastReviewVerdict`/`"skipped:"` regression test — see Added tests)
- Lint — command: `git diff --check` (per `commands.yaml` `lint`)
  Result: exit 0, clean — no failures over `9ff1c223270c50f8ad7a1e4875da1c9de7aa7e05...HEAD`
- Build — command: `node .asd/sync.js --check` (per `commands.yaml` `build`)
  Result: exit 0, `"ok": true` — all items reported `status: "current"` except `AGENTS.md: "modified-foreign"` (expected self-hosting exemption, unchanged from entries 1–3)
- HEAD: `ea55e35a1e2237cec3ff3b50a45aebea1c7fe662` — `chore(sprint-002): close impl review-fix for iter-03`, the review-fix-close commit (impl-review iter-03 review-fix) the suite was verified at

## Defects

None found.

## Manual verification (optional)

None. Every change in this sprint's scope is Markdown/YAML/JSON/HTML prose or config with no visual UI, third-party live integration, or UX-feel surface — nothing here is genuinely impossible to automate, and the automatable surface is already fully covered above.
