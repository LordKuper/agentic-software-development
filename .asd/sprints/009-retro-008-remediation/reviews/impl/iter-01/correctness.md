[REVIEW-impl-correctness]: CONCERNS

# Review — correctness

- **Phase**: impl-review
- **Iteration**: 1 (severity floor: low — all severities reported)

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| F-1 | medium | `.asd/rules/artifact-layout.md:43-44`, `:68` vs `.asd/rules/review-policy.md:150` | AC-14 introduces a new sprint artefact, `<sprint>/reviews/<phase>/iter-NN/<reviewer>.late.md`, but the path map still lists only `<reviewer>.md, <reviewer>.part-N.md`, and `:68` declares the map exhaustive ("A sprint folder holds **only** the artifacts named above… any stray file is frozen there"). Two rules edited in the same diff contradict each other: the orchestrator following `review-policy.md` writes a file `artifact-layout.md` classifies as stray. The `.part-N.md` precedent shows every review-file variant is expected in the map. | Extend both review rows to `<reviewer>.md, <reviewer>.part-N.md, <reviewer>.late.md`, or add `<reviewer>.late.md` to the `:68` carve-out beside the coverage evidence. |
| F-2 | medium | `.asd/runtime.js:201-207` (`coverageManifestDigest`), `.asd/rules/review-policy.md:107`, `tests/run.js:3284-3287` | `coverageManifestDigest` injects `LEDGER_VOCABULARY` into every manifest before digesting, including manifests that do not carry the field. A manifest whose `digest` was stamped by the pre-sprint runtime therefore digests differently and `validateCoverageLedger` rejects it (`ledger manifest identity invalid`), while `review-policy.md:107` claims the opposite ("absent → validated as before"). The backward-tolerance test builds its `legacy` fixture by recomputing the digest with the NEW function, so it never exercises a legacy-stamped digest. Under `backward_compat: migration` this is a persisted-artefact identity break shipped with no migration. | Preferred: drop the injection — digest the manifest as written. `manifest-digest --write` already writes `vocabulary` before stamping, so emitter and validator still agree and pre-existing manifests keep their identity. If the injection is kept deliberately, correct `review-policy.md:107` and add a migration or explicit compat waiver. Either way rebuild the legacy fixture from a digest computed the old way. |
| F-3 | medium | `.asd/agents/asd-external-review.md:116` vs `:105`, `.asd/rules/external-review.md:42-51` | AC-8 makes exactly two outcomes permitted, and `:105` routes any inability to complete to the skip. The unedited `Signals emitted` block still lists `FAILED — {{wraps_cli}} unrecoverable error`, a third outcome for precisely that case. Both provider views carry the same pair. Left as is, the `F-8` hole is still authorized by the agent's own file. | Remove `FAILED` from the signal list, or requalify it to a case outside the outcome contract (e.g. dispatch-level abort before invocation) and cite `external-review.md` "Outcome contract" for everything else. Re-sync both views. |
| F-4 | medium | `.claude/agent-memory/asd-dev-critical/MEMORY.md:3` → `project_crlf-canon-edits.md:3,8,15`, caused by `.gitattributes:2` | AC-7 falsifies a durable memory the sprint did not update: `project_crlf-canon-edits.md` asserts "`.asd/**` markdown is checked out CRLF (`core.autocrlf=true`, index is LF)". With `* text=auto eol=lf` the worktree becomes LF on the next checkout, so the premise expires silently and is re-paid on every `asd-dev-critical` dispatch. Line 15 ("`git diff --check` must be clean before committing") is now contradicted by `code-style.md` §19. The sibling memory `project_sync-apply-ledger-gotcha.md:18` WAS updated for `eol=lf`, so the omission is inconsistent rather than deliberate. | State the hazard conditionally (a file that IS CRLF on disk) rather than asserting this repo's worktree is CRLF; change line 15 to `git diff --cached --check`; refresh the `MEMORY.md:3` hook line. |
| F-5 | low | `.asd/rules/review-policy.md:150` vs `.asd/rules/sprint-lifecycle.md:68-72`, `review-policy.md:175` | AC-14 adds a third route that clears `reviews.<phase>.latched`, written in `review-policy.md` — the same file that at `:175` defers all latch persistence to `sprint-lifecycle.md` "APPROVE latch" as sole home. That section names only two clearing routes and reads as complete, so an implementer working from the sole home never learns of the new one. | Add one line to `sprint-lifecycle.md` "APPROVE latch" naming the late-return admission as a third clearing route, citing `review-policy.md` "Late duplicate return". |
| F-6 | low | `.asd/rules/code-style.md:141` | "Use `git diff --cached --check` (equivalently `git diff --check HEAD`)" — not equivalent. `--cached --check` compares index↔HEAD; `git diff --check HEAD` compares worktree↔HEAD and also flags unstaged content not being committed — a false positive at a pre-commit gate, and in a shared worktree it reports a sibling's edits. This rule ships to every consumer via `managed_paths`. | Drop "equivalently", or qualify that `git diff --check HEAD` covers a superset and is acceptable only when the tree holds nothing else. |
| F-7 | low | `.asd/rules/review-policy.md:42` vs `.asd/rules/git-strategy.md:39` | AC-13b assigns the commit of reviewer-authored memory to the phase workflow, but `git-strategy.md` — sole home of commit ownership, read by the orchestrator at phase exit — still enumerates its bookkeeping as "`state.json`, `decisions-log.md`, review files, `friction-log.md`" with no memory class, and in the same sentence obliges every dispatched agent to commit "every path it authored", which a read-only reviewer cannot do. The obligation is stated on one side only — the exact failure class this sprint exists to close. Neither review workflow mentions it either. | Add reviewer memory to the orchestrator's bookkeeping list in `git-strategy.md:39` (citing `review-policy.md` "Change-surface rule"), and note that the authored-path commit obligation binds agents holding a commit tool. |
| F-8 | low | `.asd/workflows/asd-phase-impl.md:61`, `:62`, `:65` vs `sprint.md` AC-10 | AC-10 asks for fix rounds "sequentially **under one agent**". Delivered: an ordered chain with one task in flight at a time — the substantive half. Not delivered or stated: the single-agent half; step 5a still routes each fix task through `route-task` independently and step 6 dispatches per task, so a round may span several fresh `asd-dev-<tier>` instances, none seeing the others' reasoning. | Either state that a fix round's chain is dispatched to one agent (and how routing picks that tier), or record in `decisions-log.md` that "under one agent" was deliberately narrowed to "one in flight at a time". |

Dropped as nitpick: typographic apostrophe in `checkpoints.md:26`/`MEMORY.md:4`; the sprint-008 rationale sentence carried in `asd-phase-impl.md:61`.

## Section notes

- **Bugs** — `.asd/runtime.js` row/ledger logic checked: `allowedNa.get(row.i)` is never undefined, `stable()` sorts keys so `Object.assign` ordering is immaterial, the `seen.size !== expected.size` completeness check is intact. Only correctness defect found is F-2. `tests/run.js:3267-3275` mutates and restores `LEDGER_VOCABULARY` in `finally` — intentional single-source proof, safe.
- **Security** — no secrets, no injection surface. `tests/run.js:3375` uses `execFileSync('git', [...])` with fixed argv and no shell; `runtime.js` external probes unchanged.
- **Contracts** — one item: F-2 (persisted manifest identity under `backward_compat: migration`). `runtime.js` API widened only additively. `asd_version` still `7.0.0` — the bump is `pr`-phase work, not a defect here.
- **AC coverage trace** — every AC traces to a change; `ac-9` n/a per manifest. AC-17 traced structurally (each AC has a matching assertion at `tests/run.js:3223-3427`; the reconciled agent-memory assertion replaced the retired count shape at `:3039-3057`); suite greenness is the phase's terminal gate, not this reviewer's evidence. `:3375` makes the suite depend on a `git` binary on PATH — flagged for awareness. AC-18 corroborated by both provider views.
- **UI conformance** — `n/a: no UI surface in scope file list` (phase-supplied predicate); no UI inputs loaded.

## Coverage

Validated compact ledger: [`correctness.ledger.json`](./correctness.ledger.json) against immutable manifest [`correctness.manifest.json`](./correctness.manifest.json), findings [`correctness.findings.json`](./correctness.findings.json). `node .asd/runtime.js validate-ledger` → `{"ok":true}`. 21/21 files, 18/18 rules, 6/6 sections resolved; `ui-conformance` n/a per the phase-supplied predicate, `ac-9` n/a per its manifest-authorized predicate.

## Verdict

CONCERNS: 8 (4 medium, 4 low)

## Next action

Route to `impl` (review-fix mode). Per `review-policy.md` "Verify before applying", re-verify each finding's premise at current `HEAD` — F-2 in particular is a design choice, not an accident.

## Escalations

- **F-2, conditionally**: keeping the vocabulary injection accepts a digest-identity break for manifests stamped by an earlier runtime — a compatibility waiver under `backward_compat: migration`, which needs the user gate. Removing the injection needs no escalation.
