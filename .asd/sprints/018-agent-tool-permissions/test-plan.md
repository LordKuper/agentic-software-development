---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/wave-<K>/iter-NN/testing.md (verdict)
---

# Test plan — sprint 018-agent-tool-permissions

## Entry log

Appended each entry, never rewritten. `HEAD analysed` is the commit the strategy/prune passes
were scoped through; the next re-entry's delta is `git diff <this sha>...HEAD`.

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | | full change surface |

## Risk → check decisions

Impacted set, entry 1: the **full suite**. The shared-infrastructure safety valve (`sprint-lifecycle.md` "Impacted test set") fires, because the change surface touches `.asd/sync.js` (the generator behind every provider view), `core.md` (the rule hub every agent loads) and `release-manifest.json`. `tests/run.js` is one flat file with no selector (`commands.yaml` sets no `test_affected`), so a scoped run would also have to filter by name.

Every check below is static or unit level inside `tests/run.js`, following the file's existing content-contract pattern. None of them needs new infrastructure or a new dependency.

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `asd-reviewer-testing.md` grant (AC-4) | T-2 (`tests/run.js:3335`) pins the reviewer's whole tool list, `AskUserQuestion` included, and is red by intent | static | keep (rewritten in place) | The pinned claim, that the cited frontmatter really is read-only, is still true and still guarded by that test. Only the expected list changes, to `['Read', 'Glob', 'Grep']`. Deleting the test would drop the memory-citation guard (D-2 lineage) for no gain |
| `asd-ba.md`/`asd-ux.md` Bash (AC-1, AC-8) | the sprint-015 test (`:5459`) asserts "asd-ba stays shell-less". It is red by intent, since the sprint grants Bash on purpose. What still has to hold is that git writes route to the orchestrator | static | keep (rewritten in place) | Flip the grant assertion: `Bash` is in `tools`, and in neither agent's `disallowedTools`. Then assert that each agent's `Tool policy` `Run command:` bullet bounds the shell: never an artifact write, never a git write, with renames/deletes going through the orchestrator. Each token gets its own check, so a reword does not break it. The design-promote step-4 and "propose a rename" assertions in the same test are unchanged |
| `sync.js` `codex.web_search` validation (AC-3) | a bad value renders an invalid Codex TOML, or a valid documented mode gets rejected | unit | add (row in the existing `AC-1/3/5/6/7: Codex renderer rejects invalid delegate config` table), plus a relation check | Add one case, `web_search: 'on'`, which must throw `invalid web_search mode` with the table's context contract. Also take each mode `providers.md` documents for the key and check that the renderer accepts it and emits `web_search = "<mode>"`. That ties the documented set to the validator, so neither side can drift alone. Omitted-renders-nothing is already covered byte-for-byte by `canonical agent -> Codex .toml matches fixture` (the demo fixture has no key), so keep that test with no new assert |
| `sync.js` render + variant pass-through; AC-3 parity | a Codex view whose `web_search` disagrees with its Claude web grant; a `-mechanical`/`-critical` variant that drops the base key | unit | add (extend the existing `AC-3/6/7: every canonical Codex agent renders a supported delegate config` loop) | Iterate `sync.buildSyncPlan(REPO_ROOT)`'s `agent-codex` items, which include the variant metas `variantMeta` builds. Render each one and assert `web_search = "live"` exactly when the base Claude `tools` hold both `WebFetch` and `WebSearch`, and `"disabled"` otherwise. Rendering through the plan catches a `variantMeta` regression that a disk read of the generated views would miss. Rejecting a per-variant `web_search` key is covered by the existing variant-permission test's any-key guard and is not this sprint's code: `none` |
| canonical agent grants (AC-2, AC-4) | an agent regains `AskUserQuestion`; the web split drifts, including a half grant (fetch without search) or a keep-disallowed reviewer losing its `WebFetch` denial | static | add (new test `sprint-018 AC-1/AC-2/AC-4/AC-8/AC-9`) | Sweep `.asd/agents/*.md`, a set derived from the directory: 1) no `AskUserQuestion`; 2) the web grant is both tools or neither; 3) the web-less set deep-equals the four AC-2 names. That literal *is* the requirement, and its source (`sprint.md`) is not a file the suite may read; 4) each web-less agent has `WebFetch` in `disallowedTools`, and no web-granted agent disallows either tool. Variants cannot change Claude permissions (`variantMeta`), so the base covers them |
| web tool-policy lines, untrusted data (AC-9) | a new web grant ships with no scoped policy line; `core.md`'s boundary regresses to one host's fetch tool; a semantic op an agent body uses has no host mapping | static | add (same new test) | Every web-granted agent has a `Tool policy` bullet naming web/URL use. `core.md` "Untrusted-data boundary" covers search results and names no host tool (`WebFetch`/`WebSearch`/`web_search`). The ops named on `asd-dev.md`'s web policy line (split on ` / `) each have a row in the `providers.md` semantic-operation table |
| Bash bound + commit-tool definition (AC-1, AC-8) | a shell-granted creator reads `git-strategy.md`'s commit obligation as its own; a Bash grant with no bounding line | static | add (same new test) | Every agent with `Bash` has a `Run command:` policy bullet. The agents whose bullet grants `git commit` deep-equal the agents that `git-strategy.md`'s "holding a commit tool … is role policy" sentence names as holding one. This relates two sites, not a literal pinned at one, so either side can be reworded |
| `core.md` "Request user decision", `providers.md` row, retired rule phrasing (AC-5) | the section goes back to "every agent can do this"; a rule doc again tells a dispatched agent to escalate to the user itself | static | add (new test `sprint-018 AC-4/AC-5/AC-7`) | Checks on the section: a clause pairing *only* with *orchestrator*; `QUESTION` named; the `QUESTION` protocol citation; the retired "Every agent can do this" absent. The `providers.md` request-user-decision row cites `core.md` "Request user decision". The retired rule phrases (sprint-lifecycle ADVICE steps 4/6, design-principles §11, external-review stalemate) must be absent. These are removed *instruction* phrases, not topic words |
| agent bodies (AC-4) | an agent body again tells the agent itself to request a user decision or approval (plan DoD grep) | static | add (same new test) | Sweep every canonical agent body. Any clause matching `request(s) [for] user decision/approval` must carry `never`. Today the one hit is the advisor's negation. The sweep is derived from the directory, not a per-file list |
| `QUESTION` protocol home + workflow citations (AC-7) | the protocol drops a property (orchestrator asks, answer logged before further work, fresh re-dispatch with the answer); a workflow brings back `QUESTION … → relay, halt`; a workflow dispatching a QUESTION-emitting role has no handling path | static | add (same new test); **audit half expected red at HEAD → D-2** | Locate the protocol block by its bold label and check each step's property on its own. The signal-vocabulary `QUESTION` line excludes reviewers and points at the protocol. Across `.asd/workflows/*.md`, every `;`-clause where `QUESTION` comes before an arrow cites the protocol. Widened at the removal gate (orchestrator decision 4): every workflow naming a creator/dev/tester agent (the non-reviewer, non-advisor set, derived from `.asd/agents/`) cites the protocol. `asd-phase-audit.md` dispatches `asd-architect`/`asd-ba` and does not. This assert sits last in its test, so the earlier asserts keep guarding while D-2 is open |
| design accept loops, token gate, concept/stack skills (AC-7) | a dispatched creator is again told to discuss/loop until `accept` or ask for a token approval; a skill delegates "ask the user" to an agent | static | add (same new test) | In `asd-phase-design.md` steps 6/8/9, each `instruction:` sub-bullet has no removed phrase (`loop until explicit`, `discuss each`, `request user decision`). Step 6 carries the orchestrator accept loop, with re-dispatch and explicit `accept`; steps 8 and 9 reference it; step 8's token gate is orchestrator-owned and cites the protocol. No canon line delegates *to ask the user* to an agent |
| reviewer question carrier (AC-4) — audit risk "reviewer question misparsed as interrupted", impact high | a reviewer returns a bare `QUESTION`; the carrier form drifts between template, rule and workflows | static | add (new test `sprint-018 AC-4/AC-5/AC-7: carriers`) | Reviewers are derived from the directory (`asd-reviewer-*` + `asd-external-review`). None lists `QUESTION` under `Signals emitted`. Take the `question:` item form from `t_review.md` `## Escalations` and assert `review-policy.md`'s carrier states that same form. Both review workflows' aggregation steps handle it first and treat it as verdict-bearing, never interrupted |
| `providers.md` "Declared tool policy" (AC-4, AC-5) | `providers.md:109` says *an agent* handed an out-of-policy instruction "returns `QUESTION`". That includes reviewers, which contradicts `review-policy.md`'s carrier and the signal vocabulary ("any dispatched agent except a reviewer"). A reviewer that follows it is read as an interrupted dispatch and the question is lost: the audit's high-impact risk | static | add (same carriers test); **expected red at HEAD → D-1 at step 8** | Assert that the definition line routes a reviewer's refusal through the carrier by citing `review-policy.md` "Gate Verdict Format". That pointer is the one machine-decidable part of the fix. The existing sprint-012 assert that the line names `QUESTION` stays |
| External Review stalemate carrier (AC-4) | stalemate goes back to a bare question or loses the verdict token; a workflow cannot recognise the block | static | add (same carriers test) | From `external-review.md` "Stalemate detection", take the first-line token `[REVIEW-<phase>-external]: FAIL` and the `Stalemate:` block label. Assert the agent's Approval-triggers line carries both, and both review workflows' FAIL branches parse the label and offer accept as-is / override / abort. The two-outcome contract is already pinned by the sprint-017 AC-5 test: keep |
| impl-review manual-verification collection (AC-7) | the testing reviewer is again the one expected to ask; results are collected but not passed | static | add (same carriers test) | impl-review's step-6 `asd-reviewer-testing` bullet: collect before dispatch via request user decision, pass the results in the payload, log them in the decisions-log. `asd-reviewer-testing.md` `Inputs` names the payload results. This checks the relation between the collecting site and the consuming site |
| generated views, `canon_hashes`/`upstream_hashes` (AC-6) | stale `.claude`/`.codex`/`.agents` view or ledger after the canon edits | static | keep | Covered by `node .asd/sync.js --check reports every item current` (§9), `canon_hashes has an entry for every .asd/agents/*.md` and the `upstream_hashes` match test. All pass at HEAD |
| README roster/web split mirror (AC-6) | README prose ("All five creators…", "only Correctness…") drifts from the grants | — | none | README states the split in display names inside prose sentences, and the repo owns no name→agent-id map to derive the set from. Building one is new harness surface. Owner: the impl-review documentation reviewer (AGENTS.md "Cross-file consistency"). It becomes assertable if README ever gains a per-agent grant table keyed by agent id |
| `providers.md` "Codex cannot express a URL fetch / per-tool grant" statement (AC-3) | the stated gap is deleted or goes stale | — | none | It is a fact about an external host (codex-cli 0.156.1), evidenced by Task 1's local probe. Nothing local can execute it without the external CLI. The documented-mode set in the same paragraph *is* tied to the validator (row above), which also fails if the paragraph is deleted |
| `asd-architect.md` keeps Bash (AC-1) | architect's existing grant removed | — | none | The grant did not change this sprint (the diff only removes `AskUserQuestion`). The new "every Bash agent has a bounded `Run command:` bullet" sweep still covers its bound |

## Removed tests

None proposed. The two tests that are red by intent get rewritten in place, not deleted (rows above), because the contracts they pin still exist.

| Test | Reason | In change scope |
|---|---|---|

## Added tests

Every mutation was run the same way. `node <scratchpad>/mutate.js <id>` edits one file (an anchor that must hit exactly once, or a `git show main:<file>` revert), runs `node tests/run.js` and restores the original bytes in `finally`. The restore is byte-compared, and `git status` was clean of canon edits afterwards. Every run below exited **1**. Each cell names the mutation and then the **first-failing assertion message** of the credited test, transcribed from the runner's stderr. Collateral noise is expected and not listed: `sync.js --check`, `canon_hashes` and `upstream_hashes` fail for canon mutations. So do the two sprint-018 tests that are red at HEAD on D-1/D-2; their messages stayed at the defect text except where noted. The rewording runs R1/R2 change wording, keep substance, and must stay green.

| Test | Regression proof |
|---|---|
| tests/run.js:`T-2: feedback_no-shell-review-method.md cites asd-reviewer-testing.md's frontmatter …` (expectation rewritten to `['Read','Glob','Grep']`) | M40 `AskUserQuestion` re-added to `asd-reviewer-testing.md` tools → exit 1, "the frontmatter this memory now cites must actually be the read-only grant it claims" |
| tests/run.js:`sprint-015 AC-1/AC-6/AC-7/AC-8/AC-10/AC-12: …` (BA/UX shell assertion rewritten) | M41 `asd-ba` Bash moved back to `disallowedTools` → "sprint-018 AC-1: asd-ba carries a shell on Claude"; M42 `asd-ba` artifact clause dropped → "sprint-018 AC-8: asd-ba's shell is bounded by its Run command policy line, which must forbid writing an artifact"; M43 `asd-ux` git-write clause dropped → "AC-6/sprint-018 AC-8: asd-ux's Run command line must forbid git writes" (second loop member); M44 `asd-ux` orchestrator route dropped → "…asd-ux's Run command line must route renames/deletes to the orchestrator". Rewording R1 (BA line rewritten with the clause order swapped, same substance) → this test stayed green |
| tests/run.js:`AC-1/3/5/6/7: Codex renderer rejects invalid delegate config with context` (+ `web_search` case, documented-mode relation) | M36 validation line removed from `sync.js` → "Missing expected exception: invalid web_search"; M37 `indexed` dropped from the validator list → "providers.md documents web_search mode \"indexed\", so sync.js must accept and render it" (a later member of the loop, reached by leaving `disabled`/`cached` accepted); M39 render line removed → "…mode \"disabled\" … Got: name = \"runtime-fixture\"" |
| tests/run.js:`AC-3/6/7: every canonical Codex agent renders a supported delegate config` (+ `web_search` parity over `buildSyncPlan` items, variants included) | M38 `delete codex.web_search` in `variantMeta` → "sprint-018 AC-3: asd-dev-critical must render web_search = \"live\"" (a variant, which a disk read of the committed views would have missed); M2 advisor loses `WebSearch` → "asd-advisor must render web_search = \"disabled\""; M3 efficiency given web → "asd-reviewer-efficiency must render web_search = \"live\"" |
| tests/run.js:`sprint-018 AC-1/AC-2/AC-4/AC-8/AC-9: …grants…` | M1 `AskUserQuestion` added to `asd-ux` → "AC-4: a dispatched agent never reaches the user"; M2 → "AC-2: web access is one grant"; M3 → "AC-2: exactly these four keep web withheld"; M4 `WebFetch` removed from testing's `disallowedTools` → "AC-2: asd-reviewer-testing keeps WebFetch in disallowedTools"; M5 advisor web line reworded without web/URL → "AC-9: asd-advisor carries web tools, so its Tool policy must scope"; M6 `core.md` reverted to `main` → "AC-9: the untrusted-data rule must cover web search results"; M6b only the host-tool name restored → "AC-9: … names no host tool"; M7 `search the web` row deleted from `providers.md` → "AC-9: agent bodies name the semantic op \"search the web\""; M8 architect `Run command:` bullet renamed → "AC-8: every Bash grant is bounded by a Run command policy line"; M9 `asd-ba` added to `git-strategy.md`'s holders → "AC-8: git-strategy.md names the commit-tool holders … the two sites must agree" |
| tests/run.js:`sprint-018 AC-4/AC-5/AC-7: only the main orchestrator prompts the user …` | fail-first at HEAD e2d919d (D-2): "AC-7: every workflow dispatching a role that can return QUESTION must cite the QUESTION protocol" (`.asd/workflows/asd-phase-audit.md`). M6 `core.md` reverted → "AC-5: core.md must reserve request user decision to the main orchestrator"; M11 "Every agent can do this." re-inserted → "AC-5: the retired \"every agent can prompt\" claim must stay gone"; M12 orchestrator-only cell removed from the `providers.md` row → "AC-5: the providers.md host mapping must point at the orchestrator-only rule"; M13 ADVICE step 4 restored → "…must not tell a dispatched agent to reach the user itself (\"escalates to the user normally\")"; M28 `external-review.md` reverted → same assert, "(\"escalates to user with options\")"; M14 old signal line → "AC-4/AC-7: the QUESTION signal must exclude reviewers"; M15 decisions-log clause removed → "AC-7: step 2 - the answer is on disk before any further work"; M16 "Re-dispatches … fresh" → "Resumes" → "AC-7: step 3 - a fresh re-dispatch"; M17 design step 13 restored to `QUESTION → relay, halt` → "AC-7: a QUESTION branch routes through the protocol"; M18 `asd-dev` "Request user decision for ambiguity" restored → "AC-4: no agent body may instruct the agent itself to request a user decision"; M19 `asd-concept` "Delegate to agent `asd-ba` to ask user" restored → "AC-7: a skill or workflow collects user input inline"; M20 "loop until explicit `accept`" added to step 6's instruction → "AC-7: design step 6's creator instruction must not carry user contact"; M21 re-dispatch dropped from the accept loop → "AC-7: the accept loop is the orchestrator's"; M22 protocol citation dropped from the token gate → "AC-7: UX's token approval is an orchestrator gate". Rewording R2 (core.md sentence reworded, same substance) → only the D-2 message fired |
| tests/run.js:`sprint-018 AC-4/AC-5/AC-7: a reviewer's question and External Review's stalemate ride the verdict-bearing report …` | fail-first at HEAD e2d919d (D-1): "AC-4/AC-5: the out-of-policy refusal must route a reviewer through its question carrier" (`.asd/rules/providers.md` "Declared tool policy"). M24 `QUESTION` re-added to testing's Signals → "AC-4: asd-reviewer-testing never returns a bare QUESTION"; M25 carrier citation dropped in documentation → "AC-4: asd-reviewer-documentation routes a question through the carrier and must cite its home"; M26 `options:` dropped from the `t_review.md` item → "AC-4: the carrier form in review-policy.md and the item t_review.md ships must be one shape"; M27 carrier sentence reworded without "bare `QUESTION`" → "AC-4: review-policy.md must state the reviewer question carrier"; M28 `external-review.md` reverted → "AC-4: a stalemate returns the FAIL verdict token plus a Stalemate block"; M29 token dropped from the agent trigger → "AC-4: External Review's stalemate trigger must return the same token and block"; M30 impl-review `question:` bullet unmarked → "AC-4: .asd/workflows/asd-phase-impl-review.md must handle reviewer questions before routing" (second loop member); M31 design-review "never interrupted" dropped → "AC-4: .asd/workflows/asd-phase-design-review.md must not treat a question-carrying report as an interrupted dispatch"; M32 impl-review stalemate `abort` option dropped → "…must recognise External Review's Stalemate block and ask the user its options"; M33 payload clause dropped → "AC-7: impl-review collects the manual-verification results"; M34 testing `Inputs` line removed → "AC-7: the testing reviewer reads the results from its payload" |

## Suite run

- Command: `node tests/run.js`
- Scope: impacted = full (safety valve: `sync.js`, `core.md`, `release-manifest.json` are framework-wide)
- Result: fail — 226/228 passed, 2 failed, 0 skipped (exit 1). Both failures are code defects D-1 and D-2 below. They are assertions this entry added, red at HEAD because canon lacks the contract. The two tests red before strategy (T-2, sprint-015), 223/225 at e2d919d, now pass
- Lint / build: pass — `git diff --check` exit 0; `node .asd/sync.js --check` exit 0, `"ok": true`
- HEAD: d3587cc

## Defects

| ID | Entry | Location | Symptom | Failing test | Status | Fix commit |
|---|---|---|---|---|---|---|
| D-1 | 1 | .asd/rules/providers.md | AssertionError [ERR_ASSERTION]: AC-4/AC-5: the out-of-policy refusal must route a reviewer through its question carrier - "an agent ... returns `QUESTION`" alone makes a reviewer return a bare QUESTION, which reads as an interrupted dispatch | sprint-018 AC-4/AC-5/AC-7: a reviewer's question and External Review's stalemate ride the verdict-bearing report - one carrier form across template, rule, agents and both review workflows - and impl-review collects manual-verification results before dispatching the testing reviewer | pending | |
| D-2 | 1 | .asd/workflows/asd-phase-audit.md | AssertionError [ERR_ASSERTION]: AC-7: every workflow dispatching a role that can return QUESTION must cite the QUESTION protocol, or that question has no handling path in the phase | sprint-018 AC-4/AC-5/AC-7: only the main orchestrator prompts the user - core.md says so, the QUESTION protocol carries every dispatched question, every creator/dev/tester-dispatching workflow cites it, and no agent body, skill or design step hands user contact to a dispatched agent | pending | |
