[REVIEW-impl-efficiency]: FAIL

# Review — efficiency

- **Phase**: impl-review
- **Iteration**: 1
- **Severity floor**: low
- **Section gate**: perf predicate FALSE (executable files in scope) → the four measurable performance sections reviewed; `PERF_BUDGET` n/a (confirmed: `.asd/project/custom-coding-rules.md` has no perf-budgets section)

## Findings

| # | Severity | Category | Location | Description | Suggested fix |
|---|---|---|---|---|---|
| F1 | critical | simplify (escalation required) | `.asd/runtime.js:1-254` | **SC-1 god/sprawling module.** One new module carries three responsibility clusters with three independent reasons to change, each governed by a *different* SSoT rule doc: (a) external-CLI process probing + negative-cache persistence, lines 33-129, governed by `external-review.md` "Detection and negative cache"; (b) task-cost routing policy, lines 131-150, governed by `providers.md` "Task-class variants and routing"; (c) review coverage-ledger schema validation, lines 152-210, governed by `review-policy.md` "Coverage ledger"; plus a shared CLI dispatcher (212-252) whose arg handling is a common blast radius for all three. Evidence is responsibility-based, not size-based: a change to the ledger row schema has no relation to a change in the auth-probe contract or the tier ladder, yet all three land in one file and one `main()`. | Split along the responsibility seams into `.asd/runtime/preflight.js`, `.asd/runtime/routing.js`, `.asd/runtime/coverage.js`, with `.asd/runtime.js` reduced to the CLI dispatcher that requires them — decomposition only, no facade, no new abstraction, identical external `node .asd/runtime.js <cmd>` contract. **Escalation:** executing this expands the canonical path surface beyond `sprint.md` scope — `sprint-lifecycle.md` "Self-hosting" allowlists `.asd/runtime.js` (a single file, not a directory), and `release-manifest.json` `managed_paths`/`upstream_hashes` plus `core.md`/`AGENTS.md`/`README.md` mirrors would all need matching edits. Reviewer's own recommendation: keep the single file as a deliberate convention mirroring `sync.js`/`update.js` and record the decision — the three clusters share no mutable state today and the split's mirror ripple exceeds its benefit at 254 lines. |
| F2 | critical | simplify | `.asd/runtime.js:97`, `:36`, `:9` | **OE-6 premature config flag.** `const timeoutMs = input.timeoutMs \|\| 3000;` is an optional knob no caller chooses non-default: `external-review.md:31` enumerates the preflight input fields and does not include a timeout; no workflow step (`asd-phase-impl-review.md:1a`, `asd-phase-design-review.md:3a`) supplies one; no test sets it. Its existence drags in dead machinery: `MAX_TIMEOUT_MS` (line 9) and the clamp at line 36 can only ever evaluate to the constant 3000. | Drop `input.timeoutMs`; make `runLocal` take no timeout parameter and use a single `PROBE_TIMEOUT_MS = 3000` constant directly in the `spawnSync` options. Removes the knob, the clamp and the second constant (net ~4 lines). Re-add a caller-settable timeout only when a caller actually needs one. |
| F3 | critical | simplify | `.asd/agents/asd-dev.md:13`, `.asd/agents/asd-tester.md:13`, `.asd/rules/providers.md` "Task-class variants and routing" | **OE-13 duplicate retained "for compatibility".** The `standard` variant is byte-identical to the base agent it derives from — base `asd-dev` is `claude: sonnet/medium`, `codex: terra/medium`; variant `standard` re-declares exactly that. After `variantMeta` merge the generated `asd-dev-standard` differs from `asd-dev` only in `name` and an appended `Task class: standard.` sentence. Same for `asd-tester`. Net effect: 4 generated agent definitions duplicating their base, while `providers.md` says routing always selects `<base>-<tier>` yet the base ID is "kept for compatibility" with no named consumer — so for dev/tester, one of each identical pair is dead weight. | Delete the `"standard"` entry from both `variants` blocks and state in `providers.md` that tier `standard` dispatches the **base** agent id (`asd-dev` / `asd-tester`); keep only the genuinely differing `mechanical` and `critical` variants. Drops 4 duplicate generated files and removes the ambiguity about which of two identical agents a dispatcher should pick. Update the README `17 agent definitions: 11 roles + 6 tier variants` counts and the `tests/run.js` `generatedAgentCount` expectation in the same change. |
| F4 | low | simplify | `.asd/runtime.js:55-69`, `:111-114` | **PA-3 unbounded allocation.** Negative-cache entries are pruned only when a lookup hits that exact key and finds it expired. `cacheKey` binds `auth_generation` (`mtime:size` of the credential file, line 81) — every credential/token refresh mints a fresh key, orphaning the previous entry permanently. The cache file grows without bound across refreshes, and `readCache` parses the whole accumulated file on every preflight. | Prune on read: in `readCache`'s existing validation loop, add `&& entry.retry_after > now` to the accept predicate (thread the caller's `now` in, defaulting to `Date.now()`), so any expired entry is dropped the next time the cache is opened or rewritten. One predicate, no new structure. |
| F5 | low | simplify | `.asd/sync.js:1243`, `:1257-1258`, `:1326-1331` | **PA-6 unneeded parse roundtrips.** `buildSyncPlan` now reads and parses every agent canon file purely to validate the name and enumerate variants, then discards the parse; `renderFullFileItem` re-reads and re-parses the same canon file once per plan item. With variants, an agent's canon is read/parsed up to 4× more per sync run than before (`asd-dev`: 1 plan-time + 8 render-time reads vs 2 previously). The plumbing seam already exists — plan items carry `metaOverride` — it just is not used for the base pair. | Attach the already-computed `{canonRawNormalized, meta, body}` to each plan item (or memoize by `canonPath` inside the plan builder) and have `renderFullFileItem` prefer it, falling back to reading only when absent. |
| F6 | low | simplify | `.asd/runtime.js:149`, `.asd/rules/providers.md` "Task-class variants and routing" | **CV-1 complication that does not earn its weight.** `routeTask` returns `selector: 'orchestrator'` — a literal that can never take another value — and `providers.md` mandates persisting it per task under `state.json.task_routing[taskId]`. A field with a single possible value carries zero information per record while adding bytes to every routing decision in a repo whose own hard rule is "every change must minimize runtime tokens"; nothing in the diff reads it. | Drop `selector` from the `routeTask` return and from the persisted record in `providers.md`. The workflow writing `task_routing` already knows the actor; re-introduce the field when a second selector actually exists. |

Notable `pass` calls worth recording, since each was a near-trip:

- **OE-7 (defensive code)** — `removeIfEmpty`'s `typeof sync.removeIfEmptyDir === 'function'` guard in `.asd/migrations/5.0.0.js` looked like a guard for an impossible case (`sync.js:1629` exports it, and five other `sync.*` calls in the same file are unguarded). Passed: `update.js` can leave a consumer-modified `.asd/sync.js` unreplaced before migrations run, so a stale export set is reachable, and `.asd/migrations/4.0.0.js:65-67` already established this exact pattern. (Correctness reviewer reached the opposite conclusion — see its F8.)
- **HP-1 / PA-2** — `externalPreflight` spawns both probes before consulting the negative cache. Passed: specified deliberately in `external-review.md:33` and structurally required, since `cacheKey` binds `auth_ready`. Bounded at 2 subprocesses / ~6 s per review dispatch.
- **OE-6 (`user_gates`)** — the new `strict|adaptive` mode is not premature: `.asd/project/config.yaml:1` selects `adaptive`, `t_config.yaml:1` defaults `strict`, so both branches have real choosers.
- **PA-4/PA-5** — `structuredClone` usage is test-fixture-only on tiny objects; `Object.assign` copies in `variantMeta`/`coverageManifestDigest` are shallow and bounded.
- **ALG-1/2/3** — `validateCoverageLedger` and `rowsById` already use `Set`/`Map` lookups rather than nested scans; `buildSyncPlan`'s agents×variants loop is bounded by a 3-value suffix whitelist.

Out of this reviewer's rubric, referred to correctness/documentation: `asd-phase-impl.md` inserts step `2a` before step `2`, `asd-phase-design-review.md` inserts `3a` before `4`, and `asd-phase-plan.md` now runs 1-2-3-4-5-7.

## Coverage

Manifest: [`efficiency.manifest.json`](efficiency.manifest.json) (digest `5f29b8a6…099b641`). Validated compact ledger: [`efficiency.ledger.json`](efficiency.ledger.json). Finding-id array: [`efficiency.findings.json`](efficiency.findings.json). `node .asd/runtime.js validate-ledger` returned `{"ok":true}`.

n/a predicates used: `PERF_BUDGET` section and rules `PB-1`, `RG-1` — `no budgets defined`.

## Verdict
FAIL: 6 (3 critical, 3 low; 1 requires escalation)

## Next action
Route back to `impl` (review-fix mode) with F2-F6 as autofixable `simplify` findings — none needs user approval and none adds an abstraction. F1 escalates to the user first per Complication Approval (below); the sprint re-enters impl-review via `impl-test`.

## Escalations
- finding **F1**: requires user approval (scope expansion / canonical-path-surface change). **What**: split `.asd/runtime.js` into `.asd/runtime/{preflight,routing,coverage}.js` with `runtime.js` kept as the CLI dispatcher. **Why**: one module currently changes for three unrelated reasons, each driven by a different rule doc — an SC-1 trip that policy makes critical and undroppable. **Why escalate rather than autofix**: the split needs a canonical directory that `sprint-lifecycle.md` "Self-hosting" does not allowlist, plus matching edits to `release-manifest.json` (`managed_paths`, `upstream_hashes`), `core.md`, `AGENTS.md` and `README.md` — outside this sprint's stated scope. **Alternatives**: (a) perform the split and widen the allowlist to `.asd/runtime/`; (b) *reviewer-recommended* — keep the single file as a deliberate convention consistent with `sync.js`/`update.js`, record the decision in `decisions-log.md`, and revisit when a fourth command or cross-cluster coupling appears; the three clusters share no mutable state today, so the split would reduce no coupling.
