---
responsibility:
  owns: test approach for sprint change scope, removal reasons, no-test decisions, suite run result, code defects found by tests, manual-verification spec (single home — never duplicated in a review file)
  excludes: task breakdown, requirements, review verdicts, code, change surface (derivable from the diff)
  delegates_to: plan.md (tasks), persistent docs (requirements), reviews/impl/iter-NN/testing.md (verdict)
---

# Test plan — sprint 010-agent-doc-economy

## Entry log

| Entry | HEAD analysed | Scope |
|---|---|---|
| 1 | af784cc | full change surface |
| 2 | 92e433c | delta `af784cc...92e433c`: D-1's fix (`7501224`), the AC-10 scope addition (`b81fd7e`), sprint bookkeeping |
| 3 | 3b080b1 | delta `92e433c...3b080b1`: the iteration-1 review-fix round (`63e6b6d`, `348daf5`, `3cdb0d8`, `3e9ecdb`, `a8f4dcd`) plus T-1/T-2/T-3, the three testing-review findings routed to this role |
| 4 | 9c5fa35 | delta `fb1aeeb...9c5fa35`: the iteration-2 review-fix round (`9e540b3`, `72bad90`, `8663e60`, `2ae44c6`, `9c5fa35`), scoped to the two findings this role owns — TST-01 (the agent-memory class with no recorded decision) and EFF-2 (this role's own memory files). The dev chain's canon edits are covered by the pins already asserting those files; nothing in that delta is a new executable surface |
| 4 (cont.) | dd47159 | the phase's own gate over the **dev chain's** iteration-2 commits, which entry 4 recorded only as a blanket "every existing pin holds" — replaced below by one row per cut. Ranges read: `git diff 11bf405..dd47159` and `git log 9c5fa35..dd47159` hold sprint bookkeeping only (`decisions-log.md`, `state.json`, `test-plan.md`); the dev chain itself is `5add9f5..2ae44c6`, an ancestor of the entry-4 test commit, so the 179/179 already recorded at `11bf405` was run over it. That made the run a check on the pins that existed, never a decision about the cuts — this continuation makes the decision |
| 5 | 74a076d | delta `1950c00..74a076d`: the iteration-3 review-fix round — two canon edits in `060318e` (the false denial dropped from `asd-phase-impl-review.md`'s clean-worktree precondition, DOC-1a; the unscoped sole-statement claim narrowed in `review-policy.md`, DOC-1b) plus three agent-memory writes (`acd4891`, `a98a7b0`, `74a076d`). This entry continues a dispatch a host rate limit cut off mid-pass: that dispatch left `tests/run.js` uncommitted with two new tests, inherited here and **re-derived against source and re-proven by mutation** rather than trusted — a proof this role did not run is not a proof it can record |

| 6 | 94d8779 | delta `14a8ce5..94d8779`: the test-fix round that closed this role's own D-3 and D-4 (`ac3073a` retargets both citations, `01cfad1` marks both rows `fixed`, `ef235e4` a dev memory write, `6d3d0c0`/`94d8779` bookkeeping). Range gated before use rather than trusted: `git log 14a8ce5..HEAD` holds exactly the round it names. Scope is the delta plus the two consequences that round deliberately left to this role, both of them this role's own test text — the emptied pinned list and the message clause describing it |

| 7 | 93f8a20 | delta `36233d5..93f8a20`: the iteration-4 review-fix round — EXT-1's canon bounding of the sole-statement claim (`b7f58c4`), the external reviewer's write-ban carve-out (`5b04a80`), DOC-1's memory rewrite in `asd-external-review` (`13dc229`) and one `asd-dev-critical` memory entry (`79d413d`), plus review bookkeeping. Range gated before use rather than trusted: `git log 36233d5..93f8a20` holds exactly that round, and `--stat` is four canon/memory files, the manifest hashes and the iter-04 review artefacts — nothing executable |

**Impacted set = full suite** (`sprint-lifecycle.md` "Impacted test set", safety valve). The surface touches `.asd/runtime.js`, `.asd/sync.js`, `.asd/release-manifest.json` and every rule doc — framework-wide files by definition. `commands.yaml` carries no `test_affected`, so no native selector overrides it.

## Risk → check decisions

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `runtime.js` `--write` stamps `row_example` | a published manifest field left outside the digest is editable on disk after stamping; every ledger citing that digest still validates | unit | add | the `manifest-digest` CLI test amended: pins the stamped field set through the untouched `fingerprint` primitive, then sweeps *every* field `--write` stamps for digest coverage, so a third constant is covered the day it lands |
| `runtime.js` `row_example` equality + backward-compat branch | adding the second published constant invalidates every manifest stamped when only `vocabulary` existed | unit | add | the `validate-ledger` backward-tolerance test amended: parametrised over both constants, plus the mixed legacy fixture (`vocabulary` present, `row_example` absent) built from `fingerprint`, not from the digester under test |
| `runtime.js` `LEDGER_ROW_EXAMPLE` content | an example the validator rejects teaches every reviewer a ledger shape that fails the blocking gate — visible only as a re-dispatch | unit | add | new test fills the placeholders and validates the row; the status is keyed to `LEDGER_VOCABULARY.p`, not to a literal |
| `sync.js` Claude `effort` validation | an invalid effort renders into a generated view and the host silently ignores it, so canon, README and the tier matrix all claim an effort the agent never ran at | unit | add | new test covers the vocabulary boundary (`ultra` is Codex-only), absence, Codex asymmetry, and the field's emission condition — **found D-1** |
| AC-9 / G-12 rule-doc list | `core.md` "See also" and the `AGENTS.md` copy drifted two entries and stayed green for sprints; Task 10 replaced the copy with a pointer | static | add | new test asserts the index is a bijection with `.asd/rules/*.md` **and** that neither `AGENTS.md` nor `t_AGENTS.md` re-lists the docs beside the pointer — the invariant after Task 10, not the list-against-list check G-12 first described |
| AC-7 economy rule (`artifact-layout.md`) | a second home for the cut-or-keep procedure; or the rubric bullet stated as prose, which never reaches the blocking ledger | static | add | new test: sole-home sweep over canon for the three named tests, both pointer sites, and `Documentation economy` resolving as a rubric **id** under review-policy.md's G-9 derivation |
| AC-1 wave declaration (4 sites) | `impl` re-deriving its own order discards the isolation rule silently — the F-1 failure itself | static | add | new test binds definition → plan workflow → impl steps 5/6 → template slot, keyed on citations and on the inter-wave barrier |
| AC-5b authorised-paths diff read | placed in the fix-mode-only step, initial mode loses the gate; duplicated, the two copies drift | static | add | new test locates it inside the all-modes step 9 block and asserts it appears exactly once in the file |
| AC-2 whole-tree command ban | the `custom-coding-rules.md` mirror silently falls behind as the ban grows | static | add | the staging-prohibition sole-home test amended: the command list is **derived** from git-strategy.md's own clause and each member required in the mirror |
| AC-3 co-author agent memory | an author who cannot stage a co-held file is covered by nobody, so the write never reaches the reviewed diff | static | add | the `AC-13b` agent-memory commit test amended across its three sites (owner, definition, reachability pointer) |
| AC-4 availability skip | a review that never ran is recorded nowhere the retro reads | static | add | the External Review outcome-contract test amended; keyed to the same `sprint-lifecycle.md` "Friction log" citation every phase workflow carries |
| AC-6a enforcement split | one workflow keeps the transcription branch, the other rejects — or an interrupted dispatch is re-encoded into a verdict it never returned | static | add | the interrupted-dispatch test amended: the bounded-at-one and never-supplies-missing-evidence clauses, plus both workflows' acting sites and the interrupted-dispatch exclusion |
| AC-6a partition semantics (which of the eight classes re-encoding can fix) | a wrong partition mislabels a return | — | none | The workflow branches on the re-run, not on the class ("So the re-run, not a failure message, decides which half"), so no code reads the partition. The safety property that makes transcription safe — a re-encode cannot manufacture evidence — is the helper's, and is already covered by the compact-ledger fraud test (identity, completeness, predicate and finding-reference fraud). A test of the partition would assert my own re-encoder |
| AC-5a fail-first mutation must be restored (`code-style.md` §17) | a mutation left on disk ships as authored work | — | none | Single home, no mirror: no file restates the bullet, so there is no drift surface to assert. Whether an agent restored a mutation is runtime behaviour no suite observes; the acting checks are AC-5b's gate (covered above, its diff read is what catches an unrestored mutation) and `asd-reviewer-testing` reading this file. **Superseded at entry 3** (T-1..T-3 row block below): drift was the wrong risk to weigh, deletion is the live one, and the bullet is now asserted |
| AC-8 corpus audit (`audit.md`, 23 `E-N` + 12 `C-N`) | a finding neither applied nor deferred | — | none | Sprint artefact with no enumerable canonical mirror; disposition is recorded per finding in `decisions-log.md` and checked by the impl-review Documentation reviewer. Nothing here is a literal token in a tracked file |
| ~18.7 KB of prose deleted from agents, skills, workflows, README | a deletion takes a load-bearing instruction with it | static | keep | Every literal the suite pins is re-checked by the full run below — that is what a green suite does prove here. It does not prove the deletions safe (`plan.md` Risks, `audit.md` R-6); that judgement is the Documentation reviewer's, now under the rule this sprint landed |
| generated `.claude/`, `.codex/`, `.agents/skills/` views | a canon edit not resynced ships a stale view | static | keep | `node .asd/sync.js --check` (the `build` command) plus the `--check` reports every item current test; 72/72 current |
| `.asd/release-manifest.json` hash updates | a tracked source whose recorded hash no longer matches | static | keep | Existing `canon_hashes` / `upstream_hashes` tests cover both directions |
| **Entry 2** — `sync.js` D-1 fix (`7501224`): the effort check moved to its emission site | the check regresses to a sibling-guarded position and an agent declaring `effort` without `model` renders unvalidated again | unit | keep | Already covered: the entry-1 test's last case *is* the emission-site path (`effortWithoutModel`), and it is what failed at entry 1. Re-proven fail-first at this HEAD against `7501224^`. Amended with a fixture guard only — see Added tests |
| `sync.js` variant render path (`variantMeta`) | a task variant declaring an effort with no model slips past the emission-site check | — | none | No second path exists to test: `variantMeta` rejects a variant whose `claude.model` is not a string, and variants render through the same `transformAgentClaude` line the fix now guards. A test here would assert the fixture, not the code |
| AC-10 authoring reach (`artifact-layout.md` rule body, `code-style.md` §1, `providers.md` role table) | the rule's home is granted to a role that never authors under it — or `code-style.md` §1 goes on naming SSoT alone, leaving the economy rule review-only for the two roles that read §1 at all | static | add | The reach claim is a table sweep, not a judgement: every `providers.md` role row whose context is a fixed list must grant `artifact-layout.md` (the one row that grants per consulting question is the derived exemption), guarded by a row-count assert so a parse miss cannot pass vacuously. Plus the home carrying both sides (authoring obligation and `FAIL` consequence), and §1 enumerating every `(iron rule)` heading the home declares |
| AC-10 obligation restated in the eleven agent bodies | a per-agent copy re-homes the rule the sprint just gave one home | — | none | Declined deliberately, not overlooked: such a copy would be reworded, so a literal sweep would miss it while reporting coverage — the class of assertion `code-style.md` §17 rejects. The copy that would actually drift is the three-test *procedure*, already swept across all canon Markdown by the AC-7/G-9 test |
| `code-style.md` §1 rewording (`b81fd7e`) vs the entry-1 sole-home sweep | the sweep stops reaching the file the AC-10 edit touched | static | keep | Re-proven at this HEAD: inserting a `**removal** —` restatement into §1 still fires `.asd/rules/code-style.md must cite the economy rule rather than restating its three tests`. The new bullet cites the home, it does not copy it |
| **Entry 3** — `runtime.js` `n_a` shape rejection (D-2's fix, `a8f4dcd`) | `n_a` is the one manifest field an orchestrator hand-builds per dispatch, and nothing bound the shape it writes to the shape `validateCoverageLedger` reads back out: mis-keyed, every truthful `n/a` row is rejected as unauthorized while the malformed manifest itself validates | unit | add | T-1(a) and T-1(b) in one test. The emitted nested shape — row type, then id, then predicate list, as `buildManifest` models and all four iter-01 manifests carry — must authorize an `n/a` row of **every** row type; then each rejection branch of the fix: mis-keyed by id (the realized F-1 shape, paired with a ledger carrying no `n/a` row at all, which is the combination that passed silently), a non-object `n_a`, and absence still tolerated so the fix cannot make `n_a` mandatory |
| `sync.js` `effort` emission guard `c.effort !== undefined` (`348daf5`) | `"effort": ""` skipped both the emission and the vocabulary check: canon claims an effort the generated view never carried, and `--check` stays green over the mismatch | unit | add | one case on the existing C-10 test, which External Review asked for by name — the same silent-ignore class C-10 opened, reached from the emitter side rather than the vocabulary side |
| `sync.js` collapsed unknown-family throw (`348daf5`) | the Claude branch built its diagnostic without the agent argument it was handed, so the argument was dead and the message carried no agent name | unit | add | one case asserting the Claude-path message names agent, family, resolved model and effort; reverting to the provider-special-cased pair fails it |
| AC-5a `code-style.md` §17 fail-first restore bullet | **T-2, decided.** Entry 1 recorded `none` on drift grounds. Drift was the wrong risk: the bullet has no mirror, so what threatens it is deletion — under an economy rule this same sprint made an iron rule and bound at authoring time | static | add | Resolution: two `assert.ok`s on §17, added to the AC-5b test (the gate whose whole purpose is catching an unrestored mutation), rather than a restated `none`. The preserve-list is **not** a sufficient guard: a procedural instruction is none of its named classes — contract token, enumeration whose completeness is the rule, collapsed case distinction, failure mode with symptom — so the rule protects this bullet nowhere, and "an agent that follows it acts no differently for reading it" is precisely the removal argument a future economy pass would make. One assert per sentence: the consequence sentence is separately cuttable, and without it a green suite reads as evidence that the tree is clean |
| `asd-phase-impl.md` `## Operations used` run-command grant (`3cdb0d8`) | the step-9 gate requires a committed-plus-uncommitted diff read, which only git answers, with no declared operation behind it — an obligation the phase cannot discharge | static | add | one assert beside the existing gate assertions, in the shape the AC-1/AC-2 test already uses for `asd-dev`'s git grant |
| AC-10 reach sweep exemption set | **T-3.** The row-count guard covers vacuity on the row set but not on the exemption set: any number of rows adopting the exemption wording drop out of the sweep silently, while this file asserts the exemption is singular | static | add | the exemption set is now derived from the table and compared to `` [`asd-advisor`] `` exactly, and only then is the remainder looped |
| `artifact-layout.md` economy rule corrections (`63e6b6d`) | OR-joined, the rule authorises cutting on provenance alone; with its prohibition bullet unnarrowed it authorises deleting this framework's own safety/security/authority bans; with the new normative class unscoped it would instead shield every restatement from the SSoT rule stated above it in the same file | static | add | three asserts on the AC-7 test — removal necessary with the other two only corroborating, the normative class scoped `at its home`, and the standalone prohibition named in the preserve-list. Each pins a landed fix with a recorded finding behind it (EX-2, EX-6, EX-7) |
| restatement deletions in both review workflows, `asd-phase-impl.md`, `review-policy.md` and six agent bodies (`3e9ecdb`) | a deletion takes a pinned literal or a load-bearing instruction with it | static | keep | Re-run at this HEAD: every existing pin holds, including the three sites this commit moved — `review-policy.md`'s `row_example` publication (now a clause on the vocabulary paragraph, still one line carrying all three keys, which the AC-6b test reads off it), the documentation reviewer's `**Documentation economy**` rubric id, and both review workflows' interrupted-dispatch acting sites. Whether a deletion was safe beyond the literals pinned stays the Documentation reviewer's judgement, as at entry 1 |
| **Entry 4** — every `.claude/agent-memory/**` file this sprint writes (TST-01: fourteen files across four directories once this entry's own three land, six of them at the time the finding was raised) | a memory file written but not indexed, or an index line whose target does not exist. Only `MEMORY.md` is always in context, so an unindexed memory is one the agent never loads and writing it was a no-op — silent in every diff, since both halves are well-formed Markdown | static | add | Recorded nowhere across entries 1-3: the omission was silent, not argued, and the class is assertable — a bijection check already existed, hardcoded to `asd-dev-critical` and `asd-tester-critical`, so the round's writes into `asd-reviewer-correctness` and `asd-external-review` sat outside it. Fixed by **deriving** the set from `.claude/agents/*.md` (the dispatchable roster, tier variants included) rather than extending the hardcoded array: a hardcoded list is what failed, and re-hardcoding four names would fail again the next time an agent writes its first memory. A directory no roster name matches is out of the loop by construction — its files load in no dispatch at all — and the fallout set is compared to its expected single member (`asd-pm`), so a misspelled directory cannot join it silently. `asd-pm` is a retired agent whose `MEMORY.md` carries a dangling link since PR #21: pre-existing, outside this sprint's change surface, and not this role's to edit — reported, not swept in |
| Entry 4 — this role's own memory files, rewritten under EFF-2 | a merge loses a recorded failure mode, or the rewrite leaves a memory file unindexed | static | keep | Same check as the row above, which now reads this directory: the three files stay a bijection with `MEMORY.md`. Content is judged against `artifact-layout.md` "Documentation economy", whose `Never cut` boundary keeps every failure-mode-with-symptom entry — the merge deleted the duplicated procedure statements, not the failure modes the superseded entries contributed |
| Entry 4 — the dev chain's restatement deletions (`9e540b3`, `72bad90`, `8663e60`, `2ae44c6`) in `review-policy.md`, `sprint-lifecycle.md`, both review workflows and two agent bodies | a deletion takes a pinned literal or a load-bearing instruction with it | static | keep | Re-run at this HEAD: every existing pin holds, including the latch sites both review workflows edited and the correctness reviewer's write ban. No executable source is in the delta (`git diff --stat 5add9f5..9c5fa35`: canon Markdown, one memory file, the regenerated views and their manifest hashes), so no unit-level surface was added. Whether a deletion was safe beyond the literals pinned stays the Documentation reviewer's judgement, as at entries 1 and 3. **Superseded at entry 4 (cont.)** by the five rows below: a green re-run only proves the pins that existed, and this row read as a completeness claim it could not make. Two of the five cuts turned out to be unpinned on both sides |
| a repo-wide sweep asserting every emitted `*.manifest.json` keys `n_a` by row type | an orchestrator emits a mis-keyed manifest again, as it did on all four this iteration | — | none | Checked rather than assumed: all 51 manifests under `.asd/sprints/**` key `n_a` by row type today, so the sweep is green — and redundant, because `a8f4dcd` now rejects that shape at dispatch, before any ledger is validated. §17 forbids authoring a duplicate of an existing check, and archived sprints' artefacts sit outside any change surface |
| **Entry 4 (cont.)** — `72bad90` cut the correctness reviewer's four-item nitpick enumeration, leaving only the class name (`Never raise nitpick categories`) | the cut is safe only if the enumeration is reachable from the agent at dispatch. If `review-policy.md` "Nitpick drop list" is later cut by an economy pass, or a reviewer's `providers.md` row stops granting that file, five reviewers carry an instruction naming a class whose members exist nowhere they read — and the gate silently widens rather than failing | static | add | The reach is a table sweep, not judgement, and was unpinned on **both** sides: nothing asserted the section exists and nothing asserted the grant. New test: the section must enumerate (≥5 bullets — a class name with no members is unfollowable), the instructed-agent set is derived from `.asd/agents/*.md` by the token `nitpick` and compared exactly against the review-agent file set (so a new reviewer that never got the instruction fails here instead of shipping permissive), and each such agent's `providers.md` row must grant `review-policy.md`, guarded by a row-count assert against a vacuous parse |
| the drifted copy itself — `72bad90`'s deleted list had aged to four of the home's five | a future agent body re-acquires a copy that drifts again | static | add | **Scope stated, not overclaimed.** A sole-home sweep on the one distinctive token (`you could also`) across all canon Markdown, with the exemption set *derived and compared exactly* to the two `.asd/templates/external-review/t_prompt-external-*.md` files. Those are legitimate second copies — the wrapped CLI is handed that text and cannot read our rules — and the comparison guards both directions: a new copy joins the list, and deleting one from a prompt leaves the external reviewer told to drop categories it is never shown. Deliberately **not** a claim that no paraphrase exists: row 46 already decided that a reworded copy defeats a literal sweep, so the assert message says the token is what is sole-homed |
| `9e540b3` cut `and additionally clears every APPROVE latch sprint-wide (below)` from `review-policy.md`'s DoD paragraph | the cut removes the only rule-level statement outside `sprint-lifecycle.md`. The surviving acting sites are the two step-9 red branches in `asd-phase-impl-review.md` — and `grep sprint-wide tests/run.js` was empty, so the whole invalidation contract had no assertion at any site. `sprint-lifecycle.md` calls that paragraph "the contract the full-suite step must satisfy"; with no acting site pinned, a later economy pass on the workflow leaves a contract nothing performs | static | add | New test (bundled with the row below — same commit, same subject): `sprint-lifecycle.md` "APPROVE latch" must keep `**Red-full-suite invalidation.**` and its blast radius (`clear BOTH … to {}`, since a rule saying only "clear the latch" is satisfied by clearing one map), both step-9 red branches must clear latches, and `review-policy.md`'s DoD line must not re-acquire the clearing. The branch loop is guarded by a `=== 2` count so a regex that stops matching cannot pass over an empty list |
| `9e540b3` narrowed three false non-restatement declarations (`review-policy.md`'s `not restated here or in either review workflow`; both workflows' latch-filter `, not restated here`; both workflows' `and the availability-skip carve-out`) | each was false at the moment it was written — the same bullet spells the mechanic out one clause later. A false "not restated here" is read as licence to delete the SSoT copy, which is the one an agent that never opens that workflow depends on. This is the exact class the sprint's economy rule creates pressure to act on | static | add | Assertable without wording coupling, because each site is pinned as a **pair**: the bullet must still spell the mechanic out (`skipped entirely this iteration`; `External Review's availability skip is recorded as`) **and** must not carry the denial. The positive half is what keeps the negative half non-vacuous — a rewording that drops the restatement fails the positive assert and forces the editor back to both sites rather than silently re-permitting the denial |
| `8663e60` cut `Never bury verdict in prose.` from `asd-external-review.md`; `72bad90` cut the correctness reviewer's `Reports findings, does not fix.`, `report only` and `Never fix code or docs yourself — emit findings only` | a verdict-parse or write-scope contract loses its last statement | — | none | Checked per cut, not blanket. (a) Read all five `## Gate Verdict Format` sections side by side: none of the four internal reviewers ever carried the prohibition, and the cut leaves external's closing line identical to correctness/documentation/efficiency (testing's differs only in being phase-fixed, `[REVIEW-impl-testing]`). The parse rule *is* the contract — the prohibition only restated its consequence — and the token stays pinned by the existing `[REVIEW-<phase>-<reviewer>]` mirrors. (b) The three write-scope cuts leave the ban stated twice in the same file (`Scope: read-only review`, `Authority: … never modifies code or docs`) and, more to the point, **config-enforced**: the AC-15/iter-05 test already asserts each internal reviewer's frontmatter carries no `Bash`, and `disallowedTools` bars `Edit`. A fourth prose copy is what §17 calls a duplicate; asserting it would pin wording over the mechanism |
| `2ae44c6` changed one citation in `sprint-lifecycle.md` (`asd-advisor.md` Do's → Don'ts) | the citation points at a section holding nothing of the kind, so an agent that follows it proceeds unguided while the sprint reads as documented. Unpinned on both sides, and the second wrong-citation fix this sprint | static | add | First recorded as `none` on the ground that the durable form is a repo-wide link checker (new infrastructure, Complication Approval). Rejected on re-reading: that argument only rules out the *general* form, and the specific one is not a bare literal but a **derived relation** — scan the advisor's `## Do's`/`## Don'ts` for the no-log bullet, assert exactly one section holds it, then assert the lifecycle step cites *that* heading. Reword either side freely; the check survives, and it goes red the moment the rule moves without its citation. `.asd/agents/asd-advisor.md` carries `Never log the consult — no file is written for this exchange` under Don'ts, so the fix is correct |
| **Entry 5** — `060318e` dropped `— not restated here` from `asd-phase-impl-review.md`'s clean-worktree precondition (DOC-1a) | the denial was false in the same breath that it was written: the bullet restates its home's trigger and timing one clause later. A false denial reads as licence to delete the home copy — and the reciprocal risk is the opposite one, an economy pass cutting the *acting* bullet as the restatement it visibly is, taking the only executable instruction (`git status --porcelain`) and the only stated consequence (`FAILED`) with it | static | add | Pinned as a reciprocal pair, not as two literals: the home in `sprint-lifecycle.md` must exist and must **name** the workflow section it delegates the mechanic to, and the test derives the acting site from that name (`## Preconditions`), so a rename on either side alone fails. At the acting site: command present, signal present, `sole SSoT` citation present, denial absent. The positive three are what keep the denial assert from going vacuous under a rewording |
| `060318e` narrowed `review-policy.md`'s `Sole statement of this claim.` to a scoped one (DOC-1b) | the unscoped form was false when written — both review workflows and `sprint-lifecycle.md` also state the reviewer performs no write — so the owning side claimed every other site merely links, which contradicts them and invites a cut at whichever site is read next | static | add | One assert on the existing AC-15 test, whose two standing asserts (the `providers.md` grant fact, the `Gate Verdict Format` citation) already read this paragraph and keep the new negative non-vacuous. **Scope stated, not overclaimed**: it guards the literal from coming back, never the truth of a reworded ownership claim |
| **(a) the false-declaration class itself** — three rounds, three files (`9e540b3` two workflows + `review-policy.md`; `060318e` a workflow + `review-policy.md`) | a declaration that denies a restatement its own site performs, or that claims sole statement over sites that also state it, keeps being written and keeps being caught only by a human reviewer | — | none, and this is the honest limit | **Decided, with the search recorded.** Truth of a denial is a semantic-equivalence judgement between the denying site and the SSoT; over this corpus no derivable proxy separates a true denial from a false one, because the restatement is a paraphrase every time (`code-style.md` §17's rejected class: an assertion that reports coverage while missing what it claims to cover). What *is* derivable was measured, not assumed: 41 declaration lines across canon, all reachable by regex, but the only machine-decidable property of a declaration is its **pointer**, which is the row below. Per-site pair pinning (positive restatement + absent denial) stays the answer for a declaration a round actually touches, and now covers five sites across three rounds |
| the derivable half of the same class: **a citation naming a section that does not exist** | the sibling failure, and the one with a track record. Sprint 006 renamed `checkpoints.md` `## Re-running a phase` → `## Re-run` (`8298f15`) and `external-review.md` `## Iteration-aware diff` → `## Iteration semantics` in the same PR; its documentation reviewer (F5) caught two citations left behind and fixed them by hand, and **two more survived and are still dangling today**. A declaration makes its pointer load-bearing — it tells the reader the content is at the target and nowhere else — so a dangling one leaves the content reachable from no site at all | static | add | Supersedes the entry-4 `none` that declined this as "a repo-wide link checker, new infrastructure". That argument rules out the general form only: this is one 25-line derivation over one attested citation grammar (`` `<file>.md` "<Section>" ``) inside the existing runner — no dependency, no harness, nothing to configure. Measured before writing: 185 citations in canon, 183 resolve. Resolution accepts a `## heading` **or** a `**bold label**` (10 of 185 cite a bold label — an established form, and a heading-only check would redden a correct edit), and falls back base name → repo root → `.asd/templates/t_<base>` so `AGENTS.md` and `audit.md` resolve without a hardcoded pair. Two tiers: zero tolerance on a declaration line, exact both-directions comparison on the rest — see **D-3 / D-4** |
| **(b)** EFF finding below floor: the iter-02 declaration-pair test asserted `!policyLatch.includes('review workflow')`, a topic phrase, where the removed instruction phrase was `not restated here or in either review workflow` | an unrelated correct edit that legitimately names the review workflows in that bullet reddens the suite; the dev confirmed it did not touch that line, so the finding is about the assertion, not the canon | static | **fix, kept** | Re-keyed to the denial being **extended by a conjunction** (`/not restated here\s*(,\s*)?(or|nor|and not)\b/i`) rather than to the topic word. Two-sided proof, both run: the pre-`9e540b3` literal still fires it, and adding a legitimate sentence naming the review workflows to the same bullet leaves it green. This is the same correction `.claude/agent-memory/asd-tester-critical` records as "assert removed phrases, not topic words" — canon here routinely narrates the alternative it just rejected in the same bullet |
| Entry 5 — the round's three agent-memory writes (`asd-external-review`'s two rewritten files + index, `asd-dev-critical` and `asd-reviewer-testing` one file each) | a rewrite that de-indexes a file, or an index line whose target no longer exists — silent in every diff, since both halves are well-formed Markdown | static | keep | Covered by the roster-derived bijection test (entry 4, TST-01), which reads every directory a dispatchable agent can load, `asd-external-review` included. `git diff --name-status` over the range is five `M` lines, no add/rename/delete, so no structural change to the bijection; green at this HEAD confirms both rewritten descriptions still point at files that exist |
| Entry 5 — the two canon edits vs. every literal the suite already pins | an economy edit crosses a pinned literal | static | keep | Narrower than a blanket keep, and checked in both directions: the two edited paragraphs are each read by an existing test (AC-15 for the `review-policy.md` one; the new pair test plus the citation sweep for the workflow one), and both edits were **re-applied as mutations** in this entry, so what the suite says about them is a result, not an assumption |
| **Entry 6** — `ac3073a` retargeted both dangling citations (`sprint-lifecycle.md` → `checkpoints.md` "Re-run"; `asd-reviewer-correctness.md` → `external-review.md` "Iteration semantics") | a retarget that lands on a *second* non-existent heading closes the defect row while leaving the reader exactly as unguided — the defect class here is a pointer, and a fix to a pointer is checkable only by resolving it | static | keep | Covered by construction and **proven rather than assumed**: the sweep resolves all 185 citations, these two included, and each half of the fix was reverted as a mutation — both went red naming the site. A literal pin on the two new headings would be the duplicate §17 prunes, and would redden the day either heading is correctly renamed with its citation |
| Entry 6 — the zero-tolerance denial tier, now that `ac3073a` has emptied the pinned list | with the general tier comparing against `[]`, the denial tier compares a **subset** of the same set against `[]`. It cannot fire where the general one passes, so it adds no coverage; its `denials >= 25` floor then guards a filter with no consequence, and a floor that guards nothing reads as depth the test does not have | — | **remove** | The separation earned its place only while the general tier tolerated a pinned exemption the denial tier refused — that difference was the whole content of the split, and the fix removed it. Reshaping was measured before being rejected (see Removed tests), not waved off. Coverage after the removal is unchanged at 185 relations, proven by re-running the denial-line mutation against the surviving comparison |
| Entry 6 — the surviving message's clause "a missing entry means one of the known pair was fixed … the fix belongs with deleting its line here" | the clause describes a failure mode that is now unreachable (`[]` cannot shrink) and names D-3 / D-4 as live pinned entries, so the text read at the moment of failure teaches a procedure this test no longer performs. Its other half — an entry means a citation renamed on one side only — stays the message's live purpose | static | **fix, kept** | Rewritten to keep the live half and to point the exemption policy **forward** instead of backward: should a dangler ever turn up pre-existing and out of scope, pin it with its own `D-N` row rather than dropping the sweep — except on a denial line, which may never be pinned, because the denial tells the reader the content lives at the target and nowhere else. That sentence is now the only home the denial-class rule has inside the test, and it sits where an editor about to re-open the escape hatch will read it |
| **Entry 7** — `b7f58c4` bounded `review-policy.md`'s sole-statement claim to canon and named hand-authored agent memory as outside it (EXT-1) | the previous narrowing was still false: a reviewer's own memory file restates both halves the claim says only it states. Read as licence, an over-reaching claim invites a cut at whichever restating site is opened next — and the sixth instance of this class in one sprint says the pressure is real, not hypothetical | static | add | **The dev's proposed form was rejected and a different one written.** Qualifier presence (`in canon`) is a wording lock: a synonym defeats it and a correct rewording reddens it — proven, not asserted, by a counter-mutation that rewords the carve-out and drops `in canon` and leaves the suite green. What is durable is the *substance* of the fix: the declaration keeps a carve-out for agent memory and a pointer to the rule that owns that surface. One assert on the existing AC-15 test, whose two standing positives keep it non-vacuous. Not a duplicate of the citation sweep: the sweep proves a pointer that exists resolves, this proves the declaration still carries one — deleting the sentence leaves the sweep green |
| `5b04a80` scoped the external reviewer's `no file writes at all` ban and cited its own `memory: project` grant | the body now makes a **config claim**: that this agent holds a grant its frontmatter must actually carry. Nothing in the suite bound any prose `memory: project` claim to frontmatter — measured, not assumed (`grep` over `tests/run.js`: two hits, both on `review-policy.md` prose). Unbound, the ban either forbids a channel the host grants (unscoped again) or points at a channel the config no longer grants: the config-side twin of a dangling citation | static | add | Two asserts on the existing AC-15/iter-05 test, which already performs exactly this claim→grant binding for `Bash` on this same agent. The carve-out is required **on the ban's own bullet**, keyed on the token `memory` rather than on the fix's phrasing, so a reader of the prohibition sees the exception; and `claude.memory === 'project'` binds the cited grant to the frontmatter. The first keeps the second from passing vacuously if the sentence is cut |
| Entry 7 — the round vs. this role's three assertions on the `review-policy.md` paragraph | an economy edit crosses a pinned literal, or removes text an assertion's premise depended on | static | keep | Checked by reverting the file to `b7f58c4^` rather than by reading the diff: the two positives (`Reviewers write no review artifact, code or doc`, the `memory: project` reconciliation sentence) survive the edit untouched, and the negative on the retired `Sole statement of this claim` still holds — the new wording is `Sole statement of that scope in canon`. The dev's report is accurate |
| Entry 7 — the four internal reviewers, checked for the same ban ambiguity | a second reviewer body forbids its own memory writes while its frontmatter grants them | — | none | Measured before deciding: no internal reviewer body carries a blanket file-write phrase at all (`no file writes` / `writes no file` / `never write` across the five reviewer bodies returns two hits, both in `asd-external-review.md`), and only that one body mentions `memory: project` outside frontmatter. A derived sweep over the class would be a one-member loop today — more machinery than fact, and the two asserts above already cover the member. The dev's judgement is confirmed rather than taken on report |
| Entry 7 — the `asd-external-review` and `asd-dev-critical` memory rewrites vs. the roster-derived bijection | a rewrite that de-indexes a file, or an index line whose target no longer exists | static | keep | Same check as entries 4 and 5, and the delta cannot break it structurally: `git diff --name-status` over the range is four `M` lines under `.claude/agent-memory/**`, no add, rename or delete, so file set and index set both moved only in content. Green at this HEAD confirms the re-keyed `asd-external-review/MEMORY.md` descriptions still name files that exist |
| Entry 7 — the `[[wikilink]]` cross-references two memory files gained (`[[scope-manifest-transport]]`, `[[codex-invocation-mechanics]]`) | a memory cross-reference resolving to nothing: the pointer class this sprint has been chasing, one surface further out | — | none | **Measured, and the measurement is the reason.** No single resolution rule is correct here: of the three link targets in that directory, two match a `name:` field while `[[bash-tool-limits]]` matches only a *file stem* (`name: bash-tool-command-length-limit`), so a name-keyed check reddens correct memory and a stem-keyed one reddens the other two the day they diverge. More decisive: an unresolved `[[name]]` is contract-valid here — it marks a memory worth writing later, not an error — so the assert would encode a rule the memory contract does not have. The citation sweep stays canon-scoped by design (`canonMarkdownFiles()` walks `.asd/` only), which is why the round's two new canon citations of `artifact-layout.md` "Agent memory" are swept and these are not |

## Removed tests

None. Every test covering the change surface was re-read against `code-style.md` §17's prune criteria: no duplicate, trivial, mock-confirming or implementation-coupled test was found among them, and the sprint's deletions removed no prose any test pinned — evidenced by the entry run, whose single failure was the AC-6b contract change below, not a pin on deleted text.

**Entry 4 (cont.)**: the dev chain's five cuts were checked one by one against the assertions that read those files, both for a pinned literal removed and for a premise quietly invalidated. None was: the assertions touching `asd-reviewer-correctness.md` read frontmatter (`AC-15/iter-05`, no `Bash`) and its rubric ids (`G-9`), neither reachable from a Do's/Don'ts bullet; the `[REVIEW-<phase>-<reviewer>]` mirrors are untouched by the external-review cut; and the three narrowed declarations left every cited section name intact. So nothing was orphaned and nothing earned removal.

**Entry 5**: one removal, of this role's own uncommitted work. The cut-off dispatch left a second new test, `every canon declaration that denies restating something points at a section that exists`, which resolved citations **only** on lines containing `not restated here` and **only** against a `## heading`. Deleted and replaced by the generalized sweep in Added tests, for two reasons found by re-deriving it against source rather than trusting it: (1) heading-only resolution reddens on a correct edit — 10 of canon's 185 citations legitimately target a `**bold label**`, including `sprint-lifecycle.md` "Impl-review clean-worktree precondition", the very rule the sibling test pins, so a future declaration citing it would have failed a test that was right about nothing; (2) the denial filter missed the `do not restate here` variant (`asd-phase-impl.md:74`) while its narrower scope hid the two live dangling pointers the generalized form finds. The replacement subsumes every relation the deleted form asserted and keeps the denial class at a stricter tier, so the suite count is unchanged at 184 while the pinned set grows from 20 relations to 185. In-scope (this role's own file, authored this phase), so no approval was needed.
**Entry 6**: one **assertion** removed, no test removed — the citation sweep's zero-tolerance denial tier, with the `denial` regex, the `denials` counter and its floor guard that existed only to serve it (10 lines out, 4 in; suite count unchanged at 184). `ac3073a` emptied the pinned list, so `dangling.filter((d) => d.denies)` became a subset of `dangling` with both compared against `[]` — the first cannot fire where the second passes, which is §17's duplicate. **A reshape was measured before the prune rather than declared impossible**: the one non-subsumed property available is "every denial line carries a resolvable pointer", and it is false at HEAD — 15 of canon's 41 denial lines carry no `` `file.md` "Section" `` citation and are right not to (`code-style.md` §17 is pointed at by section number; `t_test-plan.md`'s "Change surface is not restated here — it's the diff" points at no document at all). Asserting it would redden correct canon, and narrowing it to "some pointer" fails on that same line. So no live denial-tier assertion exists today; the class rule moves into the surviving assert message instead of into a dead assert. In scope (this role's own file, authored this phase), so no approval was needed. Restoring the split is one line if a future entry ever pins an exemption, and the message says so at the point of edit.

## Added tests

| Test | Regression proof |
|---|---|
| `runtime.js CLI: manifest-digest … --write stamps every published constant … each digest-covered` (amended) | fail-first at HEAD `af784cc` (the pre-AC-6b field pin, the `--write` field-set assertion); coverage sweep additionally proven by stamping a third field and excluding it from `coverageManifestDigest` — first failure: "--write stamped `extra_note`, so the digest must cover it" |
| `AC-5/AC-6b: validate-ledger tolerates a manifest predating each published constant …` (amended) | mutation: `row_example` excluded from `coverageManifestDigest` — caught, though the identity pin fires first, so the mixed-legacy row is proven by construction from `fingerprint` rather than by mutation |
| `AC-6b: the published row example is a row validateCoverageLedger accepts …` | mutation: dropped `p` from `LEDGER_ROW_EXAMPLE` — first failure: `rejected: files n/a predicate invalid: f-1`. This mutation also caught a defect in the test's own first draft (the fill supplied `p` unconditionally, so the example's key set was never exercised); fixed, then re-proven |
| `sprint-010 AC-9 (C-10): a Claude reasoning effort outside its vocabulary fails the render closed …` | fail-first against current behaviour → **D-1**. Its passing assertions are proven by the same run |
| `sprint-010 AC-9 (G-12): core.md "See also" is the rule-doc index …` | two mutations: (a) a rule doc dropped from "See also" — bijection assertion fires; (b) two rule docs re-listed in `t_AGENTS.md` — no-copy assertion fires |
| `sprint-010 AC-7/G-9: artifact-layout.md "Documentation economy" is the rule's sole home …` | two mutations: (a) rubric lead-in unbolded, so the entry stops being a derivable id — rubric assertion fires; (b) the three tests restated in `code-style.md` — sole-home sweep fires |
| `sprint-010 AC-1: the wave declaration is defined once …` | mutation: impl step 5 reverted to `from Task dependencies; topological sort` — first failure names the re-derivation. The legacy fallback clause is asserted positively, never by absence of the word `topological`, which survives on purpose |
| `sprint-010 AC-5b: the authorised-paths diff read is a condition of … the all-modes completion gate` | mutation: the bullet moved from step 9 into step 11 — gate assertion fires while the once-only count stays 1, so placement is what is proven |
| `AC-1/AC-2: … sole home of the staging prohibition` (amended) | mutation: `git stash` removed from the `custom-coding-rules.md` mirror — first failure names the un-mirrored command |
| `AC-13b/sprint-010 AC-3: … every agent-memory write whose author cannot commit it` (amended) | mutation: co-author clause removed from git-strategy.md's bookkeeping sentence |
| `AC-8/sprint-010 AC-4: … an availability skip reaches the friction log` (amended) | mutation: friction clause removed from external-review.md's skip bullet |
| `AC-6/sprint-010 AC-6a: … the bounded one-transcription enforcement branch` (amended) | mutation: `asd-phase-impl-review.md` step 7 reverted to a bare reject rule — first failure names that workflow as the acting site |

| **Entry 2** — `sprint-010 AC-10: the documentation economy rule carries both its authoring obligation and its review consequence in its own home …` | three mutations, each the test's first failure: (a) `artifact-layout.md` reverted to `b81fd7e^` — "the authoring obligation belongs in the rule's own home"; (b) `code-style.md` reverted to `b81fd7e^` — "naming one of 2 in the singular implies the other is review-only"; (c) the grant deleted from the `asd-ba` row — the per-role assert names `` `asd-ba` `` |
| **Entry 3** — `sprint-010 T-1/D-2: a manifest keying \`n_a\` by row type … is rejected by name instead of degrading to no authorized predicate` | fail-first vs D-2: `a8f4dcd`'s guard removed from `runtime.js` — first failure `D-2: keyed by id instead of by row type … Got: accepted: {"ok":true}`, which is the silent pass itself. The emitted-shape binding proven by a second mutation, the validator's lookup read flat (`manifest.n_a` for `manifest.n_a[label]`) — first failure is this test's acceptance assert, though eight other ledger tests redden with it, so that assert is a binding between emitted and read shape rather than the sole guard on the lookup |
| `sprint-010 AC-9 (C-10): … wherever the field is emitted` (amended: empty effort, live diagnostic) | two mutations: (a) the emission guard back to `if (c.effort)` — first failure `Missing expected exception: an empty effort must reach the validator and fail closed`; (b) the provider-special-cased throw pair restored — first failure is the Claude-path diagnostic assert, the message arriving with no agent, family, model or effort in it |
| `sprint-010 AC-5a/AC-5b: … the workflow declares the git operation that gate runs on, and code-style.md §17 keeps the fail-first restore obligation …` (amended, renamed) | three deletion mutations, each its test's first failure: the §17 restore sentence; the §17 `mutation left on disk is a defect` sentence; `asd-phase-impl.md`'s `- run command:` grant line |
| `sprint-010 AC-10: … grants that home to every role whose context is a fixed list` (amended: exemption count) | mutation: the `asd-tester` row reworded to adopt the exemption wording — the exemption-set comparison fires, where before that row simply dropped out of the sweep and the suite stayed green |
| `sprint-010 AC-7/G-9: … the rule's sole home …` (amended: three asserts on the corrected rule) | three mutations, each the test's first failure: (a) the economy paragraph reverted to `63e6b6d^`, OR-joining the three tests again; (b) the `Never cut` line reverted to `63e6b6d^`; (c) only the standalone-prohibition clause dropped, so the normative-class assert passes ahead of it and the prohibition assert is what fires |
| **Entry 4** — `T-2/T-4/sprint-010 TST-01: in every agent-memory directory a dispatchable agent can load, MEMORY.md and the files beside it are a bijection …` (amended, renamed: roster-derived set, no-index branch, fallout-set comparison) | four mutations, each restored in the call that read its failure. (a) `project_prompt-snapshot-is-base-branch.md` de-indexed from `asd-reviewer-correctness/MEMORY.md` — first failure names that file, "written but not indexed … writing it was a no-op"; (b) the same for `reference_codex-invocation.md` in `asd-external-review/MEMORY.md` — the two directories the old hardcoded array missed, now each proven red; (c) an unindexed `asd-ba/project_stray.md` created in a directory that exists for no agent today — fires the no-`MEMORY.md` branch, which is what proves the derivation reaches directories no list names; (d) a stray `asd-reviewer-corectness/` (misspelled) — the fallout-set comparison fires with `+ 'asd-reviewer-corectness'`, where a bare `roster.includes` filter would have dropped it green. `.claude/agent-memory/**` is outside `managed_paths`, so none of the four produced the `upstream_hashes` noise; each run showed exactly one FAIL |
| `sprint-010 AC-9 (C-10): … wherever the field is emitted` (amended: fixture guard) | fail-first re-derived at this HEAD against `7501224^` — first failure "Missing expected exception: the emitted `effort:` line is guarded by `claude.effort` alone …", the new guard passing ahead of it, so the record stands. The guard exists because the case is a literal `replace` over the shared canon: reformat the canon and it silently re-runs the model-present path and stays green. Its own first draft (`!includes('"model"')`) went red at HEAD on the Codex `"model": "sol"` line — caught by the run, corrected before this record |

| **Entry 4 (cont.)** — `sprint-010 iter-02: review-policy.md "Nitpick drop list" is the only enumeration inside the rule set every nitpick-instructed agent is granted …` | two mutations, each the test's first failure. (a) the four-item copy `72bad90` deleted re-added to `asd-reviewer-correctness.md`'s Do's — the sole-home comparison fires with the agent file joining the expected two-template list; (b) `` `review-policy.md`, `` removed from the `asd-reviewer-testing` row in `providers.md` — the per-agent grant assert fires by name. Both runs carried the expected ledger noise only (`upstream_hashes`; (a) also `canon_hashes` + `sync --check`, being a canonical agent file) |
| `sprint-010 iter-02: each latch non-restatement declaration denies only what its own site omits …` | four mutations, each the test's first failure. (a) `and additionally clears every APPROVE latch sprint-wide (below)` re-added to `review-policy.md`'s DoD line — the no-re-acquire assert fires; (b) `clear latches` removed from `asd-phase-impl-review.md`'s step-9 **Red, code defect** branch — the acting-site loop fires; (c) `, not restated here` re-added to `asd-phase-design-review.md`'s latch-filter bullet — the false-denial assert fires, naming that workflow; (d) `and the availability-skip carve-out` re-added to `asd-phase-impl-review.md` step 8 — the carve-out assert fires. (b)-(d) are literal reverts of `9e540b3`, so the test is red against the code this round replaced, not only against a synthetic edit |
| `sprint-010 iter-02: sprint-lifecycle.md's ADVICE_NEEDED step cites the asd-advisor.md section that actually carries the no-log rule …` | two mutations. (a) the citation reverted to `Do's` (i.e. to `2ae44c6^`) — first failure is the citation assert, naming the derived holder `Don'ts`; (b) the no-log bullet **moved** from Don'ts into Do's in `asd-advisor.md` — first failure is again the citation assert, now naming `Do's`, which is what proves the target is derived from the agent rather than hardcoded. Mutation (b) also found a defect in this test's own first draft: the uniqueness guard was a `deepStrictEqual` against `["Don'ts"]`, so it absorbed the moved-rule case and reported it under a message about the rule being stated in both sections. Split into a `length === 1` guard plus the citation assert, then re-proven |

| **Entry 5** — `sprint-010 iter-03: the clean-worktree precondition is a reciprocal pair …` (inherited from the cut-off dispatch, re-proven here) | four mutations, each the test's first failure inside its own test. (a) `— not restated here` re-added to the workflow precondition, a literal revert of `060318e` — the false-denial assert fires; (b) the workflow's `## Preconditions` heading renamed to `## Entry preconditions` — the section-count assert fires (`1 !== 2`), which is what proves the acting section is **derived from the home's own delegation** rather than hardcoded, and the same mutation independently reddens the citation sweep below, the class in miniature; (c) `` `git status --porcelain` `` replaced by prose — the command assert fires; (d) `` `FAILED` `` replaced by "stops the phase" — the signal assert fires. Every run's only other FAIL was the `upstream_hashes` ledger noise |
| `AC-15: review-policy.md is sole SSoT for the reviewer read-only reconciliation …` (amended: the unscoped sole-statement literal) | mutation: `review-policy.md` reverted to `Sole statement of this claim.` (i.e. to `060318e^`) — first failure in its own test is the new assert, at `tests/run.js:3184`, with `upstream_hashes` as the expected second FAIL |
| `sprint-010 iter-03: every section a canon file cites by name resolves in the file it names, no citation on a non-restatement declaration dangles at all, and the two pre-existing dangling pointers stay the exact known pair` (replaces the deleted denial-scoped form) | three mutations, each the test's first failure. (a) the `## Preconditions` rename above — the set comparison fires with the newly dangling citers added, so the sweep is proven against a **real one-sided rename**, not a synthetic marker; (b) `asd-reviewer-correctness.md`'s citation retargeted to the real heading `"Iteration semantics"` — the comparison fires in the **removal** direction (`- '.asd/agents/asd-reviewer-correctness.md -> …'`), which is how a dev fixing D-4 is told to delete its line here; (c) `asd-phase-impl-test.md`'s `"Impl-test phase"` (a declaration line) retargeted to a heading that does not exist — the **zero-tolerance denial assert** fires ahead of the set comparison, proving the two tiers are separable and that a denial-line regression is reported under its own message rather than absorbed into a set diff. Both guards were checked against an independent implementation before being set (185 citations, 41 declaration lines; floors 150 / 25) |
| `sprint-010 iter-02: each latch non-restatement declaration denies only what its own site omits …` (amended under EFF, item (b)) | two mutations, opposite directions. (a) `not restated here` → `not restated here or in either review workflow` in `review-policy.md`'s latch bullet, the exact pre-`9e540b3` literal — the re-keyed assert fires, so the fix loses none of the coverage the topic-phrase form had; (b) a legitimate sentence added to the same bullet naming the review workflows for something else — suite green but for the `upstream_hashes` noise, where the previous `!includes('review workflow')` form would have reddened. The finding was real and is closed |
| **Entry 6** — `sprint-010 iter-03: every section a canon file cites by name resolves in the file it names, with no dangling pointer left anywhere in canon` (renamed; two-tier comparison collapsed to one) | three mutations, each its test's first failure after the expected ledger noise, each restored in the call that read it. (a) `asd-phase-impl-test.md`'s `"Impl-test phase"` — a **denial** line — retargeted to a heading that does not exist: the surviving comparison fires naming the site (`+ '.asd/workflows/asd-phase-impl-test.md -> sprint-lifecycle.md "Impl-test phase mechanics"'`), which is what proves the removed tier carried no coverage away with it; (b) and (c) each half of `ac3073a` **literally reverted** — `asd-reviewer-correctness.md` back to `"Iteration-aware diff"`, `sprint-lifecycle.md` back to `"Re-running a phase"` — both fire in the `+` direction, so this round's fix is evidenced by red-on-revert rather than by a green suite over it. Ledger noise per file class as before: `upstream_hashes` on all three, plus `canon_hashes` and `sync --check` on the agent file |
| **Entry 7** — `AC-15: review-policy.md is sole SSoT for the reviewer read-only reconciliation …` (amended: the canon-bounding carve-out) | two mutations, opposite directions. (a) `review-policy.md` reverted **literally** to `b7f58c4^`, the form EXT-1 found false — first failure in its own test is the new assert at `tests/run.js:3187`, with `upstream_hashes` the only other FAIL; (b) the counter-proof that decided the assertion's form: the carve-out sentence reworded end to end **and** `in canon` deleted from the claim — suite green but for the ledger noise, where the qualifier-presence assert proposed by the dev would have reddened a correct edit |
| `AC-15/iter-05: providers.md names External Review as the sole Bash carve-out …` (amended: the memory carve-out and its grant) | two mutations, each its test's first failure after the expected noise. (a) `.asd/agents/asd-external-review.md` reverted literally to `5b04a80^` — the bullet-scoped carve-out assert fires, so the test is red against the exact text this round replaced, not only against a synthetic edit; (b) `"memory": "project"` deleted from that agent's canon frontmatter — the grant assert fires with `claude.memory` undefined. Both carried the agent-file noise class: `upstream_hashes`, `canon_hashes` and `sync --check` |
| the citation sweep, unamended — proof the round's two new citations are inside it | mutation: `## Agent memory` renamed to `## Memory for agents` in `artifact-layout.md` — the sweep fires listing seven newly dangling citers, `review-policy.md` twice (the new carve-out among them) and `.asd/agents/asd-external-review.md` once. A first attempt renamed it to `## Agent memory directories` and was a **no-op**: heading resolution is prefix-anchored on purpose, so that mutation proved nothing. Recorded rather than counted |

Every mutation was restored in the same tool call that read its failure; `git status` was clean of canon after each. Mutating any `managed_paths` file also reddens the `upstream_hashes` test — expected noise, not a second finding. Entry 2 restored from a scratchpad copy rather than `git checkout --`, so the bytes return exactly. Entry 3 did the same for all nine of its mutations, each backed up with `cp` and restored inside the tool call that read its failure; every mutated run showed exactly two `FAIL` lines, its own and the `upstream_hashes` noise, and `git status` was clean of canon after each. Entry 4's four mutations are all under `.claude/agent-memory/**`, which no `managed_paths` entry covers, so each run showed exactly one `FAIL`; the two that edited an existing index were `cp`-backed and restored in the same call, the two that added a directory were removed in it, and `git status --short .claude/agent-memory/` was empty after every one. Entry 4 (cont.) ran eight mutations (seven recorded above plus the discarded first form of the advisor one), every target a `managed_paths` file: each was `cp`-backed to the scratchpad and restored in the same tool call that read its failure, `git status --short` was checked after each and showed only this role's own two files, and each run's extra `FAIL` lines were the expected ledger noise — `upstream_hashes` always, plus `canon_hashes` and `sync --check` for the two canonical **agent** files, per that file class rather than per mutation. Entry 5 ran nine mutations across four canon files, every one `cp`-backed to the scratchpad and restored inside the tool call that read its failure, with `git status --short` inspected after each and showing only this role's own `tests/run.js`; the expected ledger noise appeared per file class as before (`upstream_hashes` on all nine, `canon_hashes` + `sync --check` on the `asd-reviewer-correctness.md` one). Nothing was inherited as proven: the cut-off dispatch's own records did not survive it, so both of its tests were re-derived against source and re-proven from scratch here, which is what surfaced the two flaws recorded under Removed tests. Entry 6 ran three mutations over two canon files and one workflow, each `cp`-backed to the scratchpad and restored inside the tool call that read its failure, with `git status --short` inspected after each and showing only this role's own `tests/run.js`; the mutation script refuses a missing anchor with exit 2 rather than writing a no-op, so a mutation that silently changed nothing cannot be recorded here as a proof. Entry 7 ran five mutations over three canon files, each `cp`-backed to the scratchpad and restored inside the tool call that read its failure, with `git status --short` inspected after each and showing only this role's own `tests/run.js`; two are literal reverts of this round's own commits, one is a deliberate counter-proof expected to stay green, and one was discarded on the spot as a no-op the anchor guard could not catch — the anchor matched and the bytes changed, but heading resolution is prefix-anchored, so the renamed heading still resolved. A mutation that changes bytes is not automatically a mutation that changes meaning; it was re-run in a form that does.

## Suite run

- Command: `node tests/run.js`
- Scope: full (safety valve, above — this repo's runner has no scoped mode either way)
- Result: pass — 184 passed / 0 failed / 0 skipped
- Lint / build: pass (`node .asd/sync.js --check` exit 0, `ok: true`, every item current — re-checked at this HEAD, since the entry's mutations touched canon and generated views; `git diff --cached --check` clean on the staged set)
- HEAD: `93f8a20` — the iteration-4 review-fix round's last commit, the tree entry 7 analysed and ran on; entry 7's own commit touches `tests/run.js` and this file only. Re-run green after every mutation was restored
- Earlier: `94d8779` — the entry-6 test-fix tree (184/184). The pinned dangling set is now **empty and asserted empty**: D-3 and D-4 are fixed in canon, so the sweep is green because canon resolves, not because two failures were exempted
- Earlier: `74a076d` — the entry-5 test commit (184/184), where the same two pointers were green only by being pinned
- Earlier: `095ccff` — the entry-4 (cont.) test commit (182/182). `dd47159` is the HEAD this continuation's delta was analysed through: `dd47159`'s own `tests/run.js` was restored over the current canon and run there, 179/179, establishing the before-state against which the three added tests are the only delta (restored byte-for-byte from a scratchpad copy in the same call)
- Earlier: `b10e053` — the entry-4 test commit (179/179); re-run green at `11bf405`, the entry's last code/memory commit, and at `9c5fa35`, the HEAD the entry-4 delta was analysed through. `fb1aeeb` — the entry-3 test commit; `3b080b1` is the HEAD the entry-3 delta was analysed through, green there too

Entry 1 ran at `af784cc`: 176 passed / 1 failed, that failure being D-1 below. Entry 2 ran at `86398b2`: 178/178. The suite has grown 171 → 182 across four entries and no test was removed: entry 3 added one test and eleven assertions to five existing ones; entry 4 added no test at all — it replaced one test's hardcoded directory array with a derivation and added two branches to it, so the count held at 179 while the covered set went from two directories to every directory a dispatchable agent can load; entry 4 (cont.) added three, all static, for a change surface that is entirely canon prose. Two of the three cover contracts that had **no** assertion at any site before this entry (the nitpick enumeration and its grant; the red-full-suite latch invalidation), which is why the count rose here where entry 4's did not — a green suite over an unpinned contract is not evidence about it, and that is the flaw in the blanket row this continuation supersedes.

Entry 7 held the count at 184 and added no test: the round's surface is four prose files, and both cuts that earned coverage were assertions on tests that already read those exact files. What changed is what is *pinned* — a declaration's carve-out and its pointer, and the first binding in this suite between a prose `memory: project` claim and the frontmatter grant behind it. The class this sprint keeps meeting is still not decidable in general (entry 5, row (a)); what is decidable, again, is the pointer — into a rule doc for the first assert, into config for the third.

Entry 5 took the count 182 → 184 and is the first entry to **remove** a test: two arrived from the cut-off dispatch, one was deleted for a narrower one that superseded it (Removed tests), and one assertion was re-keyed under an Efficiency finding. The delta that matters is not the count — the citation sweep replaced 20 pinned relations with 185, found the two dangling pointers that sprint 006's documentation review caught in their two sibling citations and missed here, and cost no new dependency, file or harness.

This file is itself agent-facing text under the rule this sprint landed — `artifact-layout.md` "Documentation economy" names `test-plan.md` in its reach. Every row above is written to change what its next reader does (`asd-reviewer-testing`, or this role on re-entry), and the entry-1/entry-2 rows are amended rather than rewritten for the same reason.

## Defects

| ID | Location | Symptom | Failing test | Status | Fix commit |
|---|---|---|---|---|---|
| D-1 | `.asd/sync.js:286` (`transformAgentClaude`) / `:168` (`resolveModelFamily`) | The Claude `effort` vocabulary check runs only inside `if (c.model)`, but the `effort:` line is emitted under `if (c.effort)`. Canon declaring `claude.effort` without `claude.model` renders `effort: bogus` into `.claude/agents/<name>.md` unvalidated — the silent-ignore failure C-10 asked to close, and an asymmetry with the Codex side, where model and effort are both mandatory and always validated. Latent today (all eleven agents declare both), so no shipped view is currently wrong | `sprint-010 AC-9 (C-10): a Claude reasoning effort outside its vocabulary fails the render closed wherever the field is emitted, symmetrically with the Codex check` | fixed | `7501224` |
| D-2 | `.asd/runtime.js` `validateCoverageLedger` | `allowedNa` reads `manifest.n_a[<row-type label>]`, so a manifest keying `n_a` any other way degrades silently: every id resolves to an empty predicate set, each truthful `n/a` row is rejected as unauthorized, and the malformed manifest itself passes because the unknown-id guard iterates an empty object. Realized this iteration on all four manifests (`friction-log.md` F-1), visible only as three reviewer returns nobody could validate | `sprint-010 T-1/D-2: a manifest keying \`n_a\` by row type - the shape a phase workflow emits - authorizes an n/a row of every row type, while any other keying is rejected by name instead of degrading to no authorized predicate` (entry 3) | fixed | `a8f4dcd` |
| D-3 | `.asd/rules/sprint-lifecycle.md:127` (`**Skip record**` bullet) | Cites `checkpoints.md` "Re-running a phase"; that heading was renamed to `## Re-run` in `8298f15` (PR #25) and the citation was never retargeted. The reader is sent to a section that does not exist — the exact failure the iteration-3 declaration fixes were about, surviving in a rule doc every phase reads. Pre-existing (citation added in PR #12), outside this sprint's change surface, reported not swept in | `sprint-010 iter-03: every section a canon file cites by name resolves in the file it names …` — pinned as a known entry, so the suite is green today and reddens the moment the pair changes in either direction | fixed | `ac3073a` |
| D-4 | `.asd/agents/asd-reviewer-correctness.md:44` (diff-payload input) | Cites `external-review.md` "Iteration-aware diff"; that heading became `## Iteration semantics` in the same PR #25 contract rewrite. Sprint 006's documentation review (iter-02 F5) found and fixed two citations of the old name — this third one was missed and still ships in a reviewer's own context, so the agent that follows it to learn its iter-2+ diff scope finds nothing. Pre-existing, outside this sprint's change surface | same test, same pinning | fixed | `ac3073a` |

**Fixing D-3 / D-4 is a two-file change.** Retarget the citation (or rename the heading back), **and** delete that entry's line from the pinned list in `tests/run.js` — the comparison is exact in both directions on purpose, so a fix without the deletion reddens the suite and says so by name. The alternative was to let the sweep fail red at this HEAD, which is the usual answer for a defect a new test finds; it was rejected here because both defects predate this sprint and a red suite blocks `impl-review` entry on work no round of this sprint touched. The local precedent is entry 4's fallout-set comparison over `asd-pm`'s dangling memory link: pre-existing, out of scope, pinned exactly and reported rather than silently tolerated. Nothing is hidden — the defect ids are in the assert message, at the moment of failure.

**Carried out at `ac3073a` / entry 6, and the sweep is stronger for it.** Both citations were retargeted and both pinned lines deleted, so the comparison now runs against an empty exemption set: every one of the 185 relations is green because it resolves, and nothing is tolerated by name. The escape hatch was never the value of this test — it was the one-time cost of finding two pre-existing defects with a sweep authored mid-sprint. What remains is the 185-relation assertion, its two floors, and a message that tells the next editor how to re-open the hatch correctly if a sweep ever finds another. Entry 6 added no test for this round and needed none; the honest report is that the round removed the exemption, the dead tier and the stale clause, and coverage is unchanged.

## Manual verification

None. No visual UI, third-party live integration or ux-feel surface in this change; every acceptance criterion resolved to an automated check or to a recorded `none` above.
