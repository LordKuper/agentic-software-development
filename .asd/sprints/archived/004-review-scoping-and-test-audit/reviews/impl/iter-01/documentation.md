[REVIEW-impl-documentation]: FAIL

# Review — documentation (impl-review, iteration 1, severity floor `low`)

- **Phase**: impl-review
- **Sprint**: `004-review-scoping-and-test-audit`
- **Iteration**: 1
- **Scope**: 58 files (diff `iter-01.diff`), self-hosting framework repo — no consumer `docs/`

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| 1 | high | `.asd/rules/core.md:18` | Stale roster in the hub rule doc every agent reads: `- **Reviewer agent** — evaluates artifacts (Quality, Implementation, Testing, UI, Simplification, Documentation, Performance, External Review).` Five of those agents were deleted this sprint. The adjacent creator line was updated (`PM, BA, UX, Architect, Dev, Tester`), the reviewer line was not. Missed by the AC-15 grep because it uses descriptive role words, not `asd-*` identifiers — the exact class Task 15 caught in `design-system.md`/`ux-principles.md`. Mirror of `review-policy.md`'s DoD table (audit row 1/8). | Replace with `(Correctness, Efficiency, Testing, Documentation, External Review)`. |
| 2 | high | `.asd/agents/asd-reviewer-correctness.md` "Section coverage ledger" + `asd-reviewer-efficiency.md` "Section coverage ledger" vs `.asd/rules/review-policy.md:97` + `.asd/templates/t_review.md:24-38` | A third mandatory ledger part (section coverage) was introduced with **no SSoT and two homes**: its contract is written out twice, once in each merged agent, while the declared owner of the ledger contract still says "Two parts, both required (template `t_review.md`)" and `t_review.md` still describes only "the complete file+rule coverage ledger" with no section rows in the reduced persisted form. Both review workflows' step-7/8 gates nonetheless *reject a review whose section ledger has a missing/blank row* — a blocking gate whose contract the gate's own SSoT contradicts. | State the section ledger once in `review-policy.md` "Coverage ledger" (three parts, third applies to reviewers declaring named rubric sections), add its rows + n/a handling to `t_review.md`, and reduce both agent files to a link. |
| 3 | high | `.asd/sync.js:1302-1305, 1338-1342, 1379-1386, 1425-1431, 1449-1453, 1477-1484`; `.asd/skills/asd-update/update.js:265-272, 380-384`; `tests/run.js:4006, 4102-4103, 4169-4170, 4183, 4220, 4141, 4043` | `code-style.md` §7 as rewritten this sprint bans **all** in-body comments (sole exception `// TODO(sprint-<NNN-slug>):`), yet the code landed in the same commit adds ~15 new in-body comment blocks inside `runCheck`, `runApply`, `main`, `applyPlan` and the new tests (e.g. `// Orphans aren't in the plan by construction…`, `// Marker gate: an orphan is deleted ONLY when…`, `// Migrations run AFTER managed-path replacement…`, `// poison require.cache with the OLD script`). Additionally `writeUpdatedManifest`'s member doc (`update.js:528-535`) describes implementation and rationale ("Recording it here, in the SAME atomic write…"), which §7's new member-doc bullet forbids. `.asd/migrations/4.0.0.js` is the one new file that is §7-clean. The rule's first release contradicts itself in its own commit. Applicability caveat: see finding #5. | Move each in-body comment's content into the enclosing function's doc comment or extract a named helper; trim member docs to purpose. If finding #5 resolves the other way, record that decision instead and narrow §7's stated audience. |
| 4 | medium | `.asd/sync.js:1450, 1480, 2717-2719 (orphan header)`; `.asd/skills/asd-update/update.js:2514-2517, 2558-2559`; `.asd/migrations/4.0.0.js:4-27, 176-179`; `tests/run.js:3967, 4059, 4077, 4149` | `code-style.md` §8 (unchanged, pre-existing): "Code (comments, doc strings, string literals) must not reference project documents (rules, ADR, PRD, UX spec, decisions log, sprint files)." The new code references `plan.md Task 12`, `audit.md finding`, `decisions-log 2026-09-03/2026-09-04`, `SKILL.md`, `sprint 004-review-scoping-and-test-audit`, and AC-N ids — including inside a **test name string literal** (`'sync.js runApply (fail-open fix, decisions-log 2026-09-04): …'`), which ships to consumers and rots the moment this sprint archives. | Replace document references with the behavioural reason ("a deleted canonical source leaves its generated view behind; delete only marker-owned files"). Same applicability caveat as #3. |
| 5 | medium | `AGENTS.md:50` vs `.asd/rules/code-style.md:3`, `.asd/agents/asd-dev.md` (Mandatory rules), `.asd/rules/sprint-lifecycle.md` "Self-hosting", `.asd/project/custom-coding-rules.md:12-15` | One fact ("does `code-style.md` govern this repo's own Node code?") has two homes with opposite answers. `AGENTS.md` says it "governs code written *by consumer dev agents* — not this repo"; but `asd-dev.md` lists `code-style.md` as mandatory during `impl`, sprint-lifecycle's self-hosting allowlist puts `.asd/sync.js`/`tests/**` in that dev's write scope, and this repo's own `custom-coding-rules.md` explicitly regulates `.asd/sync.js`/`update.js`. Latent before; load-bearing now that AC-8 gave the documentation reviewer a §7 enforcement rubric item that fires on exactly this code. Escalation candidate — resolving it either way changes a declared framework policy and (one way) implies a comment cleanup `sprint.md` puts out of scope. | Decide and state once: either drop the `AGENTS.md` exemption and treat framework Node code as code-style-bound (then fix #3/#4), or scope §7/§8 explicitly to consumer product code and say so in `code-style.md`'s audience line, with `asd-dev.md`/`custom-coding-rules.md` following. |
| 6 | medium | `README.md:206-207` and `README.md:254` | README fan-out prose drifted from the mechanism it mirrors (hard-rule mirror per `AGENTS.md`). (a) Reviewer table says "UI section **dispatch conditional** (see below)" / "perf sections **dispatch conditional** (see below)" while the paragraph directly below correctly states both agents are *always dispatched* and only a rubric **section** is n/a'd. (b) The config-schema sample still reads `scoped_fan_out: enabled # … impl-review UI/Performance dispatch conditional on diff-derived predicates (disabled = unconditional fan-out, every reviewer every iteration)` — names two deleted reviewers and describes the removed agent-level skip, contradicting `t_config.yaml`'s updated comment and `asd-phase-impl-review.md` step 5 (the declared SSoT). | (a) "UI conformance section n/a-able"/"perf sections n/a-able". (b) Restate line 254 in section terms without naming retired reviewers. |
| 7 | medium | `.asd/rules/sprint-lifecycle.md:63` | The new impacted-set definition's component 3 cites "tests tagged with an AC-N the change touches (the existing AC-citation convention, `code-style.md` §17)". §17 contains no AC-citation/tagging convention — its only AC line is "Every acceptance criterion is covered by a check at some level". The convention actually lives in `t_test-plan.md:42` ("Level and AC/risk covered are visible in the test file itself (name, path, comment)"). Worse, that convention is unimplementable under `code-style.md` §8, which forbids in-code references to PRD/sprint documents — so a component of the SSoT impacted-set definition contradicts another rule in the same file. | Re-point the citation at `t_test-plan.md`, and reconcile §8 with AC-tagging (e.g. explicitly permit an AC-N tag in a test name/path as the one allowed reference), or drop component 3. |
| 8 | medium | `.asd/rules/sprint-lifecycle.md` "State recovery" (last paragraph) vs `.asd/hooks/session-start.js:102-117` | The rule newly names `.asd/hooks/session-start.js`'s `lastReviewVerdict` as a consumer that "treats an absent key for a required reviewer as blocking UNLESS `reviews.impl.latched` carries that key". The hook was not touched this sprint and has no `latched` awareness: an iteration where every reviewer is latched writes an empty `verdicts["iter-NN"]`, so `verdicts.length > 0` fails and the hook reports `mixed` instead of `green`. Its `isSkipped` branch is also now dead per the same paragraph ("this map never carries an agent-level `skipped:` value"). Valid under the change-surface rule's exception — the new rule text made the unchanged hook incorrect. | Either teach `lastReviewVerdict` the `latched` map (and keep `skipped:` only as back-compat), or remove `session-start.js` from the rule's named-consumer list. |
| 9 | medium | `.asd/rules/review-policy.md:137` + `.asd/workflows/asd-phase-design-review.md` step 7 payload bullet vs `.asd/agents/asd-reviewer-efficiency.md` "Review rubric" | The design-review allowed-section list — the mechanism the merged agents' per-phase gate is driven by — names sections that do not exist and omits one that does: it says "Efficiency (over-engineering + structure/cohesion + **design-principles** sections)", but the agent's named rubric sections are `Over-engineering checklist`, `Structure / cohesion checklist`, `Complexity-vs-value tradeoff` (all marked `[design-review, impl-review]`) plus the five perf sections. "design-principles" is not a section, and `Complexity-vs-value tradeoff` appears on no allowed list, so its ledger row has no deterministic resolution in design-review. | Make the allowed-section list quote the agents' section headings verbatim: `Over-engineering checklist + Structure / cohesion checklist + Complexity-vs-value tradeoff`. |
| 10 | medium | `.asd/release-manifest.json` `canon_hashes` | AC-14 requires "the new `.asd/migrations/` tree in `release-manifest.json` `managed_paths` **and** `canon_hashes`", and `AGENTS.md` "Hard rules" states "a new canonical source needs a `canon_hashes` entry". `managed_paths` and `upstream_hashes` carry `.asd/migrations`/`4.0.0.js`; `canon_hashes` does not (it holds only `agents/` + `skills/`, since `computeCanonHashes` walks render sources). Either the AC is unmet or the hard rule's wording is wrong — as it stands the manifest does not match the stated contract. | Add the entry (and its computation) or amend `AGENTS.md`'s hard rule + record the AC-14 deviation in `decisions-log.md`. |
| 11 | medium | `.asd/rules/sprint-lifecycle.md:44,56`, `.asd/rules/review-policy.md:32,38,142,144`, `.asd/templates/t_commands.yaml:9`, `.asd/skills/asd-init/SKILL.md` step 8, `asd-phase-{impl,impl-review,design-review,pr}.md`, `.asd/migrations/4.0.0.js` header | Sprint-scoped identifiers baked into permanent, consumer-shipped prose: section headings `## APPROVE latch (AC-2)`, `## Impacted test set (AC-5)`, `## Change-surface rule (AC-3)`, plus inline "(AC-5)"/"(AC-2)" refs. `AC-N` is sprint-local (`sprint.md` is the acceptance-criteria source, per `review-policy.md`), so in a consumer project `AC-5` denotes *their* fifth criterion; here it dangles into `.asd/sprints/archived/004-…`. No rule doc carried this pattern before this sprint. | Drop the `(AC-N)` labels from rule headings/prose (and templates/skills); keep AC traceability in `sprint.md`/`plan.md`/`decisions-log.md`, which own it. |
| 12 | low | `.asd/rules/sprint-lifecycle.md:58` vs `:69-71` | The impacted-set section's opening sentence — "Every test run inside `impl`, `impl-test`, and `impl-review` is scoped to the impacted set" — is contradicted eleven lines later by "**Where impacted-only applies**: `impl` …; `impl-test`'s suite gate" and "**Where the full suite still runs**: … at the end of `impl-review`". A single SSoT paragraph stating both. | Reword the opener: "Every scoped test run in `impl` and `impl-test` uses the impacted set; `impl-review`'s one terminal run is deliberately unscoped (below)." |
| 13 | low | `AGENTS.md` "No build / test / lint" | Still says `tests/run.js` "only covers `.asd/sync.js`/`.asd/skills/asd-update/update.js` (the sync engine)" and lists green-test triggers as edits to those two files. It now also covers `.asd/migrations/4.0.0.js` (three dedicated test groups). | Add `.asd/migrations/**` to both sentences. |
| 14 | low | `.asd/workflows/asd-phase-plan.md:37` | Vestigial instruction after the dev merge: "assign owner role per Task in body (`dev`)". `asd-phase-impl.md` step 3 no longer parses an owner in any mode, so the field is written and never read — the kind of stale-but-harmless instruction that invites re-introducing owner routing. | Drop the bullet, or state explicitly that a single `dev` owner is implicit and needs no per-task marking. |
| 15 | low | `.asd/workflows/asd-phase-design-review.md` step 7, correctness bullet | "…the reviewer still returns one verdict covering **Documentation/Efficiency-adjacent n/a bookkeeping** only" names two other reviewers' domains to describe Correctness's own empty scope; the sentence is not parseable as written. | Rewrite: "with no allowed section in scope, the reviewer's verdict is APPROVE over an all-`n/a` section ledger." |
| 16 | low | `.asd/rules/code-style.md:108` (§17 hypothetical-risk bullet) vs `.asd/agents/asd-tester.md` "Test selection rubric" vs `.asd/workflows/asd-phase-impl-test.md` step 4 | The authoring bar is stated three times in near-identical prose ("author a test only when it covers a real, material risk on the change surface — not when hypothetical, already covered, or only a coverage number"), each closing with "full criterion at `code-style.md` §17 (SSoT)". Against the sprint's own "no fact stated twice" DoD; two of the three copies will drift. | Keep the operative wording only in §17; reduce the agent and workflow to the link plus the recorded-`none`-decision requirement (which is genuinely their own). |

## Coverage

File coverage: 58/58 scoped files checked, 0 omitted. Rule coverage: all rubric items resolved.

`n/a` rows:

| Rubric item | Reason |
|---|---|
| HTML shell wrapping / placeholders | no HTML artifact in the scope file list |
| Provenance flag correctness | no provenance-bearing artifact in scope (no `docs/`, no HTML) |
| Persistent-doc actuality vs implementation | no `docs/` tree (`self_hosting: enabled`, lean `documents.*` profile) — framework-mode substitute applied instead |

## Verdict

FAIL: 16 findings — 3 high, 8 medium, 5 low. One finding (#5) requires user escalation.

## Next action

Route to `impl` review-fix mode for findings #1-#4 and #6-#16 (all mechanical prose/manifest/comment fixes, no new abstraction). Finding #5 needs a decision before #3/#4 can be resolved coherently — its outcome determines whether the §7/§8 fixes are mandatory or whether the rules' audience line is narrowed instead.

## Escalations

- **finding #5** — requires user approval (concept/scope): "does `code-style.md` govern this repo's own Node code?" `AGENTS.md:50` says no; `asd-dev.md`, `sprint-lifecycle.md` "Self-hosting" and `.asd/project/custom-coding-rules.md` imply yes. Option A: code-style binds framework code → strip the new in-body comments and document references (#3, #4), accepting a bounded cleanup limited to code this sprint wrote (existing code stays out of scope per `sprint.md`). Option B: §7/§8 scope to consumer product code only → amend `code-style.md`'s audience line and `asd-reviewer-documentation.md`'s §7 rubric item to say so, and #3/#4 close as not-applicable.
