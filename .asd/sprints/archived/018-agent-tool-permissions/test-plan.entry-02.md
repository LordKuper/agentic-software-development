# Test plan — entry 2 (rotated segment)

Rotated from `test-plan.md` at entry 3's strategy pass (`artifact-layout.md` "Test plan" rotation). Includes the review-fix wave-1/iter-01 rows recorded while entry 2 was live. A cross-span reader reads this segment in ordinal order, then the live file.

## Risk → check decisions

Entry 2 (delta since entry 1: `git diff 6c9d8d7...HEAD`). Impacted set: the **full suite**. The safety valve still fires because the delta touches `providers.md` (every agent's host mapping) and `release-manifest.json`.

| Change | Material risk | Chosen check | Decision | Reason |
|---|---|---|---|---|
| `providers.md` "Declared tool policy" D-1 fix (0533ef8) | the reviewer carve-out is dropped or reworded out of the refusal line, and a reviewer goes back to returning a bare `QUESTION` | static | none | The entry-1 carriers test already pins it (rotated `test-plan.entry-01.md`). Re-proven on the fix itself: the line reverted to `0533ef8~1` → exit 1, `sprint-018 AC-4/AC-5/AC-7: a reviewer's question and External Review's stalemate …` fails first on "AC-4/AC-5: the out-of-policy refusal must route a reviewer through its question carrier". A new test would duplicate that assertion |
| `asd-phase-audit.md` step 3c D-2 fix (4df9bf4) | the audit workflow loses its `QUESTION` branch, or the branch goes back to relay-halt | static | none | Pinned twice by the entry-1 orchestrator-only test: the dispatcher-citation assert, and the `QUESTION … →` clause sweep, which now reaches step 3c's arrow clause. Re-proven on the fix itself: the file reverted to `4df9bf4~1` → exit 1, `sprint-018 AC-4/AC-5/AC-7: only the main orchestrator prompts the user …` fails on "AC-7: every workflow dispatching a role that can return QUESTION must cite the QUESTION protocol" |
| `release-manifest.json` hash refresh | stale ledger after the two canon edits | static | keep | The existing `upstream_hashes` and `canon_hashes` tests cover it; they pass at dd8f6ba |
| review-fix wave-1/iter-01 `testing.md TST-1-1`: `providers.md` "Declared tool policy" carve-out | the entry-2 `none` row above over-claimed. The pin checked only the citation, so a reword like `(see review-policy.md …)` stayed green while the line again sent reviewers to a bare `QUESTION` | static | add (supersedes the entry-2 D-1 row's claim) | The carriers test now requires one clause (split on `.`/`;`/` — `) that names the reviewer **and** cites `review-policy.md` "Gate Verdict Format". This checks the relation, not a phrase: rewording R3 (the carve-out rewritten as its own `;` clause) stays green |
| review-fix wave-1/iter-01 `testing.md TST-1-2`: BA/UX `Run command:` allowlist half (AC-8) | a line widened to "any command; never …" still passed, because only the never-half was pinned; UX's old `designmd-*` glob reached `designmd-install`, which writes `package.json`/lockfile (dev fix 009f2e8) | static | add (sprint-015 test, BA/UX loop) | Each line needs ≥1 backticked command in an `only` clause that is not a `never` clause. That allowlist must not cover (literally or by a `*` glob) any command its own body says `Never run` — two derived sites, not a pinned command list |
| review-fix wave-1/iter-01 correctness 2 / dev 0ae55c7: External Review stalemate options renamed to stop / continue fixing / abort, defined once in `external-review.md` "Stalemate detection"; new `## Stalemate` section in `t_review-report.md` | the old pin (`accept as-is`/`override`/`abort` in each workflow) was red by intent; the emitters (agent trigger, report template) drift from the home | static | keep (rewritten in place, carriers test) | The options are derived from the home's bold bullets (exactly 3, each with an effect). The agent trigger and the template's `Stalemate:` line must list exactly those options. The template section and both workflows' stalemate lines cite the home, and the workflows no longer restate option names |

## Removed tests

| Test | Reason | In change scope |
|---|---|---|

## Added tests

Entry 2 added none: both fixes are guarded by assertions added in entry 1, and each fix was re-proven by reverting the fix commit (`node <scratchpad>/mutate.js F1|F2`, a `git show <sha>~1:<file>` revert restored in `finally` with a byte compare; the worktree was clean of canon edits afterwards). The rows below are review-fix wave-1/iter-01, proved the same way (`MUT=./mut2.js node <scratchpad>/mutate.js`).

| Test | Regression proof |
|---|---|
| tests/run.js:`sprint-018 AC-4/AC-5/AC-7: a reviewer's question and External Review's stalemate …` (TST-1-1 clause assert) | T1a: carve-out reworded to `(see review-policy.md "Gate Verdict Format")` → exit 1, "AC-4/AC-5 (TST-1-1): one clause must name the reviewer and route it to its carrier home"; F1 re-recorded: `providers.md` reverted to `0533ef8~1` → exit 1, same test, same message. Rewording R3 → only the `upstream_hashes` noise failed; this test stayed green |
| tests/run.js:`sprint-015 AC-1/AC-6/AC-7/AC-8/AC-10/AC-12: …` (TST-1-2 allowlist asserts) | T2a: BA line widened to `Run command: any command; never …` → exit 1, "sprint-018 AC-8 (TST-1-2): asd-ba's Run command line must name the commands it may run under an "only" scope"; T2b: UX allowlist restored to `designmd-*` (second loop member) → exit 1, "sprint-018 AC-8: asd-ux's Run command allowlist must not reach a command its own body says never to run" |
| tests/run.js:`sprint-018 AC-4/AC-5/AC-7: a reviewer's question and External Review's stalemate …` (stalemate options re-pinned to the home) | S1: `abort` bullet removed from the home → exit 1, "AC-4: "Stalemate detection" is the one home of the stalemate options, each with its effect - got ["stop","continue fixing"]"; S2: template options set back to accept as-is/override/abort → "AC-4: the Stalemate block the report template renders must offer exactly the options external-review.md defines"; S3: same drift in the agent trigger → "AC-4: External Review's stalemate trigger must offer exactly the options"; S6: template citation dropped → "AC-4: the report template's Stalemate section must point at the options' home"; S4/S5: citation dropped from the design-review / impl-review stalemate line → "AC-4: .asd/workflows/asd-phase-design-review.md must recognise External Review's Stalemate block, ask the user and take the options from their home" (and the impl-review message for S5). All exit 1 |
