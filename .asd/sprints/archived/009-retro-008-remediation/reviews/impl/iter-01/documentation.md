[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: 1
- **Severity floor**: low (all severities reported)

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| F-1 | high | `.asd/agents/asd-external-review.md:112-117` ("Signals emitted") vs `:105`, `.asd/rules/external-review.md:42-51` | AC-8 is delivered in the Don'ts and in "Outcome contract" (exactly two permitted outcomes; any inability to complete returns the availability skip), but the same agent file still advertises a third and fourth outcome: `FAILED — {{wraps_cli}} unrecoverable error` and `ABORT — precondition not met: <artefact>`. "Unrecoverable error" is precisely the case `:105` routes to the skip, so the file instructs both behaviours for one condition — the `F-8` class this AC exists to close. | Remove `FAILED` from Signals emitted (or restate it as "never — an unrecoverable wrapped-CLI error returns the availability skip"), and either drop `ABORT` or scope it explicitly to a pre-invocation precondition the Outcome contract exempts. |
| F-2 | high | `.claude/agent-memory/asd-dev-critical/MEMORY.md:3` and `project_crlf-canon-edits.md:8`, `:15` | AC-7's `.gitattributes` (`* text=auto eol=lf`) deliberately falsifies this memory's durable premise "`.asd/**` markdown is checked out CRLF". Its "How to apply" line `git diff --check` must be clean before committing now contradicts `code-style.md` §19's measured blind spot. The sibling memory updated this sprint already knows about `.gitattributes`, so the two files disagree with each other. This file loads on every `asd-dev-critical` dispatch — exactly the "false durable line paid per dispatch" cost sprint 008 paid twice. | Update `project_crlf-canon-edits.md` and the `MEMORY.md` hook line: worktree is LF after the next checkout, `\r?\n` anchoring survives only as a legacy-worktree caveat, staged lint form is `git diff --cached --check`. |
| F-3 | high | `tests/run.js:2446-2448`, `3046-3047`, `3228`, `3265-3266`, `3352-3353` | Five comments added inside function (test-callback) bodies. `code-style.md` §7 forbids in-body comments unconditionally, and `AGENTS.md` states code-style governs `tests/run.js` with "no exemption for framework code"; `custom-coding-rules.md` grants no carve-out. Each duplicates reasoning its own `assert` message already carries. The file carries ~60 pre-existing instances — the change-surface rule scopes this finding to the five new ones only. | Delete the five comments (their content already lives in the adjacent assertion messages), or — if the pattern is an intended convention — escalate for an explicit `custom-coding-rules.md` carve-out rather than leaving §7 contradicted by canon. |
| F-4 | medium | `.asd/rules/sprint-lifecycle.md:111` vs `.gitattributes`, `.asd/agents/asd-dev.md:65-66` | The self-hosting Dev write list is an exhaustive allowlist and `asd-dev.md:66` + `asd-phase-impl.md:49` both defer to it as THE allowlist. Root `.gitattributes` — written by Task 10 this sprint — is not in it, so the rule is false at HEAD and the next dev touching that file has no stated authority. | Add `.gitattributes` beside `.gitignore` in the allowlist. No other mirror changes: it is deliberately absent from `managed_paths`, templates and the README consumer folder map, per AC-7 scope. |
| F-5 | medium | `.asd/rules/artifact-layout.md:43-44`, `:68` vs `.asd/rules/review-policy.md:150` | AC-14 introduces `reviews/<phase>/iter-NN/<reviewer>.late.md`. The path map enumerates only `<reviewer>.md, <reviewer>.part-N.md`, and its purity contract states nothing else is written under a sprint folder. A workflow correctly following AC-14 violates the layout rule, and a future Documentation reviewer will correctly flag the file as stray. `tests/run.js:3081-3082` asserts the map names `.part-N.md`; the same coupling now needs `.late.md`. | Add `<reviewer>.late.md` to both `reviews/design/iter-NN/` and `reviews/impl/iter-NN/` lines of the path map, and extend the existing test assertion the same way. |
| F-6 | medium | `.asd/rules/review-policy.md:150` vs `.asd/rules/sprint-lifecycle.md:58-72` | AC-14 adds a third latch-clearing route, but `review-policy.md:175` itself declares `sprint-lifecycle.md` "APPROVE latch" the sole home of latch persistence, and that section enumerates exactly two clearing routes, each explicitly reconciled against the other ("no second mechanism for that route", "This is a DISTINCT clearing route"). An orchestrator reading the declared sole home never learns route three. Same two-sided-obligation shape the sprint's own goal targets. | Add the late-duplicate-return route to `sprint-lifecycle.md` "APPROVE latch" as a one-line third clearing route citing `review-policy.md` "Late duplicate return" (keep the mechanics in review-policy). |
| F-7 | medium | `.asd/rules/review-policy.md:42` vs `.asd/rules/git-strategy.md:39`, both review workflows | AC-13b states the obligation but no site that must perform it carries it: `git-strategy.md` "Commit before review" — edited this sprint for AC-1, and the home of commit ownership — enumerates the orchestrator's bookkeeping with no memory class and no pointer to the carve-out, and neither review workflow's review-file write step mentions committing memory. The obligation is stated where the reviewer reads, never where the committer acts — the failure mode AC-13b was written to remove. | Add the agent-memory file class (with its `review-policy.md` "Change-surface rule" citation) to `git-strategy.md`'s orchestrator bookkeeping sentence, and name the commit in both review workflows' review-file write step. |
| F-8 | low | `.asd/rules/checkpoints.md:30` vs `.asd/templates/t_review.md:21-23` | The *iterations charged* unit counts iterations "whose findings name that `AC-N`", but the finding row format has no AC field and only the Correctness reviewer is required to trace AC-N at all. Cost incurred through findings that never spell the id reads as `0`, which the rule then instructs the actor to state explicitly — a systematically low-biased number presented as evidence at a hard gate. | State the derivation honestly ("…findings need not cite an AC, so this is a lower bound"), or add an optional `AC` column to `t_review.md`'s finding table. |
| F-9 | low | `.asd/rules/review-policy.md:101` vs `.asd/runtime.js:201-206` | "The manifest contains its SHA-256 digest … calculated over the manifest excluding `digest`" is now incomplete: `coverageManifestDigest` injects the canonical `vocabulary` before hashing when absent, so for a pre-sprint manifest the digest is not a hash of the manifest's own content. Anyone re-deriving a legacy digest from this prose computes a different value; the accurate statement exists only in the JSDoc. | Extend the sentence: "…and with `vocabulary` defaulted to the validator's constant when absent, so stamping an older manifest never moves its identity." |

## Verified rather than accepted (AC-16 asked for the conclusion to be checked)

- **`README.md` — no change genuinely required.** Phase list, agent roster and both model-tier columns untouched by the diff (both edited agents' frontmatter models/efforts unchanged; only tool-policy prose and Don'ts moved). Config schema gained no key. The folder map is the consumer-project layout, so a repo-only `.gitattributes` correctly does not appear. `README.md:326`, `:353` already carry the agent-memory mirrors and cite `artifact-layout.md` "Agent memory", still the correct home after the collapse. Command list unchanged.
- **`core.md` "See also" — complete.** All twelve non-`core` rule docs listed; none added or removed.
- **Eleven-phase chain and template variables — untouched.** `t_plan.md`'s new `Reachability:` placeholder is the artifact-template namespace `core.md` explicitly separates.
- **`.asd/release-manifest.json`** — both edited agents' `canon_hashes` present and byte-identical to their `upstream_hashes` counterparts; `.asd/runtime.js` has its entry; `.gitattributes` correctly absent from `managed_paths`. `asd_version` stays `7.0.0`, correct until the `pr` bump.
- **AC-5 vocabulary chain is genuinely single-homed**: `runtime.js` constant → stamped by `manifest-digest --write` → enforced in the validator; this review's own manifest carries exactly that constant. The required/optional split in prose matches the code.
- **AC-15 literal coupling holds**: `checkpoints.md`'s counted string is byte-identical to the entry `asd-phase-impl.md:112` emits.
- **AC-3 two-sided**: `sprint-lifecycle.md:7` states the obligation, `asd-phase-scope.md:6` implements and cites it, `sprint.md` gains no section.
- **Agent-memory single-home holds**: `artifact-layout.md:91` is the only statement; `sprint-lifecycle.md:117`, `external-review.md:71`, `t_prompt-external-impl.md:20` and `custom-coding-rules.md:15` cite without restating. No fifth copy in canon.
- **Memory durable claims verified against source**: `project_sync-apply-ledger-gotcha.md` checks out (ledger recompute on `--apply`, bare `--apply` exits 1, exported helpers, real output shape). `project_parallel-agent-commit-sweep.md` is consistent with `git-strategy.md:39` as edited. `project_testability-envelope.md`'s claims are true at HEAD.

## Coverage

Validated compact ledger: [`documentation.ledger.json`](./documentation.ledger.json) against [`documentation.manifest.json`](./documentation.manifest.json), findings [`documentation.findings.json`](./documentation.findings.json). `validate-ledger` → `{"ok":true}`. 21/21 files, 10/10 rules, 9/9 sections resolved; `provenance`, `traceability`, `persistent-actuality` and `html-shell-wrapping` n/a on manifest-authorized predicates.

## Verdict

CONCERNS: 9 (3 high, 4 medium, 2 low)

## Next action

Route to `impl` review-fix mode. All nine are autofixable without escalation. F-1, F-2 and F-3 leave a live contradiction inside a file an agent loads on every dispatch — fix those first. Per "Verify before applying", re-read each cited path; F-3 is scoped to the five new comments only.

## Escalations

None required. Conditional only: if `tests/run.js`'s in-body comment style is judged an intended project convention rather than a §7 violation (F-3), that resolution is a `custom-coding-rules.md` carve-out and needs a user decision; the default fix does not.
