[REVIEW-impl-testing]: FAIL

# Review — testing

- **Phase**: impl-review
- **Iteration**: 1
- **Severity floor**: low

Method note: the dispatched diff payload in the session scratchpad disappeared mid-review; findings were derived from the identical persisted copy at [`scope.diff`](scope.diff) plus direct file reads (this reviewer has no shell). Suite arithmetic corroborated statically: `^test\(` in `tests/run.js` = 118, matching the reported 118/118; `111 + 7 new tests = 118` confirms the earlier `103/111` run predates every test added this sprint.

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| F1 | high | `tests/run.js:1066` (vs its own comment `:1056-1065`, title `:1031`); `test-plan.md` "Removed tests" row; AC-17 | The pre-existing drift gate was narrowed from `items.filter(i => i.status !== 'current')` to `items.filter(i => i.target !== 'AGENTS.md' && i.status !== 'current')`. The test's own comment states the previous allowlist "would have silently accepted permanent drift"; the title still claims "including AGENTS.md" / "no exemption". The exemption is load-bearing, not cosmetic: `AGENTS.md`'s managed block was edited this sprint while `.asd/sync-state.json:5-8` was not, and `runApply` never re-baselines a self-sourced block (`.asd/sync.js:1491-1498`), so `--check` currently reports `modified-foreign`. The reported green 118/118 therefore masks a real sync-drift failure, and AC-17's "applicable … sync … tests pass" is satisfied only by the exclusion. The change is also unrecorded — `test-plan.md` "Removed tests" says "None", but narrowing an existing regression assertion is a partial removal needing a recorded, valid reason (T-2). | Restore the unconditional assertion and fix the underlying drift (re-baseline `.asd/sync-state.json`'s `AGENTS.md` digest in this change, or add a self-sourced re-baseline path to `sync.js --apply`). If drift is instead to be accepted, that is debt acceptance — record it in `test-plan.md` and escalate for user approval; do not keep a filter that contradicts the test's documented purpose. |
| F2 | medium | `.asd/hooks/session-start.js:157`; `test-plan.md` "Gate/session prose and workflows \| keep" row; AC-21 | The sprint's only new executable hook branch (`phase === 'pr' ? (state.pr?.state === 'closure-pending' ? 'await-user-closure' : 'await-merge') : nextPhase(phase)`) implements AC-21's mandatory closure gate and has no check, while the suite already runs the hook end-to-end over `state.json` fixtures (`tests/run.js:2046`, `:2058`, `:2116`). The `keep` decision's stated reason ("existing runner and sync/hash checks cover generated views and hooks") is inaccurate: sync/hash checks prove only that generated bytes match canon, never that a branch emits the right next-step text — an undeclared no-test decision, not an honest `none` (T-3). Same gap applies to the other high-risk prose contracts folded into that single row (AC-7 PM-responsibility transfer, AC-13 `audit: auto`, AC-18 `user_gates` fail-closed), for which the repo already has a contract-string precedent at `tests/run.js:2074`. | Add two fixture-driven hook cases (`closure-pending` → `await-user-closure`; `pr` without it → `await-merge`). Split the blanket `keep` row: state per-AC what is genuinely untestable prose and add cheap contract-string guards for the rest (e.g. "no canonical rule/workflow/agent file references `asd-pm`" — currently true, but unguarded). |
| F3 | medium | `test-plan.md` Defects row D-2; `tests/run.js:2222`, `:2243` | D-2 records "production fix supplied before an automated fail-first run" — three fixed defects (shell-sensitive Windows fallback, caller-controlled auth probe, unbounded retry) with neither a recorded fail-first run nor an equivalent targeted mutation, which `code-style.md` §17 requires for every fixed defect (T-4). D-1's fail-first is also uncorroborated by the recorded run stamps (the `103/111` run predates all 7 new tests), though it is at least claimed observed. | For each D-2 sub-defect, record an equivalent targeted mutation in `test-plan.md` (e.g. revert `runLocal`'s `.cmd` branch / drop the `retryAfter > now + MAX_NEGATIVE_TTL_MS` bound / drop the `authArgs` rejection, noting which test fails and its stamp). Stamp D-1's failing run (HEAD + test name) so the claim is verifiable. |
| F4 | medium | `tests/run.js:2222-2241` (AC-5), `tests/run.js:2261` (AC-10) | Covered code paths have unasserted AC clauses: (a) AC-5's "recovery when command, selected model, or authentication state changes" — `cacheKey` (`.asd/runtime.js:84-86`) mixes command/model/auth-generation, but the test proves only *expiry*-based recovery, so a regression pinning the key would let a negative cache suppress a recovered auth or a switched model; (b) AC-5's "secrets and sensitive command output are never persisted or logged" — the test reads the cache file at `:2240` but asserts nothing about its shape; (c) AC-10's "`haiku` with no unsupported effort override" is implemented by `delete claude.effort` (`.asd/sync.js:2027` in diff) but the variant test asserts only `model: haiku` and tools, never the absence of `effort:` (T-7). | Add: same fingerprint under a different `model`/`command`/`credentialPath` mtime → `local-ready`, not `negative-cache`; assert the persisted entry's key set is exactly `{status, retry_after}`; assert the rendered mechanical variant contains no `effort:` line. |
| F5 | medium | `tests/run.js:2193-2211`; `.asd/runtime.js:201`; AC-2 | The test's title claims "finding-reference fraud" coverage, but `actualFindings` is `['F-1']` in every case, so the ledger↔report finding-set equality check (`.asd/runtime.js:201` — duplicate ids, ledger findings absent from the report, report findings missing from the ledger) is never exercised. That check is the anti-forgery core of AC-2's "unlinked findings" clause and its only guard (T-8). | Add three cases against the same fixture: `ledger.findings=['F-1','F-2']` with `actualFindings=['F-1']`; `ledger.findings=[]` with `actualFindings=['F-1']`; `ledger.findings=['F-1','F-1']`. All must throw `/ledger findings invalid/`. |
| F6 | low | `tests/run.js:2244` | `if (process.platform !== 'win32') return;` makes the AC-4 Windows-shim test a silent pass on every non-Windows host — it prints `ok - …` and inflates the 118/118 total without asserting anything, hiding that the only check for D-2's metacharacter/`.cmd` fallback is platform-bound (T-6). | Report it as an explicit skip, or drive `runLocal`'s branch selection through an injected platform so the literal-argument contract is asserted on every host. If kept as-is, mark it: `// flaky-pattern: platform-conditional early return counts as a pass off-Windows`. |
| F7 | medium | `.asd/runtime.js:212-252` (untested); `.asd/workflows/asd-phase-impl-review.md` steps 1a and 7; AC-2, AC-4 | The gate the workflow actually invokes is the CLI (`node .asd/runtime.js validate-ledger --manifest … --ledger … --findings …`, `external-preflight --input …`), but every new test calls the module exports directly. `parseFlagArgs`, `inputJson`, and the exit-code contract (0 ok / 1 not-local-ready / 2 throw) are unverified, so a flag-parsing or exit-code regression would make a blocking gate pass silently — a boundary/contract risk the chosen unit level does not reach (T-1). The suite already has the cheap precedent: `execFileSync` against `session-start.js` (`tests/run.js:2046`) and `sync.js --check/--apply` (`:1658`, `:1686`). | Add two `execFileSync` cases: valid manifest/ledger/findings fixtures → exit 0 with `{"ok":true}`; tampered ledger → non-zero exit and no `ok` on stdout. Optionally one `external-preflight` case asserting exit 1 on `command-unavailable`. |

Positives worth keeping (no action): the risk→check ladder chooses unit/temp-repo levels with no e2e overkill; the PM-migration test (`tests/run.js:2271-2307`) covers intact-delete, hand-modified skip, unmarked-consumer skip, symlink-escape skip and idempotence — genuinely edge-complete for a destructive migration; the negative-cache test injects `now` instead of sleeping (deterministic); no test was deleted this sprint (`-test(` count = 0), so the "Removed tests: None" row is accurate apart from F1's narrowing.

## Coverage

Manifest: [`testing.manifest.json`](testing.manifest.json) (digest `e9611d9d…5981d`). Validated compact ledger: [`testing.ledger.json`](testing.ledger.json). Finding-id array: [`testing.findings.json`](testing.findings.json). `node .asd/runtime.js validate-ledger` returned `{"ok":true}`.

`T-9` n/a (`no-stub-change-this-sprint`): `.asd/project/stubs.md:16` records "no open stubs"; no `// TODO(sprint-006-workflow-cost-routing):` marker exists in any scoped file (all repo hits are rule/agent prose describing the marker format). `T-10` n/a (`automation-possible`): `test-plan.md`'s manual-verification table records no steps and none is warranted — AC-3..AC-6 / AC-10..AC-12 / AC-18..AC-21 are either automatable locally (F2/F4/F7 say where) or, for live model access, explicitly out of scope by AC-4's own "unknown until a bounded actual-model request" contract. No user reply needed.

Hash freshness in `.asd/release-manifest.json` was corroborated structurally (paired `canon_hashes`/`upstream_hashes` entries), not recomputed — no shell available to this reviewer.

## Verdict
FAIL: 7 (1 high, 5 medium, 1 low)

## Next action
Route F1 first: it is not a test-authoring fix — either the `AGENTS.md` drift is repaired in this change (re-baseline `.asd/sync-state.json`, or add a self-sourced re-baseline path in `sync.js`) and the unconditional assertion at `tests/run.js:1066` restored, or the drift is escalated to the user as accepted debt. F2–F7 are creator-autofixable in `impl-test` (Tester): add the listed checks, record D-2's equivalent targeted mutations, and split `test-plan.md`'s blanket `keep` row into honest per-AC `none` decisions. Re-run `node tests/run.js` unscoped and restamp the Suite-run HEAD.

## Escalations
- finding F1: requires user approval if the resolution is to keep the assertion narrowed (accepting known debt / waiving a quality protection — hard gate per AC-21, `review-policy.md`). Repairing the drift instead needs no escalation.
