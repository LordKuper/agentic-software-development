[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: 1
- **Severity floor**: low

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| F1 | medium | `README.md:243-284` (home: `.asd/templates/t_config.yaml:1`) | Config-schema mirror gap. `t_config.yaml` gains a new top-level `user_gates: strict # adaptive \| strict` key and `/asd-init` (`.asd/skills/asd-init/SKILL.md:29,52,57,121`) now collects/writes it, but README's Configuration YAML block never lists it. `AGENTS.md` "Hard rules" requires the README config schema be updated in the same change. | Add `user_gates: strict   # adaptive \| strict — closure always requires user approval` as the first key of the README config block, above `self_hosting`, matching `t_config.yaml` ordering. |
| F2 | medium | `README.md:295-316` | Folder-map mirror gap: `.asd/runtime.js` is absent. It is a new canonical component (`managed_paths`, `core.md:22-23` glossary, `t_AGENTS.md:30`, invoked by four workflows) but README's folder map lists only `sync.js`, `rules/`, `templates/`, `agents/`, `skills/`, `workflows/`, `hooks/`, `migrations/`. README never mentions `runtime.js` anywhere. | Add `runtime.js  # deterministic routing, external-review preflight, coverage-ledger validation (invoked by phase workflows)` next to `sync.js` in the folder map. |
| F3 | medium | `AGENTS.md:14`, `:16`, `:50`, `:25-28` | Self-hosting canon doc stale for `.asd/runtime.js`. L14 still claims `tests/run.js` "only covers `.asd/sync.js`/`update.js`/`.asd/migrations/**`" — `tests/run.js:18` now imports `../.asd/runtime.js` and section 15 tests four of its commands. L16's green-suite trigger list, L50's code-style governance list, and the Architecture component bullets all omit `runtime.js`. | Add `.asd/runtime.js` to L14's covered set, L16's trigger list and L50's Node-code list; add a `**Runtime helper** (.asd/runtime.js)` bullet to the Architecture list. |
| F4 | medium | Home: `.asd/rules/external-review.md:20-29`. Stale restatements: `.asd/rules/sprint-lifecycle.md:62`, `:273`, `.asd/workflows/asd-phase-design-review.md:39`, `.asd/workflows/asd-phase-impl-review.md:45`, `.asd/skills/asd-init/SKILL.md:82` | The SSoT renamed the section to "Detection and negative cache", replaced the agent's `--version` probe with the orchestrator-run `runtime.js external-preflight`, and added a second skip trigger (active negative cache). Five downstream sites still cite `external-review.md` "Detection" (now a dead section name) and still name "wrapped-CLI probe fails" as the sole trigger — narrower than, and contradicting, the new home. `asd-external-review.md` itself says "do not make an extra availability probe". | Update all five citations to `external-review.md` "Detection and negative cache" and replace "wrapped-CLI probe fails" with "the phase-supplied preflight returns a non-ready status (unavailable command/auth, or active negative cache)". |
| F5 | low | `.asd/workflows/asd-phase-impl.md:88` (home: `.asd/rules/checkpoints.md`) | Dangling cross-reference: cites "pause-message format per `checkpoints.md`", but this diff deleted checkpoints.md's "Pause message format" section entirely (rewritten as "Gate mechanics"). | Repoint to `checkpoints.md` "Gate mechanics", or to `core.md` "User-decision presentation format" if the Problem/Options/Recommended/Consequences shape is what is meant. |
| F6 | medium | `.asd/templates/t_state.json:6` vs `.asd/rules/checkpoints.md:5` and `.asd/workflows/asd-phase-scope.md:1-9` | `{{USER_GATES}}` has no documented writer. checkpoints.md's whole policy keys off `state.json.user_gates`, but the scope workflow rewrite dropped the old placeholder-filling instruction and never says the value is seeded from `config.yaml.user_gates`; no rule or workflow names the filler. As written, a new sprint either persists the literal placeholder or falls into the "absent legacy value ⇒ strict" branch, silently disabling adaptive mode. | In `asd-phase-scope.md` step 2 (state seeding), state explicitly that `{{USER_GATES}}` is filled from `config.yaml.user_gates` (default `strict`) and that `{{DOC_*}}` placeholders are filled from normalized `documents.*` — no placeholder literal may survive the write. |
| F7 | medium | `.asd/rules/sprint-lifecycle.md:8` and `.asd/workflows/asd-phase-scope.md:3` vs `.asd/templates/t_state.json:5` | Both say scope must "freeze the effective audit boolean **and reason** in state", but `t_state.json` has no field for the reason (or for the normalized `auto\|always\|off` mode) — `documents.audit` holds only the effective value. The mandated fact has no home in the state schema. | Add the field to `t_state.json` (e.g. `"documents_audit": { "mode": …, "effective": …, "reason": … }`) or drop the "and reason" requirement from sprint-lifecycle.md/asd-phase-scope.md — one home, one statement. |
| F8 | medium | `.asd/rules/core.md:17` (glossary), `.asd/rules/providers.md:79-91` (Role-scoped context table) | The role that absorbed the deleted `asd-pm` has no canonical definition. It is named four different ways across the diff — "main phase orchestrator" (`core.md:17`, `sprint-lifecycle.md:5`), "phase orchestrator" (`artifact-layout.md:150,196`), "main orchestrator" (`t_manual-steps.md:21`), "the orchestrator" (`git-strategy.md:35`, workflows) — has no glossary entry of its own, and no row in providers.md's new "Role-scoped context" table, whose preamble says "Every role loads core.md … then reads only the row for its current responsibility". Its mandatory rule set is therefore undefined after `asd-pm.md`'s deletion. | Add a `**Main orchestrator**` glossary term to `core.md` and use that one token everywhere; add a `main orchestrator` row to providers.md "Role-scoped context" naming its rules (`checkpoints.md`, `sprint-lifecycle.md`, `git-strategy.md`, `artifact-layout.md`, `language-policy.md`, `review-policy.md`). |
| F9 | high | `tests/run.js:1056-1067` | The changed assertion at L1066 re-introduces `item.target !== 'AGENTS.md'`, i.e. exactly the allowlist that the immediately preceding in-body comment (L1062-1065) documents as previously removed because it "would have silently accepted permanent drift instead of proving the re-baseline actually happened". The comment now asserts the opposite of what the code does, and the documented invariant ("MUST still be re-baselined to `current` after each hand-edit") is silently dropped. In-body narration is banned outright by `code-style.md` §7. | Either restore the unexempted assertion and re-baseline `AGENTS.md` via `sync.js --apply`, or, if the exemption is intentional, delete the now-false comment block and encode the reason in the assertion message rather than an in-body comment. |
| F10 | medium | `.asd/migrations/5.0.0.js:1-53` (convention home: `.asd/migrations/4.0.0.js:1-28`) | No documentation at all: no migration-contract file header and no doc comment on the exported `migrate` member, which `code-style.md` §7 makes mandatory for exported members. `4.0.0.js` documents the contract, `ctx` shape, MigrationReport shape, scope and explicit non-scope; `5.0.0.js` returns a *different* report shape (`skippedModified`, `skippedUnsafe` added) documented nowhere, and its two security-relevant helpers (`intactGeneratedView`, `staysWithinRepo`) carry no doc for a non-obvious contract. | Add a file header mirroring `4.0.0.js` (target version, ctx contract, MigrationReport shape incl. the two new buckets, scope, "never touches" list) and a purpose-only doc on the exported `migrate`. |
| F11 | low | `.asd/rules/artifact-layout.md:196` (home: `.asd/rules/sprint-lifecycle.md` "PR phase") | The closure/archival sequence (closure-pending → explicit approval → companion PR → terminal write + move) is now restated in full in four places: `artifact-layout.md:196`, `sprint-lifecycle.md` "PR phase" + "State recovery", `git-strategy.md` "Finalize after closure", `asd-phase-pr.md`. artifact-layout.md previously linked to its home; the diff deleted that link and replaced it with a full restatement. | Keep artifact-layout.md to the path fact it owns (`.asd/sprints/archived/<NNN-slug>/`) and link the sequence to `sprint-lifecycle.md` "PR phase" instead of restating it. |
| F12 | low | `CHANGELOG.md:5-16` | Grouping violates `git-strategy.md` "Versioning & Changelog" (`Added\|Changed\|Deprecated\|Removed\|Fixed\|Security`, consumer-facing impact only): the breaking `asd-pm` removal appears only in the lead paragraph with no `### Removed` group, and the `### Changed` bullet "Batch integration was evaluated and rejected; no Batch transport is added" documents a non-change/internal deliberation rather than consumer-facing impact. | Add `### Removed` with the `asd-pm` agent and its generated views (plus the 5.0.0 migration note); drop the Batch bullet or move that rationale to the sprint decisions-log. |

## Coverage

Manifest: [`documentation.manifest.json`](documentation.manifest.json) (digest `01767030…b513ce`). Validated compact ledger: [`documentation.ledger.json`](documentation.ledger.json). Finding-id array: [`documentation.findings.json`](documentation.findings.json). `node .asd/runtime.js validate-ledger` returned `{"ok":true}`.

n/a predicates used: `HTML_SHELL`/`PROVENANCE` (rules `D-3`, `D-4`) — `no-html-artifact-in-scope`; `TRACEABILITY`/`PERSISTENT_ACTUALITY` (rules `D-5`, `D-6`) — `documents-disabled`.

## Verdict
CONCERNS: 12

## Next action
Route to `asd-dev` in impl review-fix mode. Priority: F9 (documented invariant silently dropped by the changed assertion — decide exemption vs re-baseline first), then mirror gaps F1/F2/F3 and the undefined-orchestrator/state-schema gaps F6/F7/F8, then stale cross-references F4/F5, then F10/F11/F12. F1-F3 and F9 must land in the same change per `AGENTS.md` "Hard rules"; no `sync.js --apply` is required for any of them (all targets are canon, docs or tests, none generated).

## Escalations
None — no finding requires user approval (no concept change, new abstraction, scope expansion, or contract change beyond what sprint AC-14 already authorizes).
