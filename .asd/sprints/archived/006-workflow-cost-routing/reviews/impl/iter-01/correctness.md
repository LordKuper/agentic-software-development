[REVIEW-impl-correctness]: CONCERNS

# Review — correctness

- **Phase**: impl-review
- **Iteration**: 1
- **Severity floor**: low

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| F1 | high | `tests/run.js:1066` (with `AGENTS.md`, `.asd/sync-state.json`) | The `sync.js --check` "no drift" regression assertion was narrowed to `item.target !== 'AGENTS.md' && item.status !== 'current'`. `AGENTS.md`'s managed block was edited this sprint while `.asd/sync-state.json` was not (absent from the change set), so `statusSelfSourcedManagedBlock` → `statusManagedBlock` (`.asd/sync.js:487`) now returns `modified-foreign` for it: the repo really is drifted, and the guard a previous sprint added for exactly this failure (its own inline comment: "Fails at parent 317aa50 (AGENTS.md was `modified-foreign`)") was silenced instead of the drift being fixed. Waiving an existing quality protection is a hard user gate (`checkpoints.md` "Hard in both modes"), and no AC or plan Task asks for this exclusion. | Re-baseline the self-sourced managed block, then restore the unfiltered `parsed.items.filter((item) => item.status !== 'current')`. **Orchestrator correction:** this reviewer's suggested `node .asd/sync.js --apply AGENTS.md` does not work — `runApply` short-circuits every self-sourced target with `applied: false` (`.asd/sync.js:1491-1498`). The repair needs either a direct `.asd/sync-state.json` digest update or a new self-sourced re-baseline path in `sync.js`. See the testing reviewer's F1, which reached the same conclusion. |
| F2 | medium | `.asd/runtime.js:226-248` vs `.asd/rules/review-policy.md:97` | `review-policy.md` requires each dispatch manifest to carry a SHA-256 `digest` "calculated by `.asd/runtime.js`", and `validateCoverageLedger` hard-fails on any mismatch (`runtime.js:180`), but `main()` exposes only `validate-ledger`, `external-preflight`, `external-record-failure`, `route-task` — there is no invocable way to produce the digest, so the orchestrator has no deterministic path to a valid manifest (it must improvise an inline `node -e`). The usage string (`runtime.js:247`) also names only two of the four commands. | Add a `manifest-digest --manifest <path>` subcommand (print digest, or `--write` it into the file) exporting `coverageManifestDigest`, reference it from `review-policy.md` "Coverage ledger" and both review workflows, and update the usage string to list all commands. |
| F3 | medium | `.asd/workflows/asd-phase-pr.md:5-9` (open mode) | The self-hosting DoD guard `max(.asd/migrations/*.js filename version) <= asd_version` was deleted with the old step 4 and is preserved nowhere: `git-strategy.md:63-67` and `sprint-lifecycle.md:100-108` only describe the version bump, not the migration-reachability check. Under `backward_compat: migration` this was the check that prevents shipping a migration (e.g. `.asd/migrations/5.0.0.js`) that no consumer ever runs because the SemVer bump does not reach it. | Restore the check in the open-mode DoD list (step 1) or add it as a normative line in `git-strategy.md` "Versioning & Changelog"; keep it blocking, like every other DoD check. |
| F4 | medium | `.asd/templates/t_state.json:6`, `.asd/workflows/asd-phase-scope.md:5-7`, `.asd/rules/checkpoints.md:5`, `.asd/templates/t_config.yaml:1` | AC-18's policy is read exclusively from `state.json.user_gates`, the template seeds it as the placeholder `{{USER_GATES}}`, and the config defines a separate top-level `user_gates` — but no workflow or skill step maps config → state. Scope step 3 normalizes `documents.audit` only. An unresolved `{{USER_GATES}}` is an "invalid or unreadable policy", which `checkpoints.md` says blocks: fail-closed but sprint-breaking, and the precedence between `config.user_gates` and `state.json.user_gates` (and the no-sprint standalone-skill case) is undefined. AC-18 is therefore partially implemented with no `stubs.md`/migration follow-up. | Add an explicit scope step: "read `config.user_gates`, accept only `adaptive\|strict` (absent → `strict`), freeze it into `state.json.user_gates`"; state in `checkpoints.md` that config is the source and state the frozen copy, and that a standalone skill with no sprint reads config (default `strict`). |
| F5 | low | `.asd/runtime.js:36` | `Math.min(Math.max(Number(timeoutMs) \|\| 0, 1), MAX_TIMEOUT_MS)` silently converts any non-numeric, zero-after-coercion or negative `timeoutMs` into a **1 ms** timeout, so a perfectly available CLI returns `command-unavailable`/`authentication-unavailable`, which can then be negative-cached via `external-record-failure`. | Validate the input: reject a non-finite or non-positive `timeoutMs` with `fail(...)`, or fall back to the 3000 ms default instead of clamping to 1. (Efficiency's F2 proposes deleting the knob outright, which also resolves this.) |
| F6 | low | `.asd/runtime.js:55-75`, `:119-129` | The negative cache only ever deletes the one key it just re-probed and found expired. Since `cacheKey` binds `auth_generation` (`mtime:size` of the credential file) and the selected model, every credential refresh or model change mints a new key, so `entries` grows without bound in the on-disk cache file. | Prune all entries whose `retry_after <= now` inside `readCache`/`writeCache` (and cap entry count) so the file stays bounded. (Duplicate of efficiency F4.) |
| F7 | low | `.asd/runtime.js:79-81` | `authGeneration` does `fs.existsSync(credentialPath)` then `fs.statSync(credentialPath)`; a credential file rotated/removed between the two calls throws, escapes to `main`'s catch and exits 2 instead of returning a clean non-ready preflight status. | Wrap the `statSync` in `try/catch` and return `'unknown'` on failure (same fallback as the missing-file branch). |
| F8 | low | `.asd/migrations/5.0.0.js:19-28`, `:14-17` | `intactGeneratedView` re-implements `sync.js`'s `splitMarkerAndBody` (`.asd/sync.js:214-223`) marker/body split instead of reusing it; if the marker format changes, the migration silently classifies intact framework-owned views as modified and leaves orphans behind. `removeIfEmpty`'s `typeof sync.removeIfEmptyDir === 'function'` fallback is called dead code here — `removeIfEmptyDir` is exported (`.asd/sync.js:1629`). | Export `splitMarkerAndBody` (or a small `isIntactGeneratedView(target)` helper) from `sync.js` and call it from the migration. **Note the reviewer disagreement**: efficiency passed the same guard under OE-7, arguing `update.js` can leave a consumer-modified `sync.js` in place so a stale export set is reachable, with `4.0.0.js:65-67` as precedent. Resolve the guard question before touching it; the `splitMarkerAndBody` duplication stands regardless. |
| F9 | low | `.asd/workflows/asd-phase-impl.md:49` | Step `2a` ("Before each task dispatch, run `route-task`…") is placed **before** step 2 (mode detection) and six steps before the actual task dispatch loop (step 6), so the documented sequence no longer matches execution order — routing is described as happening before the workflow even knows whether tasks, findings or defects are being processed. | Renumber: move the routing step to immediately precede the step-6 dispatch loop (e.g. `5a`), keeping the "before each task dispatch" wording. |
| F10 | low | `.asd/workflows/asd-phase-plan.md:40-44` | Step 4's inner instruction still hard-codes the strict mechanic ("write `<sprint>/plan.md`; loop until explicit `accept`" / "on explicit `accept`: append decisions-log entry"), while the new step 5 says to apply `checkpoints.md` and record "either user approval or adaptive evidence" — an orchestrator following step 4 literally always demands `accept`, defeating adaptive mode. Numbering also jumps 5 → 7. | Reword step 4's last two bullets to defer to `checkpoints.md` for the gate class, and close the numbering gap. |
| F11 | low | `.asd/rules/sprint-lifecycle.md:232`, `:263` | Two stale references to the removed PR flow: line 232 points DoD verification at "`asd-phase-pr.md` step 4", which no longer exists (open mode is now four steps, DoD is step 1); line 263 "Sprint immutability" still describes pre-merge archival plus a "merge-mode terminal write" as the normal path, contradicting `artifact-layout.md` "Sprint archival" and this file's own line 230 (archive only after closure approval). | Repoint line 232 at open-mode step 1 and rewrite line 263 to describe the closure-time archive move, scoping the pre-merge wording to the legacy-recovery case only. |
| F12 | low | `README.md:134` | The phase mermaid still labels the audit node `audit["audit<br/><i>BA · Architect</i>"]` while the same file's phase table and `sprint-lifecycle.md`'s phase table say "Architect (BA conditional)" — the README mirror required by the repo's hard rule contradicts AC-15's single-audit-owner change. | Relabel the node to `audit<br/><i>Architect (BA conditional)</i>`. |
| F13 | low | `.asd/runtime.js:148` | The recorded `reason` is `'no-downgrade'` whenever `tier === input.priorTier`, including the ordinary case where the freshly computed tier simply equals the prior one and no downgrade was ever attempted. AC-11 requires the recorded reason to be accurate resumable evidence; this mislabels normal re-entries. | Emit `'no-downgrade'` only when the computed tier was actually raised by the `priorTier` clamp (compare pre- and post-clamp tier), otherwise keep the computed reason. |
| F14 | low | `.asd/runtime.js:147` | `execution` is `'command'` only when `input.priorTier === undefined`, but both workflows pass `priorTier` on every re-entry (`asd-phase-impl.md:49`, `asd-phase-impl-test.md:29`). A zero-judgment deterministic operation (state serialization, file-set enumeration) therefore stops using the free command path on any re-run, contrary to AC-10. | Gate `execution: 'command'` on `deterministicCommand && tier === 'mechanical'` alone; the tier clamp already prevents a genuine downgrade, since `priorTier` above `mechanical` yields a non-mechanical tier. |
| F15 | low | `.asd/rules/providers.md:102` vs `.asd/runtime.js:149` | The rule says the orchestrator "calls `route-task` … and persists `{execution,tier,reason,selector,resolved_model}`", but `routeTask` returns only `{tier, execution, reason, selector}` — `resolved_model` has no producer, so the AC-11 "resolved model identifier when available" evidence has no defined source. | Either resolve the concrete model in the helper from `release-manifest.json` `model_families` for the selected tier/provider and return it, or state explicitly in `providers.md` that the orchestrator fills `resolved_model` from the manifest after the call. (Efficiency's F6 separately proposes dropping `selector`.) |

## Coverage

Manifest: [`correctness.manifest.json`](correctness.manifest.json) (digest `8b774fa6…d4dd8e9`). Validated compact ledger: [`correctness.ledger.json`](correctness.ledger.json). Finding-id array: [`correctness.findings.json`](correctness.findings.json). `node .asd/runtime.js validate-ledger` returned `{"ok":true}`.

`UI_CONFORMANCE` and rules `UI-1`..`UI-7` are `n/a: no-ui-surface-in-scope` per the phase workflow's step-5 predicate; no UI input was loaded.

### AC trace notes (supporting AC-1/AC-2/AC-3)

- AC-1 `.asd/project/config.yaml:38` `scoped_fan_out: enabled`; `asd-phase-impl-review.md:5-6` keeps both section-degrading predicates while dispatching every reviewer. AC-2 `review-policy.md:97-104` + `runtime.js:152-210` + `t_review.md:24-26`. AC-3/AC-5/AC-6 `asd-external-review.md` frontmatter, `external-review.md` "Detection and negative cache", `runtime.js:89-129`. AC-4 preflight wired at `asd-phase-design-review.md:3a` / `asd-phase-impl-review.md:1a`, no model request. AC-7/AC-14 canonical `asd-pm.md` deleted, no live reference remains outside `tests/`, `CHANGELOG.md` and archived sprints; `.claude/agents/` + `.codex/agents/` hold 11 roles + 6 variants, no PM view; `.asd/migrations/5.0.0.js` covers consumers. AC-8 `asd-phase-impl-test.md:25,40,51,55`. AC-9 `providers.md` "Role-scoped context" covers all 11 roles. AC-10/AC-11/AC-12 `runtime.js:132-150`, `sync.js:934-970,1234-1262`, `providers.md:96-102` — see F13/F14/F15 for the residual gaps. AC-13 `asd-phase-scope.md:7`, `t_config.yaml:16`, `state.json.documents.audit=true`. AC-15 `asd-architect.md`/`asd-ba.md`/`t_audit.md`/`asd-phase-audit.md` — mirror gap in F12. AC-16 recorded in `decisions-log.md:42-46`. AC-17 hash/roster/README guards in `tests/run.js` — weakened for `AGENTS.md`, see F1. AC-18..AC-23 `checkpoints.md`, `sprint-lifecycle.md:5-9`, `asd-phase-pr.md` closure mode — seeding gap in F4.
- No partial AC implementation was left without either a finding above or an explicit follow-up; `.asd/project/stubs.md` holds no open stub for this sprint.

## Verdict
CONCERNS: 15

## Next action
Creator (`asd-dev`) autofixes F1–F15 in review-fix mode — F1 first (repair the `AGENTS.md` drift, restore the unfiltered drift assertion, re-run `node tests/run.js`), then F2–F4 (runtime digest command, restored migration-version DoD check, `user_gates` seeding step), then the low-severity items. Sprint returns through `impl-test` to `impl-review`.

## Escalations
None from this reviewer. Note that F1 as it currently stands *is* an un-approved quality-protection waiver — repairing it removes the need for a user decision; keeping the exclusion would require an explicit hard-gate approval per `checkpoints.md`.
