[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: wave-1/iter-01

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| DOC-1 | high | `.asd/rules/review-policy.md:146` (Gate Verdict Format), `.asd/rules/review-policy.md:91` (Memory-fix dispatch, "An owner without one (reviewers, …)"), `.asd/rules/providers.md:46`, `README.md:215`, `CHANGELOG.md:21`, `.claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md:8`, `.claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md:8-10` | AC-14 asks for the claim to be "corrected to match the host", but the new text says the opposite of what the host does. Canon now says "`memory: project` loads it, but the host serves a reviewer no write tool", and every reviewer memory-fix routing depends on that premise. This dispatch disproves it: canon `asd-reviewer-documentation` declares `tools: [Read, Glob, Grep]` and `disallowedTools: [Edit, Bash, WebFetch]`, yet this dispatch has a `Write` tool, and the host-injected memory instructions tell it to write memory with `Write`. So `memory: project` gives the agent `Write`, and nothing disallows it. All four internal reviewers disallow `Edit` but not `Write`, and so do the advisor and External Review. As a result, providers.md's "config-enforced … no Write" does not cover the reviewer's own memory directory, although the same sentence says it does, and README's "…or their own memory on any provider" is false on Claude. The 017 F-4 evidence (audit.md:138) read the `tools` list, not a dispatch's actual tool set. | Make the premise true rather than rewording around it: add `"Write"` to `claude.disallowedTools` on the four internal reviewers (and External Review and the advisor, if the same premise applies to them), run `sync.js --apply`, refresh the hashes, and pin it in `tests/run.js` next to the AC-15 check. The alternative is to reword every listed site to say the host serves `Write` through `memory` and policy forbids using it. Also replace CHANGELOG's sprint-internal `(AC-14)` with the rule home. The two memory lines go to their owners through the memory-fix dispatch. |
| DOC-2 | medium | `.asd/rules/sprint-lifecycle.md:49` ("Scope per iteration") | The change made unchanged text wrong. The wave-K iteration-1 scope still lists "reviewer memory writes — `review-policy.md` "Diff reachability"" as a change source. After AC-14 a reviewer writes no memory, and "Diff reachability" now describes the orchestrator committing memory-fix text inside `impl`. That commit is a review-fix commit and never falls between waves of one impl-review entry. The line contradicts the SSoT, and the `tests/run.js` sweep does not catch it. The reviewer-documentation memory line 42-43 therefore overstates what that test covers. | Drop ", reviewer memory writes" (or change it to "memory-fix commits"). Narrow the memory line to what the regex actually matches (via memory-fix dispatch). |
| DOC-3 | medium | `.asd/agents/asd-tester.md:88`, `.asd/workflows/asd-phase-impl-test.md:30` vs `.asd/rules/artifact-layout.md:98` | The acting sites for the AC-16 leftover-term check have a different reach than its home. (a) The home says the sprint "searches for every remaining mention of it", with agent memory as an added area, but the tester's Do's line limits the check to "over `.claude/agent-memory/**`". No agent acts on the canon/README part, and DOC-2 is a leftover that part would have caught. (b) The home says "from its first `impl-test` entry that search covers…", but both acting sites run it only on entry 1, so a re-entry after a review-fix is never checked again. | Make the tester line cite the whole check ("run the leftover-term check `artifact-layout.md` "Agent memory" requires, memory included"). Then either say "on entry 1" in the home, or have re-entries rerun the check over their delta. |
| DOC-4 | low | `.asd/templates/t_retrospective.html:11` | The new comment restates the retro row id definition (A-N/P-N, N = 1-based ordinal, covered-by rows counted, English literals). The definition's single home is `sprint-lifecycle.md:285` "Retro row id", which `asd-phase-retro.md` step 6 already cites for the writer. The copy changes no reader's behaviour, and it cites "Retro intake" instead of the definition's home. | Cut it to a pointer, e.g. `<!-- Row ids and Acts on literals: .asd/rules/sprint-lifecycle.md "Retro phase" (Retro row id); intake parses them. -->`. |
| DOC-5 | low | `.asd/agents/asd-tester.md:3` (`description`) | The trigger text says the tester is "dispatched fresh in review-fix to amend only test-plan.md's risk and added-test rows". That reads as if row amendment were the dispatch's purpose. The dispatching workflow (`asd-phase-impl.md` step 3/5) sends it review findings located in test files; the row limit only bounds what it may touch in `test-plan.md`. The description no longer names what the dispatch is for, which breaks the agent-to-workflow dispatch mirror. | Reword, e.g. "…and in review-fix for findings located in test files, amending only test-plan.md's risk and added-test rows." |

## Coverage (internal reviewers only)

```json
{"manifest_digest":"a5177f6e911a1557489259ff975304698926d4a4aa10d831116831756ea9e133","findings":["DOC-1","DOC-2","DOC-3","DOC-4","DOC-5"],"files":[{"i":".asd/agents/asd-external-review.md","s":"checked"},{"i":".asd/agents/asd-tester.md","s":"checked"},{"i":".asd/release-manifest.json","s":"checked"},{"i":".asd/rules/artifact-layout.md","s":"checked"},{"i":".asd/rules/checkpoints.md","s":"checked"},{"i":".asd/rules/external-review.md","s":"checked"},{"i":".asd/rules/git-strategy.md","s":"checked"},{"i":".asd/rules/providers.md","s":"checked"},{"i":".asd/rules/review-policy.md","s":"checked"},{"i":".asd/rules/sprint-lifecycle.md","s":"checked"},{"i":".asd/runtime.js","s":"checked"},{"i":".asd/templates/t_decisions-log.md","s":"checked"},{"i":".asd/templates/t_retro-backlog.md","s":"checked"},{"i":".asd/templates/t_retrospective.html","s":"checked"},{"i":".asd/workflows/asd-phase-design-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-review.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl-test.md","s":"checked"},{"i":".asd/workflows/asd-phase-impl.md","s":"checked"},{"i":".asd/workflows/asd-phase-retro.md","s":"checked"},{"i":".asd/workflows/asd-phase-scope.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/MEMORY.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/project_parallel-agent-commit-sweep.md","s":"checked"},{"i":".claude/agent-memory/asd-dev-critical/project_parallel-wave-home-citations.md","s":"checked"},{"i":".claude/agent-memory/asd-reviewer-documentation/project_reviewer-write-scope-declaration.md","s":"checked"},{"i":".claude/agent-memory/asd-reviewer-testing/feedback_no-shell-review-method.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/feedback_fail-first-and-none-honesty.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/project_mutation-runs-trip-the-hash-ledger.md","s":"checked"},{"i":".claude/agent-memory/asd-tester-critical/project_testability-envelope.md","s":"checked"},{"i":"CHANGELOG.md","s":"checked"},{"i":"README.md","s":"checked"},{"i":"tests/run.js","s":"checked"}],"rules":[{"i":"SSoT","s":"finding","f":"DOC-2"},{"i":"Template adherence","s":"pass"},{"i":"HTML shell wrapping","s":"pass"},{"i":"Provenance","s":"pass"},{"i":"Traceability","s":"finding","f":"DOC-3"},{"i":"Persistent actuality (impl-review)","s":"finding","f":"DOC-1"},{"i":"In-code doc comments (impl-review, `code-style.md` §7)","s":"pass"},{"i":"Stub-resolution verification (impl-review)","s":"pass"},{"i":"Framework mode (`self_hosting: enabled`, impl-review only)","s":"finding","f":"DOC-5"},{"i":"Documentation economy","s":"finding","f":"DOC-4"},{"i":"Custom rules consistency","s":"pass"},{"i":".asd/project/custom-common-rules.md","s":"pass"},{"i":".asd/project/custom-coding-rules.md","s":"pass"}],"sections":[]}
```

Notes that support the passes:
- **Stubs:** `stubs.md` has no rows, and there is no `TODO(sprint-` marker.
- **§7:** no in-body `//` comments, and the new JSDoc states purpose and contract.
- **Hashes:** freshness checked structurally (test-pinned), not recomputed.
- **Version:** `asd_version` 13.2.0 matches the CHANGELOG heading.
- **Retro intake:** the home agrees with scope steps 2a and 4, the checkpoints hard list and inventory, `artifact-layout.md` "Retro backlog", `t_retro-backlog.md` and the runtime.
- **README:** the scope and retro rows, the folder map and the `/asd-update` row are updated.

## Verdict
CONCERNS: 5

## Next action
Route to `impl` review-fix:
- DOC-2 to DOC-5 go to the canon owner (dev).
- DOC-1 goes to the dev for the canon, README and CHANGELOG sites, after the user answers the question below. Its two agent-memory sites go to their owners through the memory-fix dispatch.

## Escalations (optional)
- question: DOC-1 — The host gives reviewers `Write` through `memory: project`, which contradicts the new canon premise. Which fix?; options: (a) add `"Write"` to reviewer (and External Review/advisor) `claude.disallowedTools`, which makes the corrected claim true and keeps the memory-fix route / (b) keep the grant and reword canon, README and CHANGELOG to "host serves `Write`, policy forbids its use", accepting that the policy is not host-enforced
  answer: (b) — on Claude a reviewer holds `Write` for its own memory via `memory: project`; canon states that truthfully (policy, not host, keeps it to its memory dir); a finding in its memory is fixed by that owner in a fresh memory-fix dispatch using its own write tool, the orchestrator commits it; no disallowedTools change (user, 2026-09-26)

Notes (outside the manifest list):
- `.claude/agent-memory/asd-reviewer-documentation/feedback_no-shell-doc-review-method.md` still says "A stale line in *your own* memory is corrected in the dispatch that finds it, not raised", which contradicts the memory-fix route. Its owner should get a memory-fix dispatch.
- No memory was written in this dispatch, although the host offered `Write`.
