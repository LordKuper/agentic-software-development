---
responsibility:
  owns: append-only chronology of approved decisions across project lifetime
  excludes: sprint state, code review notes, custom rules
  delegates_to: .asd/sprints/ (sprint state), reviews/ (review notes), custom-common-rules.md / custom-design-rules.md / custom-coding-rules.md (rules)
---

# Decisions Log

Append-only. Never edited or removed.

## Entry format

```markdown
## YYYY-MM-DD — <one-line summary>

- **Decision**: <what was decided>
- **Rationale**: <why>
- **Affected docs**: <links> (optional)
```

## Entries

## 2026-08-31 — ASD self-hosting bootstrap

- **Decision**: Enabled `self_hosting: enabled` and per-sprint `documents.*` toggles (audit only for this repo); bootstrapped `.asd/project/` for the framework repo itself.
- **Rationale**: Lets ASD's own future changes flow through `/asd-sprint` instead of ad-hoc edits, per `plans/self-hosting-and-optional-documents.md`. This bootstrap patch itself was applied directly (no ASD-managed source to bootstrap from before this point).
- **Affected docs**: `.asd/rules/sprint-lifecycle.md` ("Self-hosting", "Optional documents"), `.asd/templates/t_config.yaml`, `.asd/templates/t_state.json`, `.asd/sync.js`, all `.asd/workflows/asd-phase-*.md`, `.asd/rules/review-policy.md`, `.asd/rules/external-review.md`, `.asd/agents/asd-backend-dev.md`, `.asd/agents/asd-frontend-dev.md`, `.asd/skills/asd-init/SKILL.md`, `.asd/skills/asd-update/SKILL.md`, `README.md`, `AGENTS.md`.

## 2026-09-01 — Re-init: enable external review, tighten critical-review cap, auto_pr

- **Decision**: `review.external_review`: disabled → enabled; `review.iterations_critical`: 10 → 3; `git.auto_pr`: false → true.
- **Rationale**: Codex CLI confirmed on PATH (`codex-cli 0.150.1`), so Codex-wrapped external review can run alongside internal reviewers during design-review/impl-review. Critical-severity review loop capped tighter (3 instead of 10) to bound iteration cost. PR auto-opens once Definition of Done is met, no extra confirmation step.
- **Affected docs**: `.asd/project/config.yaml`.

## 2026-09-01 — Adopt forge-inspired git/PR/release practices

- **Decision**: Fetch+fast-forward verification and explicit no-direct-push-to-main rule added to branch creation; PR title required to follow Conventional Commits; PR body template gained Verification/Version/Related-issues sections; self-hosting sprints now bump `asd_version` (SemVer) and update `CHANGELOG.md` at PR open, tag + `gh release create` at PR merge. GitHub repo: squash-merge only (merge-commit/rebase-merge disabled), auto-delete branch on merge, branch protection on `main` (required `sync-check` status, no force-push, no deletion, enforced for admins too). `asd_version` bumped 1.1.0 → 1.2.0 as the first application of this policy (`canon_hashes`/`upstream_hashes` recomputed for every changed managed-path file).
- **Rationale**: Evaluated `LordKuper/forge`'s `AGENTS.md` per user request; adopted the practices with real value for a framework repo with a load-bearing version (`asd_version` already consumed by `/asd-update` but never bumped since introduction) and no branch protection at all. Skipped forge's portability/.NET rules (not applicable — no compiled code) and its post-PR-open inline-review-comment loop (ASD's pre-PR-open, coverage-ledger-gated review is already stricter).
- **Affected docs**: `.asd/rules/git-strategy.md`, `.asd/rules/sprint-lifecycle.md`, `.asd/workflows/asd-phase-scope.md`, `.asd/workflows/asd-phase-pr.md`, `.asd/agents/asd-pm.md`, `.asd/templates/t_pr-description.md`, `.asd/release-manifest.json`, `CHANGELOG.md` (new), GitHub repo settings + branch protection on `main`.

## 2026-09-01 — Automate release-manifest.json hash-ledger recompute

- **Decision**: `node .asd/sync.js --apply <file...>` now recomputes `canon_hashes` and `upstream_hashes` in `.asd/release-manifest.json` from actual on-disk content after every apply (whole-repo, independent of which targets were requested), writing back only when a value actually changed. `posixJoin`/`walkDir`/`expandManagedPath`/`hashIfFile` moved from `.asd/skills/asd-update/update.js` into `.asd/sync.js` (exported, aliased back in update.js) to avoid duplicating the same file-tree walk in both places.
- **Rationale**: Editing a canonical file previously left both hash ledgers stale until someone hand-wrote a throwaway script to recompute them (`tests/run.js` catches the drift but doesn't fix it) — exactly the gap hit while adopting the forge-inspired versioning practices above. Folded into `--apply` per AGENTS.md's existing "run sync.js --apply after editing canon" instruction, so it's the same step, not a new one to remember.
- **Affected docs**: `.asd/sync.js`, `.asd/skills/asd-update/update.js`, `.asd/release-manifest.json` (recomputed `.asd/sync.js`/`.asd/skills/asd-update/update.js` entries).

## 2026-09-01 — Sprint 001 scope approved: rename persistent docs root `design/` → `docs/`

- **Decision**: Approved the refined scope of sprint `001-rename-design-to-docs`: rename the project-wide persistent documentation root from `design/` to `docs/` across all canonical ASD sources (`.asd/rules`, `.asd/templates`, `.asd/agents`, `.asd/skills`, `.asd/workflows`, `.asd/release-manifest.json`, `.asd/project/*`, `README.md`, `AGENTS.md`), then regenerate the provider views via `node .asd/sync.js --apply`. Pure root-segment rename — the `product/`/`architecture/`/`ux/` subtree keeps its structure, file names, and contents. Explicitly out of scope: the sprint-local `<sprint>/design/` draft folder, the `design`/`design-review`/`design-promote` phase names and `asd-phase-design*` dispatch, the `DESIGN.md` / `design-system.html` / `design-principles.md` / `design-system.md` / `custom-design-rules.md` file names, any `tests/` or test-data-location convention (deferred by the user to consumer projects), any restructuring under the documentation root, and rewriting historical `decisions-log.md`/`CHANGELOG.md` entries.
- **Rationale**: `docs/` is the conventional, self-explanatory name for a project's living documentation tree; the current `design/` collides conceptually with the `design` *phase* and its sprint-local draft folder of the same name, which is a recurring source of ambiguity in the rules and agent prompts. The rename is reference-only in this repo — no `design/` folder exists on disk here, since `documents.prd`/`ux_spec`/`adr`/`c4` are disabled in the lean self-hosting profile.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/sprint.md` (AC-1…AC-8 — the acceptance-criteria source of record for this sprint, since `documents.prd` is disabled), `.asd/sprints/001-rename-design-to-docs/state.json`. Frozen `documents` snapshot for this sprint: `audit` enabled; `prd`, `ux_spec`, `adr`, `c4` disabled (pre-existing lean self-hosting profile from `.asd/project/config.yaml`, unrelated to this decision) — the `design`, `design-review`, and `design-promote` phases will therefore be no-ops.

## 2026-09-01 — Sprint 001 audit approved; consumer migration documented-only, AC-7 made verifiable

- **Decision**: Approved `audit.md` for sprint `001-rename-design-to-docs` (BA docs-side scan + Architect code-side scan; 13 gaps G-1…G-13, 14 risks R-1…R-14). Two escalations resolved and folded into `sprint.md`: (1) **R-13** — consumer migration takes the **documentation-only** path (option a): new **AC-9** requires a `CHANGELOG.md` entry documenting the breaking rename and the consumer steps (`git mv design docs`, update `commands.yaml` `designmd-*` aliases, then `/asd-update` followed by `/asd-sync`). Rejected: teaching `asd-update`/`asd-sync` a one-shot consumer-folder rename (b), a dual-root config key (c), and silent deferral (d). (2) **G-1 / AC-7** — prose phrases denoting the renamed root ("persistent design docs", "persistent design-artifact counterpart") are reworded **line by line where they mean the root**, leaving phase-scoped uses of the word `design` untouched (option a); **AC-7 rewritten** to a mechanically verifiable form: search patterns `design/` **and** `design\\` with an explicit exclusion list (`<sprint>/design/`, `reviews/design/`, `asd-phase-design*`, the `design/design-review` phase pair, `design-system*`, `design-principles*`, `t_design-md-delta.yaml`, `.asd/sprints/**`, `decisions-log.md`, `CHANGELOG.md`).
- **Rationale**: `config.yaml`'s `backward_compat: migration` makes silence on R-13 unavailable, but ASD has no migration machinery to reach for — `managed_paths` is `.asd/`-only, there are zero `fs.rename` calls anywhere (G-12), and the docs root is a prose literal with no config indirection to flip (G-11), so automation (b/c) would break `/asd-update`'s never-touch-consumer-content invariant for a consumer set that is currently empty. A CHANGELOG migration note discharges the policy at near-zero cost and keeps the sprint a rename. AC-7's original wording ("no remaining reference … spelled `design/`") had no mechanical test — a bare grep returns ~180 legitimate out-of-scope hits (G-7) and is separator-blind, missing 4 real Windows-path occurrences in `t_commands.yaml:21,23` and `asd-init/SKILL.md:101,103` (R-10). Since `sync.js --check` has no targets under `.asd/rules|templates|workflows` (G-10), a bare `--apply` regenerates nothing (G-9), and `tests/run.js` has no `design` fixture, a half-renamed tree can show three green signals at once (R-11) — the AC-7 grep is the sprint's only real completeness gate, so it had to be made executable. Also carried into plan: `--apply` must be passed explicit file lists per touched target (G-9), and a final `--apply` is mandatory so `upstream_hashes` are not shipped stale, which would otherwise make consumers silently *skip* the renamed rules (R-12).
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/audit.md` (approved), `.asd/sprints/001-rename-design-to-docs/sprint.md` (AC-7 reworded, AC-9 added), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: audit`).

## 2026-09-01 — Sprint 001 design skipped (no documents enabled)

- **Decision**: `design` phase skipped as a no-op for sprint `001-rename-design-to-docs` — the frozen `documents` snapshot has `prd`, `ux_spec`, `adr`, and `c4` all disabled, so the phase's entire applicable-artifact set is empty. No creator dispatched, no approval gate, phase appended to `state.json.skipped_phases`.
- **Rationale**: Deterministic consequence of the frozen `documents` config per `.asd/rules/sprint-lifecycle.md` "No-op phase rule", not a decision requiring approval.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: design`, `skipped_phases: ["design"]`).

## 2026-09-01 — Sprint 001 design-review skipped (no in-scope drafts)

- **Decision**: `design-review` phase skipped as a no-op for sprint `001-rename-design-to-docs` — the `design` phase was itself a no-op and no `<sprint>/design/` drafts exist, so the intersected review scope is empty. No reviewer dispatched, no approval gate, phase appended to `state.json.skipped_phases`.
- **Rationale**: Deterministic consequence of the frozen `documents` config per `.asd/rules/sprint-lifecycle.md` "No-op phase rule", not a decision requiring approval.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: design-review`, `skipped_phases: ["design", "design-review"]`).

## 2026-09-01 — Sprint 001 design-promote skipped (nothing to promote)

- **Decision**: `design-promote` phase skipped as a no-op for sprint `001-rename-design-to-docs` — both `design` and `design-review` were themselves no-ops and no drafts exist in `<sprint>/design/`, so the promotion scope is empty. No decomposition, no creator dispatched, no C4 registry mutation, no persistent docs-root writes, no approval gate; phase appended to `state.json.skipped_phases`.
- **Rationale**: Deterministic consequence of the frozen `documents` config per `.asd/rules/sprint-lifecycle.md` "No-op phase rule", not a decision requiring approval.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: design-promote`, `skipped_phases: ["design", "design-review", "design-promote"]`).

## 2026-09-01 — Plan approved for sprint 001-rename-design-to-docs

- **Decision**: Approved `plan.md` for sprint `001-rename-design-to-docs`: 11 tasks, `backend-dev` owner throughout (prose/config/markdown infrastructure, no UI). Decomposition is one task per canonical directory (`.asd/rules`, `.asd/templates`, `.asd/agents`, `.asd/skills`, `.asd/workflows`, `.asd/project`), plus CHANGELOG (Task 7), README + AGENTS mirrors (Task 8), provider-view regeneration (Task 9), manifest ledger recompute (Task 10), and final verification (Task 11). Mandatory ordering baked into Dependencies: all canon edits (1–8) → per-file `sync.js --apply <file...>` (9) → bare `sync.js --apply` for the ledger (10) → the AC-7 greps (11). Two sub-decisions taken here: (a) **file ownership stays one-file-one-task** — the three cross-cutting atomic sets (R-4 `':(exclude)design/**'` pathspecs, R-10 backslash `design\\ux\\DESIGN.md` aliases, G-1 prose reword) are subtasks inside their owning file-group task, with integrity guaranteed by targeted `exclude)design` and `design\\ux` greps in Task 11, rather than being split into separate tasks that would put 5 files under joint ownership; (b) **the AC-9 CHANGELOG entry is authored now, in impl, under an `## Unreleased` heading**, and the `pr` phase only renames that heading to the concrete version alongside the `asd_version` bump. Stub-inclusion step was a no-op — `.asd/project/stubs.md` holds only the placeholder row and `audit.md`'s "Related open stubs" section confirms none.
- **Rationale**: The sprint's danger is not build breakage but silent partial success — audit R-11 established that a half-renamed tree yields green `sync.js --check`, green `tests/run.js`, and a freshly rewritten manifest ledger simultaneously, because `--check` has no targets under `.asd/rules|templates|workflows` (G-10), `tests/run.js` has no `design` fixture, and a bare `--apply` writes zero targets (G-9). The plan therefore encodes the sequencing as hard dependencies and makes the two AC-7 greps the sole completeness gate, run last. Per-file subtasks carry audit.md's exact line numbers plus explicit do-NOT-touch lists, because ~180 of the 426 raw hits are out-of-scope exclusions that frequently share a line with in-scope refs (R-1) — no global regex replace is permitted. Splitting the atomic sets into their own tasks was rejected: it would trade a low, line-level consistency risk for joint ownership of 5 files across parallel devs. Authoring the CHANGELOG in impl rather than deferring to `pr` closes AC-9 inside the phase that owns it and removes any contention between `impl` and `pr` over the same file.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/plan.md` (new), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: plan`).

## 2026-09-01 — Impl assessment approved for sprint 001-rename-design-to-docs

- **Decision**: Impl assessment approved; sprint advances to `impl-test`. All 11 `plan.md` tasks and all 89 subtasks are complete across 11 commits (`346dd6c` … `9285051`), 91 files changed (+658/−458): rules 8, templates 10, agents 14, skills 6, workflows 7, project config 1, root docs 2 (`README.md`, `AGENTS.md`), `CHANGELOG.md` 1, regenerated provider views 40, `release-manifest.json` 1. AC coverage: AC-1 canon rename (Tasks 1–6, 8); AC-2 subtree intact, leading segment only; AC-3 `<sprint>/design/`, `reviews/design/`, the `design`/`design-review`/`design-promote` phase names and `asd-phase-design*` files untouched; AC-4 `DESIGN.md`, `design-system.html`, `design-principles.md`, `design-system.md`, `custom-design-rules.md`, `t_design-md-delta.yaml` names unchanged; AC-5 per-file `sync.js --apply` (Task 9) plus bare `--apply` ledger recompute (Task 10); AC-6 `node tests/run.js` green in Task 11 (re-run deferred to `impl-test`, which owns the suite gate); AC-7 both repo-wide greps (`design/`, `design\\`) and both targeted greps (`exclude)design`, `design\\ux`) zero in-scope hits; AC-8 `README.md` folder map, command table, config excerpt and `AGENTS.md:32`; AC-9 breaking `CHANGELOG.md` entry under `## Unreleased` with the consumer migration order. Impl completion gate re-verified independently of the dev report: build (`node .asd/sync.js --check`) exit 0 / `"ok": true`, lint (`git diff --check`) exit 0 / empty. No manual steps were registered (`manual-steps.md` absent) and the sprint introduced no stubs — `.asd/project/stubs.md` holds only its placeholder row.
- **Rationale**: The completion gate's only non-`current` sync target, `AGENTS.md: modified-foreign`, was checked against the pre-sprint baseline commit `72182cf` in a throwaway worktree and found identical there, so it is the expected self-hosting state (the root `AGENTS.md` is deliberately self-sourced rather than regenerated from `t_AGENTS.md`) and not a Task 8 regression — the gate therefore passes rather than bouncing back to the dev. Both targeted greps were re-run at assessment time instead of trusting Task 11's report, since audit R-11 established that this sprint's automated signals are all blind to rename completeness and the greps are the sole real gate. The tests were deliberately not re-run here: `impl` must not pre-empt the `impl-test` suite gate.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/plan.md` (all checkboxes ticked), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl` → `impl-test`).

## 2026-09-01 — impl-test: suite green (77/77), 0 added/0 removed tests for sprint 001-rename-design-to-docs

- **Decision**: `impl-test` phase complete for sprint `001-rename-design-to-docs` — suite green (77/77 passed, 0 failed, 0 skipped), lint and build (`node .asd/sync.js --check`) clean, zero defects. Zero tests added, zero removed: every risk→check row resolved to a `none` decision with a recorded reason, so no removal gate was triggered and no prune/author work was needed. `state.json.test_defects_pending` confirmed `null` (initial-mode entry, never set). Sprint advances to `impl-review`.
- **Rationale**: The change is a pure reference rename across Markdown/YAML/JSON infrastructure with no runtime behaviour to assert — the existing 77 tests all cover `.asd/sync.js`/`update.js` engine behaviour, which the rename does not touch, so none became stale and none needed adjusting. The rename's real completeness risk (audit R-11: a half-renamed tree shows green `--check`, green `tests/run.js`, and a freshly written manifest ledger at once) is a *static* risk, and the cheapest reliable check for it is the AC-7 grep pair (`design/` and the separator-aware `design\\`), which impl Task 11 ran and the test-engineer re-verified independently — adding a runtime test that greps the repo would be a coverage-number test with no behavioural value, explicitly barred by `code-style.md` §17. Per `asd-phase-impl-test.md` step 9, a green suite carries no user gate.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/test-plan.md` (new, committed `c01466c`), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl-test` → `impl-review`).

## 2026-09-01 — impl-review iter-01: CONCERNS/FAIL → impl review-fix for sprint 001-rename-design-to-docs

- **Decision**: impl-review iteration 1 aggregated: 2 reviewers APPROVE (ui, performance), 5 CONCERNS (quality 2, implementation 1 high, testing 6, simplification 1, documentation 4), 1 FAIL (external, 4 findings). DoD not met (`review-policy.md` DoD table requires all reviewers APPROVE) → route to `impl` review-fix mode; `state.json.review_fixes_pending = "iter-01"`, so impl fix mode reads findings from `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-01/`. FAIL escalation resolved by user (Complication Approval): findings #1/#2/#4 **accepted** — the two over-renamed sprint-draft references (`t_config.yaml:13`, `README.md:227`, `t_sprint.md:5`, plus the `.asd/project/config.yaml:15` mirror outside the diff pathspec) and the `CHANGELOG.md:8` `git mv design docs` edge case for consumers with an existing `docs/` dir stay in the fix set, no override; finding #3 (`t_plan.md:23-25` relative-link depth `../../` → `../../../`) **accepted-fix-now**, not deferred. 4 of 4 FAIL findings survive into the fix set; none overridden.
- **Rationale**: Findings #1/#2/#4 are genuine AC-3 regressions (sprint-draft `design/` references must not be renamed) plus a migration-instruction defect that would break consumer upgrades, so no override was defensible. Finding #3 is a pre-existing depth bug exposed but not caused by the rename, and it contradicts the plan-approved G-5 "preserve `../../` exactly" convention — the user chose to fix it in-sprint anyway because the Quality reviewer raised the same line independently (its finding #2), so deferring would leave a known-broken link flagged twice in the same review round. Escalation record written into `external.md` under `## Escalation resolution` so review-fix mode is self-contained.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-01/external.md` (`## Escalation resolution` appended), `.asd/sprints/001-rename-design-to-docs/state.json` (`reviews.impl.verdicts["iter-01"]`, `review_fixes_pending: "iter-01"`).

## 2026-09-01 — impl fix for iter-01: findings resolved for sprint 001-rename-design-to-docs

- **Decision**: impl review-fix mode for `iter-01` complete; all impl-review findings resolved across 7 commits (`623818b` … `430796f`, 22 files, +66/−51), `state.json.review_fixes_pending` cleared to `null`, sprint routes back to `impl-test`. Content findings (`asd-backend-dev`, 5 commits): (a) `623818b` reverted the 3 over-renamed sprint-draft references (`t_config.yaml`, `t_sprint.md`, `README.md`) plus the `.asd/project/config.yaml` mirror, restoring AC-3; (b) `f3ceb4b` corrected `t_plan.md`'s `delegates_to` wording and the relative-link depth `../../` → `../../../`; (c) `e1b225f` applied the G-1 persistent-docs wording convention to the confirmed docs-root references (`language-policy.md`, `review-policy.md`, `asd-reviewer-simplification.md`, `asd-init/SKILL.md`, `t_test-plan.md`, the impl/impl-test/impl-review/plan workflows); (d) `0a5fc70` extended the `CHANGELOG.md` migration step so consumers with a pre-existing `docs/` directory get a merge path instead of a failing `git mv design docs`; (e) `f8b9545` regenerated the affected provider views and recomputed the manifest hash ledger. Testing findings (`asd-test-engineer`, 2 commits): `7a2d299` strengthened the `tests/run.js` drift assertion, `430796f` corrected the `test-plan.md` record per the Testing reviewer's findings. Impl completion gate re-verified independently of both dev reports: build (`node .asd/sync.js --check`) exit 0 / `"ok": true` with every target `current` except the known self-hosting `AGENTS.md: modified-foreign` exemption, lint (`git diff --check`) exit 0 / clean. Tests deliberately not re-run here — the suite is the `impl-test` gate.
- **Rationale**: Every finding in the fix set was one the user had already accepted at the iter-01 escalation (no overrides), so the fix scope was fully determined going in and needed no further gate — per `asd-phase-impl.md` "Execution mode", fix modes have no user-facing assessment checkpoint. The `AGENTS.md: modified-foreign` sync status was again treated as expected rather than a regression: the root `AGENTS.md` is deliberately self-sourced under self-hosting and is never regenerated from `t_AGENTS.md`, the same determination made at the initial-mode impl assessment. `f8b9545` was kept as its own commit because regenerated provider views and the recomputed `release-manifest.json` ledger are a mechanical consequence of the four preceding canon edits, not a logical change of their own. Routing is `impl-test` rather than straight back to `impl-review`: per `sprint-lifecycle.md`, every impl exit re-enters the cycle through the suite gate, so the strengthened `tests/run.js` assertion runs against the fixed tree before reviewers see it again.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl-review` → `impl`, `review_fixes_pending: "iter-01"` → `null`), `.asd/sprints/001-rename-design-to-docs/test-plan.md`, `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-01/`.

## 2026-09-01 — impl-test re-entry: suite green (77/77) after review-fix iter-01 for sprint 001-rename-design-to-docs

- **Decision**: `impl-test` re-entry complete for sprint `001-rename-design-to-docs` — suite green (77/77 passed, 0 failed, 0 skipped), 0 code defects. Re-run against `56a0c11` (`chore: close impl review-fix for iter-01`), which includes the two testing-finding commits `7a2d299` (strengthened `sync.js --check` drift assertion) and `430796f` (`test-plan.md` record correction). Gate re-verified independently of the test-engineer report: `node tests/run.js` exit 0 / 77/77; lint `git diff --check` exit 0 / clean; build `node .asd/sync.js --check` exit 0 / `"ok": true` with every target `current` except the known self-hosting `AGENTS.md: modified-foreign` exemption. No tests added or removed this re-entry, so no removal gate and no prune/author pass; `test-plan.md`'s `Suite run` section rewritten to record the post-fix run, grep completeness/over-rename rows carried forward unchanged (their underlying files were not touched again). `state.json.test_defects_pending` confirmed `null`. Sprint advances to `impl-review` for iteration 2; `reviews.impl.iteration`/`verdicts["iter-01"]` retained — the back-step to `impl` inside the `impl⇄impl-test⇄impl-review` cycle is not earlier than `impl`, so no rollback reset applies.
- **Rationale**: The review-fix pass changed prose, templates, regenerated views, and one test fixture — no production behaviour of `sync.js`/`update.js`, so the existing 77 engine tests remained the right and complete check set and none became stale. The one test-side change from iter-01 (the strengthened drift assertion) is exercised by this run and passed, which is the evidence that finding #1's fix actually holds rather than merely being reported as done. The `AGENTS.md: modified-foreign` status was again treated as expected, not a defect: under `self_hosting: enabled` the root `AGENTS.md` is deliberately self-sourced and never regenerated from `t_AGENTS.md` — the same determination made at the initial impl assessment, and now encoded as an explicit allowlist in `tests/run.js:950` so it is asserted rather than assumed. Per `asd-phase-impl-test.md` step 9, a green suite carries no user gate.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/test-plan.md` (`Suite run` section rewritten), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl` → `impl-review`).

## 2026-09-01 — impl-review iter-02: CONCERNS → impl review-fix for sprint 001-rename-design-to-docs

- **Decision**: impl-review iteration 2 aggregated: 5 APPROVE (ui, performance, implementation, testing, external — external is a *skip* verdict, not an endorsement: `codex exec` and its one permitted retry both exited 1 on `You've hit your usage limit… try again at 2:09 PM`, so per `external-review.md` § Detection the review was skipped without user prompt and removed from this iteration's DoD count), 3 CONCERNS (quality 1 medium, simplification 2 medium, documentation 2 medium). No FAIL, no escalation — every reviewer's `## Escalations` reads "None", all five findings are creator-autofixable within existing files. DoD not met (`review-policy.md` DoD table requires all reviewers APPROVE) → route to `impl` review-fix mode; `state.json.review_fixes_pending = "iter-02"`, so impl fix mode reads findings from `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-02/`. Fix set: (1) Quality #1 — `tests/run.js:950-965` drift assertion can pass vacuously on an empty/partial `buildSyncPlan` enumeration; add a coverage assertion against the canon target set, not just per-item cleanliness. (2) Simplification #1 — unify the `design/` vs `persistent docs` vocabulary across `t_test-plan.md:5`, `t_config.yaml:13`, `asd-phase-impl.md:41,69`. (3) Simplification #2 — restructure `CHANGELOG.md:8`'s ~330-word migration bullet into a numbered list in place, every branch and warning verbatim, explicitly **not** a `MIGRATION.md`/script/`/asd-migrate` command. (4) Documentation #1 — `t_test-plan.md:5` reworded to `persistent docs (requirements)`, **correcting a misclassification introduced by the iter-01 fix pass**, which reverted it to `design/ docs (requirements)`. (5) Documentation #2 — `sprint.md` AC-1/AC-7 exclusion set and `plan.md` Task 2 checkbox annotations brought into line with the already-approved `t_sprint.md:5` sibling-reference exception; routed to the fix pass as bookkeeping, no fresh user decision sought.
- **Rationale**: The iter-01 escalation approved reverting `t_config.yaml:13`, `README.md:227`, `t_sprint.md:5` and the `config.yaml:15` mirror — `t_test-plan.md` was never covered by that approval, and the fix pass extended the revert to it by analogy. Documentation's disambiguation test resolves it the other way and I verified both files independently: `t_test-plan.md` is authored during `impl-test`, i.e. *after* design-promote, and `asd-phase-impl-test.md:11` (reworded to "persistent docs" in that same fix pass) names the persistent root as the requirements source at that moment, so the template and the workflow that writes it currently contradict each other. `t_sprint.md:5` is the opposite case — authored at `scope`, before design-promote, with all sibling `delegates_to` items sprint-local and no persistent-root corroboration — so its revert stands and is explicitly out of the fix set. That same corroboration test, not Simplification's blanket "pick one vocabulary", governs the remaining sites in Simplification #1 where the two findings conflict. Documentation #2 is recorded as a fix-set item because the AC-7 gate is a literal grep the `pr` phase re-runs: left unamended it reports a false failure on a reference the user already approved keeping. It is bookkeeping against a granted decision rather than a requirement change, so it needs no new approval — but it is routed through `impl` rather than edited here so the whole iter-02 fix set lands as one reviewable pass. External's skip is recorded as time-boxed, not permanent: quota resets at 14:09 local, and a substantive external verdict on the iter-01 fix set remains desirable if the sprint is still open then.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`reviews.impl.verdicts["iter-02"]` recorded, `review_fixes_pending: null` → `"iter-02"`), `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-02/` (8 reviewer files).

## 2026-09-01 — impl fix for iter-02: findings resolved for sprint 001-rename-design-to-docs

- **Decision**: impl review-fix mode for `iter-02` complete; all four CONCERNS findings resolved across 4 commits (`5f39c1a`, `0e6cd2e`, `0781e9b`, `191ad36`), `state.json.review_fixes_pending` cleared to `null`, `phase: impl-review` → `impl`, sprint routes back to `impl-test`. Content findings (`asd-backend-dev`): (a) `5f39c1a` hardened the `tests/run.js` drift assertion against a vacuous pass — every `--check` item must report `current`, with `SELF_SOURCED_ALLOWLIST = {AGENTS.md}` asserting the self-hosting exemption rather than assuming it (Quality #1); (b) `0e6cd2e` corrected `t_test-plan.md:5` to `persistent docs (requirements)` — the wording form, not a revert — and unified the same vocabulary in `asd-phase-impl.md` (Documentation #1, Simplification #1); (c) `0781e9b` restructured the `CHANGELOG.md` migration bullet into a numbered list in place, both cases verbatim, no `MIGRATION.md`/script/command introduced (Simplification #2); (d) `191ad36` recomputed the manifest hash ledger. Bookkeeping finding (Documentation #2, `asd-pm`, this entry's commit): `sprint.md` AC-1 narrowed to a per-site corroboration test with `t_sprint.md:5` recorded as the explicit approved exception (and `t_test-plan.md` explicitly named as *not* an exception), AC-7's exclusion set extended with that same line so the `pr`-phase grep gate does not report a false failure; `plan.md` Task 2's `t_sprint.md` subtask annotated "reverted, not renamed" and its `t_test-plan.md` subtask annotated as a reword. Impl completion gate re-verified independently of the dev report: build (`node .asd/sync.js --check`) exit 0 / `"ok": true`, every target `current` except the known self-hosting `AGENTS.md: modified-foreign` exemption; lint (`git diff --check`) exit 0 / clean.
- **Rationale**: The fix set was fully determined at the iter-02 aggregation, so per `asd-phase-impl.md` "Execution mode" the fix pass carried no user-facing assessment checkpoint. Documentation #2 was executed without a fresh user decision: the underlying exception was already granted at the iter-01 escalation, so amending AC-1/AC-7 records a decision the user made rather than changing a requirement — the reviewer, the aggregating PM instance and this instance independently reached that reading. AC-1 was rewritten as a *classification rule plus named exception* rather than by simply deleting `t_sprint.md` from the file list, so the sprint's completeness criterion stays a positive statement of what "in scope" means and the exception is auditable at the `pr` gate instead of silently absent. `t_test-plan.md` is called out in AC-1 as explicitly not an exception, closing the ambiguity that caused the iter-01 over-revert. Tests deliberately not re-run here — the suite is the `impl-test` gate, and the hardened assertion from `5f39c1a` must run there against the fixed tree.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/sprint.md` (AC-1, AC-7), `.asd/sprints/001-rename-design-to-docs/plan.md` (Task 2 subtasks), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase`, `review_fixes_pending`), `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-02/`.

## 2026-09-01 — impl-test re-entry: suite green (77/77) after review-fix iter-02, 0 code defects for sprint 001-rename-design-to-docs

- **Decision**: `impl-test` re-entry complete for sprint `001-rename-design-to-docs` — suite green (77/77 passed, 0 failed, 0 skipped), 0 code defects, no `D-N` row added. Re-run against `8319a1f` (`chore: close impl review-fix for iter-02`), whose head carries `5f39c1a` (drift-assertion coverage guard, Quality #1), `0e6cd2e` (persistent-docs vocabulary, Documentation #1 / Simplification #1), `0781e9b` (`CHANGELOG.md` numbered migration list, Simplification #2) and `191ad36` (hash-ledger recompute). All three gates re-verified independently of the test-engineer report, exit codes captured directly rather than read off a summary: `node tests/run.js` exit 0 / `77/77 passed`; lint `git diff --check` exit 0 / clean; build `node .asd/sync.js --check` exit 0 / `"ok": true`, every target `current` except the known self-hosting `AGENTS.md: modified-foreign` exemption. The new coverage-guard block was inspected in source (`tests/run.js:955-973`) rather than trusted from the green run: it builds `targets` from `parsed.items.map(item => item.target)` and then walks `.asd/agents/*.md` and `.asd/skills/*/SKILL.md` off disk, asserting each expected `.claude/agents/<n>.md`, `.codex/agents/<n>.toml`, `.claude/skills/<n>/SKILL.md`, `.agents/skills/<n>/SKILL.md` string is present — enumeration independent of `buildSyncPlan()`, which is what actually closes the vacuous-pass gap. No tests added or removed this re-entry, so no removal gate and no prune/author pass ran. `state.json.test_defects_pending` confirmed `null`. Sprint advances to `impl-review` iteration 3; `reviews/impl/iter-03/` created. **Severity floor for iteration 3 = `high`** — low and medium findings are dropped this iteration.
- **Rationale**: Floor computed from this repo's actual `.asd/project/config.yaml` values (`iterations_low: 1`, `iterations_medium: 1`, `iterations_high: 2`, `iterations_critical: 3`), not `review-policy.md`'s documented defaults: cumulative low = 1 → iter 1 (floor low), cumulative medium = 2 → iter 2 (floor medium), cumulative high = 4 → iters 3-4 (floor high), cumulative critical = 7 → iters 5-7. Iteration 3 therefore sits in the `high` band, so the five medium findings' severity class is no longer admissible — a deliberate convergence device, and every iter-02 finding was in fact fixed rather than aged out. The over-engineering and structure/cohesion checklists remain `critical` and undroppable regardless of floor. The review-fix pass touched one test fixture and three prose/template files with no change to `sync.js`/`update.js` runtime behaviour, so the existing 77 engine tests stayed the complete and correct check set and none went stale. No rollback reset applies: the back-step to `impl` was inside the `impl⇄impl-test⇄impl-review` cycle, which is not earlier than `impl`, so `reviews.impl.iteration` keeps incrementing and `verdicts["iter-01"]`/`["iter-02"]` are retained. Per `asd-phase-impl-test.md` step 9, a green suite carries no user gate.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/test-plan.md` (`Suite run` rewritten, vacuous-pass risk row + `Added tests` updated), `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl` → `impl-review`, `reviews.impl.iteration: 2` → `3`, `updated_at`), `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-03/` (created, empty).

## 2026-09-01 — impl-review iter-03: APPROVE (8/8, DoD met) — advancing to pr phase

- **Decision**: impl-review iteration 3 aggregated **8/8 APPROVE** — quality, implementation, testing, ui, simplification, documentation, performance, external. DoD met per `review-policy.md`'s DoD table (impl-review requires all eight reviewers APPROVE in the same iteration). No FAIL, no escalation — every reviewer's `## Escalations` reads "None"; no CONCERNS at or above the iteration-3 `high` severity floor, so nothing routes back to `impl` and `state.json.review_fixes_pending` stays `null`. Verdicts recorded under `reviews.impl.verdicts["iter-03"]`; `phase: impl-review` → `pr`. External Review is a substantive verdict this iteration, not a skip: the iteration-2 codex quota window has reset (`codex-cli 0.150.1` probe clean), `codex exec --sandbox read-only` ran the full incremental payload (`gpt-5.6-sol`, high effort, 75,680 tokens) and returned APPROVE with zero kept and zero dropped findings, so `external` counts toward this iteration's DoD rather than being removed from it. All eight verdict tokens were read from the written reviewer files rather than taken from a summary, and each internal reviewer's File-coverage ledger was confirmed to list all five scoped files (`.asd/release-manifest.json`, `.asd/templates/t_test-plan.md`, `.asd/workflows/asd-phase-impl.md`, `CHANGELOG.md`, `tests/run.js`) with no blank or unresolved row.
- **Rationale**: Both iter-02 CONCERNS clusters are confirmed resolved by the reviewers that raised them, not merely reported fixed by the dev: Documentation re-verified `t_test-plan.md:5` now reads `persistent docs (requirements)` and matches `asd-phase-impl-test.md:11`, and re-checked the AC-1/AC-7 bookkeeping against the granting `decisions-log.md` entries; Simplification re-ran its guard and confirmed no `MIGRATION.md`, script, command, or `managed_paths` entry was introduced by the CHANGELOG restructure, and that the reflow is fact-preserving. Quality and Testing both independently re-derived that the new `tests/run.js` coverage guard enumerates canon off disk rather than through `buildSyncPlan()`, which is what actually closes the vacuous-pass class the finding targeted. Three reviewers converged on the same residual gap — the guard omits the third conditional canon tree `.asd/hooks/*.js` — and all three independently rated it `medium`, below the iteration-3 `high` floor, so it is sub-floor and correctly excluded from the verdict; it is carried as follow-up for the next sprint touching `.asd/sync.js`, deliberately not reopening the impl loop for a hypothetical future regression in a file this sprint never touched. Testing and Performance both disclosed a read-only toolset (no shell), so their suite/diff claims rest on static corroboration plus the independently re-verified impl-test gate (77/77, exit 0) rather than a fresh execution — acceptable because reviewers are read-only by host-level contract and the suite gate belongs to impl-test. UI is `n/a` throughout with a documented non-ABORT justification: this repo ships no rendered surface, so the missing `docs/ux/` baseline is an absence of UI, not an absence of governance. The all-APPROVE branch of `asd-phase-impl-review.md` step 7 carries no user gate — DoD is a deterministic read of the verdict set against the policy table — so the sprint advances to `pr` on the recorded verdicts alone. No rollback reset applies: the phase moves forward, not earlier than `impl`.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`phase: impl-review` → `pr`, `reviews.impl.verdicts["iter-03"]` recorded 8× APPROVE, `review_fixes_pending` confirmed `null`, `updated_at`), `.asd/sprints/001-rename-design-to-docs/reviews/impl/iter-03/` (8 reviewer files).

## 2026-09-01 — pr DoD block resolved: asd_version 2.0.0 bump unblocked by decoupling fixture tests (option (a) approved)

- **Decision**: `pr` phase, open mode. Self-hosting version bump computed as **1.2.0 → 2.0.0** (MAJOR): current `asd_version` is ≥ 1.0.0 and the sprint's change is breaking (`CHANGELOG.md` entry framed `**BREAKING:**`, PR title carries the Conventional Commits `!` marker), so `git-strategy.md` "Versioning & Changelog" maps it to MAJOR. `CHANGELOG.md`'s `## Unreleased` heading renamed to `## v2.0.0`, matching the existing dateless `## v1.2.0` / `## v1.1.0` heading format in the same file. The bump immediately blocked the DoD "tests pass" check: four fixture-comparison tests (`canonical agent -> Claude .md`, `-> Codex .toml`, `canonical skill -> Claude SKILL.md`, `-> Codex SKILL.md`) failed 73/77 because the expected fixtures baked the literal string `asd_version=1.2.0` into the ownership-marker line while the rendered side reads the live `manifest.asd_version`. PR creation was halted and the block relayed with three options; the user approved **option (a)** — fix the coupling in `tests/run.js` rather than hand-patch the four fixture strings (option (b)) or ship without a bump (option (c)). Fix landed as `a907d3f` (test-engineer): fixtures now carry a `{{FIXTURE_ASD_VERSION}}` placeholder substituted with the live manifest value at comparison time via a new `readExpectedFixture()` helper; the pending `release-manifest.json` + `CHANGELOG.md` bump is bundled into the same commit. DoD re-verified independently at `a907d3f`, exit codes captured directly: `node tests/run.js` exit 0 / 77/77; `git diff --check` exit 0 / clean; `node .asd/sync.js --check` exit 0 / no drift. Working tree clean apart from an untracked, unrelated `.claude/agent-memory/` directory, which is not part of the PR. Remaining DoD checks re-confirmed unchanged: every `plan.md` task checkbox `- [x]`; AC-1..AC-9 covered per the iter-03 implementation and documentation coverage ledgers; 8/8 reviewer verdict tokens read from the iter-03 review files; `stubs.md` holds zero rows for this sprint and the repo contains no `// TODO(sprint-001-rename-design-to-docs):` marker.
- **Rationale**: Option (a) was recommended and approved because the failure is structural, not a regression of this sprint's content: the marker's `asd_version` field is stamped from the manifest at render time, so *every* future self-hosting bump would break the same four tests regardless of what the sprint changed — option (b) would have re-armed the trap for the next release. The fix is confined to the test harness (`tests/**` is declared dev write scope in `sprint-lifecycle.md` "Self-hosting") and changes no `sync.js`/`update.js` runtime behaviour, so it neither widens the sprint's product surface nor invalidates the iter-03 all-APPROVE verdict set; the four affected tests still assert the marker prefix and full rendered body, so coverage is preserved rather than weakened. Routing it to a test-engineer rather than editing it here respects the PM write-scope restriction (sprint metadata, decisions-log, plus the version/changelog bump the `pr` workflow assigns to PM). Option (c) was rejected outright: shipping a breaking change without a version bump would leave the merge-mode tag/release step with nothing to tag and would strand consumers on an unversioned migration.
- **Affected docs**: `.asd/release-manifest.json` (`asd_version` 1.2.0 → 2.0.0), `CHANGELOG.md` (`## Unreleased` → `## v2.0.0`), `tests/run.js` + `tests/fixtures/expected/**` (commit `a907d3f`, test-engineer).

## 2026-09-01 — sprint 001-rename-design-to-docs PR opened: https://github.com/LordKuper/agentic-software-development/pull/14

- **Decision**: `pr` phase, open mode, complete. User confirmed the composed title and body without edits, so `git push -u origin sprint/001-rename-design-to-docs` ran (new remote branch, tracking set) followed by `gh pr create --base main` — **PR #14** opened, state verified `OPEN` via `gh pr view` rather than inferred from the create output, head `sprint/001-rename-design-to-docs` → base `main`. Title `feat!: rename persistent docs root design/ to docs/` (Conventional Commits, `!` breaking marker, 50 chars — becomes the squash-merge subject per `git-strategy.md`); body from `t_pr-description.md` with the Version section filled (1.2.0 → 2.0.0) because `self_hosting: enabled`. Full DoD was re-verified at `a907d3f`/`8b02741` immediately before opening, exit codes captured directly: `node tests/run.js` exit 0 / 77/77, `git diff --check` exit 0, `node .asd/sync.js --check` exit 0 / no drift, every `plan.md` checkbox `- [x]`, AC-1..AC-9 covered per the iter-03 coverage ledgers, 8/8 reviewer APPROVE tokens read from the iter-03 files, zero sprint stubs and zero `// TODO(sprint-001-rename-design-to-docs):` markers. `gh`'s "1 uncommitted change" warning refers only to the untracked `.claude/agent-memory/` directory, which is unrelated to the sprint and deliberately outside the PR. `state.json.pr` set to `{number: 14, url: …, state: "open"}` with `phase` kept at `pr`. **Sprint stays active** — nothing archived, no tag created.
- **Rationale**: Archival and the self-hosting `v2.0.0` annotated tag + `gh release create` belong to `pr` merge mode, which only runs on a later re-entry after `gh pr view 14` reports `MERGED` (`asd-phase-pr.md` "Workflow — merge mode"); doing either now would tag a commit that may still change under review and would archive a sprint whose work is not yet on `main`. The PR was opened rather than left as a printed summary because `git.gh_enabled: true` and `git.auto_pr: true`, and only after an explicit user confirmation of the exact title and body — the `checkpoints.md` final PR gate is not satisfiable by an inferred approval.
- **Affected docs**: `.asd/sprints/001-rename-design-to-docs/state.json` (`pr` null → `{14, url, "open"}`, `updated_at`); remote branch `origin/sprint/001-rename-design-to-docs`; PR #14.

## 2026-09-01 — sprint 001-rename-design-to-docs completed, archived, PR https://github.com/LordKuper/agentic-software-development/pull/14

- **Decision**: `pr` phase, merge mode, complete. Merge verified independently rather than taken from the dispatch message: `gh pr view 14 --json state,mergeCommit,mergedAt` returned `state: MERGED`, merge commit `9537b1e234f78c3e6970f5440dce8f221dc5f38a`, `mergedAt: 2026-09-01T11:53:20Z`. Self-hosting release step done first: `git fetch origin --prune` + `git checkout main` + `git merge --ff-only origin/main` moved local `main` from `72182cf` to `9537b1e` (`feat!: rename persistent docs root design/ to docs/ (#14)`), annotated tag `v2.0.0` created **on the merge commit** (`git show v2.0.0^{commit}` re-verified as `9537b1e…`, not the sprint-branch tip) and pushed, then `gh release create v2.0.0 --notes-file <extracted ## v2.0.0 CHANGELOG section>` → https://github.com/LordKuper/agentic-software-development/releases/tag/v2.0.0. Sprint folder moved `.asd/sprints/001-rename-design-to-docs/` → `.asd/sprints/archived/001-rename-design-to-docs/` via `git mv` (68 files, all recorded as pure renames `R`, no content change), `archived/` created for the first time. `state.json` updated in its new location: `pr.state: "open"` → `"merged"`, `phase: "pr"` → `"done"`, `archived_at: 2026-09-01T11:57:22Z` added. The archive commit lands on a follow-up branch `chore/archive-sprint-001` off `main` and reaches `main` via its own PR, **not** by a direct push to `main` and **not** on the sprint branch.
- **Rationale**: The workflow's literal instruction (`asd-phase-pr.md` merge-mode step 3: "commit move … and push to sprint branch") was unexecutable here — `git fetch --prune` reported `[deleted] origin/sprint/001-rename-design-to-docs`, since this repo squash-merges with auto-delete-branch. Following it literally would have resurrected a deleted branch carrying obsolete pre-squash history (local tip `d20e9e5`, divergent from the squashed `9537b1e`) that nothing would ever merge, leaving the archived folder permanently off `main`. Committing straight to `main` was rejected instead of chosen: `git-strategy.md` "Forbidden" states `Never commit or push directly to git.base_branch` without carving out merge mode, and `main` additionally carries branch protection (no force-push, required `sync-check` status). The conflict was structural, so it was escalated to the user rather than resolved silently; the user approved the follow-up-PR route, which satisfies both documents' intent — the archive lands on `main`, and it lands via a PR. `.claude/agent-memory/` stayed untracked and out of the commit: it is agent-local scratch, unrelated to the sprint.
- **Affected docs**: `.asd/sprints/archived/001-rename-design-to-docs/**` (moved, formerly `.asd/sprints/001-rename-design-to-docs/**`), `.asd/sprints/archived/001-rename-design-to-docs/state.json` (`phase`, `pr.state`, `archived_at`, `updated_at`), `.asd/project/decisions-log.md`; tag `v2.0.0`, GitHub Release v2.0.0, PR #14.

## 2026-09-01 — FOLLOW-UP (framework gap, not fixed): asd-phase-pr.md merge-mode archival targets a branch that no longer exists under squash-merge + auto-delete

- **Decision**: Recorded as an open framework defect for a future sprint; **deliberately not fixed in this sprint** (sprint 001 was already merged and archived — an archived sprint is immutable per `sprint-lifecycle.md` "Sprint immutability", and the fix is out of its approved scope). Two concrete text defects to address: (1) `.asd/workflows/asd-phase-pr.md` merge-mode step 3 says "commit move with message `chore: archive sprint <NNN-slug>` and push to sprint branch", and its "Artefacts produced" section likewise says "Archive commit on sprint branch" — both are stale for any repo using squash-merge with auto-delete-branch, because the sprint branch is deleted remotely at the moment merge mode becomes reachable. This is not an edge case for ASD itself: `git-strategy.md` "PR creation" mandates a Conventional Commits PR title precisely because it "becomes the squash-merge commit subject", so squash-merge is the assumed strategy. Whatever replacement is chosen (follow-up archive PR, or an explicit narrow exception permitting the archive commit on `git.base_branch`) must be reconciled with `git-strategy.md` "Forbidden" — `Never commit or push directly to git.base_branch` — which currently makes the only other obvious route a rule violation. (2) Minor, same area: `archived_at` is written by `asd-phase-pr.md` merge-mode step 3 but appears in neither `.asd/templates/t_state.json` nor `.asd/rules/sprint-lifecycle.md` "State recovery", so the field is undocumented in the schema it belongs to.
- **Rationale**: Logged here rather than left in chat so the gap survives this session — the decisions-log is the project's append-only chronology and is the only durable home for a finding that belongs to no sprint artefact. Sprint 001 hit this live: the documented path was impossible, and resolving it needed a user decision mid-phase. Left unfixed, every future self-hosting sprint reaching merge mode hits the same halt. Any fix must also update `README.md` if it changes the documented phase behaviour, per this repo's hard rule.
- **Affected docs**: `.asd/workflows/asd-phase-pr.md` (merge-mode step 3 + "Artefacts produced"), `.asd/templates/t_state.json`, `.asd/rules/sprint-lifecycle.md` ("State recovery"), `.asd/rules/git-strategy.md` ("Forbidden") — none edited, all listed as the surface a future sprint must touch.
