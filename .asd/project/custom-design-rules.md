---
responsibility:
  owns: project-owner custom rules read during design and design-review phases
  excludes: universal rules, code/test rules
  delegates_to: custom-common-rules.md (all phases), custom-coding-rules.md (impl/impl-review)
---

# Custom Design Rules

Project rules applying only to design artifacts (PRD, UX spec, ADR, C4). Read by `asd-ba`, `asd-ux-designer`, `asd-architect`, design-review reviewers.

`documents.prd`/`ux_spec`/`adr`/`c4` are all disabled for this repo (lean framework-dev profile) — this file stays empty until one is re-enabled for a specific sprint. ASD never overwrites this file.
