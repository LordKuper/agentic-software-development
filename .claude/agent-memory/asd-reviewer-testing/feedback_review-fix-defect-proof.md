---
name: review-fix-defect-proof
description: Code defects found by impl-review (COR-*) and fixed in a review-fix round never enter the test-plan Defects table, so their §17 fail-first record is easy to skip; also re-read existing assert messages a prose fix made false, and messages claiming more than their predicate checks (incl. citation-only carve-out asserts); compare each fix's `none` reason with its sibling rows; sentence splitters break at "e.g."; `keep` rows claiming "verbatim" — check the regex capture group; grep canon to falsify "no longer restates" / "any new line automatically" reasons
metadata:
  type: feedback
---

A correctness finding fixed in an impl-review fix round (sprint 013 iter-02: COR-1-2, `defectStalemate` consecutive entries) is still a "fixed defect" under `code-style.md` §17. It needs a recorded fail-first run or mutation: command, non-zero exit, failing test name. The test-plan `Defects` table only holds impl-test `D-N` rows, so the fix shows up only as an inverted assertion plus a `keep` row saying "each bound has a case that fails if it flips". That is the bare claim §17 rejects.

**Why:** the regression test can be fine (replaying it against the pre-fix code shows it would fail) while the proof record is still missing. The iteration-2 Entry log row makes the record look settled.

**How to apply:** at iter ≥ 2, map each code-fix id in the decisions-log `impl fix for iter-NN` line to an `Added tests` Regression-proof cell. A `keep` or `n/a` there means a medium finding. Replay the assert against the pre-fix code to decide whether the fix is only a record gap or also a real test gap. Separately, when a finding fixes a prose rule that an existing assert already pins by word match, re-read that assert's message. Sprint 013: `tests/run.js` AC-16 still said stdin syntax is picked from `platform` after COR-1-1 keyed it on the host shell. See [[no-shell-review-method]].

Asymmetric `none` (sprint 014 iter-02): framework "code" fixes are often prose rules the orchestrator executes. The tester pinned COR-2's prose carve-out statically (`never … landed` regex) but gave COR-1's equally prose carve-out (leftovers limited to the failed dispatch's authorised paths) `none` with "assertable only if moved into runtime.js". The reason is false by the entry's own sibling row. Check every fix-id `none` against how sibling fix rows in the same entry were checked; a clause deletion with a silent data-loss effect and no effect-level pin elsewhere is medium.

Sentence-split asserts (sprint 014 iter-03, AC-2): `text.split(/(?<=\.) /)` also splits at `e.g. `, so the "sentence" an assert reads may start mid-clause. Replay which fragment the `.find()` returns before trusting a mutation record; it held there, but a later `e.g.` inserted before a pinned clause silently moves it into another fragment. Also `\bnever\b[^;]*\bX\b` regexes accept X in a positive clause after an unrelated `never` — below high floor alone.

Capture-group scope (sprint 015 iter-02, P2-3): a `keep` row said the AC-8 assert requires the `"<doc> skipped this sprint by user"` line verbatim, but the regex was `/"<doc> (skipped …)"/` — group 1 is only the suffix, so the pre-fix form still passes. When a `keep` cites an existing `.includes(match[1])`, check what the group actually spans against the pre-fix text.

Message vs predicate (sprint 012 iter-01): an assert whose message claims two properties while its predicate checks one — the asd-init step-13 seed assert said "registry seed precedes it unconditionally" but only checked `documents.c4` precedes `c4/model`. Replay a mutation that breaks only the unchecked half; green means a finding. Related (sprint 014): a unit fixture injecting a narrower input than production (`templates: ['plan.md']` vs the real `templateNames`, which includes `AGENTS.md`) can pin a "boundary non-match" the shipped CLI path contradicts. Sprint 018 iter-01 (D-1): a carve-out fix pinned only by "line cites `<doc>` §X" passes when the carve-out subject (`a reviewer`) is reworded away but the citation kept — compare with a sibling assert in the same test that pins subject AND citation (the signal-vocabulary line did); raised low.

Falsifiable reasons (sprint 018 iter-02, all medium): (a) a class sweep keyed on one literal phrase (`returns \`QUESTION\``) while its row claims "any new generic line automatically" — grep synonyms (`via \`QUESTION\``) in the swept dir; core.md Autonomy line escaped. (b) "the workflows no longer restate option names" — grep the emitters; they did, and the rewritten test dropped the only pin there. (c) a `keep` row for a COR fix citing an entry-1 pin that passes on the pre-fix text (web-policy bullet regex `\bweb\b|\bURL\b` matched the fetch-only line) — replay the pin against the pre-fix wording.
